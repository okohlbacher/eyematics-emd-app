# v1.12 — Deferred Questions (to review at end of autonomous run)

Per the "push user questions to the end" directive, decisions that would normally
prompt the user were made with a sensible default during the autonomous run and
logged here for your review. Each entry: the question, the default I took, and why.

## Already-locked decisions (for reference — not open)
- D1 global admin thresholds · D1b plausibility ranges centralized + admin-editable ·
  D2 QUAL-001 persists with SavedSearch · D3 multi-select centers IN ·
  PROT-001 → 'unauthenticated' · single milestone v1.12 (37–45).

## Open items (default taken — confirm at end)

_(appended during the run)_

### Q-40-1 — Threshold snapshot with saved cohort (Phase 40)
- **Question:** When QUAL-001 check-parameter selection persists with a SavedSearch, should the cohort also snapshot the *threshold values* in effect at save time, so its quality result is reproducible even after an admin later edits thresholds?
- **Default taken:** NO snapshot — saved cohorts use the current (live) global thresholds at evaluation time. Simpler; consistent with the global-thresholds decision (D1) and avoids per-cohort threshold provenance (explicitly deferred). The persisted field is only the *parameter selection* (which checks to run), not the threshold values.
- **Why:** D1 chose global thresholds precisely to avoid snapshot/provenance complexity; snapshotting here would reintroduce it.
- **Confirm at end / Phase 45.**

### Q-40-2 — Stale-anonymous note (carryover from PROT-001)
- The v1.10 doc said "still anonymous users in protocol." Phase 38 relabels the unauthenticated fallback. If the user meant *deleted* users appearing as anonymous (a different path), that needs a separate look. Default: assume they meant the 401 fallback (now fixed). Confirm at Phase 45.

### Q-41 — Phase 41 browser-confirm items (UAT at Phase 45)
Automated 8/8 passed; confirm visually: (a) CenterMultiSelect chip interaction with live data; (b) Grundgesamtheit shrinks correctly with the time-range filter on real data; (c) the top flag-status control sits above the scroll fold. Also confirm QUAL-023 "couldn't find absolute values" is resolved (and which page you were on — main Datenqualität vs Dokumentationsqualität).
