# Clarification needed — case-detail X-axis (relative vs calendar time)

**Context (what v1.15 currently does):** following your earlier feedback ("aggregate by relative time since start … would require relative time on the x axis"), the **individual patient trajectory** AND the **cohort overlay** are now both on a **relative "Monate seit Erstvisite"** X-axis (0, 7, 14 … Mon). See screenshot.

**Your round-4 note:** "Use non-relative timestamps for the individual view." — but you also said the relative cohort aggregation "works."

These are two separate choices, so we want to get it exactly right before changing it:

- **The X-axis of the single-patient chart** — calendar dates (e.g. 09/2023 … 07/2024) **or** relative months since the patient's first visit?
- **The cohort overlay alignment** — keep aggregating peers by relative time since *each peer's* own first visit (so the patient is compared to peers at the same elapsed treatment time)? You confirmed this "works".

**Question — which combination do you want for the individual case view?**
1. **Calendar-date X-axis for the patient**, cohort overlay still aligned by relative time (mapped onto the patient's dates). ← our reading of your note
2. **Keep the relative-month X-axis** as it is now (no change).
3. **A toggle** to switch the individual chart between calendar dates and relative months.

(Note: the IVI/injection markers + highlighting follow whichever axis you choose; that's no extra effort.)
