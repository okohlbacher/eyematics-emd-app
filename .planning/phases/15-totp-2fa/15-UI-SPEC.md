---
phase: 15
slug: totp-2fa
status: approved
reviewed_at: 2026-04-17
shadcn_initialized: false
preset: none
created: 2026-04-17
---

# Phase 15 — UI Design Contract: TOTP 2FA

> Visual and interaction contract for Phase 15 TOTP 2FA. Generated by gsd-ui-researcher.
> Consumed by gsd-planner, gsd-executor, gsd-ui-checker, gsd-ui-auditor.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none — manual Tailwind v4 | Codebase: no components.json found |
| Preset | not applicable | shadcn not initialized |
| Component library | none — hand-rolled Tailwind components | Codebase: src/components/* |
| Icon library | lucide-react | Codebase: AdminPage.tsx, PasswordChangePage.tsx imports |
| Font | system-ui, -apple-system, sans-serif | Codebase: src/index.css body rule |

**shadcn gate result:** No components.json found. Stack is React/Vite. shadcn not initialized and not recommended for this phase — Phase 15 adds two new pages that must match existing hand-rolled Tailwind patterns precisely. No shadcn initialization required.

---

## Spacing Scale

Declared values (must be multiples of 4). All values observed in existing codebase and carried forward.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (`gap-1`), checkbox-label gap |
| sm | 8px | Compact element spacing (`gap-2`), form field internal padding vertical, recovery code grid gap |
| input-padding-x | 12px | OTP/form input horizontal padding (`px-3`) — existing codebase standard (LoginPage, PasswordChangePage) |
| md | 16px | Default element spacing (`gap-4`), form field margin-bottom, card padding horizontal |
| lg | 24px | Section padding (`gap-6`), card header bottom margin |
| xl | 32px | Layout gaps (`p-8` card padding), section breaks |
| 2xl | 48px | Major vertical spacing — fullscreen page centering |
| 3xl | 64px | Not used in these components |

Exceptions:
- Touch target for "Copy all" and "Download" buttons: minimum 44px height via `py-2` + font-size + border

---

## Typography

All sizes are as-observed from PasswordChangePage.tsx (the template), LoginPage.tsx, and AdminPage.tsx.

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Body / form labels | 14px | 500 (medium) | 1.4 | `text-sm font-medium` |
| Body / helper text | 14px | 500 (medium) | 1.5 | `text-sm font-medium` |
| Page heading | 20px | 600 (semibold) | 1.2 | `text-xl font-semibold` |
| Recovery code mono | 14px | 500 (medium) | 1.4 | `text-sm font-mono font-medium` |

Only 2 weights in use: **500 medium** and **600 semibold** (matching existing Tailwind component patterns: `font-medium` for body/labels/mono, `font-semibold` for headings).

---

## Color

All values derived from existing codebase — PasswordChangePage.tsx, AdminPage.tsx, LoginPage.tsx.

| Role | Tailwind Token | Approximate Hex | Usage |
|------|---------------|-----------------|-------|
| Dominant (60%) | `bg-gray-50` | #F9FAFB | Fullscreen page background (TotpEnrollPage outer div) |
| Secondary (30%) | `bg-white` + `border-gray-200` | #FFFFFF / #E5E7EB | Card surface, form container, recovery codes panel |
| Accent (10%) | `bg-blue-600` / `text-blue-500` | #2563EB / #3B82F6 | Primary CTA button, focus ring (`focus:ring-blue-500`) |
| Destructive | `text-red-500` / `hover:text-red-700` | #EF4444 / #B91C1C | Error messages, "Reset 2FA" destructive button in AdminPage |
| Warning / notice | `text-amber-500` | #F59E0B | Lock icon on enrollment page (same as PasswordChangePage Lock icon) |
| Muted text | `text-gray-600` | #4B5563 | Subtitle text, helper copy |
| Disabled | `opacity-50` on button | — | Loading/disabled state on submit button |

Accent reserved for:
- Primary CTA button background (`bg-blue-600`)
- Input focus ring (`focus:ring-blue-500`)
- Link-style icon buttons (existing pattern: `text-blue-500` on role icons)

Destructive reserved for:
- Inline error messages below form fields (`text-red-600` — matches PasswordChangePage)
- "Reset 2FA" button in AdminPage user row (matches existing delete button pattern: `text-red-500 hover:text-red-700`)
- Load error text in table body

---

## Component Inventory

New components to create for this phase:

### TotpEnrollPage (`src/pages/TotpEnrollPage.tsx`)

Full-page interstitial — mirrors PasswordChangePage exactly.

**Primary focal point:** amber Shield icon + page heading (`text-xl font-semibold`) — eye enters top-center of the card, follows to subtitle, then to the QR code image or confirmation input field. The card is the sole focus container on a neutral `bg-gray-50` fullscreen background; no competing elements.

**Layout:**
- Outer: `min-h-screen flex items-center justify-center bg-gray-50 p-4`
- Card: `bg-white rounded-xl border border-gray-200 shadow-sm p-8 w-full max-w-md`
- Header row: `flex items-center gap-3 mb-6` with `<Shield className="w-6 h-6 text-amber-500" />` + `<h1 className="text-xl font-semibold text-gray-900">`
- Subtitle: `text-sm text-gray-600 mb-6`

**Step 1 — QR Code display:**
- QR code image: `<img src={qrDataUrl} alt="TOTP QR code" className="w-40 h-40 mx-auto my-4" />`
- Manual key section: collapsible `<details>` with `<summary className="text-sm text-blue-600 cursor-pointer">` + monospace key display `className="text-sm font-mono font-medium bg-gray-100 rounded px-2 py-1 break-all"`
- 6-digit confirmation input: `type="text"` `inputMode="numeric"` `maxLength={9}` `placeholder="123456 or recovery code"` — same input styling as existing OTP field: `w-full border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500`
- Submit button: `w-full bg-blue-600 text-white rounded-lg px-4 py-2 text-sm font-medium hover:bg-blue-700 disabled:opacity-50`

**Step 2 — Recovery codes display (shown after successful TOTP confirmation):**
- Rendered as `RecoveryCodesPanel` (see below) before final session JWT is issued
- Must include "I have saved my recovery codes" checkbox gate before "Continue" button is enabled

### RecoveryCodesPanel (`src/components/RecoveryCodesPanel.tsx`)

Displayed inline within TotpEnrollPage after enrollment confirmation.

**Layout:**
- Container: `bg-gray-50 border border-gray-200 rounded-lg p-4 my-4`
- Heading: `text-sm font-semibold text-gray-900 mb-3`
- Warning banner: `bg-amber-50 border border-amber-200 rounded-lg px-3 py-2 text-sm text-amber-800 mb-3`
- Code grid: `grid grid-cols-2 gap-2` of `<code className="text-sm font-mono font-medium bg-white border border-gray-200 rounded px-2 py-1 text-center text-gray-800">`
- Action row: `flex gap-2 mt-3`
  - Copy all button: `text-sm text-blue-600 hover:text-blue-800 border border-blue-200 rounded px-3 py-1`
  - Download button: `text-sm text-blue-600 hover:text-blue-800 border border-blue-200 rounded px-3 py-1`
- Checkbox gate: `flex items-start gap-2 mt-4` with `<input type="checkbox">` + `<label className="text-sm text-gray-700">`
- Continue button: disabled until checkbox checked; when enabled: same blue primary button styling

### AdminPage "Reset 2FA" button (addition to existing `src/pages/AdminPage.tsx`)

Inserted in the user row actions column, visible only when `u.totpEnabled === true`.

**Button:**
- Icon: `<ShieldOff className="w-4 h-4" />` from lucide-react (new icon import)
- Visible text label: `Reset 2FA` — rendered as `<span>` after the icon, matching the text+icon pattern of other AdminPage action buttons
- Full button content: `<ShieldOff className="w-4 h-4" /><span>Reset 2FA</span>`
- Style: `text-amber-600 hover:text-amber-800 inline-flex items-center gap-1` — matches amber warning tone for a non-delete but security-impacting action
- Positioned: before the existing delete button in the actions cell
- `title={t('adminResetTotp')}` for tooltip

**Confirmation dialog:**
- Native `window.confirm()` — matches existing `handleDelete` pattern in AdminPage.tsx exactly
- Copy: "Reset 2FA for {username}? They will be required to re-enroll on next login."

---

## Copywriting Contract

All copy is bilingual (DE + EN) per established i18n pattern in `src/i18n/translations.ts`. New translation keys are defined here; executor must add them to translations.ts.

| Element | Key | EN Copy | DE Copy |
|---------|-----|---------|---------|
| Page heading | `totpEnrollTitle` | Set Up Two-Factor Authentication | Zwei-Faktor-Authentisierung einrichten |
| Page subtitle | `totpEnrollSubtitle` | Scan the QR code with your authenticator app (e.g. Google Authenticator, Authy), then enter the 6-digit code to confirm. | Scannen Sie den QR-Code mit Ihrer Authenticator-App (z.B. Google Authenticator, Authy) und geben Sie den 6-stelligen Code zur Bestätigung ein. |
| Manual key toggle | `totpEnrollManualKey` | Can't scan? Enter key manually | Kein Scan möglich? Schlüssel manuell eingeben |
| Confirmation input label | `totpEnrollCodeLabel` | Confirmation Code | Bestätigungscode |
| Confirmation input placeholder | `totpEnrollCodePlaceholder` | 6-digit code | 6-stelliger Code |
| Primary CTA (confirm enrollment) | `totpEnrollSubmit` | Activate Two-Factor Authentication | Zwei-Faktor-Authentisierung aktivieren |
| Loading state | (inline) | Activating… | Wird aktiviert… |
| Recovery codes heading | `totpRecoveryCodesTitle` | Save Your Recovery Codes | Wiederherstellungscodes speichern |
| Recovery codes warning | `totpRecoveryCodesWarning` | These codes will not be shown again. Store them somewhere safe. If you lose access to your authenticator app, you can use one of these codes to log in — each code can only be used once. | Diese Codes werden nicht erneut angezeigt. Bewahren Sie sie an einem sicheren Ort auf. Falls Sie keinen Zugriff auf Ihre Authenticator-App haben, können Sie einen dieser Codes zur Anmeldung verwenden — jeder Code kann nur einmal verwendet werden. |
| Copy all button | `totpCopyAllCodes` | Copy all codes | Alle Codes kopieren |
| Download button | `totpDownloadCodes` | Download as .txt | Als .txt herunterladen |
| Checkbox label | `totpSavedCodesCheckbox` | I have saved my recovery codes in a safe place | Ich habe meine Wiederherstellungscodes sicher gespeichert |
| Continue CTA (after checkbox) | `totpEnrollDone` | Continue to App | Zur App |
| Invalid code error | `totpEnrollErrorInvalid` | Invalid code. Please check your authenticator app and try again. | Ungültiger Code. Bitte überprüfen Sie Ihre Authenticator-App und versuchen Sie es erneut. |
| Expired token error | `totpEnrollErrorExpired` | Your enrollment session has expired. Please log in again. | Ihre Anmeldesitzung ist abgelaufen. Bitte melden Sie sich erneut an. |
| Generic error | `totpEnrollErrorGeneric` | An error occurred. Please try again. | Ein Fehler ist aufgetreten. Bitte versuchen Sie es erneut. |
| Recovery code used warning (on login) | `totpRecoveryCodeUsedWarning` | You logged in with a recovery code. {N} recovery codes remaining. Contact your administrator to reset 2FA and re-enroll if needed. | Sie haben sich mit einem Wiederherstellungscode angemeldet. Noch {N} Wiederherstellungscodes verbleibend. Wenden Sie sich an Ihren Administrator, um 2FA zurückzusetzen und sich bei Bedarf erneut anzumelden. |
| Admin reset button tooltip | `adminResetTotp` | Reset 2FA | 2FA zurücksetzen |
| Admin reset confirmation | `adminResetTotpConfirm` | Reset 2FA for {username}? They will be required to re-enroll on next login. | 2FA für {username} zurücksetzen? Sie müssen sich bei der nächsten Anmeldung erneut registrieren. |
| Admin reset success | `adminResetTotpSuccess` | 2FA reset for {username} | 2FA für {username} zurückgesetzt |
| LoginPage OTP placeholder update | `loginOtpPlaceholder` | Code or recovery code | Code oder Wiederherstellungscode |

**Empty state:** Not applicable to this phase — TotpEnrollPage is always reached with a pending enrollment token (no empty state possible). RecoveryCodesPanel always shows exactly 10 codes.

**Destructive actions in this phase:**
1. Admin "Reset 2FA" — native `window.confirm()` dialog using `adminResetTotpConfirm` copy (matches existing `handleDelete` pattern). No inline confirmation UI needed.

---

## Interaction States

### TotpEnrollPage

| State | Trigger | Visual |
|-------|---------|--------|
| Loading (QR fetch) | On mount, calling `POST /api/auth/totp/enroll` | Spinner in QR code area: `w-40 h-40 mx-auto my-4 flex items-center justify-center bg-gray-100 rounded` |
| QR loaded | API returns `qrDataUrl` | `<img>` renders with QR code |
| Manual key expanded | User clicks `<details>` summary | Monospace key block reveals below toggle |
| Code entered, submitting | User submits form | Button shows "Activating…", `disabled` attribute set |
| Code invalid | API returns 400 | `<p className="text-sm text-red-600">` below input — `totpEnrollErrorInvalid` |
| Token expired | API returns 401 | `<p className="text-sm text-red-600">` — `totpEnrollErrorExpired` |
| Enrollment success | API returns 200 + `recoveryCodes` | Step 1 form replaced with `RecoveryCodesPanel` |

### RecoveryCodesPanel

| State | Trigger | Visual |
|-------|---------|--------|
| Initial | Panel shown | 10 codes displayed, checkbox unchecked, Continue button `disabled:opacity-50` |
| Codes copied | Copy all clicked | Button label changes to "Copied!" for 2 seconds (matches existing `copied` translation key) |
| File downloaded | Download clicked | Browser native download, no UI change |
| Checkbox checked | User ticks checkbox | Continue button becomes active (`bg-blue-600` full opacity) |
| Continue clicked | Submit | AuthContext receives session JWT, page unmounts |

### AdminPage Reset 2FA

| State | Trigger | Visual |
|-------|---------|--------|
| Button visible | `u.totpEnabled === true` | `<ShieldOff>` + "Reset 2FA" amber button appears in actions cell |
| Button hidden | `u.totpEnabled !== true` | Button not rendered |
| Confirm dismissed | User cancels `window.confirm()` | No change |
| Reset in progress | `DELETE` sent | Button disabled (optimistic: no spinner, matches delete pattern) |
| Reset success | API returns 200 | `loadUsers()` called, table refreshes, button disappears for that user |
| Reset error | API returns non-200 | `alert()` with `mutationErrorGeneric` copy (matches existing error handling pattern) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No third-party UI registries used in this phase. All new UI is hand-rolled Tailwind following existing codebase patterns.

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|---------------|
| QR code image alt text | `alt="TOTP QR code — scan with your authenticator app"` |
| Manual key | Rendered in `<code>` element inside `<details>`; keyboard accessible |
| OTP input | `aria-label={t('totpEnrollCodeLabel')}` |
| Recovery codes | Each `<code>` element has readable content; grid uses `role="list"` with `role="listitem"` |
| Checkbox | `<label>` explicitly associated via `htmlFor` |
| Continue button | `aria-disabled="true"` when checkbox unchecked (in addition to `disabled`) |
| Admin Reset 2FA button | `aria-label={t('adminResetTotp') + ' ' + u.username}` — includes username for screen reader context |
| Focus management | On TotpEnrollPage mount: `autoFocus` on OTP input (matches PasswordChangePage `autoFocus` on first field) |

---

## File Paths

| File | Action | Purpose |
|------|--------|---------|
| `src/pages/TotpEnrollPage.tsx` | CREATE | Full-page enrollment interstitial |
| `src/components/RecoveryCodesPanel.tsx` | CREATE | Recovery codes display with copy/download/checkbox |
| `src/pages/AdminPage.tsx` | MODIFY | Add "Reset 2FA" button + `handleResetTotp` handler |
| `src/pages/LoginPage.tsx` | MODIFY | Remove `maxLength={6}`, update placeholder to `loginOtpPlaceholder` |
| `src/i18n/translations.ts` | MODIFY | Add 20 new TOTP i18n keys (DE + EN) |
| `src/context/AuthContext.tsx` | MODIFY | Add `requiresTotpEnrollment`, `pendingEnrollToken` state; add enrollment functions |
| `src/App.tsx` | MODIFY | Add `if (requiresTotpEnrollment) return <TotpEnrollPage />` gate in AppRoutes |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
