---
phase: 15
plan: 04
type: execute
wave: 3
depends_on: [15-02, 15-03]
files_modified:
  - src/pages/AdminPage.tsx
autonomous: false
requirements: [SEC-04]
user_setup: []

must_haves:
  truths:
    - "AdminPage user table shows a 'Reset 2FA' button in the actions cell ONLY for users with totpEnabled === true (D-04)"
    - "Clicking Reset 2FA triggers a native window.confirm() dialog with the user's name in the copy"
    - "On confirm, a DELETE to /api/auth/users/:username/totp is sent via authFetch"
    - "On 200 response, loadUsers() refreshes the table and the button disappears for that row"
    - "On non-200 response, an alert shows the generic mutation error (matches existing handleDelete pattern)"
    - "Button uses ShieldOff icon + visible 'Reset 2FA' text label (per 15-UI-SPEC accessibility contract)"
  artifacts:
    - path: "src/pages/AdminPage.tsx"
      provides: "handleResetTotp handler and Reset 2FA button in user row actions"
      contains: "handleResetTotp"
  key_links:
    - from: "AdminPage user row actions cell"
      to: "handleResetTotp"
      via: "onClick when u.totpEnabled === true"
      pattern: "handleResetTotp"
    - from: "handleResetTotp"
      to: "DELETE /api/auth/users/:username/totp"
      via: "authFetch"
      pattern: "/totp"
---

<objective>
Add the admin-facing 2FA reset affordance to AdminPage: a ShieldOff icon button with visible 'Reset 2FA' label, visible only for enrolled users, wired to Plan 02's DELETE endpoint with a native confirm dialog.

Purpose: Closes D-04 from CONTEXT.md — gives admins the documented recovery path for users locked out of TOTP. Depends on Plan 02's backend endpoint and Plan 03's i18n keys.
Output: AdminPage.tsx with a new handler + new button in the actions cell + new useState-free addition (mirrors existing handleDelete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-totp-2fa/15-CONTEXT.md
@.planning/phases/15-totp-2fa/15-UI-SPEC.md

@src/pages/AdminPage.tsx
@src/services/authHeaders.ts

Existing handleDelete pattern (AdminPage.tsx line 241+ — to replicate exactly):

  const handleDelete = async (targetUsername: string) => {
    if (!window.confirm(`Delete user ${targetUsername}?`)) return;
    try {
      const resp = await authFetch(`/api/auth/users/${encodeURIComponent(targetUsername)}`, {
        method: 'DELETE',
      });
      if (!resp.ok) {
        alert(t('mutationErrorGeneric'));
        return;
      }
      await loadUsers();
    } catch {
      alert(t('mutationErrorGeneric'));
    }
  };

Existing delete button rendering (AdminPage.tsx line 553):

  <button onClick={() => void handleDelete(u.username)} className="text-red-500 hover:text-red-700 ..."> ... </button>

UserRecord (already in scope via Task 2 of Plan 01): `u.totpEnabled?: boolean` — conditional render key.

i18n keys already present (added by Plan 03 Task 3): `adminResetTotp`, `adminResetTotpConfirm`, `adminResetTotpSuccess`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add handleResetTotp handler + Reset 2FA button in AdminPage</name>
  <files>src/pages/AdminPage.tsx</files>
  <read_first>
    - src/pages/AdminPage.tsx (full file — locate handleDelete at line 241+, and its button invocation at line 553)
    - .planning/phases/15-totp-2fa/15-UI-SPEC.md (AdminPage "Reset 2FA" button section)
    - .planning/phases/15-totp-2fa/15-CONTEXT.md (D-04)
  </read_first>
  <action>
Edit 1 — add `ShieldOff` to the existing lucide-react import in AdminPage.tsx. Find the import line (grep `from 'lucide-react'`) and add `ShieldOff` to the destructured list, preserving alphabetical order if the existing list is sorted.

Edit 2 — add the handler immediately after the existing `handleDelete` function (around line 258). Use the exact same structure:

  const handleResetTotp = async (targetUsername: string) => {
    const confirmCopy = t('adminResetTotpConfirm').replace('{username}', targetUsername);
    if (!window.confirm(confirmCopy)) return;
    try {
      const resp = await authFetch(
        `/api/auth/users/${encodeURIComponent(targetUsername)}/totp`,
        { method: 'DELETE' },
      );
      if (!resp.ok) {
        alert(t('mutationErrorGeneric'));
        return;
      }
      await loadUsers();
    } catch {
      alert(t('mutationErrorGeneric'));
    }
  };

Edit 3 — add the button in the actions cell of the user row. Find the existing delete button (around line 553) and insert the Reset 2FA button IMMEDIATELY BEFORE it, so in the DOM order the Reset 2FA button precedes Delete:

  {u.totpEnabled === true && (
    <button
      type="button"
      onClick={() => void handleResetTotp(u.username)}
      className="text-amber-600 hover:text-amber-800 inline-flex items-center gap-1"
      title={t('adminResetTotp')}
      aria-label={`${t('adminResetTotp')} ${u.username}`}
    >
      <ShieldOff className="w-4 h-4" />
      <span>{t('adminResetTotp')}</span>
    </button>
  )}

The button is omitted entirely (not rendered) when `u.totpEnabled !== true` — no disabled/greyed state.

Edit 4 — no other changes. Do NOT alter existing delete/reset-password/edit handlers. Do NOT modify the loadUsers function. Do NOT add new state.
  </action>
  <verify>
    <automated>grep -q "ShieldOff" src/pages/AdminPage.tsx &amp;&amp; grep -q "handleResetTotp" src/pages/AdminPage.tsx &amp;&amp; grep -q "/totp" src/pages/AdminPage.tsx &amp;&amp; grep -q "u.totpEnabled === true" src/pages/AdminPage.tsx &amp;&amp; grep -q "adminResetTotp" src/pages/AdminPage.tsx &amp;&amp; npx tsc --noEmit -p tsconfig.app.json 2>&amp;1 | tee /tmp/tsc-04 &amp;&amp; ! grep -q "AdminPage.tsx" /tmp/tsc-04 &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/AdminPage.tsx imports `ShieldOff` from 'lucide-react' (grep on the import line)
    - src/pages/AdminPage.tsx contains `const handleResetTotp = async` function definition
    - handleResetTotp calls `authFetch` with URL containing `/totp` and method `DELETE`
    - handleResetTotp uses `t('adminResetTotpConfirm').replace('{username}', targetUsername)` for the confirm dialog
    - The button JSX contains `u.totpEnabled === true &&` as the conditional guard (grep literal)
    - The button contains `<ShieldOff className="w-4 h-4" />` element
    - The button contains `<span>{t('adminResetTotp')}</span>` for the visible label
    - The button uses `className="text-amber-600 hover:text-amber-800 inline-flex items-center gap-1"`
    - The button has `aria-label={`${t('adminResetTotp')} ${u.username}`}` (template string form)
    - `npx tsc --noEmit -p tsconfig.app.json` reports zero errors from AdminPage.tsx
    - `npm run build` exits 0
    - Existing handleDelete function is unchanged (grep for the original `handleDelete = async` signature still matches)
  </acceptance_criteria>
  <done>Admin can click Reset 2FA on enrolled users; button is hidden for unenrolled users; full UI contract per 15-UI-SPEC met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end verification of TOTP enrollment + admin reset</name>
  <files>n/a — manual UI verification only</files>
  <what-built>
    Complete Phase 15 vertical slice: backend (Plans 01, 02) + frontend enrollment UX (Plan 03) + admin reset (this plan).
    All automated tests pass. This checkpoint verifies the visual/interaction behaviour that cannot be asserted with vitest: QR code rendering, authenticator app compatibility, recovery codes download, i18n rendering in both DE and EN, admin button visibility toggle.
  </what-built>
  <action>
Run the numbered how-to-verify steps below against a dev server. This is a MANUAL verification — do not attempt to automate. Record each step's outcome (pass/fail) in the resulting SUMMARY.md.
  </action>
  <how-to-verify>
1. Start dev server: `npm run dev`. Confirm it compiles with zero errors.
2. Ensure `public/settings.yaml` has `twoFactorEnabled: true`. If it currently has false, temporarily set to true and restart server.
3. Open the app at http://localhost:5173 and log in as an UNENROLLED user (a user with no `totpEnabled` field in data/users.json — any standard seeded user will be unenrolled on first run).
4. Expected: after password login, the page renders the TotpEnrollPage (amber Shield icon, "Set Up Two-Factor Authentication" heading, QR code image, "Can't scan?" details toggle, 6-digit input).
5. Open Google Authenticator (or Authy) on phone and scan the QR code. Confirm the app shows a 6-digit rotating code labelled "EyeMatics:<username>".
6. Enter the 6-digit code in the input and click "Activate Two-Factor Authentication".
7. Expected: page transitions to the RecoveryCodesPanel showing exactly 10 codes in a 2-column grid, amber warning banner, Copy all + Download buttons, checkbox, Continue button (disabled).
8. Click "Copy all codes" — verify clipboard contains 10 codes separated by newlines; button briefly says "Copied!".
9. Click "Download as .txt" — verify a `recovery-codes.txt` downloads with 10 lines.
10. Try to click Continue without checking the box — it must be disabled (no navigation).
11. Check the checkbox — Continue becomes enabled. Click it.
12. Expected: normal app loads (landing page).
13. Log out, log back in with the same user. Enter a current 6-digit code from the authenticator app at the OTP step. Expected: login succeeds.
14. Log out. Log in again. At the OTP step, enter one of the saved recovery codes instead of a TOTP code. Expected: login succeeds; a page-level or toast notice references that a recovery code was used (inspect response body in Network tab for `recoveryCodeUsed: true`).
15. Log in as admin. Visit /admin. Find the test user's row. Expected: a "Reset 2FA" button with ShieldOff icon and amber text is visible in the actions cell (BEFORE the Delete button).
16. Click "Reset 2FA". Expected: native confirm dialog appears with copy "Reset 2FA for <username>? They will be required to re-enroll on next login."
17. Click OK. Expected: table refreshes; the Reset 2FA button disappears for that user (because totpEnabled is now cleared).
18. Log out of admin. Log in as the reset user. Expected: TotpEnrollPage appears again (re-enrollment forced).
19. Switch language to DE (header toggle). Re-run steps 3–4 briefly to confirm German strings render (heading "Zwei-Faktor-Authentisierung einrichten", subtitle German text, button "Zwei-Faktor-Authentisierung aktivieren").
20. Verify audit log: as admin, visit /audit. Search for recent events. Expected: entries with action = `totp-enrolled`, `totp-recovery-used`, `totp-reset` for the operations performed above; `totpSecret` and `enrollToken` fields MUST appear as [REDACTED] in the event bodies.
  </how-to-verify>
  <verify>Human confirms all 20 steps pass. Any failing step blocks phase closure.</verify>
  <done>All 20 verification steps pass; user types "approved" to close the phase.</done>
  <resume-signal>Type "approved" to mark Phase 15 complete, or describe any failing step (which number, what you saw, what you expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser→AdminPage DOM | Admin session JWT attached by authFetch; role check is server-side |
| AdminPage button visibility | Controlled by client-side `u.totpEnabled === true`; server still enforces admin role on DELETE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-23 | Elevation of Privilege | Non-admin manually crafts DELETE /users/:username/totp | mitigate | Server handler (Plan 02 Task 3) rejects `req.auth.role !== 'admin'` with 403 regardless of client-side button visibility |
| T-15-24 | Tampering | Admin triggers reset on wrong username by typo | mitigate | Task 1: confirm dialog includes the target username in the copy (`adminResetTotpConfirm` template); audit event `totp-reset` records the actor+target |
| T-15-25 | Repudiation | Admin disputes a reset action | mitigate | Plan 02 Task 3 writes audit event `totp-reset` with actor username (from req.auth.sub) and target (from req.params.username); immutable SQLite audit log |
| T-15-26 | Spoofing | Rendering the button to unenrolled users misleads admin | mitigate | Task 1: `u.totpEnabled === true` conditional ensures the button is rendered only for users with active TOTP enrollment; hidden entirely otherwise |

Residual risks: None beyond those already accepted in Plans 01–03.
</threat_model>

<verification>
- `npm run build` exits 0
- `npx tsc --noEmit -p tsconfig.app.json` reports zero errors
- `npm run lint` passes
- `npm test` full suite exits 0 (449 baseline + all Phase 15 tests added across Plans 01–03)
- Human checkpoint (Task 2) signed off
</verification>

<success_criteria>
- Reset 2FA button visible in AdminPage for enrolled users only
- Clicking it triggers confirm dialog, DELETEs, refreshes table, clears enrollment
- After reset, affected user is gated back into TotpEnrollPage on next login
- End-to-end enrollment + authentication + admin reset flow verified manually
- Audit events totp-enrolled, totp-recovery-used, totp-reset all observable with redacted secrets
- Both DE and EN i18n strings render correctly
</success_criteria>

<output>
After completion, create `.planning/phases/15-totp-2fa/15-04-SUMMARY.md` with:
- Diff stats for AdminPage.tsx (expected ~+35 lines)
- Human checkpoint outcome (pass/fail and notes)
- Phase 15 close-out checklist:
  - SEC-04 requirement satisfied (per-user TOTP, ±1 window, QR enrollment, admin reset)
  - SEC-05 requirement satisfied (bcrypt-hashed recovery codes, burned on use)
  - Static otpCode fallback preserved for unenrolled users
  - Audit trail redacts totpSecret and enrollToken
- Total new test count added to the suite across the phase
</output>
