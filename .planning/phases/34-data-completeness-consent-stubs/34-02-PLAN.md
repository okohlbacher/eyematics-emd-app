---
phase: 34-data-completeness-consent-stubs
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - shared/patientCases.ts
  - src/services/fhirLoader.ts
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "A stub Patient (zero Observations) never appears in extractPatientCases output"
    - "extractCenters().patientCount counts only non-stub (clinical) patients"
    - "A countRawPatients helper returns the total Patient count (stubs + full) for the denominator"
  artifacts:
    - path: "shared/patientCases.ts"
      provides: "stub-exclusion filter at extractPatientCases chokepoint (D-03)"
      contains: "observationsByRef"
    - path: "src/services/fhirLoader.ts"
      provides: "stub-excluding patientCount (D-04) + countRawPatients (D-09)"
      exports: ["countRawPatients", "extractCenters"]
  key_links:
    - from: "src/pages/LandingPage.tsx"
      to: "countRawPatients"
      via: "import from ../services/fhirLoader (wired in Plan 04)"
      pattern: "export function countRawPatients"
    - from: "extractPatientCases"
      to: "observationsByRef"
      via: "filter patients with >0 observations before map"
      pattern: "observationsByRef\\.get"
---

<objective>
Enforce stub isolation at the single chokepoint and add the denominator helper — the pure-logic core of DASH-01. This is the H2 stub-isolation invariant.

Purpose: One edit guarantees every clinical consumer (cohort builder, outcomes/trajectories, quality, case detail, charts, server aggregation) is stub-free by construction (D-03). The per-center count stays clinically meaningful (D-04). A separate raw count provides the completeness denominator (D-09).
Output: Modified `extractPatientCases` (D-03 filter), modified `extractCenters` `patientCount` (D-04), and a new exported `countRawPatients` helper (D-09). Turns the `tests/stubIsolation.test.ts` assertions from Plan 01 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-data-completeness-consent-stubs/34-CONTEXT.md
@.planning/phases/34-data-completeness-consent-stubs/34-RESEARCH.md
@.planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md

<interfaces>
<!-- Current chokepoint structure (verified). Executor must preserve ordering. -->
From shared/patientCases.ts (extractPatientCases, line 65):
- `resourcesOfType<Observation>(bundles, 'Observation')` builds `observations` (line 68).
- `groupBySubject(observations)` builds `observationsByRef: Map<string, Observation[]>` (line 75).
- `return patients.map((pat) => { const ref = `Patient/${pat.id}`; ... })` (line 81).
- The filter MUST be inserted AFTER line 75 (observationsByRef built) and BEFORE the map on line 81.

From src/services/fhirLoader.ts (extractCenters, line 51):
- `resourcesOfType<Patient>(bundles, 'Patient')` builds `patients` (line 53).
- `orgPatients = patients.filter(p => p.meta?.source === org.id)` (line 59).
- `patientCount: orgPatients.length` (line 67) — current ALL-patient count.
- `resourcesOfType` helper exists (lines 40-48); reuse, do not duplicate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add D-03 stub-exclusion filter to extractPatientCases</name>
  <read_first>
    - shared/patientCases.ts (the file being modified — read lines 65-99, the full extractPatientCases body; note groupBySubject on line 75 and the patients.map on line 81)
    - tests/stubIsolation.test.ts (the Plan 01 scaffold — this task makes its extractPatientCases assertion green)
    - .planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md (§"shared/patientCases.ts — stub exclusion at extractPatientCases (D-03)" — exact insertion point and filter predicate)
  </read_first>
  <behavior>
    - A Patient with zero Observations (a stub) is absent from `extractPatientCases` output.
    - A Patient with ≥1 Observation (full patient) is present in `extractPatientCases` output, unchanged in shape.
    - No tag/profile/extension is consulted — recognition is purely the absence of Observations (D-01).
  </behavior>
  <action>
    In `extractPatientCases` (`shared/patientCases.ts`), insert a filter between the `observationsByRef = groupBySubject(observations)` line and the `return patients.map(...)` line. The filter keeps only patients where `(observationsByRef.get(`Patient/${pat.id}`) ?? []).length > 0`, assigning the result to a `clinicalPatients` const, and changes the map to iterate `clinicalPatients` instead of `patients`.

    Do NOT move or remove the existing `resourcesOfType('Observation')` call or the `groupBySubject(observations)` call — `observationsByRef` must already be built before the filter (preserve current ordering). Do NOT add any tag/profile/extension check (D-01 forbids it). This is the ONLY place stubs are filtered — do not add per-consumer exclusion anywhere else (research Anti-Patterns).
  </action>
  <verify>
    <automated>npx vitest run tests/stubIsolation.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "clinicalPatients" shared/patientCases.ts` returns a match; the `return` maps over `clinicalPatients`, not `patients`.
    - `grep -c "tag\|profile\|extension" shared/patientCases.ts` shows NO stub-discriminator use (D-01).
    - In `tests/stubIsolation.test.ts`, the test "stub patients are excluded from extractPatientCases output" passes: output does NOT contain `pat-stub-001` and DOES contain `pat-full-001`.
    - `npx vitest run tests/stubIsolation.test.ts` shows the extractPatientCases assertion GREEN.
  </acceptance_criteria>
  <done>extractPatientCases drops zero-Observation patients at the single chokepoint; the stub-isolation regression test is green for the extraction assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exclude stubs from patientCount (D-04) and add countRawPatients (D-09)</name>
  <read_first>
    - src/services/fhirLoader.ts (the file being modified — read lines 40-71, resourcesOfType + extractCenters; confirm Patient/Organization imports at lines 1-7)
    - src/types/fhir.ts (confirm Observation is exported via the barrel for the import)
    - tests/stubIsolation.test.ts (the Plan 01 scaffold — this task makes its extractCenters patientCount assertion green)
    - .planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md (§"src/services/fhirLoader.ts — two changes" — patientCount replacement + countRawPatients helper)
  </read_first>
  <behavior>
    - `extractCenters(bundles).find(c => c.id === org).patientCount` counts only patients with ≥1 Observation (stubs excluded).
    - `countRawPatients(bundles)` returns the total count of ALL Patient resources across bundles (stubs + full).
    - `countRawPatients` does NOT apply an extra center filter (server already restricts bundles by req.auth.centers — D-09 / research Pitfall 3).
  </behavior>
  <action>
    Two changes in `src/services/fhirLoader.ts`:

    (1) D-04: In `extractCenters`, add `const observations = resourcesOfType<Observation>(bundles, 'Observation');` after the existing `patients` extraction (line ~53), importing the `Observation` type from `'../types/fhir'`. Change `patientCount: orgPatients.length` (line 67) to count only `orgPatients` that have at least one Observation whose `subject.reference === `Patient/${p.id}``. Use the same structural rule as D-03 (absence of Observations = stub). For per-center O(N) efficiency you may pre-index observation subject references into a Set before the `orgs.map`.

    (2) D-09: Add and export a new function `countRawPatients(bundles: FhirBundle[]): number` after `extractCenters` that sums, across all bundles, the count of entries whose `resource.resourceType === 'Patient'`. It takes NO center-filter argument — the bundles passed in are already server-filtered by the user's permitted centers (research Pitfall 3, Assumption A2). Add a doc comment citing D-09.

    Do NOT change `loadAllBundles`, `invalidateBundleCache`, or the existing `resourcesOfType` helper signature.
  </action>
  <verify>
    <automated>npx vitest run tests/stubIsolation.test.ts 2>&1 | tail -15; grep -c "export function countRawPatients" src/services/fhirLoader.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function countRawPatients" src/services/fhirLoader.ts` returns a match.
    - In `tests/stubIsolation.test.ts`, "extractCenters patientCount excludes stubs (D-04)" passes: `org-test` `patientCount === 1`.
    - `countRawPatients` over a bundle with 1 full + 1 stub patient returns `2` (add/extend a test assertion in tests/stubIsolation.test.ts if the Plan 01 scaffold did not include one).
    - `countRawPatients` signature takes only `bundles` (no center-id parameter).
    - `npx tsc -b --noEmit` clean; `npx vitest run tests/stubIsolation.test.ts` fully green.
  </acceptance_criteria>
  <done>patientCount excludes stubs, countRawPatients returns the raw total, and the full stubIsolation test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle data → clinical extraction | Stub Patient resources must not cross into any clinical surface |
| bundle data → per-user site restriction | Denominator must respect server-side center filtering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | `extractPatientCases` (`shared/patientCases.ts`) | mitigate | Single-chokepoint filter drops zero-Observation patients (D-03); `tests/stubIsolation.test.ts` asserts no stub id ever appears in output. This is the H2 invariant. |
| T-34-04 | Information Disclosure | `countRawPatients` denominator | mitigate | Helper operates only on the `bundles` already filtered server-side by `req.auth.centers`; takes no client-supplied center override (Pitfall 3 / A2). Per-center `patientCount` stays clinical-only (D-04), so no stub demographic leaks via center counts. |
| T-34-05 | Tampering | per-consumer filtering drift | accept | By construction all consumers flow through `extractPatientCases`; adding exclusion elsewhere is explicitly forbidden, removing drift risk. |
</threat_model>

<verification>
- `npx vitest run tests/stubIsolation.test.ts` fully green (D-03 + D-04 + countRawPatients).
- `npx tsc -b --noEmit` clean.
- `npm run test:ci` shows no regression in the broader suite (cohort/outcomes/quality tests still green — they flow through the modified extractPatientCases).
</verification>

<success_criteria>
- Stubs are excluded from `extractPatientCases` and from `extractCenters().patientCount`.
- `countRawPatients` exported and returns the stub-inclusive total.
- No per-consumer stub-exclusion logic added; no tag/profile/extension discriminator used.
- Existing 619+ baseline stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-data-completeness-consent-stubs/34-02-SUMMARY.md`
</output>
