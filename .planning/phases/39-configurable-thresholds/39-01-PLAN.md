---
phase: 39-configurable-thresholds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/settingsService.ts
  - config/settings.yaml
  - src/config/clinicalThresholds.ts
  - src/utils/qualityMetrics.ts
  - shared/thresholdConfig.ts
  - server/settingsApi.ts
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]

must_haves:
  truths:
    - "clinicalThresholds.ts critical constants resolve from AppSettings (settings.yaml), not literals"
    - "qualityMetrics.ts plausibility ranges resolve from AppSettings (settings.yaml), not literals"
    - "settings.yaml carries thresholds.* and plausibility.* with the current production defaults"
    - "Server can read settings-derived threshold values at request time (getThresholdSettings)"
    - "Non-admin cannot WRITE thresholds (PUT /api/settings stays admin-only); schema rejects malformed threshold/plausibility values"
  artifacts:
    - path: "src/services/settingsService.ts"
      provides: "thresholds + plausibility on AppSettings/DEFAULTS"
      contains: "thresholds"
    - path: "config/settings.yaml"
      provides: "persisted thresholds.* and plausibility.* defaults"
      contains: "plausibility"
    - path: "shared/thresholdConfig.ts"
      provides: "threshold shape + validation usable by client and server"
      contains: "validateThresholds"
    - path: "server/settingsApi.ts"
      provides: "server-side getThresholdSettings + PUT schema validation for thresholds/plausibility"
      contains: "getThresholdSettings"
  key_links:
    - from: "src/config/clinicalThresholds.ts"
      to: "src/services/settingsService.ts"
      via: "getSettings() accessor"
      pattern: "getSettings"
    - from: "src/utils/qualityMetrics.ts"
      to: "src/services/settingsService.ts"
      via: "getSettings() accessor in range helpers"
      pattern: "getSettings"
    - from: "server/settingsApi.ts"
      to: "config/settings.yaml"
      via: "call-time fs read in getThresholdSettings"
      pattern: "getThresholdSettings"
---

<objective>
Move all clinical thresholds and plausibility ranges into `config/settings.yaml` behind the established `settingsService` / `AppSettings` / `DEFAULTS` pattern, and replace the hardcoded constants in `clinicalThresholds.ts` and `qualityMetrics.ts` with settings-backed accessors. Add a shared validation helper and a server-side call-time reader so Plans 02 (UI) and 03 (server parity) can build on a single source of truth.

Purpose: CFG-01/CFG-02 require these values to be admin-configurable and centralized; CFG-03 requires the server to read the same values. This plan is the foundation both downstream plans depend on.
Output: extended `AppSettings`/`DEFAULTS`, populated `settings.yaml`, settings-backed accessors at every consuming call site, `shared/thresholdConfig.ts` validation, server `getThresholdSettings` + PUT schema validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-configurable-thresholds/39-CONTEXT.md
@CLAUDE.md

<read_first>
@src/services/settingsService.ts
@config/settings.yaml
@src/config/clinicalThresholds.ts
@src/utils/qualityMetrics.ts
@server/settingsApi.ts
@src/services/ttlConversion.ts
</read_first>

<interfaces>
<!-- Existing AppSettings shape (settingsService.ts) is flat + namespaced.
     therapyInterrupterDays, therapyBreakerDays, crtImplausibleThresholdUm ALREADY exist
     at the top level and ALREADY map to settings.yaml keys + applyFilters options.
     Do NOT duplicate those three; group the NEW values under thresholds.* and plausibility.*. -->

Current flat fields (keep as-is, already wired into applyFilters options):
  therapyInterrupterDays: number   // default 120
  therapyBreakerDays: number       // default 365
  crtImplausibleThresholdUm: number // default 400

NEW namespaces to add to AppSettings + DEFAULTS:
  thresholds?: {
    criticalCrtUm: number;       // default 400  (was CRITICAL_CRT_THRESHOLD)
    criticalVisus: number;       // default 0.1  (was CRITICAL_VISUS_THRESHOLD)
    criticalIopMmHg: number;     // default 21   (was CRITICAL_IOP_THRESHOLD)
    visusJump: number;           // default 0.3  (was VISUS_JUMP_THRESHOLD)
  }
  plausibility?: {
    visusMin: number; visusMax: number;  // defaults 0, 2.0
    crtMin: number;   crtMax: number;     // defaults 100, 800
    iopMin: number;   iopMax: number;     // defaults 5, 40
  }

Server-side call-time read pattern to mirror — server/settingsApi.ts:166 getAuthSettings():
  reads SETTINGS_FILE via fs at call time (NOT boot-cached), merges over defaults, returns plain object.

Client validation pattern to mirror — src/services/ttlConversion.ts validateTtl():
  pure function returning a string union of error codes or 'ok'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared threshold config shape + validation helper</name>
  <files>shared/thresholdConfig.ts, shared/__tests__/thresholdConfig.test.ts (or tests/thresholdConfig.test.ts per repo test layout)</files>
  <read_first>shared/patientCases.ts (ApplyFiltersOptions), src/services/ttlConversion.ts (validateTtl pattern)</read_first>
  <behavior>
    - validateThresholds returns 'ok' for the production defaults (criticalCrtUm 400, criticalVisus 0.1, criticalIopMmHg 21, visusJump 0.3)
    - returns a specific error code when criticalVisus is outside (0, 2], criticalCrtUm <= 0, criticalIopMmHg <= 0, or visusJump <= 0
    - validatePlausibility returns 'ok' for defaults (visus 0..2, crt 100..800, iop 5..40)
    - returns a specific error code when any min >= its max, or any bound is non-finite/negative (visus min may be 0)
    - both helpers are pure: no fs, no fetch, no browser globals (importable from server tsconfig)
  </behavior>
  <action>Create `shared/thresholdConfig.ts` exporting two TS interfaces (ThresholdConfig, PlausibilityConfig matching the `<interfaces>` shapes — camelCase per D-05), the default literals `THRESHOLD_DEFAULTS` / `PLAUSIBILITY_DEFAULTS`, and two pure validators `validateThresholds(t): ThresholdError | 'ok'` and `validatePlausibility(p): PlausibilityError | 'ok'` modeled on `validateTtl` (string-union error codes, no thrown control flow for validation results — but follow throw-only D-03 for genuinely exceptional inputs). Keep the file free of browser/node globals so server code can import it. Write the test file first (RED), covering the cases in `<behavior>` using `queryByText`-free pure assertions (this is a logic module, not RTL).</action>
  <verify>
    <automated>npx vitest run thresholdConfig 2>&1 | grep -qE "passed|PASS"</automated>
  </verify>
  <done>thresholdConfig.ts exports interfaces, defaults, and both validators; its test passes; no browser/node imports in the file (grep -v '^//' shared/thresholdConfig.ts | grep -Eqc "window|document|node:fs" returns 0).</done>
</task>

<task type="auto">
  <name>Task 2: Extend AppSettings/DEFAULTS + settings.yaml; replace client constants with accessors</name>
  <files>src/services/settingsService.ts, config/settings.yaml, src/config/clinicalThresholds.ts, src/utils/qualityMetrics.ts</files>
  <read_first>src/services/settingsService.ts, config/settings.yaml, src/config/clinicalThresholds.ts, src/utils/qualityMetrics.ts, shared/thresholdConfig.ts</read_first>
  <action>In `settingsService.ts`, add `thresholds?` and `plausibility?` to the `AppSettings` interface and populate `DEFAULTS` from the `THRESHOLD_DEFAULTS`/`PLAUSIBILITY_DEFAULTS` re-exported by `shared/thresholdConfig.ts` (single source — do not re-type the numbers). The existing deep `merge` already handles nested namespaces, so no merge changes are needed. In `config/settings.yaml`, add a `thresholds:` block (criticalCrtUm 400, criticalVisus 0.1, criticalIopMmHg 21, visusJump 0.3) and a `plausibility:` block (visusMin 0, visusMax 2.0, crtMin 100, crtMax 800, iopMin 5, iopMax 40) with brief comments referencing CFG-01/CFG-02. In `clinicalThresholds.ts`, keep the four exported names as thin shims (per Claude's discretion in CONTEXT) but have them READ from `getSettings()` — convert the four `export const` literals into `export const X = () => getSettings().thresholds?.criticalCrtUm ?? <default>` accessor getters, OR (preferred to minimize churn) export them as functions and update the four consumers (`QualityCaseDetail.tsx`, `VisusCrtChart.tsx`, `ClinicalParametersRow.tsx`, `useCaseData.ts`) to call them. Choose the lower-churn option and apply it consistently; leave CHART_COLORS untouched. In `qualityMetrics.ts`, change the three private range helpers (`isVisusInRange`, `isCrtInRange`, `isIopInRange`) to read bounds from `getSettings().plausibility ?? PLAUSIBILITY_DEFAULTS` instead of hardcoded literals. Do NOT introduce env vars (settings.yaml is the single source).</action>
  <verify>
    <automated>grep -Eqc "thresholds|plausibility" config/settings.yaml && grep -q "getSettings" src/config/clinicalThresholds.ts && grep -q "getSettings" src/utils/qualityMetrics.ts && npx vitest run clinicalThresholds qualityMetrics 2>&1 | grep -qiE "passed|PASS|no test files"</automated>
  </verify>
  <done>settings.yaml has thresholds.* and plausibility.*; clinicalThresholds.ts and qualityMetrics.ts both read from getSettings(); no literal 400/0.1/21/0.3 or 0..2/100..800/5..40 remain as the source of truth (the only literals are the shared defaults fallback); all four threshold consumers compile.</done>
</task>

<task type="auto">
  <name>Task 3: Server-side getThresholdSettings + PUT schema validation</name>
  <files>server/settingsApi.ts</files>
  <read_first>server/settingsApi.ts (getAuthSettings + validateSettingsSchema + PUT handler), shared/thresholdConfig.ts</read_first>
  <action>Add an exported `getThresholdSettings()` to `server/settingsApi.ts` that mirrors `getAuthSettings()` exactly: read `SETTINGS_FILE` via `fs` at call time (NOT boot-cached), `yaml.load`, merge `parsed.thresholds`/`parsed.plausibility` over `THRESHOLD_DEFAULTS`/`PLAUSIBILITY_DEFAULTS` imported from `shared/thresholdConfig.js`, and return `{ thresholds, plausibility }`. On any read/parse failure, return defaults (no throw — parity with getAuthSettings catch). Extend `validateSettingsSchema` to reject malformed `thresholds`/`plausibility` blocks: when present, each must be an object and each numeric field finite, with criticalVisus in (0,2], positive criticalCrtUm/criticalIopMmHg/visusJump, and every plausibility min < its max (reuse the shared validators by calling `validateThresholds`/`validatePlausibility` and mapping a non-'ok' result to a 400 message string). Leave the existing admin-only PUT guard (`req.auth?.role !== 'admin'` → 403) and the non-admin GET strip logic unchanged — thresholds are operational params returned to non-admins per CONTEXT W5; do NOT add them to the GET strip allowlist.</action>
  <verify>
    <automated>grep -q "export function getThresholdSettings" server/settingsApi.ts && grep -q "validateThresholds\|validatePlausibility" server/settingsApi.ts && npx vitest run settingsApi 2>&1 | grep -qiE "passed|PASS|no test files"</automated>
  </verify>
  <done>getThresholdSettings reads at call time and returns merged defaults; validateSettingsSchema rejects out-of-range thresholds/plausibility with a 400; admin-only PUT guard and non-admin GET behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PUT /api/settings | threshold/plausibility values written to settings.yaml cross here (untrusted body) |
| settings.yaml → server read | getThresholdSettings reads operator-controlled file at request time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | PUT /api/settings (thresholds write) | mitigate | Existing admin-only guard (`req.auth?.role !== 'admin'` → 403) is preserved unchanged; thresholds inherit it — no new write path added. Verified by grep that the guard line is untouched. |
| T-39-02 | Tampering | validateSettingsSchema | mitigate | New threshold/plausibility blocks validated server-side via shared validators; out-of-range values (e.g. negative CRT, inverted min/max) rejected with 400 before write — a tampered body cannot persist nonsensical clinical bounds. |
| T-39-03 | Information Disclosure | GET /api/settings (non-admin) | accept | Thresholds/plausibility are operational params (like auth TTLs), not secrets — returned to non-admins per CONTEXT W5 decision. They are NOT added to the strip allowlist. No PII or secret material involved. |
</threat_model>

<verification>
- `npm run test:ci` stays green (902 baseline).
- `npx vitest run thresholdConfig clinicalThresholds qualityMetrics settingsApi` passes.
- `grep` gates in each task confirm settings-backed accessors and server reader exist.
</verification>

<success_criteria>
- AppSettings/DEFAULTS carry thresholds.* and plausibility.*; settings.yaml persists the production defaults.
- clinicalThresholds.ts and qualityMetrics.ts read every threshold/range from getSettings() — no remaining literal source of truth.
- shared/thresholdConfig.ts provides reusable validation for both client (Plan 02) and server (Plan 03).
- server/settingsApi.ts exposes getThresholdSettings() and validates threshold/plausibility writes; admin-only PUT unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/39-configurable-thresholds/39-01-SUMMARY.md`
</output>
