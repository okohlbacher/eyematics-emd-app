---
phase: 01-production-express-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/issueApi.ts
  - server/settingsApi.ts
  - tsconfig.server.json
  - package.json
autonomous: true
requirements:
  - BACK-02
  - BACK-05
  - BACK-06

must_haves:
  truths:
    - "issueApiHandler and settingsApiHandler are exported standalone functions with raw Node http types"
    - "Vite dev plugins delegate to the extracted handlers (npm run dev unchanged)"
    - "tsconfig.server.json exists for server type-checking"
    - "npm scripts build:server, build:all, and start are defined"
    - "express, http-proxy-middleware, @types/express, and tsx are installed"
  artifacts:
    - path: "server/issueApi.ts"
      provides: "Extracted issueApiHandler function + unchanged Vite plugin wrapper"
      exports: ["issueApiHandler", "issueApiPlugin"]
    - path: "server/settingsApi.ts"
      provides: "Extracted settingsApiHandler function + unchanged Vite plugin wrapper"
      exports: ["settingsApiHandler", "settingsApiPlugin"]
    - path: "tsconfig.server.json"
      provides: "Server-only type-check config (noEmit)"
      contains: "noEmit"
    - path: "package.json"
      provides: "Updated scripts and dependencies"
      contains: "build:all"
  key_links:
    - from: "server/issueApi.ts:issueApiPlugin"
      to: "server/issueApi.ts:issueApiHandler"
      via: "server.middlewares.use(issueApiHandler)"
      pattern: "middlewares\\.use\\(issueApiHandler\\)"
    - from: "server/settingsApi.ts:settingsApiPlugin"
      to: "server/settingsApi.ts:settingsApiHandler"
      via: "server.middlewares.use(settingsApiHandler)"
      pattern: "middlewares\\.use\\(settingsApiHandler\\)"
---

<objective>
Extract reusable handler functions from Vite plugins and set up build infrastructure for the Express server.

Purpose: Per D-01, existing raw Node http handlers must be wrapped — not rewritten. This plan lifts the middleware callback body from each Vite plugin into a named exported function. The Vite plugin wrapper calls that function. The Express server (Plan 02) will also call that function. This ensures zero code duplication and unchanged `npm run dev` behavior (BACK-06).

Output: Extracted handlers in issueApi.ts and settingsApi.ts, tsconfig.server.json, updated package.json with dependencies and scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-production-express-backend/01-CONTEXT.md
@.planning/phases/01-production-express-backend/01-RESEARCH.md

@server/issueApi.ts
@server/settingsApi.ts
@server/utils.ts
@vite.config.ts
@package.json
@tsconfig.json
@tsconfig.node.json

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From server/utils.ts:
```typescript
export function readBody(req: import('http').IncomingMessage, maxSize?: number): Promise<string>;
export function validateAuth(req: import('http').IncomingMessage, requiredRole?: string): { username: string; role: string } | null;
export function sendError(res: import('http').ServerResponse, status: number, publicMessage: string, internalError?: unknown): void;
```

From vite.config.ts:
```typescript
import { issueApiPlugin } from './server/issueApi'
import { settingsApiPlugin } from './server/settingsApi'
// Both must continue to export Plugin-returning functions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract handler functions from Vite plugins</name>
  <files>server/issueApi.ts, server/settingsApi.ts</files>
  <read_first>server/issueApi.ts, server/settingsApi.ts, server/utils.ts, vite.config.ts</read_first>
  <action>
Per D-01: wrap existing raw Node http handlers, do NOT rewrite as Express handlers.

**server/issueApi.ts:**
1. Create a new exported function `issueApiHandler(req: IncomingMessage, res: ServerResponse, next: () => void): void` that contains the entire middleware callback body currently inside `configureServer`. Move the body verbatim — the `if (req.method === 'POST' && req.url === '/api/issues')` block, the `GET /api/issues/export` block, the `GET /api/issues` block, and the `next()` fallthrough. Add `import type { IncomingMessage, ServerResponse } from 'node:http';` at the top.
2. Simplify the `issueApiPlugin()` function to delegate: `server.middlewares.use(issueApiHandler);`
3. Keep ALL other code unchanged (ensureDir, loadAllIssues, validateIssueBody, FEEDBACK_DIR, imports).

**server/settingsApi.ts:**
1. Create a new exported function `settingsApiHandler(req: IncomingMessage, res: ServerResponse, next: () => void): void` that contains the entire middleware callback body currently inside `configureServer`. Move the body verbatim — the `GET /api/settings` block, the `PUT /api/settings` block, and the `next()` fallthrough. Add `import type { IncomingMessage, ServerResponse } from 'node:http';` at the top.
2. Simplify the `settingsApiPlugin()` function to delegate: `server.middlewares.use(settingsApiHandler);`
3. Keep ALL other code unchanged (SETTINGS_FILE, validateSettingsSchema, imports).

Both files must continue to export their Plugin-returning functions (issueApiPlugin, settingsApiPlugin) so vite.config.ts imports remain unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json && echo "Type check passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function issueApiHandler" server/issueApi.ts` returns 1
    - `grep -c "export function issueApiPlugin" server/issueApi.ts` returns 1
    - `grep -c "export function settingsApiHandler" server/settingsApi.ts` returns 1
    - `grep -c "export function settingsApiPlugin" server/settingsApi.ts` returns 1
    - `grep "middlewares.use(issueApiHandler)" server/issueApi.ts` matches
    - `grep "middlewares.use(settingsApiHandler)" server/settingsApi.ts` matches
    - `grep "IncomingMessage" server/issueApi.ts` matches (raw Node types in signature)
    - `grep "IncomingMessage" server/settingsApi.ts` matches (raw Node types in signature)
    - `npx tsc --noEmit -p tsconfig.node.json` passes (Vite plugin types still valid)
    - `npm run dev` starts without errors (manual spot check)
  </acceptance_criteria>
  <done>Both issueApiHandler and settingsApiHandler are exported standalone functions with IncomingMessage/ServerResponse signatures. Vite plugin wrappers delegate to them. Type checking passes. Vite dev mode is unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies, create tsconfig.server.json, update npm scripts</name>
  <files>package.json, tsconfig.server.json</files>
  <read_first>package.json, tsconfig.json, tsconfig.node.json</read_first>
  <action>
Per D-07 (Claude's discretion on build strategy) and research recommendations:

**Install dependencies:**
```bash
npm install express http-proxy-middleware
npm install --save-dev @types/express tsx
```

**Create tsconfig.server.json** (type-check only, NOT added to root tsconfig.json references per Pitfall 3):
```json
{
  "compilerOptions": {
    "target": "es2023",
    "lib": ["ES2023"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "types": ["node"],
    "skipLibCheck": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["server"]
}
```

IMPORTANT: Do NOT include `vite.config.ts` in the server tsconfig `include` — it is already covered by tsconfig.node.json. Do NOT add tsconfig.server.json as a reference in tsconfig.json.

**Update package.json scripts** (preserve existing scripts, add new ones):
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:server": "tsc --noEmit -p tsconfig.server.json",
    "build:all": "npm run build && npm run build:server",
    "start": "tsx server/index.ts",
    "lint": "eslint .",
    "preview": "vite preview"
  }
}
```

The `start` script uses tsx to run server/index.ts directly (no compile step). The `build:all` script runs the Vite build + server type check. The `build:server` script type-checks server code only.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const ok = p.scripts['build:all'] && p.scripts.start && p.scripts['build:server'] && p.dependencies.express && p.dependencies['http-proxy-middleware'] && p.devDependencies.tsx && p.devDependencies['@types/express']; console.log(ok ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "build:all" package.json` matches
    - `grep "build:server" package.json` matches
    - `grep '"start"' package.json` matches and contains "tsx server/index.ts"
    - `grep "express" package.json` shows express in dependencies
    - `grep "http-proxy-middleware" package.json` shows in dependencies
    - `grep "tsx" package.json` shows in devDependencies
    - `grep "@types/express" package.json` shows in devDependencies
    - `test -f tsconfig.server.json` succeeds
    - `grep "noEmit" tsconfig.server.json` matches
    - `grep -c "tsconfig.server" tsconfig.json` returns 0 (NOT added as reference)
    - `npx tsc --noEmit -p tsconfig.server.json` passes
  </acceptance_criteria>
  <done>Dependencies installed. tsconfig.server.json exists for server type-checking (noEmit, standalone). Package.json has build:all, build:server, and start scripts. Server type check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan only restructures existing code; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tsconfig.server.json | accept | Type-check config only; no runtime impact. Misconfiguration caught by CI. |
| T-01-02 | Info Disclosure | Handler extraction | accept | No new code paths; same auth checks preserved verbatim from existing handlers. |
</threat_model>

<verification>
1. `npx tsc --noEmit -p tsconfig.node.json` passes (Vite plugins still valid)
2. `npx tsc --noEmit -p tsconfig.server.json` passes (server code type-checks)
3. `npm run dev` starts and serves the app (BACK-06)
4. `grep "export function issueApiHandler" server/issueApi.ts` confirms extraction
5. `grep "export function settingsApiHandler" server/settingsApi.ts` confirms extraction
</verification>

<success_criteria>
- Handler functions extracted with raw Node http types (IncomingMessage/ServerResponse)
- Vite plugin wrappers delegate to extracted handlers
- `npm run dev` behavior identical to before (BACK-06)
- All dependencies installed (express, http-proxy-middleware, @types/express, tsx)
- tsconfig.server.json provides server-only type checking
- npm scripts ready: build:all, build:server, start
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-express-backend/01-01-SUMMARY.md`
</output>
