---
phase: 41-docquality-multicenter-ux
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - src/pages/QualityPage.tsx
  - src/i18n/translations.ts
  - tests/QualityPage.test.tsx
autonomous: true
requirements: [QUAL-022, QUAL-023]

must_haves:
  truths:
    - "Applying a time-range filter on the quality page updates the Grundgesamtheit (denominator) to only cases within that range"
    - "Summary card counts (unchecked / in_progress / reviewed) and percentages respect the active time range"
    - "Absolute counts are clearly visible on the overview without hover or secondary navigation"
    - "The time-range control reuses the DocQualityPage TimeRange pattern (6m / 1y / all)"
  artifacts:
    - path: "src/pages/QualityPage.tsx"
      provides: "Time-range state driving scoped denominator + summary counts; prominent absolute counts"
      contains: "TimeRange"
    - path: "tests/QualityPage.test.tsx"
      provides: "RTL coverage that denominator + counts shrink with a narrower time range"
  key_links:
    - from: "src/pages/QualityPage.tsx"
      to: "src/utils/qualityMetrics.ts (filterCasesByTimeRange)"
      via: "import + apply to scopedCases before count derivation"
      pattern: "filterCasesByTimeRange"
    - from: "QualityPage SummaryCard total prop"
      to: "time-filtered case set length"
      via: "Grundgesamtheit = timeScopedCases.length"
      pattern: "timeScoped"
---

<objective>
Add a time-range filter to the quality page so the Grundgesamtheit (population
denominator) and all summary counts reflect the active range, and make the
absolute counts clearly discoverable on the overview.

Purpose: QUAL-022 — the denominator must reflect the time-range filter (today it
is a static `cases.length` with no time state). QUAL-023 — the user reported they
"cannot find the absolute values" though SummaryCards render count+%; increase
prominence and clear labeling of the absolute counts.
Output: time-range state on QualityPage reusing the DocQualityPage TimeRange
pattern; denominator + counts driven by the time-scoped set; clearer absolute-count
labeling on SummaryCards; RTL test proving the denominator shrinks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docquality-multicenter-ux/41-CONTEXT.md
@CLAUDE.md
@.planning/phases/41-docquality-multicenter-ux/41-01-PLAN.md

<read_first>
- src/pages/QualityPage.tsx — `scopedCases` useMemo (lines 119-129) is the base set
  (cohort-scoped or full `cases`); `caseStatus` (139-154) and `statusCounts` (156-160)
  derive from scopedCases; SummaryCard component (lines 29-42) renders count, optional
  percentage (count/total), and label; summary cards row (lines 293-298) passes
  `total={scopedCases.length}` — this is the Grundgesamtheit that currently ignores time.
  NOTE: 41-01 (Wave 1) rewires the center filter on this same file — build on its result.
- src/pages/DocQualityPage.tsx — the reuse pattern: `timeRange` useState<TimeRange>('all')
  (line 57), `filterCasesByTimeRange` applied per center (DocQualityPage.tsx via buildCenterMetrics),
  a Filter toggle button (lines 143-153) and a time-range badge in the header (lines 133-137),
  QualityFilterBar with `showCenterFilter={false}` (lines 176-185).
- src/utils/qualityMetrics.ts — `TimeRange = '6m' | '1y' | 'all'` (line 36);
  `filterCasesByTimeRange(cases, range)` (line 105) drops observations outside the cutoff
  but KEEPS the case records. IMPORTANT: for a denominator-by-case the plan must decide
  case-level inclusion (see Task 1 action) — filterCasesByTimeRange alone does not drop cases.
- src/components/doc-quality/QualityFilterBar.tsx — reusable time-range button group
  (`showCenterFilter={false}` renders time-range only). Reuse it; do NOT modify it.
- src/i18n/translations.ts — docQualityTimeRange (line 556), docQualityLast6Months,
  docQualityLastYear, docQualityAllTime keys already exist; reuse them. Add new quality
  keys for the absolute-count label here.
</read_first>

<interfaces>
From src/utils/qualityMetrics.ts:
```typescript
export type TimeRange = '6m' | '1y' | 'all';
export function cutoffDate(range: TimeRange): Date | null;          // null for 'all'
export function filterCasesByTimeRange(cases: PatientCase[], range: TimeRange): PatientCase[];
```
From src/components/doc-quality/QualityFilterBar.tsx (reusable, showCenterFilter optional):
```typescript
export interface QualityFilterBarProps {
  timeRange: TimeRange;
  onTimeRangeChange: (range: TimeRange) => void;
  selectedCenter: string;          // pass '' / unused when showCenterFilter=false
  onCenterChange: (centerId: string) => void;
  centerOptions: CenterOption[];
  showCenterFilter?: boolean;      // pass false to render time-range only
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Time-range state drives Grundgesamtheit + summary counts</name>
  <files>src/pages/QualityPage.tsx, tests/QualityPage.test.tsx</files>
  <behavior>
    - Default time range is 'all'; denominator equals the cohort-scoped case count (unchanged from today).
    - Selecting '6m' or '1y' reduces the Grundgesamtheit denominator to only cases that have at least one observation within the cutoff window (a case with no in-window observations is excluded from the denominator).
    - statusCounts (unchecked / in_progress / reviewed) and the displayed percentages recompute against the time-scoped set.
    - Switching back to 'all' restores the full scoped denominator.
  </behavior>
  <action>
    Add `const [timeRange, setTimeRange] = useState<TimeRange>('all')` to QualityPage
    (import TimeRange + filterCasesByTimeRange + cutoffDate from src/utils/qualityMetrics)
    — QUAL-022, reusing the DocQualityPage pattern per CONTEXT D "QUAL-022 time filter".
    Introduce a `timeScopedCases` useMemo derived from `scopedCases`: a case is INCLUDED when
    `timeRange === 'all'` OR it has at least one observation with `effectiveDateTime >= cutoffDate(timeRange)`
    (use cutoffDate; reuse filterCasesByTimeRange's date logic — note that function trims
    observations but does not drop cases, so do the case-level inclusion test here).
    Drive `caseStatus`, `statusCounts`, `therapyStatuses`, `centerNames`, and `filteredCases`
    off `timeScopedCases` instead of `scopedCases` so EVERYTHING respects the range. Change
    the SummaryCard `total={scopedCases.length}` props (lines 294-296) to
    `total={timeScopedCases.length}` so the Grundgesamtheit denominator reflects the filter.
    Render the time-range control by reusing QualityFilterBar with `showCenterFilter={false}`
    (place it near the cohort-scope/export row, lines 258-290), wiring `timeRange` +
    `setTimeRange`. camelCase TS (D-05); throw-only (D-03); async/await if touched (D-04).
    Write tests/QualityPage.test.tsx (RTL, NO jest-dom — `queryByText().not.toBeNull()` /
    `.toBeNull()`): mount with a fixture where some cases have only old observations; assert
    the rendered denominator drops when switching to '6m' and restores on 'all'. Wrap in the
    same providers other page tests use (DataContext/LanguageContext — grep an existing page
    test for the wrapper).
  </action>
  <verify>
    <automated>npx vitest run tests/QualityPage.test.tsx</automated>
  </verify>
  <done>timeRange state exists; denominator + statusCounts shrink with '6m'/'1y' and restore on 'all'; QualityFilterBar time-range control renders on the page; test proves the denominator change.</done>
</task>

<task type="auto">
  <name>Task 2: Make absolute counts prominent + clearly labeled (QUAL-023)</name>
  <files>src/pages/QualityPage.tsx, src/i18n/translations.ts</files>
  <action>
    QUAL-023 — the user could not find absolute values though SummaryCard renders count+%.
    Increase prominence of the absolute count in the SummaryCard component (lines 29-42):
    keep the bold count as the primary visual element and ensure the percentage is clearly
    secondary (smaller / muted, which it already is) AND add an explicit absolute-count
    context so the number reads as "N of TOTAL". Concretely: when `total` is provided, render
    a clear "N / total" form (e.g. count prominently, with a `{count} / {total}` sublabel) so
    the absolute numerator AND denominator are both visible without hover. Add a visible
    Grundgesamtheit label/line near the summary cards row (lines 293-298) showing the active
    population size, e.g. `t('qualityPopulationLabel'): {timeScopedCases.length}`. Add i18n keys
    `qualityPopulationLabel` (de "Grundgesamtheit" / en "Population") and, if used,
    `qualityOfTotal` (de "von" / en "of") to translations.ts in the quality block. Do not change
    the underlying counts — this is presentation/labeling only (CONTEXT default: increase
    prominence + clear labels). Keep the Datenqualität overview as the target surface.
  </action>
  <verify>
    <automated>npm run lint && npx vitest run tests/QualityPage.test.tsx</automated>
  </verify>
  <done>Absolute count and population total are both visible on the overview without hover; Grundgesamtheit label present; i18n keys added for de+en; counts unchanged in value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client time-range state → displayed counts | Purely presentational; no server data crosses; counts derived from already-authorized cases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Information Disclosure | timeScopedCases denominator | accept | Time filtering only narrows already-authorized client data; cannot reveal unauthorized centers (server already filtered bundles). No new boundary crossed. |
| T-41-05 | Tampering | timeRange state | accept | Client-only display state; worst case shows a wider local count, never unauthorized data. Low risk. |
</threat_model>

<verification>
- `npm run lint` clean.
- `npx vitest run tests/QualityPage.test.tsx` green.
- Manual: open /quality, toggle time-range to 6m → Grundgesamtheit denominator and percentages drop; absolute counts visibly labeled without hover.
</verification>

<success_criteria>
- Grundgesamtheit + summary counts respect the active time range (QUAL-022).
- Absolute counts clearly discoverable without hover/secondary nav (QUAL-023).
- Time-range control reuses the DocQualityPage / QualityFilterBar pattern.
- `npm run test:ci` stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/41-docquality-multicenter-ux/41-02-SUMMARY.md`
</output>
