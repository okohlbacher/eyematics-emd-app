---
phase: 36-architecture-review-compaction
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - shared/types/fhir.ts
  - tests/stubIsolation.test.ts
  - tests/augmentReferenceBundles.test.ts
  - .planning/reviews/v1.11-arch-review/APPROVED.md
autonomous: false
requirements: [ARCH-03]
must_haves:
  truths:
    - "The user has explicitly approved which compaction items to apply before any product code is modified"
    - "Only approved remediations are applied; deferred items are recorded, not applied"
    - "npm run test:ci passes with zero failures after remediation"
    - "npm run knip reports no NEW dead code versus the documented baseline (the Encounter/Consent unused exports are resolved)"
    - "npm run lint passes with the two Phase 34 import-sort warnings resolved"
    - "No behavior regressions are detectable"
  artifacts:
    - path: ".planning/reviews/v1.11-arch-review/APPROVED.md"
      provides: "Record of which COMPACTION-PLAN items the user approved vs deferred"
      contains: "Approved"
  key_links:
    - from: ".planning/reviews/v1.11-arch-review/APPROVED.md"
      to: ".planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md"
      via: "approved items reference compaction finding IDs"
      pattern: "F-[0-9]"
---

<objective>
Present the prioritized compaction plan to the user for explicit approval, then apply ONLY the approved remediations while keeping the test, knip, and lint gates green and introducing no behavior regressions.

Purpose: ARCH-03 (approved compaction remediations applied with no behavior regressions; `npm run test:ci` green, `npm run knip` no new dead code, `npm run lint` passing). The approval gate is the one mandatory human checkpoint of this phase — compaction is irreversible-ish and must not be auto-applied wholesale (36-CONTEXT.md locked decision).

Output: An APPROVED.md record of the user's decisions, the applied source/test changes for the approved items (including the known baseline: resolve `Encounter`/`Consent` unused exports and the two import-sort lint warnings), all three gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-architecture-review-compaction/36-CONTEXT.md
@.planning/reviews/v1.11-arch-review/FINDINGS.md
@.planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md
@.planning/phases/36-architecture-review-compaction/36-01-SUMMARY.md

<interfaces>
From shared/types/fhir.ts (the known baseline dead-code items, added Phase 34, intentionally not wired into PatientCase per D-01/D-02):
- `export interface Encounter extends FhirResource` (line ~94) — knip: unused exported type
- `export interface Consent extends FhirResource` (line ~102) — knip: unused exported type

Resolution options for the baseline (decide per CODEX/user direction at remediation time):
- drop the `export` keyword if the type is only used internally, OR
- consume the types in the generator/loader typing so the export is referenced, OR
- add a justified `knip` ignore entry in knip.json.
</interfaces>

<notes>
- Gate baselines (from 36-CONTEXT.md, confirmed this session): `npm run test:ci` 901/901 green; `npm run knip` reports 2 unused exported types (Encounter/Consent) + 4 config hints; `npm run lint` exits 0 with 2 import-sort warnings in tests/stubIsolation.test.ts and tests/augmentReferenceBundles.test.ts.
- The 2 lint warnings are auto-fixable: `npm run lint:fix` (eslint --fix) resolves simple-import-sort/imports.
- `files_modified` lists the KNOWN baseline-remediation files. If the user approves additional APPLY items that touch other files, those files are modified too — but stay strictly within the approved set. Record any such files in the SUMMARY.
- Conservative posture (locked): large module restructures / SoC refactors default to DEFER unless the user explicitly opts in at the checkpoint. Do NOT apply DEFER items.
- "No NEW dead code" means: relative to the documented baseline, knip introduces nothing new AND the baseline Encounter/Consent items are resolved (so the unused-exports count drops to 0 or is justified by an ignore).
</notes>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approval gate — user selects which compaction items to apply</name>
  <read_first>
    - .planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md (the prioritized APPLY/DEFER recommendations)
    - .planning/reviews/v1.11-arch-review/FINDINGS.md (severity context for each item)
    - .planning/phases/36-architecture-review-compaction/36-01-SUMMARY.md (finding counts + APPLY/DEFER split)
  </read_first>
  <decision>Which compaction remediations should be applied in this milestone-closing phase?</decision>
  <context>
    Compaction is irreversible-ish and must not be auto-applied wholesale (36-CONTEXT.md locked decision). Present the COMPACTION-PLAN.md items grouped by the recommended APPLY (low-risk: dead code, safe de-duplication, lint/knip hygiene) and DEFER (large module restructures / SoC refactors). The default conservative posture recommends approving the APPLY set and deferring the rest. The two known baseline items (resolve Encounter/Consent unused exports; fix the two import-sort lint warnings) are part of the recommended APPLY set and are required to satisfy the "no new dead code / lint passes" gate regardless.
  </context>
  <options>
    <option id="apply-recommended">
      <name>Apply the recommended APPLY set, defer the rest</name>
      <pros>Conservative, matches locked risk posture; closes the milestone with hygiene fixes and safe de-duplication; deferred refactors recorded as future debt</pros>
      <cons>Larger structural improvements wait for a future milestone</cons>
    </option>
    <option id="apply-custom">
      <name>Apply a custom subset (user names specific F-NN items to apply and/or opt into specific DEFER items)</name>
      <pros>User controls exactly what lands; can opt into a specific high-value refactor</pros>
      <cons>Opting into a restructure raises regression risk; must keep all gates green</cons>
    </option>
    <option id="apply-baseline-only">
      <name>Apply only the baseline hygiene items (Encounter/Consent + lint warnings)</name>
      <pros>Minimal change; guaranteed to satisfy the gates; lowest risk to close the milestone</pros>
      <cons>Leaves CODEX-surfaced de-duplication/dead-code findings unaddressed (recorded as debt)</cons>
    </option>
  </options>
  <action>
    Present the COMPACTION-PLAN.md items to the user grouped by APPLY/DEFER with finding IDs and file references. Capture the user's selection. Use the Write tool to create `.planning/reviews/v1.11-arch-review/APPROVED.md` recording, per finding ID, the disposition (Approved-to-apply / Deferred) and the user's verbatim instruction where given. This file is the authoritative apply list for Task 2 — do not apply anything not listed as Approved here.
  </action>
  <acceptance_criteria>
    - `test -f .planning/reviews/v1.11-arch-review/APPROVED.md` succeeds
    - `grep -qi "Approved" .planning/reviews/v1.11-arch-review/APPROVED.md` succeeds
    - `grep -qE "F-[0-9]" .planning/reviews/v1.11-arch-review/APPROVED.md` succeeds (decisions reference finding IDs)
  </acceptance_criteria>
  <resume-signal>Select: apply-recommended, apply-custom (name the F-NN items), or apply-baseline-only</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply approved remediations and keep all three gates green</name>
  <files>shared/types/fhir.ts, tests/stubIsolation.test.ts, tests/augmentReferenceBundles.test.ts (plus any additional files named in APPROVED.md)</files>
  <read_first>
    - .planning/reviews/v1.11-arch-review/APPROVED.md (the authoritative apply list from Task 1)
    - .planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md (file references and category per item)
    - shared/types/fhir.ts (the Encounter/Consent unused-export baseline item)
    - knip.json (entry/project globs and ignore list, in case an item resolves via a justified ignore)
  </read_first>
  <action>
    Apply ONLY the items marked Approved-to-apply in APPROVED.md, using the Edit tool per file (never heredoc). For the known baseline items: resolve the `Encounter`/`Consent` unused exports in `shared/types/fhir.ts` by the approach recorded in APPROVED.md (drop `export`, consume in generator/loader typing, or add a justified knip.json ignore) so `npm run knip` no longer reports them; and run `npm run lint:fix` to resolve the two import-sort warnings in `tests/stubIsolation.test.ts` and `tests/augmentReferenceBundles.test.ts`. For any additional approved CODEX items, apply the de-duplication / dead-code removal exactly as scoped in the compaction plan — no opportunistic extra changes beyond the approved set. Do NOT touch any DEFER item. After applying, run the three gates in order and iterate until all pass: `npm run test:ci` (zero failures), `npm run knip` (no new dead code vs. baseline — the Encounter/Consent items resolved, 4 config hints acceptable), `npm run lint` (target 0 warnings). If any approved change would break a behavior test, prefer reverting that single item and recording it as deferred over weakening a test.
  </action>
  <acceptance_criteria>
    - `npm run test:ci` exits 0 with zero test failures
    - `npm run knip 2>&1 | grep -A2 "Unused exported types"` does NOT list `Encounter` or `Consent` (baseline dead code resolved)
    - `npm run lint 2>&1 | grep -c "warning"` returns `0` (import-sort warnings resolved)
    - `git diff --name-only` shows only files listed in APPROVED.md (no out-of-scope edits)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:ci && npm run lint 2>&1 | grep -q "warning" && echo "LINT WARNINGS REMAIN" && exit 1 || (npm run knip 2>&1 | grep -q "Encounter" && echo "BASELINE DEAD CODE REMAINS" && exit 1 || echo PASS)</automated>
  </verify>
  <done>All Approved-to-apply items from APPROVED.md are applied and no DEFER items touched; `npm run test:ci` is green (zero failures); `npm run knip` no longer reports the Encounter/Consent unused exports (no new dead code); `npm run lint` passes with zero warnings; diff is confined to the approved files.</done>
</task>

</tasks>

<verification>
- APPROVED.md records the user's apply/defer decisions per finding ID.
- Applied changes are confined to the approved set; deferred items untouched.
- `npm run test:ci` → 0 failures. `npm run knip` → no Encounter/Consent unused exports, no other new dead code. `npm run lint` → 0 warnings.
</verification>

<success_criteria>
- ARCH-03 satisfied: approved compaction remediations applied with no behavior regressions; test:ci green, knip no new dead code, lint passing.
- The mandatory approval gate was honored before any product code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/36-architecture-review-compaction/36-02-SUMMARY.md` recording which finding IDs were applied vs deferred, the files touched, how the Encounter/Consent baseline was resolved, and the final gate results (test count, knip status, lint warning count). Note any deferred items so Plan 36-03 / MILESTONES.md can record them as carried debt.
</output>
