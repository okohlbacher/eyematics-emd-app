---
phase: 36-architecture-review-compaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reviews/v1.11-arch-review/PROMPT.md
  - .planning/reviews/v1.11-arch-review/codex.stdout
  - .planning/reviews/v1.11-arch-review/FINDINGS.md
  - .planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "A severity-classified findings report exists covering architecture, separation of concerns, and overall design, produced with CODEX"
    - "A prioritized compaction plan exists with concrete file references for each finding"
    - "Product source code is unchanged by this plan (review is read-only)"
  artifacts:
    - path: ".planning/reviews/v1.11-arch-review/codex.stdout"
      provides: "Raw CODEX adversarial review transcript"
      min_lines: 20
    - path: ".planning/reviews/v1.11-arch-review/FINDINGS.md"
      provides: "Severity-classified findings (Critical/High/Medium/Low) with file:line references"
      contains: "## "
    - path: ".planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md"
      provides: "Prioritized remediation items with file references and apply/defer recommendation"
      contains: "Recommendation"
  key_links:
    - from: ".planning/reviews/v1.11-arch-review/FINDINGS.md"
      to: ".planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md"
      via: "each compaction item cites a finding ID"
      pattern: "F-[0-9]"
---

<objective>
Run an adversarial full-codebase architecture review with the CODEX CLI over the application source (server/, src/, shared/, scripts/), classify the findings by severity, and synthesize a prioritized compaction plan with concrete file references per finding.

Purpose: ARCH-01 (adversarial CODEX review producing a severity-classified findings report) and ARCH-02 (distill findings into a prioritized compaction/de-bloat plan with concrete file references). This is the evidence base the user approves against in Plan 36-02.

Output: Three docs under `.planning/reviews/v1.11-arch-review/` — the CODEX prompt, the raw transcript, the classified FINDINGS.md, and the COMPACTION-PLAN.md. NO product source code is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-architecture-review-compaction/36-CONTEXT.md
@.planning/reviews/v1.7-full-review/PROMPT.md

<notes>
- CODEX CLI confirmed available: `codex-cli 0.128.0` at `/opt/homebrew/bin/codex`.
- `codex exec` runs non-interactively. When stdin is piped (or `-` is the prompt arg), the prompt is read from stdin — NEVER shell-interpolate the prompt (the prior full-review precedent pipes the prompt to avoid quoting/injection issues).
- `codex exec` supports `--sandbox read-only` to guarantee the review applies nothing.
- Known baseline inputs to seed the review (from 36-CONTEXT.md): knip reports 2 unused exported types `Encounter`/`Consent` in `shared/types/fhir.ts` (added Phase 34, intentionally not wired per D-01/D-02) plus 4 config hints; lint reports 2 auto-fixable import-sort warnings in `tests/stubIsolation.test.ts` and `tests/augmentReferenceBundles.test.ts`.
- Source size for scoping: server/ 23 files, src/ 81 files, shared/ 9 files, scripts/ 5 files.
- Default risk posture is conservative: dead code, safe de-duplication, lint/knip hygiene = recommend APPLY; large module restructures / SoC refactors = surface but default DEFER.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the CODEX review prompt and run the adversarial review (read-only)</name>
  <files>.planning/reviews/v1.11-arch-review/PROMPT.md, .planning/reviews/v1.11-arch-review/codex.stdout</files>
  <read_first>
    - .planning/reviews/v1.7-full-review/PROMPT.md (established prompt structure: dimensions list, severity scale, file:line output format)
    - .planning/phases/36-architecture-review-compaction/36-CONTEXT.md (the five target dimensions and the seed baseline)
  </read_first>
  <action>
    Use the Write tool to create `.planning/reviews/v1.11-arch-review/PROMPT.md`. The prompt MUST:
    - State the working directory and scope the review to `server/**`, `src/**`, `shared/**`, `scripts/**` (application source only).
    - Ask explicitly for the five compaction dimensions named in 36-CONTEXT.md: dead code, redundant or premature abstractions, duplicated logic, oversized modules, and separation-of-concerns violations.
    - Require each finding to carry a severity (Critical / High / Medium / Low), a `file:line` reference, the problem, and a recommended fix — mirroring the v1.7 PROMPT.md output format.
    - Seed the review with the known baseline: the two unused exported types `Encounter`/`Consent` in `shared/types/fhir.ts` and the two import-sort lint warnings, asking CODEX to confirm or extend.
    - Instruct CODEX that the review is READ-ONLY — produce findings, change nothing.
    Then run the review NON-INTERACTIVELY by piping the prompt to codex via stdin (never as a shell-interpolated argument): redirect the PROMPT.md file into `codex exec --sandbox read-only -` and capture all output to `.planning/reviews/v1.11-arch-review/codex.stdout`. Use the exact form: `codex exec --sandbox read-only - < .planning/reviews/v1.11-arch-review/PROMPT.md > .planning/reviews/v1.11-arch-review/codex.stdout 2>&1`. Allow a long timeout (full-codebase pass). If `--sandbox read-only` is rejected by this codex version, fall back to `codex exec -` with the same stdin piping and note the fallback in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `test -f .planning/reviews/v1.11-arch-review/PROMPT.md` succeeds
    - `grep -qi "read-only" .planning/reviews/v1.11-arch-review/PROMPT.md` succeeds (review declared read-only in prompt)
    - `grep -qi "separation of concerns" .planning/reviews/v1.11-arch-review/PROMPT.md` succeeds (SoC dimension present)
    - `test -s .planning/reviews/v1.11-arch-review/codex.stdout` succeeds (transcript non-empty)
    - `git status --porcelain server src shared scripts | grep -q .` returns FALSE (no product source modified; command must find nothing)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/reviews/v1.11-arch-review/PROMPT.md && test -s .planning/reviews/v1.11-arch-review/codex.stdout && [ -z "$(git status --porcelain server src shared scripts)" ] && echo PASS</automated>
  </verify>
  <done>PROMPT.md exists declaring a read-only review across the five dimensions; codex.stdout captures a non-empty CODEX transcript; no file under server/, src/, shared/, scripts/ is modified.</done>
</task>

<task type="auto">
  <name>Task 2: Classify findings into FINDINGS.md and synthesize COMPACTION-PLAN.md</name>
  <files>.planning/reviews/v1.11-arch-review/FINDINGS.md, .planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md</files>
  <read_first>
    - .planning/reviews/v1.11-arch-review/codex.stdout (the raw CODEX output to distill)
    - .planning/reviews/v1.7-full-review/CLAUDE.md (precedent for a severity-grouped findings doc with F-NN IDs and a Summary)
    - .planning/phases/36-architecture-review-compaction/36-CONTEXT.md (conservative risk posture: low-risk hygiene = recommend APPLY, large refactors = default DEFER)
  </read_first>
  <action>
    Use the Write tool to create `.planning/reviews/v1.11-arch-review/FINDINGS.md`: distill the codex.stdout transcript into a severity-grouped markdown report with sections `## Critical`, `## High`, `## Medium`, `## Low` (omit a section only if CODEX surfaced none — note "(0 findings)"). Assign each finding a stable ID `F-01`, `F-02`, ... and record file:line, problem, and recommended fix. Preserve CODEX's verdicts faithfully — do not soften or invent. End with a `## Summary` listing the top themes.
    Then use the Write tool to create `.planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md`: a prioritized remediation table where each row cites a finding ID (F-NN), the concrete file reference, the compaction category (dead code / redundant abstraction / duplicated logic / oversized module / SoC violation), and a per-item `Recommendation` of APPLY or DEFER. Apply the conservative posture from 36-CONTEXT.md: dead code, safe de-duplication, and lint/knip hygiene default to APPLY; large module restructures / SoC refactors default to DEFER (surfaced for the user to opt in). Explicitly include the two known baseline items (the `Encounter`/`Consent` unused exports and the two import-sort lint warnings) as APPLY rows. Do NOT use scope-reducing language ("v1", "for now", "placeholder") — these are concrete remediation items. The plan is advisory: it applies nothing (that is Plan 36-02 after user approval).
  </action>
  <acceptance_criteria>
    - `grep -qE "^## (Critical|High|Medium|Low)" .planning/reviews/v1.11-arch-review/FINDINGS.md` succeeds (severity sections present)
    - `grep -qE "F-[0-9]" .planning/reviews/v1.11-arch-review/FINDINGS.md` succeeds (finding IDs assigned)
    - `grep -qi "summary" .planning/reviews/v1.11-arch-review/FINDINGS.md` succeeds
    - `grep -qE "F-[0-9]" .planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md` succeeds (compaction items cite findings)
    - `grep -qiE "APPLY|DEFER" .planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md` succeeds (per-item recommendation present)
    - `grep -q "Encounter" .planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md` succeeds (baseline dead-code item carried in)
    - `[ -z "$(git status --porcelain server src shared scripts)" ]` (still no product source modified)
  </acceptance_criteria>
  <verify>
    <automated>grep -qE "^## (Critical|High|Medium|Low)" .planning/reviews/v1.11-arch-review/FINDINGS.md && grep -qE "F-[0-9]" .planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md && grep -qiE "APPLY|DEFER" .planning/reviews/v1.11-arch-review/COMPACTION-PLAN.md && [ -z "$(git status --porcelain server src shared scripts)" ] && echo PASS</automated>
  </verify>
  <done>FINDINGS.md groups CODEX findings by severity with F-NN IDs and a Summary; COMPACTION-PLAN.md lists prioritized remediation items each citing a finding ID, categorized and marked APPLY/DEFER per the conservative posture, including the two known baseline items; no product source modified.</done>
</task>

</tasks>

<verification>
- All three review docs exist under `.planning/reviews/v1.11-arch-review/`.
- FINDINGS.md is severity-classified with file references; COMPACTION-PLAN.md is prioritized with file references and per-item APPLY/DEFER.
- `git status --porcelain server src shared scripts` is empty — the review changed no product code.
</verification>

<success_criteria>
- ARCH-01 satisfied: severity-classified findings report produced with CODEX covering architecture, SoC, and overall design.
- ARCH-02 satisfied: prioritized compaction plan with concrete file references per finding (dead code, redundant abstractions, duplicated logic, oversized modules, SoC violations).
- Review remained read-only on product source.
</success_criteria>

<output>
After completion, create `.planning/phases/36-architecture-review-compaction/36-01-SUMMARY.md` summarizing the CODEX run, the finding counts per severity, and the APPLY-vs-DEFER split in the compaction plan (so Plan 36-02's approval gate has a quick reference).
</output>
