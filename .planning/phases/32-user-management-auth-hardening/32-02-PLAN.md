---
phase: 32-user-management-auth-hardening
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - config/settings.yaml
  - server/settingsApi.ts
  - server/initAuth.ts
  - server/rateLimiting.ts
  - server/authApi.ts
  - src/services/ttlConversion.ts
  - src/services/settingsService.ts
  - src/context/AuthContext.tsx
  - src/pages/LoginPage.tsx
  - src/components/Layout.tsx
  - src/i18n/translations.ts
  - tests/rateLimiting.test.ts
  - tests/settingsApi.test.ts
  - tests/authConfigProvider.test.ts
  - tests/inactivitySettings.test.ts
autonomous: true
requirements: [AUTHCFG-01, AUTHCFG-02, AUTHCFG-03, AUTHCFG-04]
user_setup: []

must_haves:
  truths:
    - "maxLoginAttempts and the lockout cap are read from config/settings.yaml (auth.* sub-object); no hardcoded auth values remain in server/initAuth.ts or server/rateLimiting.ts."
    - "A live updateAuthConfig() change to auth.maxLoginAttempts takes effect on the NEXT login request without a restart (the limiter singleton is rebuilt, not reused from construction time)."
    - "INACTIVITY_TIMEOUT and WARNING_BEFORE are read from config/settings.yaml; no hardcoded client constants remain in src/context/AuthContext.tsx."
    - "WARNING_BEFORE defaults to 3 minutes (warning appears 3 min before logout)."
    - "After a failed login the login page shows the number of attempts remaining (from the 401 body), on both the bad-password and unknown-user branches consistently."
    - "When the account is locked the login page shows the remaining lockout time counting down (from the 429 retryAfterMs)."
    - "An unknown user that crosses the attempt threshold receives a 429 with retryAfterMs (parity with known-user lockout — preserves non-enumeration)."
    - "The inactivity warning banner shows a live countdown of remaining time."
    - "The client reads settings before the inactivity timer initializes, falling back to safe defaults if loadSettings() rejects."
  artifacts:
    - path: "config/settings.yaml"
      provides: "auth.maxLoginAttempts, auth.lockoutCapMs, auth.inactivityTimeoutMs, auth.warningBeforeMs"
      contains: "lockoutCapMs"
    - path: "server/rateLimiting.ts"
      provides: "configurable exponential-backoff lockout cap"
      contains: "lockoutCapMs"
    - path: "src/pages/LoginPage.tsx"
      provides: "remaining-attempts + countdown lockout feedback"
      contains: "attemptsRemaining"
    - path: "src/components/Layout.tsx"
      provides: "live inactivity countdown in the warning banner"
      contains: "secondsRemaining"
  key_links:
    - from: "server/initAuth.ts getAuthConfig"
      to: "settings.auth.maxLoginAttempts / auth.lockoutCapMs"
      via: "reads from the auth sub-object"
      pattern: "auth"
    - from: "server/initAuth.ts updateAuthConfig"
      to: "server/authApi.ts resetLimiter()"
      via: "settings PUT path resets the limiter singleton so config changes apply on the next request"
      pattern: "resetLimiter"
    - from: "server/authApi.ts POST /login 401 body"
      to: "LoginPage attemptsRemaining display"
      via: "401 body carries attemptsRemaining on both branches; client reads result.attemptsRemaining"
      pattern: "attemptsRemaining"
    - from: "src/context/AuthContext.tsx inactivity timers"
      to: "settingsService.loadSettings() auth.inactivityTimeoutMs / warningBeforeMs"
      via: "timers initialized after settings load, safe-default fallback"
      pattern: "warningBeforeMs"
    - from: "src/components/Layout.tsx banner"
      to: "AuthContext inactivitySecondsRemaining"
      via: "live countdown rendered in the warning banner"
      pattern: "inactivitySecondsRemaining"
---

<objective>
Externalize the remaining hardcoded auth constants to config/settings.yaml and add the login + inactivity
feedback affordances.

Backend: move maxLoginAttempts and the lockout cap into the `auth.*` sub-object (joining the existing
refreshTokenTtlMs etc.), read by initAuth and the rate limiter; rebuild the limiter singleton when config
changes; surface attempts-remaining/lockout-time so the login page can display them. Client: source
INACTIVITY_TIMEOUT and WARNING_BEFORE from settings before the inactivity timer starts (safe-default
fallback), set WARNING_BEFORE to 3 minutes, and render a live countdown in both the login lockout message
and the inactivity banner.

Purpose: Closes AUTHCFG-01 (remaining attempts + lockout countdown), AUTHCFG-02 (live inactivity countdown,
3-min lead), AUTHCFG-03 (inactivity constants from settings), AUTHCFG-04 (maxLoginAttempts + lockout cap
from settings, applied live). Per CONTEXT: maxLoginAttempts is ALREADY settings-driven (initAuth.ts:123),
so AUTHCFG-04's real gap is the lockout CAP, relocating maxLoginAttempts into the auth.* sub-object for
coherence, AND ensuring a live config change actually takes effect (the limiter was previously frozen at
first construction).

Output: New auth.* config keys + validation, configurable rate-limiter lockout cap, a rebuildable limiter,
settings-driven client timers, login feedback, live countdowns, DE+EN i18n keys, all tests green.

NOTE (depends_on 32-01): runs after 32-01 because both plans edit server/initAuth.ts, server/authApi.ts,
and src/i18n/translations.ts. Build on 32-01's changes; do not revert them.

INTERFACE OWNERSHIP (W3 — avoid merge conflicts on initAuth.ts):
- 32-01 OWNS `UserRecord.active` and the `_migrateActiveFlag` migration in initAuth.ts. Do NOT touch them.
- 32-02 OWNS the `AuthConfig` lockout-cap extension (the `lockoutCapMs` field, getAuthConfig/updateAuthConfig
  reads) in initAuth.ts. 32-01 does not touch AuthConfig.
- These are disjoint regions of initAuth.ts; edit only the AuthConfig region here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-user-management-auth-hardening/32-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do not re-explore. -->

Settings (config/settings.yaml) — existing flat `auth` sub-object to EXTEND (do not duplicate keys):
```
auth:
  refreshTokenTtlMs: 28800000
  refreshAbsoluteCapMs: 43200000
  refreshCookieSecure: true
  # ADD: maxLoginAttempts: 5
  # ADD: lockoutCapMs: 900000          # exponential-backoff CAP, not a fixed window (see comment below)
  # ADD: inactivityTimeoutMs: 600000   # 10 min (unchanged default)
  # ADD: warningBeforeMs: 180000       # 3 min (AUTHCFG-02 — was 60000)
```
The lockout is EXPONENTIAL BACKOFF WITH A CAP (rateLimiting.ts: `Math.min(Math.pow(2,newCount)*1000,
cap)`), NOT a fixed-duration window. Name the key `lockoutCapMs` to reflect cap semantics and add a
yaml comment on that line: "# exponential backoff cap: lockout = min(2^failures * 1s, lockoutCapMs)".

NOTE: maxLoginAttempts/otpCode are currently read at TOP LEVEL by initAuth (server/initAuth.ts:123,235)
and the TOP-LEVEL maxLoginAttempts is stripped from non-admin GET /api/settings (server/settingsApi.ts:174).
Moving maxLoginAttempts under `auth.*` is the locked-decision shape; keep the top-level read as a fallback
for backward compat. The non-admin strip at :174 strips only TOP-LEVEL keys, so the `auth.*` operational
params survive (intended — see W5 note in Task 1).

getAuthConfig / AuthConfig (server/initAuth.ts:48-52, 121-125, 232-238):
```
interface AuthConfig { twoFactorEnabled: boolean; maxLoginAttempts: number; otpCode: string; }
// Read in initAuth() (:121) and updateAuthConfig() (:233). Extend to prefer settings.auth.maxLoginAttempts,
// then top-level settings.maxLoginAttempts, then default 5. ADD lockoutCapMs (default 900000).
```
updateAuthConfig (server/initAuth.ts:232-238) is called from server/settingsApi.ts:214 after the settings
PUT validates + writes — this is the SINGLE live-config-change site.

Limiter singleton (server/authApi.ts:60-66) — THE BUG: built ONCE from getAuthConfig() and cached forever:
```
let _limiter: ReturnType<typeof createRateLimiter> | null = null;
function limiter() {
  if (!_limiter) { _limiter = createRateLimiter(getAuthConfig().maxLoginAttempts); }
  return _limiter;
}
```
A live updateAuthConfig() change never reaches this cached instance. The fix exports a `resetLimiter()`
that nulls `_limiter`, and settingsApi calls it right after updateAuthConfig() so the next request rebuilds.
(initAuth must NOT import authApi — that is a circular import. The reset is driven from the settings PUT
path, which already imports both modules.)

Rate limiter (server/rateLimiting.ts):
```
const MAX_LOCKOUT_MS = 3_600_000; // currently the hardcoded 1h cap
export function createRateLimiter(maxLoginAttempts: number) { ... recordFailure:
  const lockedUntil = newCount >= maxLoginAttempts
    ? Date.now() + Math.min(Math.pow(2, newCount) * 1000, MAX_LOCKOUT_MS) : 0; ... }
// CHANGE: add a second param `lockoutCapMs = 3_600_000` used as the Math.min cap.
// KEEP the existing backoff formula (2^newCount * 1000) exactly — only the cap becomes configurable.
```
Limiter wiring (server/authApi.ts:60-66): pass the new lockoutCapMs from getAuthConfig() at construction.

Login 401/429 responses (server/authApi.ts):
- Locked (known + unknown) → 429 `{ error, retryAfterMs }` (known-user path at :191-195, :212-215).
- Bad credentials → 401 `{ error }`.
- Bad-password branch (:208-219): `recordFailure` returns `newState`; checks `isLocked(newState)` → 429.
- Unknown-user branch (:201-206): TODAY calls `recordFailure(key)` but ALWAYS returns 401 — it does NOT
  check the lock or return 429. This asymmetry must be fixed (see Task 2).
LockState (server/rateLimiting.ts:9-13) has `count` + `lockedUntil`.

LoginResult (src/context/AuthContext.tsx:30-32): the `{ ok:false }` arm carries `retryAfterMs?`. ADD
`attemptsRemaining?: number`. login() returns invalid_credentials at :256 and reads the 429 retryAfterMs
at :251-254. The 401 body is not currently parsed — add a `resp.json()` read on the 401 path to extract
attemptsRemaining.

Public config endpoint (server/authApi.ts:485-489): GET /api/auth/config returns `{ twoFactorEnabled,
provider }`. DO NOT add maxLoginAttempts here — AUTHCFG-01 only needs the POST-FAILURE "N attempts
remaining" display, which the 401 body carries. There is no pre-failure total display, so /config stays
as-is.

Client inactivity (src/context/AuthContext.tsx:50-51,155-186): `INACTIVITY_TIMEOUT = 10*60*1000`
(exported — keep export, becomes the DEFAULT), `WARNING_BEFORE = 60*1000`. Timers in
resetInactivityTimer (:155-169). `inactivityWarning` boolean exposed via context (:40,270). settingsService
(src/services/settingsService.ts) `loadSettings()` is async + cached; AppSettings.auth currently has
refreshTokenTtlMs/refreshAbsoluteCapMs — EXTEND with inactivityTimeoutMs?/warningBeforeMs?/maxLoginAttempts?.

Inactivity banner (src/components/Layout.tsx:67-72): renders `t('inactivityWarning')` when
`inactivityWarning` is true. ADD a live seconds countdown.

settingsApi validation (server/settingsApi.ts:88-113): the `auth` block validator. ADD positive-integer
checks for the four new keys, mirroring the refreshTokenTtlMs checks (warningBeforeMs < inactivityTimeoutMs;
lockoutCapMs > 0; maxLoginAttempts ≥ 1).

i18n (src/i18n/translations.ts): each entry is `{ de, en }`. `loginErrorTooMany` (:80) is the current
lock message. Existing `inactivityWarning` (:441). i18n test convention: tests/outcomesI18n.test.ts asserts
per-key `{de,en}` presence — mirror it for the new keys (no generic completeness test exists in the repo).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Externalize maxLoginAttempts + lockout cap; rebuildable, configurable rate limiter</name>
  <files>config/settings.yaml, server/settingsApi.ts, server/initAuth.ts, server/rateLimiting.ts, server/authApi.ts, tests/rateLimiting.test.ts, tests/settingsApi.test.ts</files>
  <behavior>
    - createRateLimiter(maxAttempts, lockoutCapMs) caps lockout at lockoutCapMs (e.g. with a small cap, lockedUntil ≤ now + cap).
    - createRateLimiter(maxAttempts) with no second arg defaults the cap to 3_600_000 (regression: existing behavior preserved).
    - The exponential backoff formula is unchanged: lockedUntil delta = min(2^count * 1000, cap).
    - getAuthConfig().maxLoginAttempts reads settings.auth.maxLoginAttempts when present, else top-level, else 5.
    - getAuthConfig().lockoutCapMs reads settings.auth.lockoutCapMs when present, else 900000.
    - After updateAuthConfig() lowers auth.maxLoginAttempts and resetLimiter() runs, the NEXT limiter() build uses the new cap: a username locks out after the NEW (lower) number of failures, proving the singleton was rebuilt (not frozen at construction).
    - settingsApi PUT rejects auth.maxLoginAttempts < 1, non-integer lockoutCapMs, and warningBeforeMs >= inactivityTimeoutMs with 400.
    - settingsApi PUT accepts a valid auth block with all new keys (round-trip).
  </behavior>
  <action>
    AUTHCFG-04. In config/settings.yaml extend the existing `auth:` block (do NOT add top-level dupes) with
    `maxLoginAttempts: 5`, `lockoutCapMs: 900000`, `inactivityTimeoutMs: 600000`, `warningBeforeMs: 180000`
    (3 min — AUTHCFG-02). On the `lockoutCapMs` line add a comment: "# exponential backoff cap: lockout =
    min(2^failures * 1s, lockoutCapMs), not a fixed window" (W4).
    In server/rateLimiting.ts add a second parameter `lockoutCapMs = 3_600_000` to createRateLimiter and use
    it in place of the hardcoded `MAX_LOCKOUT_MS` cap inside recordFailure; KEEP the `Math.pow(2, newCount) *
    1000` backoff formula unchanged and keep MAX_LOCKOUT_MS only as the default value. Rename references from
    the duration framing to cap framing in comments.
    In server/initAuth.ts (the AuthConfig region only — 32-02 OWNS this; do not touch UserRecord/active from
    32-01) extend AuthConfig with `lockoutCapMs: number`; in BOTH initAuth() (:121) and updateAuthConfig()
    (:233) read `settings.auth?.maxLoginAttempts ?? settings.maxLoginAttempts ?? 5` and
    `settings.auth?.lockoutCapMs ?? 900_000` (cast `settings.auth` to a Record).
    In server/authApi.ts: add and export `resetLimiter()` that sets the module `_limiter` back to null, and
    pass `getAuthConfig().lockoutCapMs` as the second arg in the `createRateLimiter(...)` call (:63). Add a
    comment that resetLimiter exists because the limiter is built lazily from config and must be rebuilt on a
    live config change.
    In server/settingsApi.ts: (a) extend the `auth` block validator (:88-113) with positive-integer checks for
    maxLoginAttempts (≥1), lockoutCapMs (>0, integer), inactivityTimeoutMs (>0, integer), warningBeforeMs (>0,
    integer, and `< inactivityTimeoutMs` when both present); (b) immediately AFTER `updateAuthConfig(parsedObj)`
    (:214), import and call `resetLimiter()` from ./authApi.js so the rate limiter rebuilds with the new cap on
    the next request (Blocker #1). (c) W5: do NOT add inactivityTimeoutMs/lockoutCapMs/warningBeforeMs to the
    non-admin strip destructure at :174 — they are operational params, not secrets; the client timer must read
    them from non-admin GET /api/settings or it will always hit the fallback.
    Throw-only (D-03); camelCase (D-05).
  </action>
  <verify>
    <automated>npm test -- tests/rateLimiting.test.ts tests/settingsApi.test.ts</automated>
  </verify>
  <done>Lockout cap is configurable (lockoutCapMs) with the backoff formula intact and defaults preserved; getAuthConfig exposes lockoutCapMs + settings-sourced maxLoginAttempts; resetLimiter() rebuilds the singleton after updateAuthConfig and a test proves a lowered cap takes effect on the next request; validator enforces the four new keys; operational params are NOT stripped for non-admins; no hardcoded auth values remain in initAuth.ts/rateLimiting.ts; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Symmetric attempts-remaining + lockout on both login branches</name>
  <files>server/authApi.ts, src/context/AuthContext.tsx, tests/authConfigProvider.test.ts</files>
  <behavior>
    - POST /login bad-password (known user) not yet locked → 401 with attemptsRemaining = max(0, maxLoginAttempts - newState.count).
    - POST /login bad-password (known user) crossing the threshold → 429 { error, retryAfterMs }, NO attemptsRemaining.
    - POST /login unknown user not yet locked → 401 with attemptsRemaining (same field, same shape as known-user 401).
    - POST /login unknown user crossing the threshold → 429 { error, retryAfterMs } (parity with known-user lockout; preserves non-enumeration). This is the previously-missing branch.
    - GET /api/auth/config still returns only { twoFactorEnabled, provider } (no maxLoginAttempts added).
    - AuthContext.login() returns { ok:false, error:'invalid_credentials', attemptsRemaining } when the 401 carries it; { ok:false, error:'account_locked', retryAfterMs } on 429 (regression).
  </behavior>
  <action>
    AUTHCFG-01 (data path). DO NOT modify GET /config (Blocker #4) — leave :485-489 returning
    `{ twoFactorEnabled, provider }`.
    In POST /login, make the two failure branches SYMMETRIC (Blocker #2):
    - Bad-password branch (:208-219): keep `const newState = limiter().recordFailure(key)`; if
      `limiter().isLocked(newState)` → 429 `{ error: 'Account locked', retryAfterMs }`; ELSE 401 with
      `attemptsRemaining: Math.max(0, getAuthConfig().maxLoginAttempts - newState.count)`.
    - Unknown-user branch (:201-206): CHANGE it to capture `const newState = limiter().recordFailure(key)`,
      then mirror the bad-password branch — if `limiter().isLocked(newState)` → 429 `{ error: 'Account
      locked', retryAfterMs }` (this is the new 429 path that did not exist), ELSE 401 with the SAME
      `attemptsRemaining` field. attemptsRemaining appears ONLY on the non-locked 401 path, on BOTH branches.
    Add a comment noting the symmetry exists to preserve T-02-05 non-enumeration parity.
    In src/context/AuthContext.tsx extend LoginResult's `{ ok:false }` arm with `attemptsRemaining?: number`.
    On the 401 path (:256) parse the body (`resp.json()`) and return `{ ok:false, error:'invalid_credentials',
    attemptsRemaining }`. Leave the 429 path (:251-254 retryAfterMs) and the OTP/verify flow untouched.
  </action>
  <verify>
    <automated>npm test -- tests/authConfigProvider.test.ts</automated>
  </verify>
  <done>Both login branches return 429 on lockout and 401-with-attemptsRemaining otherwise (symmetric); the unknown-user-crosses-threshold case returns 429 and has a test; GET /config is unchanged (no maxLoginAttempts); login() surfaces attemptsRemaining from the 401 body; locked 429 unchanged; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Settings-driven client timers + live login & inactivity countdowns</name>
  <files>src/services/settingsService.ts, src/services/ttlConversion.ts, src/context/AuthContext.tsx, src/pages/LoginPage.tsx, src/components/Layout.tsx, src/i18n/translations.ts, tests/inactivitySettings.test.ts</files>
  <behavior>
    - AuthContext sources the active inactivity timer values from loadSettings(): inactivityTimeoutMs / warningBeforeMs from settings.auth are used to schedule the timer.
    - When loadSettings() REJECTS, the inactivity timer falls back to safe defaults (INACTIVITY_TIMEOUT = 600000, WARNING_BEFORE = 180000) and still schedules (the timer never disables itself).
    - WARNING_BEFORE default = 180000 (3 min).
    - i18n: each new key (inactivityCountdown, loginAttemptsRemaining, loginLockoutCountdown) has BOTH a non-empty `de` and a non-empty `en` value (per-key presence, mirroring tests/outcomesI18n.test.ts).
    - LoginPage create+edit feedback: a failed login with result.attemptsRemaining > 0 renders loginAttemptsRemaining with the count; account_locked renders a live loginLockoutCountdown from retryAfterMs.
  </behavior>
  <action>
    AUTHCFG-03 + AUTHCFG-02 (client). In src/services/settingsService.ts extend `AppSettings.auth` and
    `DEFAULTS.auth` with `inactivityTimeoutMs` (default 600000), `warningBeforeMs` (default 180000), and
    `maxLoginAttempts` (default 5). In src/services/ttlConversion.ts add small `msToMinutes`/`minutesToMs`
    helpers (mirroring the existing hours helpers) if used by the UI; otherwise reuse existing ones.
    In src/context/AuthContext.tsx: keep `INACTIVITY_TIMEOUT` (600000) and set `WARNING_BEFORE = 180000`
    exported as the DEFAULTS, but make the ACTIVE timer values read from settings — on mount (when a user
    exists), call `settingsService.loadSettings()` and store effective `inactivityTimeoutMs`/`warningBeforeMs`
    in state/ref; the inactivity timer effect (:172-186) must wait for that load before scheduling, and on a
    rejected load fall back to the two exported defaults (M4 bootstrap decision) — never disable the timer.
    Add a live `inactivitySecondsRemaining` value to the context (computed via a 1s interval that runs only
    while `inactivityWarning` is true) and expose it on AuthContextType.
    AUTHCFG-02 (banner). In src/components/Layout.tsx render the live countdown next to
    `t('inactivityWarning')` using `inactivitySecondsRemaining` (format mm:ss) via `t('inactivityCountdown')`
    with the seconds interpolated.
    AUTHCFG-01 (display). In src/pages/LoginPage.tsx: on a failed login where `result.attemptsRemaining` is
    present (and > 0), show `t('loginAttemptsRemaining')` with the count (no /config fetch of a total — the
    401 body is the only source per Blocker #4). On `account_locked`, read `result.retryAfterMs` and show a
    live countdown (1s interval) of remaining lockout time via `t('loginLockoutCountdown')`; stop and
    re-enable the form when it reaches zero. Clean up intervals on unmount/step change.
    Add DE+EN i18n keys: `inactivityCountdown`, `loginAttemptsRemaining`, `loginLockoutCountdown` (use
    `{0}`-style placeholders consistent with existing interpolated keys). Place near the existing auth keys.
    TESTS (Blocker #3): create tests/inactivitySettings.test.ts (or extend an existing auth test) that
    (a) mocks settingsService.loadSettings to resolve with custom auth.inactivityTimeoutMs/warningBeforeMs and
    asserts the AuthContext timer uses them; (b) mocks loadSettings to REJECT and asserts the timer falls back
    to the 600000/180000 defaults and still schedules. Also add per-key `{de,en}` presence assertions for the
    three new i18n keys (mirror tests/outcomesI18n.test.ts). RTL assertions use queryByText().not.toBeNull()
    — no jest-dom.
  </action>
  <verify>
    <automated>npm test -- tests/inactivitySettings.test.ts && npm run lint && npm run build</automated>
  </verify>
  <done>Client reads inactivity timers from settings with safe-default fallback before the timer starts and a behavioral test proves both the settings-sourced and rejected-load fallback paths; WARNING_BEFORE = 3 min; inactivity banner shows a live countdown; login page shows remaining attempts after a failure and a live lockout countdown when locked; three new i18n keys have de+en presence (asserted); lint + build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → GET /api/auth/config, POST /login | attacker can read config + probe credentials |
| admin client → PUT /api/settings (auth.* block) | privileged config mutation affecting lockout policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Information disclosure | Login failure response distinguishing known vs unknown user | mitigate | Both branches return identical 429-on-lockout / 401-with-attemptsRemaining shapes; the unknown-user branch now mirrors the known-user lockout (Blocker #2), so response timing/shape does not reveal account existence. |
| T-32-07 | Denial of service | Misconfigured lockoutCapMs (too large) | mitigate | settingsApi validator bounds lockoutCapMs to a positive integer; the existing 1h hardcoded value becomes the default, and operator edits are admin-only via PUT /api/settings. |
| T-32-08 | Tampering | Client-supplied attemptsRemaining trusted by UI | accept | attemptsRemaining is display-only; all enforcement is server-side in rateLimiting.ts (F-10). A tampered client value cannot bypass the server lock. |
| T-32-09 | Spoofing / bypass | Client disabling the inactivity timer by failing settings load | mitigate | Settings-load rejection falls back to safe defaults (10 min / 3 min) rather than disabling the timer; the timer never starts with infinite/zero values, and a test asserts the fallback path. |
| T-32-10 | Tampering | Invalid auth.* values written to settings.yaml | mitigate | PUT /api/settings validates all four new keys (positive integers, warningBeforeMs < inactivityTimeoutMs) before writeSettings + updateAuthConfig + resetLimiter. |
| T-32-11 | Tampering / stale policy | Limiter frozen at construction ignoring a tightened policy | mitigate | resetLimiter() nulls the singleton after updateAuthConfig() so a lowered maxLoginAttempts/lockoutCapMs applies on the next request (Blocker #1); a test proves the rebuild. |
</threat_model>

<verification>
- `npm test -- tests/rateLimiting.test.ts tests/settingsApi.test.ts tests/authConfigProvider.test.ts tests/inactivitySettings.test.ts` passes.
- `npm run lint` and `npm run build` pass.
- Full safety net: `npm run test:ci` stays green (baseline plus new tests).
- Grep gate (the client uses settings values, not the old hardcoded 60s warning literal):
  `grep -vn '^\s*//' src/context/AuthContext.tsx | grep -c 'WARNING_BEFORE = 60 \* 1000'` returns 0.
</verification>

<success_criteria>
- maxLoginAttempts + lockout cap sourced from config/settings.yaml; no hardcoded auth values in initAuth.ts/rateLimiting.ts; a live config change applies on the next request via resetLimiter (AUTHCFG-04, Blocker #1).
- Both login failure branches are symmetric: 429 on lockout, 401-with-attemptsRemaining otherwise; unknown-user threshold crossing returns 429 (Blocker #2).
- GET /config unchanged; LoginPage uses only result.attemptsRemaining (Blocker #4).
- INACTIVITY_TIMEOUT + WARNING_BEFORE sourced from settings with tested settings + fallback paths; WARNING_BEFORE = 3 min (AUTHCFG-03, AUTHCFG-02, Blocker #3).
- Operational auth.* params are NOT stripped for non-admins (W5).
- Login page shows remaining attempts after a failure and a counting-down lockout time when locked (AUTHCFG-01).
- Inactivity banner shows a live countdown (AUTHCFG-02).
- New i18n keys have asserted de+en presence; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/32-user-management-auth-hardening/32-02-SUMMARY.md`.
</output>
