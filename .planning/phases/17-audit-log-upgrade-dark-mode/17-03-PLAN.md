---
phase: 17
plan: 03
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - src/components/outcomes/palette.ts
  - src/i18n/translations.ts
  - src/index.css
  - index.html
  - src/context/ThemeContext.tsx
autonomous: true
requirements: [VIS-01, VIS-03]
must_haves:
  truths:
    - "DARK_EYE_COLORS + DARK_COHORT_PALETTES exported from palette.ts"
    - "All DARK_EYE_COLORS pass WCAG 4.5:1 against #111827 (automated test green)"
    - "All DARK_COHORT_PALETTES pass WCAG 3.0:1 against #111827 (automated test green)"
    - "All Phase 17 i18n keys exist in EN + DE (automated test green)"
    - "Tailwind v4 class-based dark mode enabled via @custom-variant in src/index.css"
    - "index.html FOUC script applies .dark to <html> synchronously before CSS/JS loads"
    - "ThemeContext provides theme/setTheme/effectiveTheme; useTheme hook available"
  artifacts:
    - path: "src/context/ThemeContext.tsx"
      provides: "ThemeProvider + useTheme hook"
      contains: "useTheme"
    - path: "src/components/outcomes/palette.ts"
      provides: "DARK_EYE_COLORS + DARK_COHORT_PALETTES"
      contains: "DARK_EYE_COLORS"
    - path: "src/i18n/translations.ts"
      provides: "16 new i18n keys × 2 locales"
      contains: "themeLight"
    - path: "src/index.css"
      provides: "@custom-variant dark enabling dark: classes"
      contains: "@custom-variant dark"
    - path: "index.html"
      provides: "FOUC prevention inline script"
      contains: "emd-theme"
  key_links:
    - from: "index.html inline script"
      to: "document.documentElement class 'dark'"
      via: "localStorage.getItem('emd-theme')"
      pattern: "classList.add\\('dark'\\)"
    - from: "src/context/ThemeContext.tsx"
      to: "localStorage 'emd-theme' + prefers-color-scheme media query"
      via: "useState initializer + useEffect listener"
      pattern: "emd-theme"
---

<objective>
Build the dark-mode foundation: Tailwind v4 dark variant config, FOUC-prevention script, theme context, new i18n keys, and dark chart palettes with WCAG-verified hex values. Turns Plan 01 palette + i18n red tests green.

Purpose: Establishes infrastructure that Plan 04 consumes (ThemeToggle, dark: variants, chart color switching).
Output: Green contrast + i18n tests; ThemeContext ready to wrap App; CSS dark variant active.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-audit-log-upgrade-dark-mode/17-CONTEXT.md
@.planning/phases/17-audit-log-upgrade-dark-mode/17-RESEARCH.md
@.planning/phases/17-audit-log-upgrade-dark-mode/17-UI-SPEC.md
@src/context/LanguageContext.tsx
@src/components/outcomes/palette.ts
@src/i18n/translations.ts
@src/index.css
@index.html
</context>

<interfaces>
From `src/context/LanguageContext.tsx` (pattern to mirror):
```ts
export function LanguageProvider({ children }: { children: ReactNode });
export function useLanguage(): { language: 'en'|'de'; setLanguage: (l) => void; t: (k) => string };
// Uses createContext<T|null>(null), ThemeContext Provider with useState(() => localStorage.getItem(...))
// useLanguage throws if context is null
```

ThemeContext contract to export (NEW):
```ts
export type Theme = 'light' | 'dark' | 'system';
export function ThemeProvider({ children }: { children: ReactNode }): JSX.Element;
export function useTheme(): {
  theme: Theme;
  setTheme: (t: Theme) => void;
  effectiveTheme: 'light' | 'dark';
};
```

From `src/components/outcomes/palette.ts` (existing):
```ts
export const EYE_COLORS: { OD: string; OS: string; 'OD+OS': string };
export const COHORT_PALETTES: readonly string[];
export function computeContrastRatio(a: string, b: string): number;
```
New exports to add:
```ts
export const DARK_EYE_COLORS: { OD: string; OS: string; 'OD+OS': string };
export const DARK_COHORT_PALETTES: readonly string[];
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DARK_EYE_COLORS + DARK_COHORT_PALETTES to palette.ts</name>
  <files>src/components/outcomes/palette.ts</files>
  <read_first>
    - src/components/outcomes/palette.ts (existing EYE_COLORS, COHORT_PALETTES, computeContrastRatio shape)
    - tests/outcomesPalette.contrast.test.ts (Phase 17 WCAG assertions from Plan 01)
    - .planning/phases/17-audit-log-upgrade-dark-mode/17-UI-SPEC.md (Chart Colors — Dark Mode tables)
  </read_first>
  <behavior>
    - `DARK_EYE_COLORS.OD === '#93c5fd'` (blue-300)
    - `DARK_EYE_COLORS.OS === '#fca5a5'` (red-300)
    - `DARK_EYE_COLORS['OD+OS'] === '#c4b5fd'` (violet-300)
    - `DARK_COHORT_PALETTES` is `['#6ee7b7', '#fcd34d', '#67e8f9', '#f0abfc']` (emerald/amber/cyan/fuchsia-300)
    - All DARK_EYE_COLORS pass `computeContrastRatio(hex, '#111827') >= 4.5`
    - All DARK_COHORT_PALETTES pass `>= 3.0`
  </behavior>
  <action>
    Append to `src/components/outcomes/palette.ts` (after existing EYE_COLORS/COHORT_PALETTES definitions, before any default export):

    ```ts
    /**
     * Dark-mode chart palette (D-07).
     * WCAG canonical dark background: #111827 (Tailwind gray-900).
     * OD/OS/OD+OS must pass AA 4.5:1 for text/small elements (VIS-03).
     * Cohort palette entries must pass 3.0:1 (graphical threshold).
     */
    export const DARK_EYE_COLORS: { OD: string; OS: string; 'OD+OS': string } = {
      OD: '#93c5fd',      // blue-300  — ≈ 7.8:1 vs #111827
      OS: '#fca5a5',      // red-300   — ≈ 6.1:1 vs #111827
      'OD+OS': '#c4b5fd', // violet-300 — ≈ 8.2:1 vs #111827
    };

    export const DARK_COHORT_PALETTES: readonly string[] = [
      '#6ee7b7', // emerald-300
      '#fcd34d', // amber-300
      '#67e8f9', // cyan-300
      '#f0abfc', // fuchsia-300
    ] as const;
    ```

    Match existing export style in the file (tab/space, single vs double quotes — read first, then copy the style).
  </action>
  <verify>
    <automated>npx vitest run tests/outcomesPalette.contrast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/outcomes/palette.ts contains the literal string `export const DARK_EYE_COLORS`
    - src/components/outcomes/palette.ts contains `'#93c5fd'`
    - src/components/outcomes/palette.ts contains `'#fca5a5'`
    - src/components/outcomes/palette.ts contains `'#c4b5fd'`
    - src/components/outcomes/palette.ts contains `export const DARK_COHORT_PALETTES`
    - src/components/outcomes/palette.ts contains `'#6ee7b7'`, `'#fcd34d'`, `'#67e8f9'`, `'#f0abfc'`
    - `npx vitest run tests/outcomesPalette.contrast.test.ts` exits 0
  </acceptance_criteria>
  <done>Dark palettes added; WCAG test green (all 3 eye colors ≥ 4.5:1, all 4 cohort colors ≥ 3.0:1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 16 Phase 17 i18n keys to translations.ts (EN + DE)</name>
  <files>src/i18n/translations.ts</files>
  <read_first>
    - src/i18n/translations.ts (existing flat object structure; confirm `translations.en` and `translations.de` shape; look at how other key groups like `outcomes*` or `audit*` are grouped)
    - tests/outcomesI18n.test.ts (Phase 17 i18n keys describe — exact key list)
    - .planning/phases/17-audit-log-upgrade-dark-mode/17-UI-SPEC.md (Copywriting Contract — full EN copy)
  </read_first>
  <behavior>
    - `translations.en.themeLight === 'Switch to light mode'` (or EN equivalent exactly from UI-SPEC)
    - `translations.en.themeDark === 'Switch to dark mode'`
    - `translations.en.themeSystem === 'Use system theme'`
    - All 16 keys present in BOTH en and de with non-empty strings
  </behavior>
  <action>
    Add exactly 16 keys to BOTH `translations.en` and `translations.de` objects in `src/i18n/translations.ts`. Group them logically — place near existing `audit*` keys for audit filters, and add a new theme group.

    English values (copy verbatim from UI-SPEC Copywriting Contract):
    ```
    themeLight: 'Switch to light mode'
    themeDark: 'Switch to dark mode'
    themeSystem: 'Use system theme'
    auditFilterUser: 'User'
    auditFilterAllUsers: 'All users'
    auditFilterCategory: 'Category'
    auditFilterAllCategories: 'All categories'
    auditCategoryAuth: 'Auth'
    auditCategoryData: 'Data'
    auditCategoryAdmin: 'Admin'
    auditCategoryOutcomes: 'Outcomes'
    auditFilterFrom: 'From'
    auditFilterTo: 'To'
    auditFilterCohortHash: 'Search cohort hash…'
    auditFilterFailuresOnly: 'Failures only'
    auditEmptyFiltered: 'No audit entries match your filters.'
    ```

    German values (translate; use existing German style in translations.de as reference — formal "Sie" form, match punctuation/ellipsis style):
    ```
    themeLight: 'Zum hellen Modus wechseln'
    themeDark: 'Zum dunklen Modus wechseln'
    themeSystem: 'Systemdesign verwenden'
    auditFilterUser: 'Benutzer'
    auditFilterAllUsers: 'Alle Benutzer'
    auditFilterCategory: 'Kategorie'
    auditFilterAllCategories: 'Alle Kategorien'
    auditCategoryAuth: 'Auth'
    auditCategoryData: 'Daten'
    auditCategoryAdmin: 'Admin'
    auditCategoryOutcomes: 'Outcomes'
    auditFilterFrom: 'Von'
    auditFilterTo: 'Bis'
    auditFilterCohortHash: 'Kohorten-Hash suchen…'
    auditFilterFailuresOnly: 'Nur Fehler'
    auditEmptyFiltered: 'Keine Audit-Einträge entsprechen Ihren Filtern.'
    ```

    If the TranslationKey type is derived from one locale (e.g. `keyof typeof translations.en`), both locales must have identical keys — ensure DE has all 16 and EN has all 16.
  </action>
  <verify>
    <automated>npx vitest run tests/outcomesI18n.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/i18n/translations.ts contains `themeLight:`
    - src/i18n/translations.ts contains `auditFilterCohortHash:`
    - src/i18n/translations.ts contains `auditEmptyFiltered:`
    - src/i18n/translations.ts contains `'Kohorten-Hash suchen…'` (DE)
    - src/i18n/translations.ts contains `'Switch to dark mode'` (EN)
    - `npx vitest run tests/outcomesI18n.test.ts` exits 0 (all 32 new assertions pass)
  </acceptance_criteria>
  <done>16 keys × 2 locales added; i18n completeness test green.</done>
</task>

<task type="auto">
  <name>Task 3: Enable Tailwind v4 dark variant + FOUC script + ThemeContext</name>
  <files>src/index.css, index.html, src/context/ThemeContext.tsx</files>
  <read_first>
    - src/index.css (current contents — 6 lines with @import tailwindcss + body style)
    - index.html (current contents — empty head confirmed)
    - src/context/LanguageContext.tsx (full file — exact pattern to mirror for ThemeContext)
    - .planning/phases/17-audit-log-upgrade-dark-mode/17-RESEARCH.md (ThemeContext shape code example, Pattern 3 FOUC script)
  </read_first>
  <action>

    ### 3a. Add @custom-variant to src/index.css

    Immediately after the existing `@import "tailwindcss";` line, add a blank line then:
    ```css
    /* Phase 17 — enable class-based dark mode (Tailwind v4 CSS-first config) */
    @custom-variant dark (&:where(.dark, .dark *));
    ```

    Do NOT modify the existing `body` styles in index.css.

    ### 3b. Add FOUC-prevention inline script to index.html

    In index.html `<head>` (currently empty apart from meta/title/favicon), add this script tag AFTER the `<title>` element and BEFORE any `<script type="module">` that may be auto-injected by Vite:

    ```html
    <script>
      (function() {
        try {
          var t = localStorage.getItem('emd-theme');
          var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
          if (t === 'dark' || (t === 'system' && prefersDark)) {
            document.documentElement.classList.add('dark');
          }
        } catch (e) { /* localStorage unavailable — default to light */ }
      })();
    </script>
    ```

    ### 3c. Create src/context/ThemeContext.tsx

    Create a new file `src/context/ThemeContext.tsx` with exactly this structure (mirrors LanguageContext pattern):

    ```tsx
    import type { ReactNode } from 'react';
    import { createContext, useCallback, useContext, useEffect, useMemo, useState } from 'react';

    export type Theme = 'light' | 'dark' | 'system';

    interface ThemeContextType {
      theme: Theme;
      setTheme: (t: Theme) => void;
      effectiveTheme: 'light' | 'dark';
    }

    const ThemeContext = createContext<ThemeContextType | null>(null);
    const STORAGE_KEY = 'emd-theme';

    function readStored(): Theme {
      try {
        const raw = localStorage.getItem(STORAGE_KEY);
        if (raw === 'light' || raw === 'dark' || raw === 'system') return raw;
      } catch { /* ignore */ }
      return 'light';
    }

    export function ThemeProvider({ children }: { children: ReactNode }) {
      const [theme, setThemeState] = useState<Theme>(readStored);
      const [systemDark, setSystemDark] = useState<boolean>(() => {
        if (typeof window === 'undefined' || !window.matchMedia) return false;
        return window.matchMedia('(prefers-color-scheme: dark)').matches;
      });

      useEffect(() => {
        if (typeof window === 'undefined' || !window.matchMedia) return;
        const mq = window.matchMedia('(prefers-color-scheme: dark)');
        const handler = (e: MediaQueryListEvent) => setSystemDark(e.matches);
        mq.addEventListener('change', handler);
        return () => mq.removeEventListener('change', handler);
      }, []);

      const effectiveTheme: 'light' | 'dark' = theme === 'system' ? (systemDark ? 'dark' : 'light') : theme;

      useEffect(() => {
        const root = document.documentElement;
        if (effectiveTheme === 'dark') {
          root.classList.add('dark');
        } else {
          root.classList.remove('dark');
        }
      }, [effectiveTheme]);

      const setTheme = useCallback((t: Theme) => {
        setThemeState(t);
        try { localStorage.setItem(STORAGE_KEY, t); } catch { /* ignore */ }
      }, []);

      const value = useMemo(() => ({ theme, setTheme, effectiveTheme }), [theme, setTheme, effectiveTheme]);
      return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
    }

    export function useTheme(): ThemeContextType {
      const ctx = useContext(ThemeContext);
      if (!ctx) throw new Error('useTheme must be used within ThemeProvider');
      return ctx;
    }
    ```

    DO NOT wrap ThemeProvider in App.tsx yet — Plan 04 integrates it.
  </action>
  <verify>
    <automated>npx vitest run && (grep -q "@custom-variant dark" src/index.css) && (grep -q "emd-theme" index.html) && (grep -q "useTheme must be used within ThemeProvider" src/context/ThemeContext.tsx)</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains the literal string `@custom-variant dark (&:where(.dark, .dark *))`
    - src/index.css still contains `@import "tailwindcss"` (not removed)
    - index.html contains the literal string `localStorage.getItem('emd-theme')`
    - index.html contains the literal string `classList.add('dark')`
    - index.html contains the literal string `prefers-color-scheme: dark`
    - src/context/ThemeContext.tsx exists
    - src/context/ThemeContext.tsx contains `export function ThemeProvider`
    - src/context/ThemeContext.tsx contains `export function useTheme`
    - src/context/ThemeContext.tsx contains `export type Theme = 'light' | 'dark' | 'system'`
    - src/context/ThemeContext.tsx contains `'emd-theme'`
    - src/context/ThemeContext.tsx contains `prefers-color-scheme: dark`
    - `npx vitest run` exits 0 (full suite green — no regressions)
  </acceptance_criteria>
  <done>CSS dark variant enabled, FOUC script in place, ThemeContext module ready for App wrapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → FOUC script | User can write any string to `emd-theme` via DevTools |
| System media query → ThemeContext | Browser-controlled — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | `emd-theme` localStorage read | mitigate | Both FOUC script (index.html) and ThemeContext.readStored() use strict `=== 'light' | 'dark' | 'system'` equality check before acting. No eval, no innerHTML, no dynamic class string — only the literal `'dark'` is added via `classList.add`. Arbitrary storage values default to light mode. |
| T-17-07 | Tampering | FOUC inline script XSS surface | mitigate | Script reads only `localStorage.getItem('emd-theme')` and uses the result in a `===` comparison; the string is NEVER rendered to the DOM or passed to innerHTML/eval. `classList.add('dark')` uses a hardcoded literal. IIFE scope prevents globals pollution. |
| T-17-08 | Denial of Service | localStorage unavailable (private mode) | accept | try/catch around localStorage access in both FOUC script and ThemeContext — silent fallback to light mode. |
</threat_model>

<verification>
- `npx vitest run tests/outcomesPalette.contrast.test.ts` → green (Task 1)
- `npx vitest run tests/outcomesI18n.test.ts` → green (Task 2)
- `npx vitest run` (full suite) → green; no regressions
- `grep "@custom-variant" src/index.css` → found
- `grep "emd-theme" index.html src/context/ThemeContext.tsx` → both files match
- `test -f src/context/ThemeContext.tsx` → exists
</verification>

<success_criteria>
- Dark palettes added + WCAG green
- 16 i18n keys × 2 locales added + completeness green
- Tailwind v4 dark variant enabled
- FOUC script runs before CSS/JS in index.html
- ThemeContext module exports ThemeProvider + useTheme + Theme type
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/17-audit-log-upgrade-dark-mode/17-03-SUMMARY.md`.
</output>
