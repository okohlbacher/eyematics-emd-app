---
phase: 33-cohort-builder-ux-advanced-filters
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - src/pages/CohortBuilderPage.tsx
  - src/pages/AdvancedFilterDialog.tsx
  - tests/advancedFilterDialog.test.tsx
autonomous: true
requirements: [COH-03, COH-04]
must_haves:
  truths:
    - "Four preset buttons (Therapie-Abbrecher, Unplausible CRT, Flagged quality, Implausible Visus) appear in the cohort builder filter panel"
    - "Clicking a preset applies its filter live; clicking the active preset again clears it"
    - "Editing any manual filter field after a preset clears the active preset"
    - "An Advanced filters trigger opens a modal exposing the 5 curated attributes"
    - "Applying advanced attributes narrows the live cohort via applyFilters"
    - "Selecting the Flagged-quality preset filters by the open quality flags from DataContext"
  artifacts:
    - path: "src/pages/AdvancedFilterDialog.tsx"
      provides: "Modal exposing the 5 curated advanced attributes (D-11)"
      contains: "advancedFiltersTitle"
    - path: "src/pages/CohortBuilderPage.tsx"
      provides: "Preset button group + advanced-dialog trigger wired to applyFilters"
      contains: "applyPreset"
    - path: "tests/advancedFilterDialog.test.tsx"
      provides: "COH-04 dialog render + advanced-filter narrowing tests"
      contains: "advancedFiltersTitle"
  key_links:
    - from: "src/pages/CohortBuilderPage.tsx"
      to: "applyFilters options param"
      via: "passes therapy + crt thresholds from getSettings()"
      pattern: "crtImplausibleThresholdUm"
    - from: "src/pages/CohortBuilderPage.tsx"
      to: "DataContext qualityFlags"
      via: "builds flaggedCaseIds Set for the flaggedQuality preset"
      pattern: "flaggedCaseIds"
    - from: "src/pages/AdvancedFilterDialog.tsx"
      to: "CohortFilter advanced fields"
      via: "onApply writes Partial<CohortFilter> to filters"
      pattern: "onApply"
---

<objective>
Add the COH-03 issue-based preset buttons and the COH-04 advanced filter modal to the cohort
builder, both wired through the Plan 01 applyFilters extension so the live results narrow
correctly. Presets are one-click, ephemeral, non-persisted-as-SavedSearch filter applications
(D-07); the advanced dialog exposes exactly the five curated attributes (D-11) via a centered
modal (UI-SPEC Surface 4 decision).

Depends on Plan 01 (extended CohortFilter + applyFilters + i18n keys + settings threshold) and
Plan 02 (CohortBuilderPage now owns the persisted filters state + Reset — this plan extends the
same file, hence Wave 3 after Plan 02).

Purpose: Give users the fastest path to clinically meaningful cohorts and a curated additional-
attribute filter set without overflowing the sidebar.
Output: AdvancedFilterDialog.tsx (new), preset button group + advanced trigger in
CohortBuilderPage, applyFilters wired with thresholds + flaggedCaseIds, dialog test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-cohort-builder-ux-advanced-filters/33-CONTEXT.md
@.planning/phases/33-cohort-builder-ux-advanced-filters/33-RESEARCH.md
@.planning/phases/33-cohort-builder-ux-advanced-filters/33-PATTERNS.md
@.planning/phases/33-cohort-builder-ux-advanced-filters/33-UI-SPEC.md

<interfaces>
CohortFilter (Plan 01): preset union (therapyBreaker | implausibleCrt | flaggedQuality |
implausibleVisus), flaggedCaseIds (Set of string), diagnosisSubtype (string[]),
hasComorbidity (boolean), hba1cRange ([number,number]), medicationCodes (string[]),
laterality (OD | OS | OU).

applyFilters(cases, filters, options?) — options carries therapyInterrupterDays,
therapyBreakerDays, crtImplausibleThresholdUm. CohortBuilderPage call site is the useMemo at
line 146; it must now pass options from getSettings() (getSettings().therapyInterrupterDays,
.therapyBreakerDays, .crtImplausibleThresholdUm).

DataContext qualityFlags: QualityFlag[] (line 88). A case is flagged when
qualityFlags.some(f => f.caseId === c.id && f.status === 'open'). Build the flaggedCaseIds Set
from these when the flaggedQuality preset is applied.

Button primitive (src/components/primitives/Button.tsx): variants ghost | soft | accent;
sizes sm | md; ghost = neutral, soft = applied-state, accent = primary CTA. size sm = px-2.5
py-1 text-xs (UI-SPEC overrides px to px-3 for preset buttons).
Badge primitive: tone teal (active advanced count).
lucide-react: Sliders (advanced trigger), X (dialog close).

i18n keys (Plan 01): cohortPresets, presetTherapyBreaker, presetImplausibleCrt,
presetFlaggedQuality, presetImplausibleVisus, advancedFilters, advancedFiltersTitle,
advancedFiltersApply, advancedFiltersClear, advancedFiltersDiscard, advancedFiltersDiagnosisSubtype,
advancedFiltersComorbidities, advancedFiltersComorbiditiesAny, advancedFiltersHba1c,
advancedFiltersMedication, advancedFiltersLaterality, advancedFiltersLateralityOD/OS/OU.

Modal scaffold analog: QualityFlagDialog.tsx line 36 overlay (fixed inset-0 bg-black/30 flex
items-center justify-center z-50), panel bg-white dark:bg-gray-800 rounded-xl shadow-xl p-6
w-full max-w-lg. Focus-trap + Escape analog: FeedbackButton.tsx lines 40-68.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the AdvancedFilterDialog modal (COH-04)</name>
  <files>src/pages/AdvancedFilterDialog.tsx, tests/advancedFilterDialog.test.tsx</files>
  <behavior>
    - When open is true the dialog renders advancedFiltersTitle and the 5 curated field controls
    - When open is false the dialog renders nothing
    - Toggling the Has-comorbidity checkbox and clicking Apply calls onApply with hasComorbidity true
    - Entering an HbA1c min/max and clicking Apply calls onApply with hba1cRange set
    - Selecting laterality OD and clicking Apply calls onApply with laterality 'OD'
    - Clicking Clear empties the dialog fields without closing (onApply/onClose not fired)
    - Clicking Discard (or the X) calls onClose without onApply
    - Escape key calls onClose
  </behavior>
  <read_first>
    - src/components/quality/QualityFlagDialog.tsx (modal scaffold, props interface style, footer justify-end)
    - src/components/FeedbackButton.tsx lines 40-68 (focus trap + Escape pattern)
    - src/pages/CohortBuilderPage.tsx lines 390-423 (existing Diagnosis checkbox control style to reuse for multi-selects)
    - 33-PATTERNS.md section "src/pages/AdvancedFilterDialog.tsx (new component)" lines 405-499 (exact scaffold, props interface, focus trap, field control pattern)
    - 33-UI-SPEC.md "Surface 4" lines 249-361 (modal decision, entry affordance, field spec table, footer, accessibility, i18n)
  </read_first>
  <action>
    Write tests/advancedFilterDialog.test.tsx FIRST (jsdom docblock, no jest-dom). Render the dialog
    with vi.fn() onApply/onClose, drive each behavior above, assert onApply call args. Run RED.

    Then create src/pages/AdvancedFilterDialog.tsx with props { open: boolean; filters: CohortFilter;
    onApply: (advancedFields: Partial<CohortFilter>) => void; onClose: () => void }. Return null when
    not open. Use the QualityFlagDialog modal scaffold (overlay + panel max-w-lg, role="dialog",
    aria-modal="true", aria-labelledby pointing to the heading) and the FeedbackButton focus-trap +
    Escape pattern. Hold the five attributes in local dialog state seeded from props.filters on open:
    diagnosisSubtype (multi-select checkboxes over the AMD/DR SNOMED subtype codes from
    shared/fhirCodes.ts), hasComorbidity (single boolean checkbox labeled advancedFiltersComorbiditiesAny),
    hba1cRange (two numeric inputs, inputMode numeric, px-2 py-1.5, "%" unit label), medicationCodes
    (multi-select checkboxes — resolve the medication code option list from the app medication
    vocabulary; if none readily available, render the control with the codes present in the loaded
    cases), laterality (three radios OD/OS/OU using the lateralityOD/OS/OU labels). Footer with three
    Button elements per UI-SPEC: Clear (advancedFiltersClear, resets local state only), Discard
    (advancedFiltersDiscard, calls onClose), Apply (variant accent, advancedFiltersApply, calls onApply
    with only the non-empty advanced fields then onClose). All combination is AND (no OR toggle). Use
    only the i18n keys from interfaces. Run RED to GREEN.
  </action>
  <verify>
    <automated>npm run test -- advancedFilterDialog --run</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/AdvancedFilterDialog.tsx exists, renders advancedFiltersTitle, returns null when not open
    - Dialog has role="dialog", aria-modal="true", Escape calls onClose, X close has aria-label advancedFiltersDiscard
    - Apply calls onApply with a Partial<CohortFilter> containing only set advanced fields
    - npm run test -- advancedFilterDialog --run exits 0
  </acceptance_criteria>
  <done>The advanced filter modal renders the 5 curated attributes, traps focus, and emits the chosen attributes on Apply.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire preset buttons + advanced-dialog trigger into CohortBuilderPage</name>
  <files>src/pages/CohortBuilderPage.tsx, tests/advancedFilterDialog.test.tsx</files>
  <behavior>
    - The filter panel shows a Quick presets section (cohortPresets label) with a 2x2 grid of 4 preset buttons above the Diagnosis checkboxes
    - Clicking the Therapie-Abbrecher preset sets filters.preset to therapyBreaker and the button shows the active (soft + teal ring) state
    - Clicking the active preset again clears filters.preset
    - Clicking the Flagged-quality preset sets filters.preset to flaggedQuality AND populates filters.flaggedCaseIds from the open qualityFlags
    - Editing any manual filter field (e.g. a diagnosis checkbox) clears filters.preset
    - The Advanced filters trigger button opens AdvancedFilterDialog; the trigger shows accent variant + a count Badge when any advanced field is set
    - applyFilters is called with the options object carrying therapy + crt thresholds from getSettings()
  </behavior>
  <read_first>
    - src/pages/CohortBuilderPage.tsx lines 1-160 (imports, filters state from Plan 02, getSettings access if present), lines 146 (applyFilters useMemo), lines 380-563 (filter panel heading, Diagnosis checkboxes, Reset)
    - src/context/DataContext.tsx line 88 (qualityFlags) and the useData/useContext hook used in CohortBuilderPage
    - 33-PATTERNS.md sections "shared/patientCases.ts" imports note lines 324-332 (options-param resolution), "Button Primitives" lines 765-778, "Modal Overlay Pattern" lines 780-789
    - 33-UI-SPEC.md "Surface 3" lines 181-245 (preset section placement, button states, grid class, toggle + clear-on-manual-edit behavior) and "Surface 4" entry affordance lines 262-279
  </read_first>
  <action>
    In CohortBuilderPage.tsx: add an applyPreset(preset) handler that sets filters.preset and, for
    flaggedQuality, also sets filters.flaggedCaseIds = new Set of caseIds where a qualityFlag has
    status 'open' (read qualityFlags from the DataContext hook). Add a clearPreset() that sets
    filters.preset to undefined (and clears flaggedCaseIds). Render the Quick presets section between
    the filter-panel heading and the Diagnosis checkboxes per UI-SPEC Surface 3: a cohortPresets
    section label, a grid grid-cols-2 gap-2 of four Button elements (presetTherapyBreaker,
    presetImplausibleCrt, presetFlaggedQuality, presetImplausibleVisus). Default state uses variant
    ghost size sm with px-3 + aria-pressed false; the active preset uses variant soft + className
    ring-1 ring-[var(--color-teal)] + aria-pressed true and toggles off on click. In every manual
    filter onChange handler (diagnosis, gender, age, visus, CRT, centers), clear filters.preset (and
    flaggedCaseIds) so a manual edit drops stale preset semantics.
    Add an Advanced filters trigger button below Reset (variant accent when hasAdvancedFilters else
    ghost, Sliders icon, optional teal Badge with activeAdvancedCount) that opens AdvancedFilterDialog;
    wire the dialog onApply to merge the returned Partial into filters and onClose to hide it.
    Update the applyFilters useMemo (line 146) to pass the third options argument {
    therapyInterrupterDays, therapyBreakerDays, crtImplausibleThresholdUm } from getSettings(). Extend
    tests/advancedFilterDialog.test.tsx (or the validation/preset tests) to cover preset toggle, clear-
    on-manual-edit, and flaggedQuality building flaggedCaseIds. Run RED to GREEN.
  </action>
  <verify>
    <automated>npm run test -- advancedFilterDialog --run</automated>
  </verify>
  <acceptance_criteria>
    - CohortBuilderPage.tsx contains applyPreset and clearPreset and renders four preset Button elements with aria-pressed
    - flaggedQuality preset builds filters.flaggedCaseIds from open qualityFlags
    - editing a manual filter field clears filters.preset
    - the applyFilters call at line 146 passes an options object containing crtImplausibleThresholdUm
    - the Advanced filters trigger opens AdvancedFilterDialog and onApply merges advanced fields into filters
    - npm run test -- advancedFilterDialog --run exits 0
  </acceptance_criteria>
  <done>Preset buttons and the advanced dialog narrow the live cohort through applyFilters; presets toggle and clear on manual edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preset button click -> filters.preset | Internal UI event setting a typed literal; no untrusted input |
| advanced dialog input -> filters | Numeric (HbA1c) + code-selection input; bounded by the rendered option lists and applyFilters predicate guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Tampering | filters.preset set from a button | accept | preset is a TypeScript literal union assigned from a fixed set of four button handlers; no free-form input. Persistence-layer whitelist (Plan 02 safe-pick / T-33-04) guards the deserialization path. |
| T-33-07 | Information Disclosure | flaggedCaseIds derived from qualityFlags | accept | Derived in-memory from DataContext which already honors per-user site restriction upstream; the Set holds case ids the user can already see. No new exposure. |
</threat_model>

<verification>
- npm run test -- advancedFilterDialog --run green (dialog + preset wiring)
- npm run test:ci green (828 baseline + Phase 33 tests; live results narrow per preset/advanced selection)
</verification>

<success_criteria>
- Four presets selectable in the cohort builder; toggle on/off; clear on manual edit (COH-03)
- Flagged preset filters by open quality flags
- Advanced modal exposes the 5 curated attributes and narrows the cohort on Apply (COH-04)
- applyFilters receives thresholds from getSettings()
</success_criteria>

<output>
After completion, create `.planning/phases/33-cohort-builder-ux-advanced-filters/33-03-SUMMARY.md`
</output>
