---
phase: 29-home-panel-ux
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/recentActivityStore.test.ts
  - tests/qualityPageDeepLink.test.tsx
  - tests/landingPageAlerts.test.tsx
  - tests/jumpBackIn.test.tsx
  - src/i18n/translations.ts
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "Four Wave 0 test files exist and run (RED — failing or skipped until production code lands)"
    - "Two new i18n keys (reviewTherapyBreakers, reviewFlaggedCases) exist in DE + EN"
    - "npm run test:ci still passes its 619 baseline (new RED tests are isolated/skipped, not regressing existing)"
  artifacts:
    - path: "tests/recentActivityStore.test.ts"
      provides: "Unit tests for store dedupe/cap-5/clear/clearAll/silent-failure"
      contains: "recentActivityStore"
    - path: "tests/qualityPageDeepLink.test.tsx"
      provides: "Deep-link filter-seeding + unknown-param fallback tests"
      contains: "therapy=breaker"
    - path: "tests/landingPageAlerts.test.tsx"
      provides: "Review-button navigation-target tests"
      contains: "/quality?therapy=breaker"
    - path: "tests/jumpBackIn.test.tsx"
      provides: "Jump Back In empty-state vs rows + row-click navigation tests"
      contains: "jump-back-in-empty"
    - path: "src/i18n/translations.ts"
      provides: "reviewTherapyBreakers + reviewFlaggedCases aria-label keys"
      contains: "reviewTherapyBreakers"
  key_links:
    - from: "tests/recentActivityStore.test.ts"
      to: "src/services/recentActivityStore.ts"
      via: "module import of getEntries/record/clear/clearAll"
      pattern: "recentActivityStore"
    - from: "src/i18n/translations.ts"
      to: "LandingPage Review buttons"
      via: "t('reviewTherapyBreakers') / t('reviewFlaggedCases') aria-labels"
      pattern: "reviewTherapyBreakers"
---

<objective>
Lay the Wave 0 foundation for Phase 29: the four test files named in VALIDATION.md (so every downstream task has an `<automated>` target) and the two missing i18n keys used as Review-button aria-labels.

Purpose: Tests-first satisfies the Nyquist rule — every later task verifies against a file that already exists. The i18n keys are a hard dependency for the LandingPage wiring in Plan 04.
Output: 4 new test files (RED), 2 new translation keys (DE + EN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-home-panel-ux/29-RESEARCH.md
@.planning/phases/29-home-panel-ux/29-PATTERNS.md
@.planning/phases/29-home-panel-ux/29-VALIDATION.md
@.planning/phases/29-home-panel-ux/29-UI-SPEC.md

<interfaces>
<!-- The record shape the store tests assert against (UI-SPEC Component Inventory). -->
RecentActivityEntry: { id: string; label: string; sub: string; path: string; visitedAt: number }
Store API (to be built in Plan 02): getEntries(username), record(username, entry), clear(username), clearAll()
QualityStatus union (shared/types/fhir.ts:175): 'unchecked' | 'in_progress' | 'reviewed'  — NO 'flagged' value
Deep-link contract: /quality?therapy=breaker → filterTherapy='breaker'; /quality?status=flagged → filterStatus='in_progress'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reviewTherapyBreakers + reviewFlaggedCases i18n keys</name>
  <files>src/i18n/translations.ts</files>
  <read_first>
    - src/i18n/translations.ts (the `attention*` / `review` key block at lines 844-861 — the structure to mirror)
    - .planning/phases/29-home-panel-ux/29-PATTERNS.md (§ `src/i18n/translations.ts` — exact new-key block + insertion point)
    - .planning/phases/29-home-panel-ux/29-UI-SPEC.md (§ Copywriting Contract; § Attention needed panel — keys are aria-labels)
  </read_first>
  <action>
    Insert two new translation keys immediately after the existing `review` key (currently line 861), before the session-management comment block. Add `reviewTherapyBreakers` with `de: 'Therapie-Abbrecher prüfen'`, `en: 'Review therapy breakers'`; add `reviewFlaggedCases` with `de: 'Markierte Fälle prüfen'`, `en: 'Review flagged cases'`. These supply screen-reader-distinguishable aria-labels for the two "Review" buttons (both show visible text `t('review')`). Do NOT modify the existing `jumpBackIn`, `jumpBackInEmpty`, or `review` keys — they already exist and are reused as-is. Keep both DE and EN; the project's translation type requires both locales per key.
  </action>
  <verify>
    <automated>grep -c "reviewTherapyBreakers\|reviewFlaggedCases" src/i18n/translations.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reviewTherapyBreakers" src/i18n/translations.ts` returns >= 1 and `grep -c "reviewFlaggedCases" src/i18n/translations.ts` returns >= 1.
    - Both keys carry a `de:` and an `en:` value (German strings 'Therapie-Abbrecher prüfen' / 'Markierte Fälle prüfen'; English 'Review therapy breakers' / 'Review flagged cases').
    - `npm run build` succeeds (TypeScript translation map type is satisfied — both locales present).
    - Existing `review` key is unchanged.
  </acceptance_criteria>
  <done>Both keys present with DE+EN; build passes; existing keys untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/recentActivityStore.test.ts (node env, RED)</name>
  <files>tests/recentActivityStore.test.ts</files>
  <read_first>
    - tests/authHeaders.test.ts (the canonical `vi.stubGlobal` mock pattern + `beforeEach` reset)
    - .planning/phases/29-home-panel-ux/29-PATTERNS.md (§ Test File Patterns — localStorage `vi.stubGlobal` with `.length`/`.key()`, store skeleton)
    - .planning/phases/29-home-panel-ux/29-RESEARCH.md (§ Validation Architecture test map; § Code Examples store skeleton)
  </read_first>
  <action>
    Create a node-environment test file (NO `// @vitest-environment jsdom` docblock). Stub `localStorage` via `vi.stubGlobal` with an object backed by a plain `Record<string,string>` exposing `getItem`, `setItem`, `removeItem`, a `length` getter, `key(i)`, and `clear()` — call `vi.stubGlobal` BEFORE importing the module under test (`src/services/recentActivityStore.ts`, which Plan 02 creates). Reset the backing record in `beforeEach`. Cover: (a) `record` dedupes by `id` and moves the repeated entry to index 0 with a fresh `visitedAt`; (b) `record` caps the list at 5 entries (oldest dropped); (c) `clear(username)` removes the `emd-recent:<username>` key only; (d) `clearAll()` removes every `emd-recent:*` key while leaving non-matching keys (e.g. `emd-theme`) intact; (e) per-username isolation — recording under user A's key does not appear under user B's key; (f) localStorage failure is swallowed — stub `setItem` to throw and assert `record` does NOT throw. Use the locked key prefix `emd-recent:`. This test is RED until Plan 02 lands the store. Use Vitest assertions (`expect(...).toEqual` / `.toBe` / `.not.toThrow`) — no jest-dom.
  </action>
  <verify>
    <automated>npx vitest run tests/recentActivityStore.test.ts 2>&1 | grep -qE "recentActivityStore|Cannot find module|FAIL|failed" && echo "test file present (RED expected)"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/recentActivityStore.test.ts` exists and is discovered by vitest (it RUNS — failing on the missing module is the expected RED state until Plan 02).
    - Tests reference the key prefix string `emd-recent:`.
    - Includes a per-username isolation test (user A vs user B keys) — proving the D-01 security control.
    - Includes a silent-failure test that stubs `localStorage.setItem` to throw and asserts no throw escapes `record`.
    - No `// @vitest-environment jsdom` docblock (node env).
    - No jest-dom matchers used.
  </acceptance_criteria>
  <done>Store test file exists, runs, asserts dedupe/cap-5/clear/clearAll/per-username-isolation/silent-failure; RED pending Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Create the three jsdom integration test files (RED)</name>
  <files>tests/qualityPageDeepLink.test.tsx, tests/landingPageAlerts.test.tsx, tests/jumpBackIn.test.tsx</files>
  <read_first>
    - .planning/phases/29-home-panel-ux/29-PATTERNS.md (§ Test File Patterns — jsdom docblock, module-level `vi.mock` for useAuth/useData/useLanguage/useRecentActivity, MemoryRouter + initialEntries, RTL no-jest-dom)
    - .planning/phases/29-home-panel-ux/29-RESEARCH.md (§ Test Conventions; § Validation Architecture test map)
    - .planning/phases/29-home-panel-ux/29-UI-SPEC.md (§ Interaction Contract — empty-state testid `jump-back-in-empty`, row layout, navigate targets; § Deep-Link Query Contract)
    - src/pages/LandingPage.tsx (current Review buttons lines 267,281-285; jump-back-in-empty testid line 246; center-row pattern)
    - src/pages/QualityPage.tsx (filterStatus/filterTherapy state lines 89-91; no early returns before component body)
  </read_first>
  <action>
    Create three jsdom-environment test files, each with a `// @vitest-environment jsdom` docblock at the top. Mock `../src/context/AuthContext` (export `useAuth` returning `{ user: { username: 'test-user', role: 'admin', centers: [] } }` plus `QUALITY_ROLES`), `../src/context/DataContext` (`useData` returning empty `activeCases`, `centers`, `cases`, `loading:false`, and any `qualityFlags`/`therapyBreakerDays` the page reads), and `../src/context/LanguageContext` (`useLanguage` returning `{ t: (k) => k, locale: 'en' }`) at module level via `vi.mock`. Use `queryByText(...).not.toBeNull()` / `.toBeNull()` and `data-testid` queries — NO jest-dom.
    (a) `tests/landingPageAlerts.test.tsx` [UX-01]: render LandingPage in a MemoryRouter, spy on `useNavigate` (mock `react-router-dom`'s `useNavigate` to return a spy while keeping `MemoryRouter`), click the therapy-breaker Review button → assert spy called with `'/quality?therapy=breaker'`; click the implausible-CRT Review button → assert spy called with `'/quality?status=flagged'`. Assert the CRT button is present even for a non-QUALITY role (gate removed in Plan 04). RED until Plan 04.
    (b) `tests/qualityPageDeepLink.test.tsx` [UX-01]: render QualityPage inside `<MemoryRouter initialEntries={['/quality?therapy=breaker']}>` and assert the therapy filter select reflects `breaker`; a second case with `initialEntries={['/quality?status=flagged']}` asserts the status filter resolves to `in_progress` (the mapped value — NOT the literal `flagged`); a third case with an unknown value (e.g. `?therapy=zzz`) asserts fallback to `all`. Mock useData so that filter controls render. RED until Plan 03.
    (c) `tests/jumpBackIn.test.tsx` [UX-02]: mock `../src/hooks/useRecentActivity` at module level. Case 1: `useRecentActivity` returns `entries: []` → assert the `data-testid="jump-back-in-empty"` element is present. Case 2: returns two entries → assert both `label`s render and `jump-back-in-empty` is absent. Case 3: clicking the first row navigates to that entry's `path` (assert against a `useNavigate` spy). RED until Plan 04.
    Do NOT inline production code; these are test scaffolds describing expected behavior. It is acceptable for these tests to fail to compile/run against not-yet-existing behavior — that is the RED state.
  </action>
  <verify>
    <automated>ls tests/qualityPageDeepLink.test.tsx tests/landingPageAlerts.test.tsx tests/jumpBackIn.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist with a `// @vitest-environment jsdom` first line.
    - `tests/landingPageAlerts.test.tsx` asserts navigation to the exact strings `/quality?therapy=breaker` and `/quality?status=flagged`.
    - `tests/qualityPageDeepLink.test.tsx` asserts the `status=flagged` case resolves to the internal value `in_progress` (NOT `flagged`), and asserts an unknown param falls back to `all`.
    - `tests/jumpBackIn.test.tsx` asserts both the `jump-back-in-empty` empty branch and a populated-rows branch, plus row-click navigation to `entry.path`.
    - All assertions use `queryByText().not.toBeNull()/.toBeNull()` or `data-testid` — no `toBeInTheDocument`/jest-dom.
    - Files are discovered by vitest (RED expected — production code lands in Plans 03/04).
  </acceptance_criteria>
  <done>Three jsdom test files exist encoding UX-01 + UX-02 behaviors; RED pending Plans 03/04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (test + i18n only) | This plan adds no runtime code paths; no untrusted input is processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | tests/recentActivityStore.test.ts | mitigate | Test explicitly asserts per-username key isolation (D-01) and silent-failure swallowing, so the security controls implemented in Plan 02 are pinned by a failing-by-default test. |
| T-29-02 | Information Disclosure | src/i18n/translations.ts | accept | New keys are static UI strings (aria-labels); contain no patient data, tokens, or secrets. No runtime data flow. |
</threat_model>

<verification>
- `grep -c "reviewTherapyBreakers\|reviewFlaggedCases" src/i18n/translations.ts` >= 2 occurrences.
- All four test files exist and are discovered by vitest.
- `npm run test:ci` does not regress the 619 baseline beyond the new (isolated/RED) Phase 29 tests — confirm pre-existing tests stay green.
</verification>

<success_criteria>
- Two new i18n keys present (DE+EN), build passes.
- Four Wave 0 test files exist, encode the VALIDATION.md per-task behaviors, and run under vitest (RED expected for not-yet-built production code).
</success_criteria>

<output>
After completion, create `.planning/phases/29-home-panel-ux/29-01-SUMMARY.md`
</output>
