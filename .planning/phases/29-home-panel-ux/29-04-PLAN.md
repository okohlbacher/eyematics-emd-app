---
phase: 29-home-panel-ux
plan: 04
type: execute
wave: 2
depends_on: [29-01, 29-02, 29-03]
files_modified:
  - src/pages/LandingPage.tsx
  - src/context/AuthContext.tsx
  - src/services/authHeaders.ts
  - src/pages/AnalysisPage.tsx
  - src/components/outcomes/OutcomesView.tsx
  - src/pages/QualityPage.tsx
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "The therapy-breaker Review button routes to /quality?therapy=breaker; the implausible-CRT button routes to /quality?status=flagged (gate removed)"
    - "Jump Back In renders a row per recent entry (most-recent first); each row's arrow routes to entry.path; empty state preserved when no entries"
    - "Opening a case in QualityPage, and visiting AnalysisPage / OutcomesView (cohort active), records a recent-activity entry"
    - "Explicit logout clears the user's recents before clearing the user; cross-tab BroadcastChannel logout clears all emd-recent:* keys"
  artifacts:
    - path: "src/pages/LandingPage.tsx"
      provides: "Review-button targets + Jump Back In conditional row list"
      contains: "/quality?therapy=breaker"
    - path: "src/context/AuthContext.tsx"
      provides: "recents clear in performLogout (D-02)"
      contains: "recentActivityStore.clear"
    - path: "src/services/authHeaders.ts"
      provides: "recents clearAll in cross-tab logout branch (D-02)"
      contains: "recentActivityStore.clearAll"
  key_links:
    - from: "src/pages/LandingPage.tsx"
      to: "useRecentActivity().entries"
      via: "conditional row render + navigate(entry.path)"
      pattern: "useRecentActivity"
    - from: "src/context/AuthContext.tsx performLogout"
      to: "recentActivityStore.clear(user?.username)"
      via: "call before setUser(null)"
      pattern: "recentActivityStore\\.clear\\("
    - from: "src/services/authHeaders.ts logout branch"
      to: "recentActivityStore.clearAll()"
      via: "call before sessionStorage.removeItem('emd-token')"
      pattern: "recentActivityStore\\.clearAll\\(\\)"
---

<objective>
Wire the home panel UX end-to-end: fix the two Review-button targets and remove the stale role gate (UX-01), render Jump Back In rows from the hook (UX-02), add recording triggers on the three review surfaces, and clear recents on both logout paths (D-02).

Purpose: Closes UX-01 (actionable alerts) and UX-02 (return-to-recent-work + privacy-on-logout).
Output: modified LandingPage, AuthContext, authHeaders, AnalysisPage, OutcomesView, QualityPage. Makes `tests/landingPageAlerts.test.tsx` and `tests/jumpBackIn.test.tsx` (Plan 01) GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-home-panel-ux/29-RESEARCH.md
@.planning/phases/29-home-panel-ux/29-PATTERNS.md
@.planning/phases/29-home-panel-ux/29-UI-SPEC.md

@src/services/recentActivityStore.ts
@src/hooks/useRecentActivity.ts

<interfaces>
<!-- Store + hook from Plan 02 -->
useRecentActivity(): { entries: RecentActivityEntry[]; record(entry: Omit<RecentActivityEntry,'visitedAt'>): void; clear(): void }
import * as recentActivityStore from '.../services/recentActivityStore'  // clear(username), clearAll()
RecentActivityEntry: { id; label; sub; path; visitedAt }

<!-- LandingPage exact change sites (verified) -->
line 11: import { useNavigate } from 'react-router-dom'   (navigate already available)
line 4 : ChevronRight already imported
line 34: const canSeeDocQuality = user ? QUALITY_ROLES.includes(user.role) : false   (this gate is removed for the CRT button)
line 246: <div data-testid="jump-back-in-empty" ...> {t('jumpBackInEmpty')} </div>   (preserve as entries.length===0 branch)
line 267: <Button variant="ghost" size="sm" onClick={() => navigate('/cohort')}>            → therapy-breaker button
lines 281-285: {canSeeDocQuality && (<Button ... onClick={() => navigate('/doc-quality')}>)} → implausible-CRT button
center-row interactive pattern: lines 176-228 (role=button, tabIndex=0, onKeyDown Enter, hover:bg-[var(--color-surface-2)])

<!-- AuthContext performLogout (lines 132-145) — clear BEFORE setUser(null); user is in closure; add user to dep array (already [user] per line 161? confirm) -->
<!-- authHeaders logout branch (lines 28-30) — clearAll() BEFORE sessionStorage.removeItem('emd-token'); plain module, no React context -->

<!-- i18n keys from Plan 01: reviewTherapyBreakers, reviewFlaggedCases (aria-labels); review/jumpBackInEmpty already exist -->
<!-- Sub-label keys (UI-SPEC Copywriting): Quality review / Outcomes / Analysis — reuse existing nav/quality keys, do not invent if covered -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Review-button targets + render Jump Back In rows</name>
  <files>src/pages/LandingPage.tsx</files>
  <read_first>
    - src/pages/LandingPage.tsx (lines 4 ChevronRight, 11 useNavigate, 34 canSeeDocQuality, 176-228 center-row pattern, 246 empty-state testid, 267 + 281-285 Review buttons)
    - .planning/phases/29-home-panel-ux/29-PATTERNS.md (§ `src/pages/LandingPage.tsx` — exact button before/after, row adaptation, imports, empty-state preserve)
    - .planning/phases/29-home-panel-ux/29-UI-SPEC.md (§ Interaction Contract Jump Back In + Attention needed; § Copywriting Contract; § Typography/Color tokens for rows)
    - .planning/phases/29-home-panel-ux/29-RESEARCH.md (§ Integration Points §3; § Pitfall 5 role-gate removal)
    - src/hooks/useRecentActivity.ts (the hook to consume)
  </read_first>
  <action>
    In `src/pages/LandingPage.tsx` add `import { useRecentActivity } from '../hooks/useRecentActivity'` (ChevronRight and useNavigate already imported). Call `const { entries } = useRecentActivity();` in the component body.
    Attention-needed buttons: change the therapy-breaker button (line 267) target from `navigate('/cohort')` to `navigate('/quality?therapy=breaker')` and add `aria-label={t('reviewTherapyBreakers')}`. Change the implausible-CRT button (lines 281-285) target from `navigate('/doc-quality')` to `navigate('/quality?status=flagged')`, add `aria-label={t('reviewFlaggedCases')}`, and REMOVE the `canSeeDocQuality &&` wrapper so the button shows for all authenticated users (D-04 discretion: `/quality` is ProtectedRoute, reachable by all roles; per RESEARCH Pitfall 5 the center filtering already scopes visible cases). Leave the `canSeeDocQuality` const in place only if still used by the center-row at line 184; otherwise remove the now-dead const. Keep the visible button text `t('review')`.
    Jump Back In panel: replace the empty-only block so that when `entries.length === 0` it renders the EXISTING `data-testid="jump-back-in-empty"` div with `t('jumpBackInEmpty')` (do not remove the testid), and when `entries.length > 0` it maps `entries` to one interactive row each. Each row mirrors the center-row accessibility pattern: `role="button"`, `tabIndex={0}`, `onClick={() => navigate(entry.path)}`, `onKeyDown={(e) => e.key === 'Enter' && navigate(entry.path)}`, `className` with `cursor-pointer hover:bg-[var(--color-surface-2)] transition-colors`, grid `gridTemplateColumns: '1fr auto'`, `px-5 py-3`, row divider `borderBottom: 1px solid var(--color-line)` except the last row. Left cell: 13px semibold `var(--color-ink)` `entry.label` over 13px regular `var(--color-ink-3)` `entry.sub`. Right cell: `<ChevronRight className="w-3.5 h-3.5 text-[var(--color-ink-3)]" aria-hidden="true" />`. Use `entry.id` (or index fallback) as React key. Use existing token classes — no new colors. camelCase; throw-only.
  </action>
  <verify>
    <automated>npx vitest run tests/landingPageAlerts.test.tsx tests/jumpBackIn.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tests/landingPageAlerts.test.tsx` GREEN: therapy button → `/quality?therapy=breaker`, CRT button → `/quality?status=flagged`, CRT button present for a non-QUALITY role.
    - `tests/jumpBackIn.test.tsx` GREEN: empty branch shows `jump-back-in-empty`; populated branch renders rows; row click navigates to `entry.path`.
    - `grep -c "navigate('/cohort')" src/pages/LandingPage.tsx` for the breaker button site is gone (replaced); `grep -c "/quality?therapy=breaker" src/pages/LandingPage.tsx` >= 1 and `/quality?status=flagged` >= 1.
    - The CRT Review button is no longer wrapped in `canSeeDocQuality &&`.
    - Both Review buttons carry distinct `aria-label`s (`reviewTherapyBreakers`, `reviewFlaggedCases`).
    - Rows use `role="button"` + `tabIndex={0}` + `onKeyDown` Enter; ChevronRight is `aria-hidden`.
    - `npm run lint` clean (no unused `canSeeDocQuality` if removed); `npm run build` clean.
  </acceptance_criteria>
  <done>Both buttons route correctly with aria-labels and no stale gate; Jump Back In shows rows or the preserved empty state; rows navigate accessibly.</done>
</task>

<task type="auto">
  <name>Task 2: Add recording triggers on the three review surfaces</name>
  <files>src/pages/QualityPage.tsx, src/pages/AnalysisPage.tsx, src/components/outcomes/OutcomesView.tsx</files>
  <read_first>
    - .planning/phases/29-home-panel-ux/29-RESEARCH.md (§ Integration Points §5 recording surfaces + QualityPage trigger note; § Pitfall 4 effect deps; § Open Questions #2)
    - .planning/phases/29-home-panel-ux/29-UI-SPEC.md (§ Interaction Contract — Recording trigger; § Copywriting — sub-label DE/EN)
    - src/pages/QualityPage.tsx (selectedCase state line 83; selectedCase.pseudonym usage)
    - src/pages/AnalysisPage.tsx (cohort/savedSearch + current URL params)
    - src/components/outcomes/OutcomesView.tsx (activeCases / cohortId; useSearchParams at line 83)
    - src/hooks/useRecentActivity.ts (record signature)
  </read_first>
  <action>
    Add `const { record } = useRecentActivity();` to each of the three components and record a visit via a `useEffect` with a narrow dependency (NEVER the component's main render state — per Pitfall 4, that loops).
    QualityPage: record only when a case is actually opened — `useEffect(() => { if (selectedCase) record({ id: selectedCase.id, label: selectedCase.pseudonym, sub: t('audit_detail_view_quality') OR a 'Quality review' sub-label key, path: '/quality' }); }, [selectedCase])`. Use `path: '/quality'` (not the filtered URL) so restoring lands on the review surface without re-pre-filtering (research §5 note). Pick the sub-label from an existing key matching the UI-SPEC copy "Quality review"/"Qualitätsprüfung"; if no exact key exists, reuse the closest existing nav/quality label rather than adding a new key (UI-SPEC says do not add keys unless sub-labels are uncovered).
    AnalysisPage: `useEffect(() => { record({ id: cohortId || 'analysis', label: savedSearch?.name || t('navAnalysis'), sub: <Analysis sub-label>, path: window.location.pathname + window.location.search }); }, [])` — mount-only (empty deps) or keyed to `cohortId` if it can change without remount.
    OutcomesView: `useEffect(() => { if (activeCases.length > 0) record({ id: cohortId || 'outcomes', label: <cohort/outcomes label>, sub: <Outcomes sub-label>, path: window.location.pathname + window.location.search }); }, [cohortId])` (or `[]` if cohortId is stable per mount) — record only when a cohort is active.
    Capture `path` as `window.location.pathname + window.location.search` for Analysis/Outcomes so existing view params (`?cohort=`, `?filter=`, `?tab=`, `?metric=`) are preserved for restoration. Keep each effect's deps minimal and stable. camelCase; async not needed.
  </action>
  <verify>
    <automated>npm run test:ci 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three files calls `useRecentActivity()` and invokes `record(...)` inside a `useEffect` with a minimal/stable dep array (`[selectedCase]`, `[]`, or `[cohortId]`) — NOT the main filter/render state.
    - QualityPage records with `id = selectedCase.id`, `label = selectedCase.pseudonym`, `path = '/quality'`, only when `selectedCase` is non-null.
    - AnalysisPage and OutcomesView record `path = window.location.pathname + window.location.search`; OutcomesView records only when `activeCases.length > 0`.
    - No new i18n keys added unless the UI-SPEC sub-label copy is genuinely uncovered (prefer reusing existing keys).
    - `npm run lint` clean (no exhaustive-deps warnings); `npm run test:ci` does not regress the 619 baseline.
  </acceptance_criteria>
  <done>Three surfaces record a deduped, restorable recent-activity entry with safe effect deps; no render loop; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Clear recents on both logout paths (D-02)</name>
  <files>src/context/AuthContext.tsx, src/services/authHeaders.ts</files>
  <read_first>
    - src/context/AuthContext.tsx (performLogout lines 132-145; the useCallback dep array; existing top-of-file service imports)
    - src/services/authHeaders.ts (BroadcastChannel handler lines 23-34; logout branch lines 28-30; existing imports)
    - .planning/phases/29-home-panel-ux/29-PATTERNS.md (§ AuthContext + § authHeaders — exact insertion points, ordering constraints, import form)
    - .planning/phases/29-home-panel-ux/29-RESEARCH.md (§ Integration Points §1; § Pitfall 2 cross-tab username)
    - src/services/recentActivityStore.ts (clear / clearAll signatures)
  </read_first>
  <action>
    AuthContext: add `import * as recentActivityStore from '../services/recentActivityStore'` at the top. In `performLogout` (lines 132-145), insert `recentActivityStore.clear(user?.username ?? '')` BEFORE `setUser(null)` (the username must be captured while `user` is still in the closure; the null-safe form handles a no-user logout). Ensure `user` is in the `useCallback` dependency array — if the current deps are `[]`, change to `[user]`; if `performLogout` already lists `[user]` (confirm against the actual file — line 161 shows a related callback with `[user, performLogout]`), no dep change is needed but verify. Do not reorder the other teardown calls (`serverLogout`, `broadcastLogout`, `setToken`, `sessionStorage.removeItem`, `invalidateBundleCache`).
    authHeaders: add `import * as recentActivityStore from './recentActivityStore'`. In the `msg.type === 'logout'` branch (lines 28-30), call `recentActivityStore.clearAll()` BEFORE `sessionStorage.removeItem('emd-token')`. Use `clearAll()` (not `clear(username)`) because this module-level BroadcastChannel listener has no React context and decoding the username from the token is fragile (Pitfall 2). Leave the `window.location.href = '/login'` redirect untouched. This is a plain TS module — no hooks; the namespace import is the correct form. camelCase; throw-only.
  </action>
  <verify>
    <automated>grep -n "recentActivityStore.clear" src/context/AuthContext.tsx && grep -n "recentActivityStore.clearAll" src/services/authHeaders.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/context/AuthContext.tsx` imports `recentActivityStore` and calls `recentActivityStore.clear(user?.username ?? '')` BEFORE `setUser(null)` inside `performLogout`; `user` is in the `performLogout` useCallback dep array.
    - `src/services/authHeaders.ts` imports `recentActivityStore` (`./recentActivityStore`) and calls `recentActivityStore.clearAll()` BEFORE `sessionStorage.removeItem('emd-token')` in the logout branch.
    - No hooks used in authHeaders (plain module); no token decoding added there.
    - `npm run build` clean; `npm run lint` clean (no exhaustive-deps warning on performLogout); `npm run test:ci` green (619 baseline, including the new Phase 29 tests now GREEN).
  </acceptance_criteria>
  <done>Both logout paths clear recents in the correct order; explicit logout uses per-username clear, cross-tab uses clearAll; no residual patient trail after sign-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logged-in session → logged-out / next user | At sign-out the recent-activity trail must not survive into the next session on a shared workstation. |
| tab A ↔ tab B (BroadcastChannel) | A logout in one tab must purge recents for all tabs/sessions in the same browser. |
| home-panel button → /quality (access control) | Removing the `canSeeDocQuality` gate exposes the CRT Review button to all authenticated roles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Information Disclosure | residual patient trail after logout (V3 Session Mgmt) | mitigate | `performLogout` calls `recentActivityStore.clear(user?.username)` BEFORE `setUser(null)` (ordering pinned so username is captured); next login starts with an empty Jump Back In panel. |
| T-29-10 | Information Disclosure | cross-tab logout leaves stale recents (V3) | mitigate | The BroadcastChannel `'logout'` handler calls `recentActivityStore.clearAll()` (purges every `emd-recent:*` key) BEFORE token removal — covers the no-username module context (Pitfall 2). |
| T-29-11 | Elevation of Privilege | `canSeeDocQuality` gate removal (V4 Access Control) | accept | `/quality` is `ProtectedRoute` (all authenticated roles already reachable), unlike the old `/doc-quality` (`QualityRoute`). The page still enforces center-restriction on the case list, so exposing the button grants no data the role could not already see. Confirmed reachable/actionable per RESEARCH Pitfall 5 + UI-SPEC decision. |
| T-29-12 | Information Disclosure | recorded entry contents | mitigate | Recording tasks store only `id`, pseudonym `label`, `sub`, and an app-internal `path` — no clinical values or tokens (inherited control from Plan 02 T-29-04). |
</threat_model>

<verification>
- `npx vitest run tests/landingPageAlerts.test.tsx tests/jumpBackIn.test.tsx tests/qualityPageDeepLink.test.tsx tests/recentActivityStore.test.ts` all GREEN.
- `grep -n "recentActivityStore.clear" src/context/AuthContext.tsx` and `grep -n "recentActivityStore.clearAll" src/services/authHeaders.ts` both match.
- `npm run test:ci` passes the full suite (619 baseline + new Phase 29 tests, no regression).
- `npm run build` and `npm run lint` clean.
</verification>

<success_criteria>
- UX-01: both Review buttons route to the correct pre-filtered `/quality` deep-links, gate removed, no dead-ends or console errors.
- UX-02: Jump Back In renders restorable rows (most-recent first) or the preserved empty state; visits are recorded; recents cleared on both logout paths.
</success_criteria>

<output>
After completion, create `.planning/phases/29-home-panel-ux/29-04-SUMMARY.md`
</output>
