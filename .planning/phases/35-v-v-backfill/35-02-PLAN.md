---
phase: 35-v-v-backfill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-admin-session-control-ui/28-VERIFICATION.md
autonomous: true
requirements: [VVBACK-02]
must_haves:
  truths:
    - "28-VERIFICATION.md exists with frontmatter status: passed"
    - "Each of SESS-01 + SESSUI-01/02/03 success criteria maps to a concrete code reference at the v1.10 tag"
    - "Each criterion cites a passing test runnable against the current tree"
    - "Every code reference uses git show v1.10:<path> / git grep <pattern> v1.10 form — no HEAD line numbers"
  artifacts:
    - path: ".planning/phases/28-admin-session-control-ui/28-VERIFICATION.md"
      provides: "Goal-backward verification report for Phase 28 (SESS-01 + SESSUI-01/02/03) anchored to v1.10"
      contains: "status: passed"
  key_links:
    - from: "28-VERIFICATION.md Observable Truths table"
      to: "server/sessionsDb.ts, server/authApi.ts, src/pages/AdminPage.tsx, src/services/ttlConversion.ts, src/pages/SettingsPage.tsx at v1.10"
      via: "git show v1.10:<path> / git grep <pattern> v1.10 citations"
      pattern: "v1\\.10:"
    - from: "28-VERIFICATION.md Behavioral Spot-Checks table"
      to: "tests/sessionRevoke.test.ts, tests/ttlConversion.test.ts, tests/settingsApi.test.ts"
      via: "passing-test evidence (npm run test:ci)"
      pattern: "test:ci"
---

<objective>
Create `.planning/phases/28-admin-session-control-ui/28-VERIFICATION.md` — a goal-backward
verification report proving Phase 28 (Admin Session Control UI, requirements SESS-01 +
SESSUI-01/02/03) was delivered as shipped in the v1.10 milestone.

Purpose: Phase 28 shipped without VERIFICATION.md (accepted as v1.10 tech debt). This plan closes
VVBACK-02 by producing the missing formal paper trail. Per H1, every code reference cites the
**`v1.10` git tag** (annotated tag object `b29e892`, commit `798dc74`) so the document is immune
to v1.11 changes (Phases 32–34) and the Phase 36 compaction.

Output: One new VERIFICATION.md following the 29/31-VERIFICATION.md exemplar structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Exemplar to mirror (structure, section order, tables, frontmatter):
@.planning/phases/29-home-panel-ux/29-VERIFICATION.md
@.planning/phases/31-subcohort-support/31-VERIFICATION.md

# v1.10 archive — Phase 28 goal + the four SESS-01/SESSUI success criteria:
@.planning/milestones/v1.10-ROADMAP.md

# Phase 28 shipped-evidence SUMMARYs (what was built, which tests, which commits):
@.planning/phases/28-admin-session-control-ui/28-02-SUMMARY.md
@.planning/phases/28-admin-session-control-ui/28-03-SUMMARY.md
@.planning/phases/28-admin-session-control-ui/28-04-SUMMARY.md

<interfaces>
<!-- Confirmed code anchors AT THE v1.10 TAG. Cite these via git show/git grep v1.10. -->
<!-- Do NOT cite current-HEAD line numbers — HEAD has drifted from v1.10 (Phases 32-34). -->

server/sessionsDb.ts @ v1.10:
  listActiveSessionsByUser(username): SessionRow[]  — filters revoked=0 AND expires_at>now, ordered issued_at DESC (SESSUI-01)
  revokeByUsername(username): number  — revokes all active sessions for a user (SESS-01)
  revokeSession(id): void  — revokes single jti (SESSUI-02)

server/authApi.ts @ v1.10 (route handlers, verified anchors):
  DELETE /sessions/:id  (admin-only, 404 unknown id)  — registered BEFORE the query-param variant
  DELETE /sessions?username=<u>  (admin-only, revokes all, returns count)  — SESS-01
  GET /sessions?username=<u>  (admin-only, returns active sessions)  — SESSUI-01
  inline admin guard: `req.auth.role !== 'admin'` → 403

src/services/ttlConversion.ts @ v1.10 (file present at tag):
  hoursToMs(hours); msToHours(ms); validateTtl(refreshHours, capHours) → 'ok'|'refreshMin'|'capMin' (SESSUI-03)

src/pages/SettingsPage.tsx @ v1.10:
  TTL form persists auth.refreshTokenTtlMs / auth.refreshAbsoluteCapMs via updateSettings → config/settings.yaml (SESSUI-03)

src/pages/AdminPage.tsx @ v1.10 (verified anchors):
  expandedSessionUser state; handleRevokeSession (DELETE /sessions/:id); handleSignOutEverywhere (DELETE /sessions?username=)
  aria-expanded session accordion (SESSUI-01/02 + SESS-01 UI)

Tests proving behavior (present at v1.10 AND in current tree):
  tests/sessionRevoke.test.ts (10 tests), tests/ttlConversion.test.ts (10 tests), tests/settingsApi.test.ts (TTL round-trip)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather v1.10-anchored evidence for SESS-01 + SESSUI-01/02/03</name>
  <files>.planning/phases/28-admin-session-control-ui/28-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/29-home-panel-ux/29-VERIFICATION.md (exemplar: frontmatter + section order)
    - .planning/milestones/v1.10-ROADMAP.md (Phase 28 section: the four success criteria — these become the Observable Truths)
    - .planning/phases/28-admin-session-control-ui/28-02-SUMMARY.md, 28-03-SUMMARY.md, 28-04-SUMMARY.md (shipped evidence + commits)
  </read_first>
  <action>
    Collect the concrete evidence for the VERIFICATION tables, citing the `v1.10` tag for every code
    reference (read-only; record output for Task 2; do NOT cite HEAD):
    - `git grep -nE "listActiveSessionsByUser|revokeByUsername|revokeSession" v1.10 -- server/sessionsDb.ts` — backend listing + revocation primitives (SESSUI-01 listing; SESS-01 all; SESSUI-02 single).
    - `git grep -nE "/sessions|req.auth.role|403" v1.10 -- server/authApi.ts` — the three admin-only endpoints (GET list, DELETE :id, DELETE ?username) + admin guard (SESSUI-01/02 + SESS-01 criteria 1–3).
    - `git show v1.10:src/services/ttlConversion.ts` — hoursToMs/msToHours/validateTtl for SESSUI-03 criterion 4; `git grep -nE "refreshTokenTtlMs|refreshAbsoluteCapMs|updateSettings" v1.10 -- src/pages/SettingsPage.tsx` — persistence to settings.yaml.
    - `git grep -nE "expandedSessionUser|handleRevokeSession|handleSignOutEverywhere|aria-expanded" v1.10 -- src/pages/AdminPage.tsx` — the UI listing/revoke/sign-out-everywhere wiring.
    - Confirm test files at the tag: `git ls-tree v1.10 -- tests/sessionRevoke.test.ts tests/ttlConversion.test.ts tests/settingsApi.test.ts`.
    - Run `npm run test:ci` and record the pass count (must be 901/901 green) as the passing-test evidence.
    Map each of the four ROADMAP Phase-28 success criteria to: the criterion text, a v1.10 code citation, and the test that proves it. Note that the three "live browser 401-after-revoke" items in 28-VALIDATION Manual-Only are advisory UI confirmations, not goal-blockers — their underlying revocation logic is automated-tested via sessionRevoke.test.ts.
  </action>
  <verify>
    <automated>git grep -q "listActiveSessionsByUser" v1.10 -- server/sessionsDb.ts && git show v1.10:src/services/ttlConversion.ts | grep -q "validateTtl" && git grep -q "handleSignOutEverywhere" v1.10 -- src/pages/AdminPage.tsx && echo ANCHORS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c "listActiveSessionsByUser" v1.10 -- server/sessionsDb.ts` returns >= 1
    - `git show v1.10:src/services/ttlConversion.ts | grep -c "validateTtl"` returns >= 1
    - `git grep -c "handleSignOutEverywhere" v1.10 -- src/pages/AdminPage.tsx` returns >= 1
    - `npm run test:ci` exits 0 (901/901 green) and the count is recorded for citation
  </acceptance_criteria>
  <done>All four Phase-28 criteria have a v1.10 code citation + passing test mapped; test suite confirmed green.</done>
</task>

<task type="auto">
  <name>Task 2: Write 28-VERIFICATION.md mirroring the exemplar structure</name>
  <files>.planning/phases/28-admin-session-control-ui/28-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/31-subcohort-support/31-VERIFICATION.md (exemplar: SC-numbered Observable Truths, Required Artifacts, Key Link, Behavioral Spot-Checks, Requirements Coverage, Human Verification Required, Gaps Summary, Summary)
    - .planning/phases/29-home-panel-ux/29-VERIFICATION.md (exemplar frontmatter shape)
  </read_first>
  <action>
    Create `.planning/phases/28-admin-session-control-ui/28-VERIFICATION.md` using the exemplar
    section order and tables, populated with Task 1 evidence. Set frontmatter:
    `phase: 28-admin-session-control-ui`, `verified:` (today's ISO timestamp), `status: passed`,
    `score: 4/4 must-haves verified`, `overrides_applied: 0`, `gaps: []`, and a `human_verification:`
    list mirroring the 31-VERIFICATION format for the three live-browser UI confirmations from
    28-VALIDATION Manual-Only (revoked session → 401 in browser; "Sign out everywhere" empties the
    UI list; TTL persists after reload) — each with test/expected/why_human fields. Because these
    are advisory UI confirmations whose logic is automated-tested, keep `status: passed` (not
    human_needed) and record them as optional confirmations.
    Body sections (match exemplar headings):
    - **Phase Goal** quoted from v1.10-ROADMAP Phase 28 goal.
    - **Observable Truths (ROADMAP Success Criteria)** table — SC1 admin lists active sessions (device/issued-at/last-used/expires-at) via GET /sessions + listActiveSessionsByUser; SC2 individual revoke via DELETE /sessions/:id → 401 next call; SC3 sign-out-everywhere via DELETE /sessions?username= → all revoked; SC4 TTL view/save persists to settings.yaml via ttlConversion + SettingsPage. Status VERIFIED, Evidence citing `v1.10:` paths/symbols.
    - **Required Artifacts** — server/sessionsDb.ts (list/revoke primitives), server/authApi.ts (3 endpoints + guard), src/services/ttlConversion.ts, src/pages/SettingsPage.tsx (TTL form), src/pages/AdminPage.tsx (accordion UI), tests/sessionRevoke.test.ts, tests/ttlConversion.test.ts, tests/settingsApi.test.ts — each at `v1.10`.
    - **Key Link Verification** — AdminPage handleRevokeSession → DELETE /sessions/:id → revokeSession; handleSignOutEverywhere → DELETE /sessions?username= → revokeByUsername; GET /sessions → listActiveSessionsByUser; SettingsPage TTL save → ttlConversion.hoursToMs → updateSettings → settings.yaml.
    - **Behavioral Spot-Checks** — the git grep/git show v1.10 commands from Task 1 + `npm run test:ci` → 901/901 PASS.
    - **Requirements Coverage** — SESS-01 / SESSUI-01 / SESSUI-02 / SESSUI-03 each SATISFIED, source plans 28-02/28-03/28-04.
    - **Human Verification Required** — list the three advisory UI confirmations (mirror 31-VERIFICATION's numbered format).
    - **Gaps Summary** — "No blocking gaps; verified as-shipped at v1.10."
    - Footer: `_Verified: <timestamp>_` / `_Verifier: Claude (gsd-verifier)_`.
    Do NOT modify any product source code. Do NOT cite current-HEAD line numbers.
  </action>
  <verify>
    <automated>grep -q "^status: passed" .planning/phases/28-admin-session-control-ui/28-VERIFICATION.md && grep -c "v1.10:" .planning/phases/28-admin-session-control-ui/28-VERIFICATION.md | awk '$1>=4{exit 0}{exit 1}' && echo VERIFICATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: passed" .planning/phases/28-admin-session-control-ui/28-VERIFICATION.md` returns 1
    - `grep -c "v1.10:" .planning/phases/28-admin-session-control-ui/28-VERIFICATION.md` returns >= 4 (one anchored citation per criterion)
    - File contains all exemplar section headings (Observable Truths, Required Artifacts, Key Link, Behavioral Spot-Checks, Requirements Coverage, Human Verification Required, Summary) — `grep -c "^###\|^##" ...` >= 7
    - `git grep -n "HEAD:" .planning/phases/28-admin-session-control-ui/28-VERIFICATION.md` returns nothing
    - No product source files appear in `git status --porcelain` (only the new .md)
  </acceptance_criteria>
  <done>28-VERIFICATION.md exists, status: passed, all 4 criteria anchored to v1.10 with passing-test evidence, structure mirrors the exemplar.</done>
</task>

</tasks>

<verification>
- `grep "^status: passed" .planning/phases/28-admin-session-control-ui/28-VERIFICATION.md` matches.
- `git grep -c "v1.10:" .planning/phases/28-admin-session-control-ui/28-VERIFICATION.md` >= 4.
- `npm run test:ci` exits 0 (no product code touched; baseline 901/901 preserved).
- `git status --porcelain` shows only `.planning/` changes.
</verification>

<success_criteria>
28-VERIFICATION.md exists, produced by goal-backward analysis of SESS-01 + SESSUI-01/02/03, each
criterion mapped to a concrete code reference at the `v1.10` tag and a passing test (VVBACK-02).
</success_criteria>

<output>
After completion, create `.planning/phases/35-v-v-backfill/35-02-SUMMARY.md`.
</output>
