---
phase: 35-v-v-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/27-stateful-session-backend/27-VERIFICATION.md
autonomous: true
requirements: [VVBACK-01]
must_haves:
  truths:
    - "27-VERIFICATION.md exists with frontmatter status: passed"
    - "Each of SESS-02/03/04 success criteria maps to a concrete code reference at the v1.10 tag"
    - "Each criterion cites a passing test runnable against the current tree"
    - "Every code reference uses git show v1.10:<path> / git grep <pattern> v1.10 form — no HEAD line numbers"
  artifacts:
    - path: ".planning/phases/27-stateful-session-backend/27-VERIFICATION.md"
      provides: "Goal-backward verification report for Phase 27 (SESS-02/03/04) anchored to v1.10"
      contains: "status: passed"
  key_links:
    - from: "27-VERIFICATION.md Observable Truths table"
      to: "server/sessionsDb.ts, server/authApi.ts, server/initAuth.ts, server/jwtUtil.ts at v1.10"
      via: "git show v1.10:<path> / git grep <pattern> v1.10 citations"
      pattern: "v1\\.10:"
    - from: "27-VERIFICATION.md Behavioral Spot-Checks table"
      to: "tests/sessionsDb.test.ts, tests/sessionRotation.test.ts, tests/rotateKey.test.ts"
      via: "passing-test evidence (npm run test:ci)"
      pattern: "test:ci"
---

<objective>
Create `.planning/phases/27-stateful-session-backend/27-VERIFICATION.md` — a goal-backward
verification report proving Phase 27 (Stateful Session Backend, requirements SESS-02/03/04)
was delivered as shipped in the v1.10 milestone.

Purpose: Phases 27 & 28 shipped without VERIFICATION.md (accepted as v1.10 tech debt). This plan
closes VVBACK-01 by producing the missing formal paper trail. Per H1, every code reference cites
the **`v1.10` git tag** (annotated tag object `b29e892`, commit `798dc74`) so the document is
immune to v1.11 changes (Phases 32–34) and the Phase 36 compaction.

Output: One new VERIFICATION.md following the 29/31-VERIFICATION.md exemplar structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Exemplar to mirror (structure, section order, tables, frontmatter):
@.planning/phases/29-home-panel-ux/29-VERIFICATION.md
@.planning/phases/31-subcohort-support/31-VERIFICATION.md

# v1.10 archive — Phase 27 goal + the four SESS-02/03/04 success criteria:
@.planning/milestones/v1.10-ROADMAP.md

# Phase 27 shipped-evidence SUMMARYs (what was built, which tests, which commits):
@.planning/phases/27-stateful-session-backend/27-02-SUMMARY.md
@.planning/phases/27-stateful-session-backend/27-03-SUMMARY.md
@.planning/phases/27-stateful-session-backend/27-04-SUMMARY.md

<interfaces>
<!-- Confirmed code anchors AT THE v1.10 TAG. Cite these via git show/git grep v1.10. -->
<!-- Do NOT cite current-HEAD line numbers — HEAD has drifted from v1.10 (Phases 32-34). -->

server/sessionsDb.ts @ v1.10 — exported API (verified via `git grep -nE "export (function|const|interface)" v1.10 -- server/sessionsDb.ts`):
  interface SessionRow; initSessionsDb; insertSession; getSession; revokeSession;
  revokeFamily; revokeByUsername; listActiveSessionsByUser; purgeExpiredSessions;
  startSessionCleanupInterval; _closeForTests

server/authApi.ts @ v1.10 (verified anchors):
  import { getSession, insertSession, ..., revokeFamily, revokeSession } from './sessionsDb.js' (line ~30)
  refresh-reuse path: `revokeFamily(payload.sid)` + 401 "Refresh token reuse detected"
  POST /rotate-key handler (admin-only)

server/initAuth.ts @ v1.10 (verified anchors):
  getJwtSecrets(): { current; prev? }  — dual-key read
  computeKeyId(secret)  — SHA256 first 8 hex
  rotateSigningKey(): { rotatedAt; prevKeyExpiresBy }  — atomic rename chain, throws NEXT_KEY_MISSING

server/jwtUtil.ts @ v1.10:
  RefreshPayload.jti required; signRefreshToken embeds jti; verifyRefreshToken dual-key fallback
  (current key first; falls back to prev on JsonWebTokenError but NOT TokenExpiredError)

Tests proving behavior (present at v1.10 AND in current tree):
  tests/sessionsDb.test.ts, tests/sessionRotation.test.ts, tests/rotateKey.test.ts, tests/authRefresh.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather v1.10-anchored evidence for SESS-02/03/04</name>
  <files>.planning/phases/27-stateful-session-backend/27-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/29-home-panel-ux/29-VERIFICATION.md (exemplar: frontmatter + Observable Truths / Required Artifacts / Key Link / Behavioral Spot-Checks / Requirements Coverage / Summary section order)
    - .planning/milestones/v1.10-ROADMAP.md (Phase 27 section: the four SESS-02/03/04 success criteria — these become the Observable Truths)
    - .planning/phases/27-stateful-session-backend/27-02-SUMMARY.md, 27-03-SUMMARY.md, 27-04-SUMMARY.md (shipped evidence)
  </read_first>
  <action>
    Collect the concrete evidence that will populate the VERIFICATION tables, citing the `v1.10`
    tag for every code reference. Run the read-only commands below and record their output for use
    in Task 2 (do NOT cite current HEAD):
    - `git show v1.10:server/sessionsDb.ts` — locate the `refresh_sessions` schema (id/sid/username/ver/issued_at/expires_at/last_used_at/revoked/key_id) for SESS-02 criterion 1, and `revokeSession`/`revokeFamily` for SESS-03.
    - `git grep -nE "Refresh token reuse detected|revokeFamily|getSession|insertSession|revokeSession" v1.10 -- server/authApi.ts` — the /refresh rotation + jti-reuse path for SESS-03 criterion 2.
    - `git grep -nE "rotateSigningKey|getJwtSecrets|computeKeyId" v1.10 -- server/initAuth.ts` and `git grep -nE "rotate-key|NEXT_KEY_MISSING" v1.10 -- server/authApi.ts` — dual-key window + admin `POST /api/auth/rotate-key` for SESS-04 criterion 3.
    - `git grep -nE "dual-key|prev|TokenExpiredError" v1.10 -- server/jwtUtil.ts` — verifyRefreshToken fallback proving prev-key tokens verify until cap (SESS-04).
    - Confirm the three test files exist at the tag: `git ls-tree v1.10 -- tests/sessionsDb.test.ts tests/sessionRotation.test.ts tests/rotateKey.test.ts`.
    - Run `npm run test:ci` once and record the pass count (must be 901/901 green on current tree) — this is the passing-test evidence for SESS-02/03/04 criterion 4 (all session-table ops covered by automated tests).
    Map each of the four ROADMAP Phase-27 success criteria to: the criterion text, a v1.10 code citation (`git show v1.10:<path>` / `git grep <pattern> v1.10`), and the test that proves it.
  </action>
  <verify>
    <automated>git show v1.10:server/sessionsDb.ts | grep -q "refresh_sessions" && git grep -q "Refresh token reuse detected" v1.10 -- server/authApi.ts && git grep -q "rotateSigningKey" v1.10 -- server/initAuth.ts && echo ANCHORS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git show v1.10:server/sessionsDb.ts | grep -c "refresh_sessions"` returns >= 1
    - `git grep -c "Refresh token reuse detected" v1.10 -- server/authApi.ts` returns 1
    - `git grep -c "rotate-key" v1.10 -- server/authApi.ts` returns >= 1
    - `npm run test:ci` exits 0 (901/901 green) and the count is recorded for citation
  </acceptance_criteria>
  <done>All four SESS criteria have a v1.10 code citation + passing test mapped; test suite confirmed green.</done>
</task>

<task type="auto">
  <name>Task 2: Write 27-VERIFICATION.md mirroring the exemplar structure</name>
  <files>.planning/phases/27-stateful-session-backend/27-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/31-subcohort-support/31-VERIFICATION.md (exemplar: SC-numbered Observable Truths table, Required Artifacts, Key Link Verification, Behavioral Spot-Checks, Requirements Coverage, Gaps Summary, Summary)
    - .planning/phases/29-home-panel-ux/29-VERIFICATION.md (exemplar frontmatter: phase/verified/status/score/overrides_applied/gaps/human_verification)
  </read_first>
  <action>
    Create `.planning/phases/27-stateful-session-backend/27-VERIFICATION.md` using the exemplar
    section order and tables, populated with the evidence gathered in Task 1. Set frontmatter:
    `phase: 27-stateful-session-backend`, `verified:` (today's ISO timestamp), `status: passed`,
    `score: 4/4 must-haves verified`, `overrides_applied: 0`, `gaps: []`, `human_verification: []`.
    Body sections (match exemplar headings):
    - **Phase Goal** line quoted from v1.10-ROADMAP Phase 27 goal.
    - **Observable Truths (ROADMAP Success Criteria)** table — one row per SESS criterion (SC1–SC4 mapping to ROADMAP criteria 1–4: refresh_sessions schema/CRUD; jti rotation + reuse→401 via family revocation; dual-key signing rotation + POST /api/auth/rotate-key graceful expiry; all ops covered by automated tests). Status VERIFIED, Evidence column citing `v1.10:server/...` paths/symbols (NOT HEAD line numbers).
    - **Required Artifacts** table — server/sessionsDb.ts, server/authApi.ts (/refresh + /rotate-key), server/initAuth.ts (dual-key), server/jwtUtil.ts (jti + fallback), and the three test files; each cited at `v1.10`.
    - **Key Link Verification** table — authApi /refresh → getSession/revokeSession/revokeFamily; authApi /rotate-key → initAuth.rotateSigningKey; jwtUtil.verifyRefreshToken → initAuth.getJwtSecrets.
    - **Behavioral Spot-Checks** table — the `git grep`/`git show v1.10:` commands from Task 1 plus `npm run test:ci` → 901/901 PASS.
    - **Requirements Coverage** table — SESS-02 / SESS-03 / SESS-04 each SATISFIED with source plans 27-02/27-03/27-04.
    - **Human Verification Required** — "None — all checks automatable" (the one manual item in 27-VALIDATION is a server-restart convenience check, not a goal-blocker; note it as advisory).
    - **Gaps Summary** — "No blocking gaps; verified as-shipped at v1.10."
    - Footer: `_Verified: <timestamp>_` / `_Verifier: Claude (gsd-verifier)_`.
    Do NOT modify any product source code. Do NOT cite current-HEAD line numbers.
  </action>
  <verify>
    <automated>grep -q "^status: passed" .planning/phases/27-stateful-session-backend/27-VERIFICATION.md && grep -c "v1.10:" .planning/phases/27-stateful-session-backend/27-VERIFICATION.md | awk '$1>=4{exit 0}{exit 1}' && echo VERIFICATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: passed" .planning/phases/27-stateful-session-backend/27-VERIFICATION.md` returns 1
    - `grep -c "v1.10:" .planning/phases/27-stateful-session-backend/27-VERIFICATION.md` returns >= 4 (one anchored citation per SESS criterion)
    - File contains all six exemplar section headings (Observable Truths, Required Artifacts, Key Link, Behavioral Spot-Checks, Requirements Coverage, Summary) — `grep -c "^###\|^##" ...` >= 6
    - `git grep -n "HEAD:" .planning/phases/27-stateful-session-backend/27-VERIFICATION.md` returns nothing (no HEAD anchors)
    - No product source files appear in `git status --porcelain` (only the new .md)
  </acceptance_criteria>
  <done>27-VERIFICATION.md exists, status: passed, all 4 SESS criteria anchored to v1.10 with passing-test evidence, structure mirrors the exemplar.</done>
</task>

</tasks>

<verification>
- `grep "^status: passed" .planning/phases/27-stateful-session-backend/27-VERIFICATION.md` matches.
- `git grep -c "v1.10:" .planning/phases/27-stateful-session-backend/27-VERIFICATION.md` >= 4.
- `npm run test:ci` exits 0 (no product code touched; baseline 901/901 preserved).
- `git status --porcelain` shows only `.planning/` changes.
</verification>

<success_criteria>
27-VERIFICATION.md exists, produced by goal-backward analysis of SESS-02/03/04, each criterion
mapped to a concrete code reference at the `v1.10` tag and a passing test (VVBACK-01).
</success_criteria>

<output>
After completion, create `.planning/phases/35-v-v-backfill/35-01-SUMMARY.md`.
</output>
