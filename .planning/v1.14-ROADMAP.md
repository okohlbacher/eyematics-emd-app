# v1.14 — Implementation Roadmap (NF UAT re-test)

Source: `EMD-v1.13-UAT-User-Response_2026-06-15`, triaged + dual adversarially reviewed in `v1.14-UAT-TRIAGE.md`.
Owner decisions locked 2026-06-15. Each phase ships with adversarial review + live-browser verification (the established v1.13 process).

## Locked decisions
- **I5 layout:** REPLACE the DocQuality completeness card with "Vollzähligkeit = present/total" **and revert the v1.13 renames** (landing → "Datenvollzähligkeit"/"Vollzähligkeit"; `dataCompleteness` → "Vollständigkeit"). Tooltips on all four cards.
- **I5 denominator:** windowed — but coherently: **numerator = patients active in the window; denominator = all registered patients (incl. stubs, NOT windowed)**. Avoids the B1 "≈100%" collapse. (Differs from the landing's all-time number by design.)
- **I3c relative-time:** DEFER (do the cheap legend/band-label fixes now).
- **I2 perf:** FULL layered fix.

---

## Phase 1 — Quick wins: labels, legend, tooltips (low risk)
- **I3a** — add both cohort median series (Visus + CRT) to the chart legend (`VisusCrtChart.tsx:239,247` remove `legendType="none"`).
- **I3b** — label the reference band explicitly as IQR (p25–p75) in legend/tooltip (it is `percentile(.25/.75)`, `useCaseData.ts:397`).
- **I4** — set the measured-Visus line `name` to the full "Visus (Dezimal, bestkorrigiert)" (`VisusCrtChart.tsx:254`); shorten/remove the under-title caption; verify legend doesn't wrap badly (de/en).
- **I6b** — add `title` to the "Bestätigen" action (`QualityCaseDetail.tsx:311`); it already has aria-label.
- **I5-tooltips** — add definition tooltips to the Plausibilität & Gesamtbewertung cards (`DocQualityPage.tsx:236-247`) so all four have them (new i18n keys).
**Verify:** browser — legend shows both medians + IQR label; Visus full label in legend; Bestätigen mouseover; 4 DocQuality tooltips.

## Phase 2 — Correctness bugs
- **I1 — hover/drill-down nearest-point.** Replace the shared-tooltip "nearest-x" resolution with the actually-hovered scatter datum: track a hovered-point state from the custom shape's mouse events; the highlight AND the drill-down click use that datum (euclidean-nearest), not `activePatientIdRef` from the axis tooltip. Fixes both the visual symptom and the latent wrong-patient drill-down. (`OutcomesPanel.tsx:266-333,406-428`)
- **I6a — show preserved annotation post-confirm.** Render a row's `errorType` for `acknowledged`/`resolved` rows too (not only `open`), and include it in the audit log — while suppressing the `CONFIRMED`/`CORRECTED` sentinel strings so they never display as a user note. (`QualityCaseDetail.tsx:485,510-517`; sentinels at `:43-44`)
**Verify:** browser — hover highlights the point under the cursor; clicking opens that exact patient; a flagged-then-confirmed row still shows its note; confirming a clean row shows no sentinel text.

## Phase 3 — I5: restore "Vollzähligkeit" across both tabs (data plumbing)
- Revert labels: landing `Einwilligungsquote` → `Datenvollzähligkeit`/`Vollzähligkeit`; DocQuality `dataCompleteness` label `Messwert-Vollständigkeit` → `Vollständigkeit`. Keep the helpful tooltip text (re-pointed).
- New DocQuality metric **Vollzähligkeit = active-in-window patients / all registered patients (incl. stubs)**, replacing the required-fields completeness card. Requires plumbing raw `bundles` (via `countRawPatients`/per-centre raw counts) into DocQualityPage + qualityMetrics (today only stub-excluded `cases` are available; per-centre raw denominator is net-new — `fhirLoader.ts:79,89`).
- Per-centre breakdown uses the per-centre raw denominator; aggregate card uses the global raw total.
- Final four cards: **Vollzähligkeit** (new) · **Vollständigkeit** (reverted) · **Plausibilität** · **Gesamtbewertung**, all with tooltips.
**Risk/verify:** the windowing semantics (numerator windowed, denominator full registration) must be reviewed so the metric stays meaningful as the window shrinks; verify the DocQuality Vollzähligkeit is consistent with the landing definition (same formula) and the per-centre numbers add up.

## Phase 4 — I2: Verläufe responsiveness (full layered fix)
1. **Derive large-cohort layer defaults before first render** — initialise `layers` (scatter/per-patient) from cohort size synchronously so the first paint never renders ~14k SVG circles, instead of switching them off in a post-render effect (`useOutcomesRouteState.ts:117,226,240`).
2. **Client-side loading/status indicator** — show the computing/rendering status for the client path too, not only server-aggregation mode (`OutcomesView.tsx:88`). Addresses NF's "unknown status… system unresponsive".
3. **Cap/downsample scatter when enabled** — reduce DOM nodes (downsample points, or drop the separate hit-halo / render via a lighter mechanism) so manually enabling scatter on a large cohort stays responsive.
4. Consider lowering `serverAggregationThresholdPatients` (default 1000, `settingsService.ts:52`) so large single cohorts use the existing server path + spinner. Web worker only if a profiling spike shows aggregation still dominates.
**Verify:** browser — Verläufe on the 245-cohort loads without a frozen first paint; a loading indicator shows; enabling scatter stays responsive. Profile before/after.

## Deferred → backlog (SEED for a later milestone)
- **I3c** relative-time aggregation (+ optional relative-time X-axis) — `useCaseData.ts` cohort overlay; clinically motivated, larger.
- **I3d** extend the cohort overlay to the other case-detail plots (distribution / Change-from-Baseline / Visus-vs-CRT) — tied to the relative-time decision.
- **I6c** correcting values in the primary system — out of scope for the demonstrator (Behoben stays a marker); acknowledged.

## Process
Per phase: plan → implement → adversarial review (internal + Codex) → live-browser verify → atomic commits. No push until the milestone is owner-approved.
