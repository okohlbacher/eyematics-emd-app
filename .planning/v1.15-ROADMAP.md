# v1.15 — Implementation Roadmap (NF UAT round 3)

Source: `EMD-v1.14-UAT-User-Response-2_2026-06-15`, triaged in `v1.15-UAT-TRIAGE.md`. Owner decisions locked 2026-06-15.
Process per phase: plan → implement → adversarial review (internal + Codex) → live-browser verify → atomic commits. No push until owner-approved.

## Phase 1 — Quick wins & quality-review UX (low risk)
- **J3a** IQR band legend entry rendered as a shaded swatch (not a line) — `VisusCrtChart.tsx`.
- **J6b** remove the "Behoben" action from QualityCaseDetail (drop UI action + vocab; keep wire `resolved`, D-05).
- **J6a** move status/annotation pills into their own fixed-width column so enabling a tag doesn't reflow the row / shift action buttons — `QualityCaseDetail.tsx`.
- **J1a** tooltip pop-up follows the hovered point (point-keyed) instead of nearest-x — `OutcomesPanel.tsx` (+ `OutcomesTooltip`); reuse `hoveredDatumRef`.

## Phase 2 — DocQuality main/centre parity
- **J5a** add a collapsible table of the (global) plausibility ranges to the main Dokumentationsqualität view (exists in the per-centre detail today).
- **J5b** centre-view metric cards match the main view: add absolute counts + definition tooltips.

## Phase 3 — Compare/split UX + line drill-down
- **J7** split-confirm opens compare with the SUB-cohorts pre-selected, parent optional/unlocked (cap at compare limit) — `CohortSplitDialog` + compare/route state.
- **J1b** click an individual trajectory line → that patient's case (alongside scatter click; IDOR gate reused) — `OutcomesPanel.tsx`.

## Phase 4 — J2 Verläufe performance (FULL: worker + progressive + persist)
- Web Worker for `computeCohortTrajectory` (off main thread) on the client path; keep server path as-is.
- Progressive / on-idle panel rendering so layer toggles + Einstellungen stay responsive during render; persistent self-explanatory loading status until panels are ready.
- Persist trajectory view state (layers, metric, scatter) in URL/store so leaving + returning does not reset it.
- Profiling spike first to confirm worker vs render split before committing the worker.

## Phase 5 — J3c relative-time axis + J3d overlay on all plots (chart rework)
- **J3c** replace the case-trajectory X-axis with relative-time-since-first-visit; adapt the IVI treatment timeline + date highlighting (`VisusCrtChart.tsx`, `useCaseData.ts`, `PatientHeader.tsx`); cohort overlay aggregates on the same relative axis (cohortTrajectory already supports relative days).
- **J3d** extend the cohort-reference overlay (median + IQR) to the Change-from-Baseline chart and the Visus/CRT distribution + Visus-vs-CRT scatter (`DistributionCharts.tsx`).

## Sequencing
P1→P2→P3 (UI-local, low risk, sequential to avoid shared-file contention) → P4 (perf, outcomes surface) → P5 (case-detail chart rework). P4 and P5 are the heavy phases; each gets a profiling/design pass + adversarial review.
