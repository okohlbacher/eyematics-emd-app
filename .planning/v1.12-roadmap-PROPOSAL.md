# v1.12 Roadmap — PROPOSAL

**Working title:** Quality, Configurability & Analysis Depth
**Status:** Proposal — converged with CODEX CLI over 3 review rounds (2026-05-25). Not yet an active milestone; awaiting product-owner decisions (Phase 37) before `/gsd-new-milestone`.
**Source:** v1.10 UAT feedback (`EMD-v1.10-Changelog-Feedback_discussion_2025-05-25.docx`) reconciled against v1.11 (Phases 32–36, shipped). **12 of the v1.10 TODOs were already closed by v1.11**; this roadmap covers the verified-open remainder plus the CODEX Tier-C items deferred from v1.11.

---

## Already closed by v1.11 (no action — recommend re-test in Phase 37)
USM center-in-edit-dialog, all-fields-mandatory, user deactivation, lockout/attempts feedback, inactivity 3-min countdown, auth constants → settings.yaml (Phase 32); dashboard two-number completeness (Phase 34); Therapie-Abbrecher routing + cohort presets + advanced filter + plausibility validation + filter persistence (Phase 33).

---

## Phases

### Phase 37 — UAT Re-test & Blocking Decisions  *(USER FEEDBACK PHASE — narrow)*
**Type:** discussion / spec. No production code. **Output:** fresh `REQUIREMENTS.md` for v1.12.
Limited to decisions that change **data shape or config schema**; all other choices are made in each phase's normal discuss step.
- **Re-test the 12 v1.11 fixes** and record any that regressed or missed intent.
- **D1 — threshold scope:** recommend **global admin-configured** (per-site/per-cohort deferred — they force threshold-snapshot provenance, intersecting F-13).
- **D1b — threshold taxonomy/editability:**
  - *Critical/action thresholds* (`clinicalThresholds.ts`: CRT critical, Visus critical, IOP critical, Visus-jump, therapy interrupter/breaker) → **admin-editable** in v1.12.
  - *Plausibility ranges* (`qualityMetrics.ts`: Visus 0..2.0, CRT 100..800, IOP 5..40) → **centralized to settings.yaml**; admin-editable optional (confirm).
- **D2 — QUAL-001 persistence:** does per-subcohort "which parameters to check" persist **with the saved cohort** (changes SavedSearch shape) or transient per session?
- **D3 — multi-select centers:** in or out of v1.12?
- **PROT-001 policy (light confirm):** deterministic default — `'unauthenticated'` for 401s; immutable historical actor for deleted users.
- *Deferred to implementing phases (not decided here):* FALL-003 label wording, responder-tooltip placement, A-06 repro, QUAL-011 absolute-value discoverability.

### Phase 38 — Audit Actor Correctness (PROT-001)  *(standalone, ships first)*
**Requirements:** PROT-001. Small security-correctness fix, no feature coupling.
- `auditMiddleware.ts:184` `'anonymous'` → `'unauthenticated'` for 401/unauth requests; deleted users keep immutable historical actor identity. Tests for both paths.
**Depends on:** 37 (policy confirm).

### Phase 39 — Configurable Clinical Thresholds + Server/Client Parity
**Requirements:** ANL-004, F-02, F-01.
- Critical/action thresholds → `config/settings.yaml` (`settingsService` types + DEFAULTS + schema validation); admin UI in `SettingsPage` (global per D1).
- Plausibility ranges centralized to settings.yaml per D1b (editable only if D1b says so).
- **F-01:** server aggregation (`outcomesAggregateApi.ts:122`) injects settings-derived thresholds (today falls back to hardcoded `patientCases.ts:132`); server-aggregate cache keying/invalidation on change; client/server parity tests.
- **Consumption map:** document which checks read critical/action thresholds vs. plausibility ranges.
**Depends on:** 37 (D1/D1b). If D1 picks scoped thresholds → split into 39A global + later scoped, and pull F-13 forward.

### Phase 40 — Quality Check Configuration & Cohort-Scoped Review
**Requirements:** QUAL-001 (EMDREQ-QUAL-001).
- Per-subcohort selection of which parameters to check (persistence per D2); quality review scoped to a chosen cohort/subcohort; reuses Phase 39 thresholds.
**Depends on:** 37 (D2), 39.

### Phase 41 — Doc-Quality Correctness & UX
**Requirements:** QUAL-011 (time-filtered Grundgesamtheit), QUAL-011 (absolute-value discoverability), QUAL-006 (approve-dropdown placement).
- Grundgesamtheit denominator respects the active time-range filter; surface absolute counts; reposition approve/flag dropdown; consume centralized plausibility ranges from Phase 39.
**Depends on:** 39.

### Phase 42 — Analysis Cohort Comparison & Labeling
**Requirements:** ANL-002 (label which cohort in interval histogram / compare plots), ANL-003 (cohort comparison in Aggregated tab: diagnosis distribution, age-vs-Visus), KOH-005 (active cohort name on `?filters=` direct-load).
**Depends on:** —.

### Phase 43 — Case Navigation, Reference & Chart Clarity
**Requirements:** FALL-001 (chart-point → case-detail drill-down; interaction decided in this phase's discuss step), FALL-006 (case-vs-cohort reference overlay), FALL-003 (CRT legend label, Visus measurement-type axis/label, interpolation-legend wording). **Chart-polish tail:** A-06 (missing axis ticks), ANL-002 (responder (i) tooltip placement).
**Depends on:** 42.

### Phase 44 — Tech-Debt Compaction + Saved-Search Hardening (CODEX Tier C)
- **44a — Behavior-preserving compaction:** F-09 (split `authApi.ts` God module), F-10 (decompose `OutcomesView.tsx`). test:ci / knip / lint gates; no behavior change.
- **44b — F-13 saved-search provenance:** bounded server-side **contract change** (server generates `id`/`createdAt`, sanitizes `filters` at API boundary) — migration plan + contract tests; NOT behavior-preserving. May ride with Phase 38 if D2 keeps QUAL-001 out of the SavedSearch shape.
**Depends on:** prior feature phases settled.

### Phase 45 — UAT Validation & Milestone Close  *(USER FEEDBACK PHASE)*
- Consolidated human UAT across all v1.12 changes — re-verify each item against intent (the v1.10→v1.11 lesson: "claimed fixed" ≠ fixed). Then milestone audit → complete.

### Backlog (not scheduled)
- QUAL-004 imputation / auto-suggest missing values.
- Multi-select centers (unless D3 pulls it in).
- F-03 Keycloak dead-path removal — blocked by KEYCLK-01 / M7.

---

## Milestone-size note
6 implementation phases (38–44) bracketed by 2 feedback phases (37, 45) is large. **Recommended optional split** (CODEX-endorsed cut line at 41|42):
- **v1.12 = Phases 37–41** — audit correctness + configurable thresholds + quality module (a complete quality/config milestone).
- **v1.13 = Phases 42–45** — analysis comparison + case navigation/clarity + compaction (42/43 share chart/cohort context; 44 is safest after they land).

---

*Converged with CODEX CLI (codex-cli 0.128.0), 3 rounds, 2026-05-25. Phase numbers continue from v1.11 (ended at 36); they renumber if the v1.12/v1.13 split is taken.*
