---
phase: 33-cohort-builder-ux-advanced-filters
plan: 04
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/pages/LandingPage.tsx
  - src/pages/QualityPage.tsx
  - src/components/quality/QualityCaseList.tsx
  - tests/landingPageAlerts.test.tsx
  - tests/qualityPageDeepLink.test.tsx
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "The dashboard CRT Review button navigates to /quality?crt=implausible (not /quality?status=flagged)"
    - "The dashboard Therapie-Abbrecher Review button still navigates to /quality?therapy=breaker"
    - "QualityPage seeds filterCrt=implausible from the crt=implausible URL param"
    - "QualityPage auto-opens the filter panel when crt=implausible is present"
    - "When filterCrt is implausible, QualityPage shows only cases whose latest CRT exceeds the threshold"
  artifacts:
    - path: "src/pages/LandingPage.tsx"
      provides: "Corrected CRT Review button route + aria-label"
      contains: "crt=implausible"
    - path: "src/pages/QualityPage.tsx"
      provides: "filterCrt URL-param seeding + filteredCases CRT clause + CRT filter control"
      contains: "filterCrt"
    - path: "tests/landingPageAlerts.test.tsx"
      provides: "Updated assertion for the corrected CRT route + aria-label"
      contains: "crt=implausible"
  key_links:
    - from: "src/pages/LandingPage.tsx"
      to: "/quality?crt=implausible"
      via: "navigate in CRT Review button onClick"
      pattern: "crt=implausible"
    - from: "src/pages/QualityPage.tsx"
      to: "crt URL param"
      via: "lazy useState initializer reading searchParams.get('crt')"
      pattern: "searchParams.get\\('crt'\\)"
---

<objective>
Fix the DASH-02 dashboard routing bug: the "Attention needed" CRT Review button currently
navigates to /quality?status=flagged (wrong destination) — route it to a new CRT-specific
Quality filter /quality?crt=implausible, and teach QualityPage to seed and apply that filter.
The Therapie-Abbrecher button is already correct and stays unchanged. The existing test that
asserts the wrong target is updated in the same task as the fix (RESEARCH Pitfall 4).

Depends on Plan 01: QualityPage's getTherapyStatus has been lifted, and
crtImplausibleThresholdUm + the reviewImplausibleCrt i18n key now exist. Wave 2 because Plan 01
edits the same QualityPage.tsx file.

Purpose: Each dashboard Review button lands where its label promises (DASH-02 success criterion 4).
Output: corrected LandingPage routing, QualityPage CRT-param seeding + filter control,
updated landingPageAlerts test, extended qualityPageDeepLink test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-cohort-builder-ux-advanced-filters/33-CONTEXT.md
@.planning/phases/33-cohort-builder-ux-advanced-filters/33-RESEARCH.md
@.planning/phases/33-cohort-builder-ux-advanced-filters/33-PATTERNS.md
@.planning/phases/33-cohort-builder-ux-advanced-filters/33-UI-SPEC.md

<interfaces>
LandingPage.tsx line 288 (CORRECT, do NOT change): therapy-breaker Button, aria-label
reviewTherapyBreakers, onClick navigate('/quality?therapy=breaker').
LandingPage.tsx line 302 (BUG, fix): CRT Button currently aria-label reviewFlaggedCases,
onClick navigate('/quality?status=flagged'). Title/subtitle copy at lines 295-300 already
describe CRT implausibility correctly — no copy change there.

QualityPage.tsx URL-param seeding (lines 90-110): lazy useState initializers — filterStatus
(status=flagged maps to in_progress), filterTherapy (therapy=breaker|interrupter), showFilters
(auto-opens when therapy or status param present). filteredCases memo at lines 167-176.
Plan 01 has already imported getTherapyStatus here; getSettings() is already used at lines 152-159.

QualityCaseList.tsx props interface (QualityCaseListProps, lines 19-40) — gains filterCrt prop +
onFilterCrtChange callback; the filter panel UI gains a CRT select.

i18n key (Plan 01): reviewImplausibleCrt (de "Unplausible CRT-Werte prüfen" / en "Review
implausible CRT readings"). settings: getSettings().crtImplausibleThresholdUm. fhirCodes:
LOINC_CRT; helper getLatestObservation(observations, LOINC_CRT).

tests/landingPageAlerts.test.tsx line 83: asserts the OLD wrong target — MUST be updated.
tests/qualityPageDeepLink.test.tsx: existing deep-link seeding coverage to extend.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QualityPage CRT-implausible URL-param seeding + filter clause</name>
  <files>src/pages/QualityPage.tsx, src/components/quality/QualityCaseList.tsx, tests/qualityPageDeepLink.test.tsx</files>
  <behavior>
    - Navigating to /quality?crt=implausible seeds filterCrt to implausible
    - With crt=implausible present, the filter panel auto-opens (showFilters true)
    - With filterCrt implausible, only cases whose latest CRT exceeds getSettings().crtImplausibleThresholdUm appear
    - With no crt param, filterCrt defaults to all and CRT filtering is inactive
    - The CRT filter control in the panel can toggle filterCrt between all and implausible
  </behavior>
  <read_first>
    - src/pages/QualityPage.tsx lines 90-176 (filterStatus/filterTherapy/showFilters lazy initializers + filteredCases memo)
    - src/components/quality/QualityCaseList.tsx lines 19-40 (props interface) and the filter-panel select markup
    - 33-PATTERNS.md section "src/pages/QualityPage.tsx — CRT URL-param seeding" lines 557-605 (exact filterCrt initializer, showFilters update, filteredCases clause)
    - 33-RESEARCH.md section 4 lines 200-248 and Pitfall 6 lines 465-467 (showFilters must include crt)
    - tests/qualityPageDeepLink.test.tsx (existing seeding test structure)
  </read_first>
  <action>
    Extend tests/qualityPageDeepLink.test.tsx FIRST with cases for each behavior above (render
    QualityPage at /quality?crt=implausible, assert the CRT-implausible cases shown and panel open;
    assert default-all with no param). Run RED.

    Then in QualityPage.tsx add a lazy useState filterCrt of type 'implausible' | 'all' initialized
    from searchParams.get('crt') === 'implausible'. Update the showFilters lazy initializer to also
    return true when searchParams.get('crt') !== null. Add a clause to the filteredCases memo (after
    the filterTherapy clause): when filterCrt === 'implausible', read the latest CRT via
    getLatestObservation(c.observations, LOINC_CRT) and exclude the case when the value is null or not
    greater than getSettings().crtImplausibleThresholdUm. In QualityCaseList.tsx add filterCrt +
    onFilterCrtChange to the props interface and render a CRT select in the filter panel (mirror the
    existing therapy/status select markup) with all + implausible options; pass them down from
    QualityPage. Run RED to GREEN.
  </action>
  <verify>
    <automated>npm run test -- qualityPageDeepLink --run</automated>
  </verify>
  <acceptance_criteria>
    - QualityPage.tsx contains a filterCrt lazy useState reading searchParams.get('crt')
    - the showFilters initializer includes searchParams.get('crt') !== null
    - the filteredCases memo has a filterCrt clause using getSettings().crtImplausibleThresholdUm and LOINC_CRT
    - QualityCaseList props include filterCrt and onFilterCrtChange
    - npm run test -- qualityPageDeepLink --run exits 0
  </acceptance_criteria>
  <done>QualityPage seeds and applies the CRT-implausible filter from the URL and via the panel control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the LandingPage CRT Review button route + aria-label</name>
  <files>src/pages/LandingPage.tsx, tests/landingPageAlerts.test.tsx</files>
  <behavior>
    - Clicking the CRT Review button navigates to /quality?crt=implausible
    - The CRT Review button aria-label resolves the reviewImplausibleCrt i18n key
    - Clicking the Therapie-Abbrecher Review button still navigates to /quality?therapy=breaker (unchanged)
  </behavior>
  <read_first>
    - src/pages/LandingPage.tsx lines 286-305 (the two Attention Review buttons)
    - 33-PATTERNS.md section "src/pages/LandingPage.tsx — DASH-02 routing fix" lines 529-553 (exact before/after)
    - 33-UI-SPEC.md "Surface 5" lines 365-388 (before/after table; reviewFlaggedCases kept, new key adopted)
    - tests/landingPageAlerts.test.tsx lines 60-95 (existing assertions — line 83 asserts the wrong target)
  </read_first>
  <action>
    Update tests/landingPageAlerts.test.tsx FIRST: change the line-83 assertion from
    toHaveBeenCalledWith('/quality?status=flagged') to toHaveBeenCalledWith('/quality?crt=implausible'),
    and update the corresponding aria-label expectation from reviewFlaggedCases to reviewImplausibleCrt.
    Keep the existing therapy-breaker assertion unchanged. Run RED.

    Then in LandingPage.tsx line 302: change the CRT Button onClick navigate target from
    '/quality?status=flagged' to '/quality?crt=implausible' and its aria-label from
    t('reviewFlaggedCases') to t('reviewImplausibleCrt'). Leave the button label text t('review')
    and the line-288 therapy-breaker button unchanged. Do NOT delete the reviewFlaggedCases i18n key.
    Run RED to GREEN.
  </action>
  <verify>
    <automated>npm run test -- landingPageAlerts --run</automated>
  </verify>
  <acceptance_criteria>
    - LandingPage.tsx CRT button navigates to '/quality?crt=implausible' with aria-label t('reviewImplausibleCrt')
    - grep -c "status=flagged" src/pages/LandingPage.tsx returns 0
    - the therapy-breaker button still navigates to '/quality?therapy=breaker'
    - tests/landingPageAlerts.test.tsx asserts '/quality?crt=implausible'
    - npm run test -- landingPageAlerts --run exits 0
  </acceptance_criteria>
  <done>The dashboard CRT Review button lands on the CRT-implausible Quality view; its test asserts the corrected target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query param (crt) -> QualityPage filter state | Untrusted navigation input crosses here |
| dashboard button -> navigate() | Internal app navigation to a same-origin route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08 | Tampering | crt URL param seeding QualityPage | mitigate | filterCrt is set ONLY to the literal 'implausible' when the param exactly equals 'implausible', else 'all' — any other value (including injection attempts) maps to 'all'. No value is reflected into the DOM or a navigation target; it only toggles a typed filter enum. |
| T-33-09 | Tampering | navigate() target in LandingPage | accept | Targets are hard-coded literal same-origin paths ('/quality?crt=implausible', '/quality?therapy=breaker'); no user input is interpolated, so no open-redirect surface. |
</threat_model>

<verification>
- npm run test -- qualityPageDeepLink --run green
- npm run test -- landingPageAlerts --run green
- npm run test:ci green (828 baseline; no other consumers of status=flagged broken)
</verification>

<success_criteria>
- CRT Review button routes to /quality?crt=implausible with the reviewImplausibleCrt label
- Therapie-Abbrecher button unchanged and still correct
- QualityPage seeds + applies filterCrt from the URL and via the panel control
- The previously-wrong landingPageAlerts assertion is corrected
</success_criteria>

<output>
After completion, create `.planning/phases/33-cohort-builder-ux-advanced-filters/33-04-SUMMARY.md`
</output>
