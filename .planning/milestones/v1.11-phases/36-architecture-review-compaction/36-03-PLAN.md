---
phase: 36-architecture-review-compaction
plan: 03
type: execute
wave: 3
depends_on: ["36-02"]
files_modified:
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [VVBACK-05]
must_haves:
  truths:
    - "npm run test:ci exits green after all v1.11 work (features, backfill, compaction)"
    - "STATE.md Deferred Items reflect that VVBACK-01..04 are closed by Phase 35"
    - "PROJECT.md current-state reflects v1.11 closure"
    - "MILESTONES.md has a v1.11 entry reflecting closure"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Deferred Items updated to closed; milestone status reflects v1.11 closure"
      contains: "VVBACK"
    - path: ".planning/MILESTONES.md"
      provides: "v1.11 milestone closure entry"
      contains: "v1.11"
    - path: ".planning/PROJECT.md"
      provides: "Current-state reflects v1.11 shipped"
      contains: "v1.11"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/phases/35"
      via: "Deferred Items note VVBACK-01..04 closed by Phase 35"
      pattern: "VVBACK-0[1-4]"
---

<objective>
Close the v1.11 milestone: confirm the full test suite is green after all v1.11 work, then update the deferred-debt tracking and current-state documents to reflect closure.

Purpose: VVBACK-05 (`npm run test:ci` passes with zero failures after all feature, backfill, review, and compaction work; STATE.md / PROJECT.md / MILESTONES.md deferred-debt entries updated to reflect closure).

Output: A confirmed-green test run plus updated STATE.md, PROJECT.md, and MILESTONES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-architecture-review-compaction/36-02-SUMMARY.md

<notes>
- STATE.md "Deferred Items" currently lists VVBACK-01..04 as "v1.11 Phase 35" tech debt to close — Phase 35 completed 2026-05-24, so these are now CLOSED. Update the table to mark them resolved (closed by Phase 35) rather than pending.
- PROJECT.md `## Status` (line ~3) and `## Current Milestone` (line ~9) describe v1.11 as in progress (Phases 32–36) — update to reflect v1.11 shipped/closed after Phase 36.
- MILESTONES.md uses a per-milestone section with phases, timeline, tests baseline, and key accomplishments (see the v1.10 entry as the format precedent). Add a v1.11 section at the top.
- Pull deferred-item carry-overs from 36-02-SUMMARY.md (any DEFER'd compaction items) and record them as accepted tech debt in the v1.11 MILESTONES.md entry and STATE.md.
- This plan only edits .planning/ docs — no product source. The test:ci gate here is a final confirmation that the tree is green, not a code change.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm green test suite, then update STATE.md and PROJECT.md</name>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/STATE.md (Deferred Items table, milestone frontmatter, Current Position)
    - .planning/PROJECT.md (## Status line ~3 and ## Current Milestone line ~9)
    - .planning/phases/36-architecture-review-compaction/36-02-SUMMARY.md (applied vs deferred compaction items)
  </read_first>
  <action>
    First run `npm run test:ci` and confirm zero failures — this is the VVBACK-05 final gate over all v1.11 work. If it is not green, STOP and report (do not edit docs over a red tree).
    Then use the Edit tool on `.planning/STATE.md`: in the "Deferred Items" section, mark VVBACK-01/02/03/04 as CLOSED by Phase 35 (2026-05-24) rather than pending — keep the table but flip the Status column. Update the milestone frontmatter `status` and the "Current Position" block to reflect Phase 36 complete / v1.11 ready to ship. If 36-02 deferred any compaction items, add them to Deferred Items as newly-accepted v1.11 tech debt with a one-line rationale.
    Then use the Edit tool on `.planning/PROJECT.md`: update `## Status` and `## Current Milestone` to state v1.11 closed (Phases 32–36 complete) and identify the next milestone/phase number. Add v1.11 to the validated/shipped narrative consistent with how prior milestones are recorded.
  </action>
  <acceptance_criteria>
    - `npm run test:ci` exits 0 with zero failures
    - `grep -qE "VVBACK-0[1-4]" .planning/STATE.md` succeeds AND the Deferred Items entries are marked closed (no remaining "Phase 35 —" pending wording for VVBACK-01..04): `grep -E "VVBACK-0[1-4]" .planning/STATE.md | grep -vi "closed\|done\|resolved" | grep -c "v1.11 Phase 35 —"` returns `0`
    - `grep -qi "v1.11" .planning/PROJECT.md` succeeds and PROJECT.md no longer describes v1.11 as "In Milestone" in-progress: `grep -c "In Milestone v1.11" .planning/PROJECT.md` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>npm run test:ci && grep -qE "VVBACK-0[1-4]" .planning/STATE.md && [ "$(grep -c 'In Milestone v1.11' .planning/PROJECT.md)" = "0" ] && echo PASS</automated>
  </verify>
  <done>`npm run test:ci` is green; STATE.md Deferred Items mark VVBACK-01..04 closed by Phase 35 (plus any newly-deferred compaction debt); PROJECT.md current-state reflects v1.11 closure and the next phase number.</done>
</task>

<task type="auto">
  <name>Task 2: Add the v1.11 milestone closure entry to MILESTONES.md</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - .planning/MILESTONES.md (the v1.10 section as the format precedent)
    - .planning/ROADMAP.md (v1.11 phase list 32–36 with completion dates and requirement coverage)
    - .planning/REQUIREMENTS.md (v1.11 requirement IDs and their status)
    - .planning/phases/36-architecture-review-compaction/36-02-SUMMARY.md (compaction outcome to summarize)
  </read_first>
  <action>
    Use the Edit tool to insert a new `## v1.11 — UAT Fixes, Data Completeness & Quality Closure (Shipped: 2026-05-24)` section at the TOP of `.planning/MILESTONES.md` (above the v1.10 section), following the v1.10 entry's structure: phases completed (32–36), timeline, tests baseline (final `npm run test:ci` count from Task 1), key accomplishments per phase (User Management & Auth Hardening; Cohort Builder UX; Data Completeness; V&V Backfill; Architecture Review & Compaction), and a "Known deferred items (accepted at close)" subsection populated from 36-02-SUMMARY.md's DEFER list (or "none" if the user approved everything). Keep the prose factual and concise; cite the compaction outcome (which findings applied, which deferred). Do NOT invent test counts — use the actual number from the Task 1 run.
  </action>
  <acceptance_criteria>
    - `grep -qE "^## v1.11" .planning/MILESTONES.md` succeeds (new section present)
    - The v1.11 section appears before the v1.10 section: `awk '/^## v1.11/{print NR; exit}' .planning/MILESTONES.md` line number is LESS THAN `awk '/^## v1.10/{print NR; exit}' .planning/MILESTONES.md`
    - `grep -qi "Architecture Review" .planning/MILESTONES.md` succeeds (Phase 36 accomplishment recorded)
    - `grep -qi "deferred" .planning/MILESTONES.md` succeeds within the v1.11 section (accepted-debt subsection present)
  </acceptance_criteria>
  <verify>
    <automated>grep -qE "^## v1.11" .planning/MILESTONES.md && [ "$(awk '/^## v1.11/{print NR; exit}' .planning/MILESTONES.md)" -lt "$(awk '/^## v1.10/{print NR; exit}' .planning/MILESTONES.md)" ] && grep -qi "Architecture Review" .planning/MILESTONES.md && echo PASS</automated>
  </verify>
  <done>MILESTONES.md has a v1.11 closure section at the top (above v1.10) covering all five phases, the final test count, the compaction outcome, and accepted deferred items.</done>
</task>

</tasks>

<verification>
- `npm run test:ci` green after all v1.11 work.
- STATE.md Deferred Items mark VVBACK-01..04 closed by Phase 35; any new compaction debt recorded.
- PROJECT.md reflects v1.11 closure; MILESTONES.md has a v1.11 entry above v1.10.
</verification>

<success_criteria>
- VVBACK-05 satisfied: test:ci green; STATE.md / PROJECT.md / MILESTONES.md deferred-debt entries updated to reflect closure.
</success_criteria>

<output>
After completion, create `.planning/phases/36-architecture-review-compaction/36-03-SUMMARY.md` recording the final test count, the doc updates made, and the v1.11 milestone closure status.
</output>
