---
phase: 34-data-completeness-consent-stubs
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/generate-center-bundle.ts
  - scripts/augment-reference-bundles.ts
  - scripts/verify-bundle-distributions.mjs
  - scripts/audit-bundle-codes.mjs
  - config/settings.yaml
  - package.json
  - public/data/center-chemnitz.json
  - public/data/center-greifswald.json
  - public/data/center-leipzig.json
  - public/data/center-muenster.json
  - public/data/center-aachen.json
  - public/data/center-tuebingen.json
  - tests/augmentReferenceBundles.test.ts
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "Every full (non-stub) patient in all 6 site bundles has one active research Consent (D-06)"
    - "Every site bundle contains stub Patients (zero Observations) + a minimal Encounter each, at a seeded [2,8]× factor (D-11)"
    - "Synthetic bundles regenerate byte-identically; reference bundles' curated resources are byte-unchanged (D-13)"
    - "CI gates (audit:bundles, verify:bundles) pass with Consent + stubs present (D-14)"
  artifacts:
    - path: "scripts/augment-reference-bundles.ts"
      provides: "idempotent append-only Consent + stub augmentation for Aachen + Tübingen (D-12/D-13)"
      contains: "alreadyAugmented"
    - path: "scripts/generate-center-bundle.ts"
      provides: "Consent + stub emission for synthetic sites (D-11/D-12)"
      contains: "stubFactor"
    - path: "config/settings.yaml"
      provides: "stubs factor config (D-11)"
      contains: "stubs"
  key_links:
    - from: "scripts/generate-center-bundle.ts"
      to: "scripts/prng.ts"
      via: "seededRandInt after the full-patient loop"
      pattern: "seededRandInt\\(rand, 2, 8\\)"
    - from: "scripts/augment-reference-bundles.ts"
      to: "public/data/center-aachen.json"
      via: "append-only write, JSON.stringify(bundle, null, 2) + newline"
      pattern: "JSON.stringify\\(.*null, 2\\)"
---

<objective>
Generate the FHIR Consent + patient-stub data across all 6 site bundles, with byte-stable synthetic regeneration and append-only reference augmentation, and extend the CI gates to cover the new resources.

Purpose: Materialize the data that makes the Datenvollzähligkeit metric meaningful — full patients get Consent (100% consented, D-06), and seeded stubs (D-11) populate the denominator. Reference curated data is never regenerated (project D-06 / D-13). CI gates keep it reproducible (D-14).
Output: Extended generator, new idempotent augmentation script + npm script, regenerated synthetic bundles, augmented reference bundles, extended `.mjs` CI gates, and the green `tests/augmentReferenceBundles.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-data-completeness-consent-stubs/34-CONTEXT.md
@.planning/phases/34-data-completeness-consent-stubs/34-RESEARCH.md
@.planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md

<interfaces>
<!-- Verified generator + PRNG + CI gate structure. -->
From scripts/prng.ts: `mulberry32(seed)`, `seededRandInt(rand, min, max)` (inclusive int), `addDays(isoAnchor, days)`. Import via `'./prng.js'`.
From scripts/generate-all-bundles.ts: SITES list has 4 synthetic sites with seeds — Chemnitz org-ukc seed 70103, Greifswald org-ukg seed 70107, Leipzig org-ukl seed 70112, Münster org-ukm seed 70114, each 45 patients. Writes with `JSON.stringify(bundle, null, 2) + '\n'`. Reference sites (Aachen, Tübingen) are deliberately NOT in SITES.
From scripts/generate-center-bundle.ts: `generateCenterBundle()`; full-patient loop runs to ~line 581; bundle assembly ~587-601 with entry array [orgEntry, ...patientEntries, ...conditionEntries, ...observationEntries, ...procedureEntries, ...medicationEntries].
Reference bundles: Aachen (org-uka, 35 patients), Tübingen (org-ukt, 30 patients).
From scripts/audit-bundle-codes.mjs: `WHITELIST_SYSTEMS` Set (loinc, atc); `interesting` resource-type Set.
From scripts/verify-bundle-distributions.mjs: `THRESHOLDS` object; per-bundle `aggregateBundle`.
package.json scripts: `generate-bundles`, `audit:bundles` (runs audit-bundle-codes + verify-bundle-distributions), `test:ci`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend generator to emit Consent + stubs for synthetic sites (D-06/D-11/D-12) and add stubs config</name>
  <read_first>
    - scripts/generate-center-bundle.ts (full file — the full-patient loop end (~line 581) and bundle assembly (~587-601); note where rand() is called so new draws go AFTER the loop)
    - scripts/prng.ts (mulberry32, seededRandInt, addDays signatures)
    - scripts/generate-all-bundles.ts (SITES seeds; write format)
    - config/settings.yaml (existing key structure; add a new stubs section)
    - .planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md (§"scripts/generate-center-bundle.ts" — Consent + stub emission blocks, exact resource shapes, Pitfall 2 ordering)
    - .planning/phases/34-data-completeness-consent-stubs/34-RESEARCH.md (Pitfall 1 + Pitfall 2 — PRNG call-order constraints)
  </read_first>
  <action>
    In `scripts/generate-center-bundle.ts`, AFTER the existing full-patient loop completes (so the existing patients' PRNG draws are unshifted — research Pitfall 2), add three resource arrays:

    (a) `consentEntries`: one FHIR `Consent` per full patient (D-06, 100% consent). Shape per D-07: `status: 'active'`; `scope.coding` = `{ system: 'http://terminology.hl7.org/CodeSystem/consentscope', code: 'research', display: 'Research' }`; `category[0].coding` = `{ system: 'http://terminology.hl7.org/CodeSystem/v3-ActCode', code: 'INFOACCESS', display: 'Information Access' }`; `patient.reference` = `Patient/pat-<sh>-<NNNN>`; `organization[0].reference` = `Organization/<centerId>`; `dateTime` = a seeded date; `policyRule.coding` = `{ system: 'http://terminology.hl7.org/CodeSystem/v3-ActCode', code: 'OPTIN' }`; `provision` = `{ type: 'permit', purpose: [{ system: 'http://terminology.hl7.org/CodeSystem/v3-ActReason', code: 'HRESCH', display: 'healthcare research' }] }`. IDs `consent-<sh>-<NNNN>`.

    (b) Draw `const stubFactor = seededRandInt(rand, stubFactorMin, stubFactorMax)` ONCE per site, where the bounds come from `config/settings.yaml` `stubs.factorMin`/`stubs.factorMax` (D-11; default 2 and 8). Apply factor to the consented count (the input `patients` count, NOT a mixed array — research Pitfall 1): `stubCount = Math.round(patients * stubFactor)`.

    (c) `stubEntries` + `stubEncounterEntries`: for each stub, a Patient resource carrying ONLY `id` (`pat-<sh>-stub-<NNNN>`), `meta.source = centerId` (D-10 site attribution), `gender` (m/f), `birthDate` = `YYYY-01-01` (year-of-birth per Claude's Discretion, Open Question 1), and NO identifier/Observation/Condition/Procedure/Medication (D-02); plus one minimal `Encounter` (`enc-<sh>-stub-<NNNN>`, `status: 'finished'`, `subject.reference` to the stub, `serviceProvider` to the org, `period.start` = a seeded visit date).

    Update the bundle `entry` array to splice `...consentEntries, ...stubEntries, ...stubEncounterEntries` in after `...patientEntries`. Then add a `stubs:` section to `config/settings.yaml` with `factorMin: 2` and `factorMax: 8` and a comment citing D-11. Read these via the generator's existing settings-loading path (match how the generator/scripts read settings.yaml; if the generator does not currently read settings, pass the bounds in from `generate-all-bundles.ts` after loading settings there, or read settings in the generator — choose the path that keeps a single config source per project rule, NO env vars).

    All new `rand()` calls MUST come after the full-patient loop (Pitfall 2). Do NOT interleave new draws with existing draws.
  </action>
  <verify>
    <automated>npm run generate-bundles && git diff --stat public/data/ && node -e "const b=require('./public/data/center-chemnitz.json'); const t={}; for(const e of b.entry){t[e.resource.resourceType]=(t[e.resource.resourceType]||0)+1;} console.log(JSON.stringify(t));"</automated>
  </verify>
  <acceptance_criteria>
    - After `npm run generate-bundles`, `public/data/center-chemnitz.json` contains `Consent` entries (count === 45, one per full patient) and `Patient` entries with ids matching `pat-ukc-stub-\d{4}` plus matching `Encounter` entries.
    - Every stub Patient has zero Observation/Condition/Procedure/MedicationStatement entries referencing it (D-02).
    - The stub count per synthetic site is `Math.round(45 * factor)` with `factor` in `[2,8]`; running `npm run generate-bundles` twice produces byte-identical files (`git diff` empty on the second run).
    - `config/settings.yaml` contains a `stubs:` section with `factorMin` and `factorMax`.
    - `grep -n "seededRandInt(rand, 2, 8)\|stubFactor" scripts/generate-center-bundle.ts` confirms the factor draw is present (or sourced from config).
  </acceptance_criteria>
  <done>The 4 synthetic bundles regenerate byte-stably with one Consent per full patient and a seeded [2,8]× stub population; stub config lives in settings.yaml.</done>
</task>

<task type="auto">
  <name>Task 2: Create idempotent append-only reference augmentation script + npm wiring (D-12/D-13) and green the augmentation test</name>
  <read_first>
    - scripts/generate-all-bundles.ts (file-I/O + write format `JSON.stringify(bundle, null, 2) + '\n'`)
    - scripts/generate-center-bundle.ts (the Consent + stub resource shapes from Task 1 — reuse identical shapes; consider extracting a shared builder per Open Question 3)
    - tests/augmentReferenceBundles.test.ts (Plan 01 scaffold — this task removes its .skip and makes it green)
    - public/data/center-aachen.json (curated reference bundle — read enough to confirm Patient/Observation/Organization shapes and ids; do NOT regenerate)
    - .planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md (§"scripts/augment-reference-bundles.ts" — idempotency guard, append-only invariant, REFERENCE_SITES seeds)
    - .planning/phases/34-data-completeness-consent-stubs/34-RESEARCH.md (Pitfall 5 — write-format exactness)
  </read_first>
  <action>
    Create `scripts/augment-reference-bundles.ts`: an idempotent, append-only script that adds Consent + stub Patient + stub Encounter resources to the two curated reference bundles WITHOUT mutating any pre-existing resource (project D-06 / D-13).

    For each reference site `{ file: 'public/data/center-aachen.json', centerId: 'org-uka', shorthand: 'uka', seed: <fixed> }` and `{ file: 'public/data/center-tuebingen.json', centerId: 'org-ukt', shorthand: 'ukt', seed: <fixed> }` (pick fixed seeds distinct from synthetic seeds, e.g. 70101 / 70116): read the JSON; if any entry already has `resourceType === 'Consent'`, log skip and continue (idempotency). Otherwise: identify full patients as those with ≥1 Observation referencing them (same structural rule as D-03); append one Consent per full patient (identical shape to Task 1); draw `stubFactor = seededRandInt(rand, factorMin, factorMax)` from `mulberry32(seed)`; append `Math.round(fullPatientCount * factor)` stub Patient + Encounter pairs (identical shape to Task 1). PUSH new entries only — never mutate or reorder existing `bundle.entry` items. Write back with EXACTLY `JSON.stringify(bundle, null, 2) + '\n'` (research Pitfall 5).

    To avoid Consent/stub-shape drift between the generator and this script (Open Question 3), prefer extracting shared `buildConsentEntry(...)` and `buildStubEntries(...)` helpers (e.g. into `scripts/bundleBuilders.ts`) imported by both — if you do, add `scripts/bundleBuilders.ts` to this plan's changes and refactor Task 1's inline blocks to call them.

    Add a `package.json` script `augment-reference-bundles` running `node --import tsx scripts/augment-reference-bundles.ts`. Run it once to augment Aachen + Tübingen. Then remove the `.skip` from `tests/augmentReferenceBundles.test.ts` and ensure its byte-identity + idempotency assertions pass.
  </action>
  <verify>
    <automated>node --import tsx scripts/augment-reference-bundles.ts && node --import tsx scripts/augment-reference-bundles.ts 2>&1 | tail -3 && npx vitest run tests/augmentReferenceBundles.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/augment-reference-bundles.ts` exists; `grep "alreadyAugmented\|already augmented" scripts/augment-reference-bundles.ts` returns a match (idempotency guard).
    - After one run, `public/data/center-aachen.json` and `center-tuebingen.json` contain Consent + `*-stub-*` Patient + Encounter entries; running the script a SECOND time logs "already augmented — skipping" and leaves `entry.length` unchanged.
    - `tests/augmentReferenceBundles.test.ts` is green with NO `it.skip`: every pre-existing resource (by `resource.id`) is byte-identical before/after, ≥1 new entry appended, and double-run idempotency holds.
    - `package.json` has an `augment-reference-bundles` script.
    - The curated full-patient Observations/Conditions/Procedures in the reference bundles are unchanged (count and content).
  </acceptance_criteria>
  <done>Reference bundles carry Consent + stubs via an idempotent append-only script; curated data is byte-untouched; the augmentation test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend CI gates for Consent + stubs (D-14) so test:ci stays green</name>
  <read_first>
    - scripts/audit-bundle-codes.mjs (WHITELIST_SYSTEMS Set ~lines 49-52; `interesting` resource-type filter ~line 112)
    - scripts/verify-bundle-distributions.mjs (THRESHOLDS ~lines 44-50; aggregateBundle ~135-176; verify ~181-242)
    - tests/augmentReferenceBundles.test.ts and tests/stubIsolation.test.ts (already green from Plans 02 + this plan's Task 2)
    - .planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md (§"scripts/verify-bundle-distributions.mjs" and §"scripts/audit-bundle-codes.mjs" — exact whitelist + threshold extensions)
    - .planning/phases/34-data-completeness-consent-stubs/34-RESEARCH.md (Pitfall 4 — Consent coding systems trip the audit whitelist)
  </read_first>
  <action>
    Two `.mjs` gate extensions (D-14):

    (1) `scripts/audit-bundle-codes.mjs`: add the three Consent coding systems to `WHITELIST_SYSTEMS` — `http://terminology.hl7.org/CodeSystem/consentscope`, `http://terminology.hl7.org/CodeSystem/v3-ActCode`, `http://terminology.hl7.org/CodeSystem/v3-ActReason` (research Pitfall 4). Do NOT add `Consent` or `Encounter` to the `interesting` resource-type set (they are structural; leaving them out means they are skipped, which is correct).

    (2) `scripts/verify-bundle-distributions.mjs`: add `stubFactorMin: 2` and `stubFactorMax: 8` to `THRESHOLDS`. Extend `aggregateBundle` to count, per bundle, stub patients (Patient resources with zero Observations referencing them) and full patients separately. Add a per-bundle assertion in `verify` that `stubCount / fullCount` falls within `[stubFactorMin, stubFactorMax]` (inclusive, allowing for the `Math.round` boundary). Keep the assertion per-bundle (not aggregate) so each site's seeded factor is validated.

    If you mirror constants between the TS settings and the `.mjs` gates (e.g. factor bounds), follow the existing drift-guard discipline noted in CLAUDE.md/STATE.md (the audit scripts hand-mirror TS constants with a drift-guard test) — keep the mirrored values consistent.

    Run the full gate. Then run the full `test:ci` to confirm no regression in the 619+ baseline now that Consent + stubs exist in every bundle.
  </action>
  <verify>
    <automated>npm run audit:bundles 2>&1 | tail -10 && npm run test:ci 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "consentscope\|v3-ActCode\|v3-ActReason" scripts/audit-bundle-codes.mjs` returns 3 (all three systems whitelisted).
    - `grep "stubFactorMin\|stubFactorMax" scripts/verify-bundle-distributions.mjs` returns matches in THRESHOLDS and the assertion.
    - `npm run audit:bundles` exits 0 (zero unresolvable codes; stub ratio in [2,8] for every bundle).
    - `npm run test:ci` exits 0 with the full baseline green (no regression from Consent/stub presence; stub-isolation + augmentation tests included).
  </acceptance_criteria>
  <done>Both CI gates accept Consent + stubs and assert the [2,8] stub ratio per site; `npm run test:ci` is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script → committed bundle JSON | Generated/augmented data crosses into version-controlled, read-only-served assets |
| augmentation script → curated reference data | Must NOT mutate any pre-existing curated resource (project D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | Tampering | reference bundle augmentation (`scripts/augment-reference-bundles.ts`) | mitigate | Append-only invariant + idempotency guard; `tests/augmentReferenceBundles.test.ts` asserts every pre-existing resource is byte-identical by `resource.id` (D-13). |
| T-34-07 | Information Disclosure | stub demographic content | mitigate | Stubs carry only gender, year-of-birth (`YYYY-01-01`), and one encounter date — no PHI, no clinical detail (D-02). Acceptance criteria assert zero clinical resources reference any stub. |
| T-34-08 | Tampering | non-reproducible data (seed drift) | mitigate | All new PRNG draws are seeded (Mulberry32) and placed after the existing patient loop (Pitfall 2); double-run byte-identity is an acceptance criterion; `verify-bundle-distributions.mjs` asserts the seeded ratio. |
| T-34-09 | Spoofing | stub bypassing site restriction | mitigate | Every stub Patient has `meta.source = centerId`, so the server `filterBundlesByCenters` applies the same per-user restriction as full patients (V4 Access Control). |
</threat_model>

<verification>
- `npm run generate-bundles` is byte-stable across two runs (synthetic).
- `scripts/augment-reference-bundles.ts` is idempotent and append-only (test green).
- `npm run audit:bundles` exits 0; stub ratio in [2,8] per bundle.
- `npm run test:ci` fully green.
</verification>

<success_criteria>
- All 6 bundles contain one Consent per full patient + a seeded [2,8]× stub population with minimal Encounters.
- Synthetic regeneration is byte-stable; reference curated resources are byte-untouched.
- CI gates extended and green; no baseline regression.
</success_criteria>

<output>
After completion, create `.planning/phases/34-data-completeness-consent-stubs/34-03-SUMMARY.md`
</output>
