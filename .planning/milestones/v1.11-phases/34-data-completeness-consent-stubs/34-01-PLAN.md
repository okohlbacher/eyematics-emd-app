---
phase: 34-data-completeness-consent-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/types/fhir.ts
  - tests/stubIsolation.test.ts
  - tests/datenvollstaendigkeitCard.test.tsx
  - tests/augmentReferenceBundles.test.ts
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "A FHIR Consent resource type is expressible in TypeScript (shared/types/fhir.ts)"
    - "A FHIR Encounter resource type is expressible in TypeScript (shared/types/fhir.ts)"
    - "Wave 0 test files exist (initially red/skipped) so downstream plans have a verification target"
  artifacts:
    - path: "shared/types/fhir.ts"
      provides: "Consent and Encounter interfaces"
      contains: "interface Consent"
    - path: "tests/stubIsolation.test.ts"
      provides: "D-03 + D-04 regression scaffold"
    - path: "tests/datenvollstaendigkeitCard.test.tsx"
      provides: "completeness card render + site-filter scaffold"
    - path: "tests/augmentReferenceBundles.test.ts"
      provides: "D-13 byte-equality + idempotency scaffold"
  key_links:
    - from: "src/types/fhir.ts"
      to: "shared/types/fhir.ts"
      via: "barrel re-export (export *)"
      pattern: "export \\* from '\\.\\./\\.\\./shared/types/fhir'"
---

<objective>
Add the `Consent` and `Encounter` FHIR resource interfaces to the shared type system, and create the three Wave 0 test scaffolds that downstream plans (02, 03, 04) will turn green.

Purpose: Establish the contracts (types) and the verification targets (failing/pending tests) before any implementation. Downstream plans implement against these, not against an exploration of the codebase.
Output: Two new exported interfaces in `shared/types/fhir.ts` (auto-flowing through the `src/types/fhir.ts` barrel), and three new test files registered with Vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-data-completeness-consent-stubs/34-CONTEXT.md
@.planning/phases/34-data-completeness-consent-stubs/34-RESEARCH.md
@.planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md

<interfaces>
<!-- Existing shared/types/fhir.ts patterns the executor must replicate. -->
<!-- Procedure interface (model shape to copy): -->
From shared/types/fhir.ts:
- `FhirResource` base interface, `FhirReference`, `FhirCodeableConcept`, `FhirCoding`, `FhirPeriod` are all already defined.
- `Procedure extends FhirResource` uses: `resourceType: 'Procedure'`, `status: string`, `subject: FhirReference`, `code: FhirCodeableConcept`, `performedDateTime?: string`.
- `src/types/fhir.ts` is a single-line barrel: `export * from '../../shared/types/fhir'` — new types auto-flow through it, NO edit needed there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Consent and Encounter interfaces to shared/types/fhir.ts</name>
  <read_first>
    - shared/types/fhir.ts (the file being modified — read the existing Procedure interface ~lines 79-87, FhirPeriod ~line 89, FhirCoding, FhirReference, FhirCodeableConcept definitions to replicate their exact shape)
    - src/types/fhir.ts (confirm it is a barrel re-export; do NOT modify it)
    - .planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md (§"shared/types/fhir.ts — add Consent and Encounter interfaces" — exact field list per D-07)
  </read_first>
  <action>
    Add two exported interfaces to `shared/types/fhir.ts`, both extending `FhirResource`, following the existing `Procedure` interface shape.

    `Encounter`: fields `resourceType: 'Encounter'`, `status: string`, `subject: FhirReference`, optional `period?: FhirPeriod` (reuse the existing `FhirPeriod` type), optional `serviceProvider?: FhirReference`.

    `Consent` (covering D-07): fields `resourceType: 'Consent'`, `status: string`, `scope: FhirCodeableConcept`, `category: FhirCodeableConcept[]`, `patient: FhirReference`, optional `organization?: FhirReference[]`, optional `dateTime?: string`, optional `policyRule?: FhirCodeableConcept`, and optional `provision?: { type: string; purpose?: FhirCoding[] }`.

    Per D-01, these interfaces are for bundle typing only. Do NOT add `Encounter` or `Consent` to the `PatientCase` interface, and do NOT reference them in `extractPatientCases`. The `Encounter` type must never be enumerated into any clinical surface (research Pitfall 6 / D-02). Do NOT modify `src/types/fhir.ts` — its `export *` barrel auto-flows the new types.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>&1 | tail -5; grep -c "interface Consent\|interface Encounter" shared/types/fhir.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "interface Consent" shared/types/fhir.ts` returns a match; `grep "interface Encounter" shared/types/fhir.ts` returns a match.
    - The `Consent` interface contains the field `provision` and the field `scope`; the `Encounter` interface contains the field `serviceProvider`.
    - `npx tsc -b --noEmit` reports no new type errors.
    - `grep -c "Encounter\|Consent" shared/patientCases.ts` shows NO new occurrence (PatientCase untouched) — `extractPatientCases` does not reference either type.
    - `src/types/fhir.ts` is unchanged (still the single `export *` barrel line).
  </acceptance_criteria>
  <done>Both interfaces are exported from `shared/types/fhir.ts`, type-check passes, and neither type is wired into PatientCase or extractPatientCases.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test scaffolds (stub isolation, completeness card, reference augmentation)</name>
  <read_first>
    - tests/audit-bundle-codes.test.ts (analog for tests/stubIsolation.test.ts and tests/augmentReferenceBundles.test.ts — non-RTL unit + spawnSync pattern)
    - tests/LandingPage.test.tsx (exact analog for tests/datenvollstaendigkeitCard.test.tsx — mock setup lines 14-71: useAuth, useData, useLanguage, useRecentActivity mocks; RTL queryByText().not.toBeNull() convention)
    - shared/patientCases.ts (extractPatientCases signature for the isolation test)
    - src/services/fhirLoader.ts (extractCenters signature for the D-04 test)
    - .planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md (§"Tests" — fixture bundle shapes, assertion patterns, mock patterns)
    - .planning/phases/34-data-completeness-consent-stubs/34-RESEARCH.md (§"Validation Architecture" → "Wave 0 Gaps")
  </read_first>
  <action>
    Create three Vitest test files. They MAY start with `it.skip` placeholders for behaviors that depend on data not yet generated (stub bundles do not exist until Plan 03), but EVERY assertion that can be satisfied with an in-memory fixture must be a real (non-skipped) test now.

    `tests/stubIsolation.test.ts` — non-RTL unit test (vitest `describe/it/expect`). Build an in-memory `FhirBundle` fixture containing: one Organization (`org-test`), one full Patient (`pat-full-001`) WITH one Observation referencing it, and one stub Patient (`pat-stub-001`) with ZERO Observations. Tests (real, not skipped): (1) `extractPatientCases([bundle])` output `.map(c => c.id)` does NOT contain `pat-stub-001` and DOES contain `pat-full-001`; (2) `extractCenters([bundle]).find(c => c.id === 'org-test').patientCount === 1`. These will be RED until Plan 02 lands the filters — that is expected.

    `tests/datenvollstaendigkeitCard.test.tsx` — RTL test with `// @vitest-environment jsdom` header. Mock `useData` (must include `bundles`, `centers`, `cases`), `useAuth`, `useLanguage`, `useRecentActivity`, and `countRawPatients` from `fhirLoader` (per PATTERNS.md mock block). Assert the card renders a percentage and the `{n} / {m} patients` sub-label using `screen.queryByText(...).not.toBeNull()` (NO jest-dom). Mark the card-presence assertions `it.skip` ONLY if the card does not yet exist — they become real in Plan 04. Include at minimum one non-skipped test that asserts the mock wiring renders the page without throwing.

    `tests/augmentReferenceBundles.test.ts` — non-RTL unit test using `node:fs` + `spawnSync` (analog tests/audit-bundle-codes.test.ts). Tests assert: (1) after running the augmentation script against a temp copy of `public/data/center-aachen.json`, every pre-existing resource (matched by `resource.id`) is byte-identical (`JSON.stringify(after) === JSON.stringify(before)`); (2) at least one new entry (Consent or stub) was appended; (3) running the script twice does not change `bundle.entry.length` (idempotency). Mark these `it.skip` (the script does not exist until Plan 03) but write the full assertion bodies so Plan 03 only needs to remove `.skip`.

    Do NOT use `toBeInTheDocument` or any jest-dom matcher anywhere (project has no jest-dom). Do NOT use watch-mode flags.
  </action>
  <verify>
    <automated>npm run test:check-skips && npx vitest run tests/stubIsolation.test.ts tests/datenvollstaendigkeitCard.test.tsx tests/augmentReferenceBundles.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `tests/`.
    - `grep -L "toBeInTheDocument" tests/datenvollstaendigkeitCard.test.tsx` lists the file (i.e. the matcher is absent).
    - `tests/stubIsolation.test.ts` contains a real (non-skipped) test referencing `extractPatientCases` and `extractCenters`, asserting `pat-stub-001` is excluded and `patientCount` is `1` — these are EXPECTED to be RED until Plan 02.
    - `tests/augmentReferenceBundles.test.ts` references `scripts/augment-reference-bundles.ts` and asserts byte-identity of pre-existing resources by `resource.id`.
    - `npm run test:check-skips` passes (any `it.skip` present is justified by the documented Wave-0-before-data ordering, and the check-skips allowlist/convention is respected).
    - Vitest loads all three files without a module-resolution or syntax error (red assertions are acceptable; collection errors are not).
  </acceptance_criteria>
  <done>The three Wave 0 test files exist, load cleanly, and contain the full assertion bodies the downstream plans will satisfy. Stub-isolation assertions are intentionally red until Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script → committed bundle JSON | Generated/augmented data crosses into version-controlled assets served read-only to clients |
| bundle data → clinical extraction (`extractPatientCases`) | Untrusted-by-shape stub resources must not cross into clinical surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `PatientCase` type / `extractPatientCases` | mitigate | This plan establishes the type contract; D-01/D-02 forbid wiring `Encounter`/`Consent` into `PatientCase`. Acceptance criteria assert no new `Encounter`/`Consent` reference in `shared/patientCases.ts`. |
| T-34-02 | Information Disclosure | new test fixtures | accept | Fixtures use synthetic placeholder IDs only (`pat-full-001`, `pat-stub-001`); no PHI, no real pseudonyms. |
</threat_model>

<verification>
- `npx tsc -b --noEmit` clean.
- The three new test files are collected by Vitest (`npx vitest run <files>` does not error at collection time).
- `npm run test:check-skips` passes.
</verification>

<success_criteria>
- `Consent` and `Encounter` interfaces exported from `shared/types/fhir.ts`, type-check clean, available via the `src/types/fhir.ts` barrel.
- Neither new type is wired into `PatientCase` or `extractPatientCases`.
- Three Wave 0 test files exist with full assertion bodies; downstream plans turn them green by removing `.skip` and landing implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/34-data-completeness-consent-stubs/34-01-SUMMARY.md`
</output>
