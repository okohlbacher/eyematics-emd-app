---
phase: 34-data-completeness-consent-stubs
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/i18n/translations.ts
  - src/pages/LandingPage.tsx
  - tests/datenvollstaendigkeitCard.test.tsx
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "The dashboard shows total patient count (consented + stubs), consented count, and the Datenvollzähligkeit fraction"
    - "The metric updates correctly when the site filter / per-user site restriction changes"
    - "The card uses a separate raw count for the denominator, NOT cases.length"
  artifacts:
    - path: "src/pages/LandingPage.tsx"
      provides: "Datenvollzähligkeit summary card (D-08/D-09/D-10)"
      contains: "countRawPatients"
    - path: "src/i18n/translations.ts"
      provides: "4 DE/EN i18n keys for the card"
      contains: "datenvollstaendigkeitCaption"
    - path: "tests/datenvollstaendigkeitCard.test.tsx"
      provides: "green card-render + site-filter test"
  key_links:
    - from: "src/pages/LandingPage.tsx"
      to: "countRawPatients (src/services/fhirLoader.ts)"
      via: "import + call on bundles from useData()"
      pattern: "countRawPatients\\(bundles\\)"
    - from: "src/pages/LandingPage.tsx"
      to: "src/i18n/translations.ts"
      via: "t('datenvollstaendigkeit...')"
      pattern: "datenvollstaendigkeit"
---

<objective>
Render the Datenvollzähligkeit summary card on the dashboard — the user-facing deliverable of DASH-01.

Purpose: Surface consented ÷ total as a single completeness metric (D-10), computed client-side (D-08) with the denominator from a separate raw count (D-09) so stubs are visible ONLY here. Honors site filtering via server-restricted bundles.
Output: 4 i18n keys, a new completeness card on LandingPage, and the green `tests/datenvollstaendigkeitCard.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-data-completeness-consent-stubs/34-CONTEXT.md
@.planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md
@.planning/phases/34-data-completeness-consent-stubs/34-UI-SPEC.md

<interfaces>
<!-- Consumed contracts (verified). -->
From src/services/fhirLoader.ts (added in Plan 02): `export function countRawPatients(bundles: FhirBundle[]): number` — raw total Patient count (stubs + full), the denominator.
From src/context/DataContext.tsx: `useData()` returns `{ loading, bundles, centers, cases }` — `bundles` is already exposed; `cases` is the stub-free clinical set (numerator).
From src/pages/LandingPage.tsx: existing KPI tiles grid (~lines 136-158) is the pattern to copy; `totalPatients = cases.length` (line 45) stays UNCHANGED; the new card uses a SEPARATE raw count.
From src/i18n/translations.ts: `key: { de: '…', en: '…' }` shape; `t()` returns the raw string (placeholders like `{n}` are replaced by callers via `.replace`).
From src/components/doc-quality/MetricCard.tsx (lines 31-36): progress bar `h-1.5 rounded-full` pattern.
From src/utils/qualityMetrics.ts (line 51): `scoreColor` threshold-function pattern to mirror for `completenessColor`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 Datenvollzähligkeit i18n keys (DE/EN)</name>
  <read_first>
    - src/i18n/translations.ts (existing camelCase key shape ~lines 1-60; LandingPage section near connectedCenters/pseudonymizedCases)
    - .planning/phases/34-data-completeness-consent-stubs/34-UI-SPEC.md (§"Copywriting Contract" — exact DE/EN strings)
    - .planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md (§"src/i18n/translations.ts — 4 new i18n keys")
  </read_first>
  <action>
    Add four keys to `src/i18n/translations.ts` following the `key: { de: '…', en: '…' }` convention, in the LandingPage key region:
    - `datenvollstaendigkeitCaption`: de `'DATENVOLLZÄHLIGKEIT'`, en `'DATA COMPLETENESS'`
    - `datenvollstaendigkeitLabel`: de `'Datenvollzähligkeit'`, en `'Data completeness'`
    - `datenvollstaendigkeitPatients`: de `'{n} / {m} Patienten'`, en `'{n} / {m} patients'`
    - `datenvollstaendigkeitAriaLabel`: de `'Datenvollzähligkeit: {pct}%'`, en `'Data completeness: {pct}%'`

    Keep the `{n}`/`{m}`/`{pct}` placeholder markers literal in the strings — `t()` does not interpolate; the card replaces them with `.replace`.
  </action>
  <verify>
    <automated>grep -c "datenvollstaendigkeit" src/i18n/translations.ts; npx tsc -b --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "datenvollstaendigkeit" src/i18n/translations.ts` returns 4.
    - Each key has both `de` and `en` properties; `datenvollstaendigkeitPatients` contains `{n}` and `{m}`; `datenvollstaendigkeitAriaLabel` contains `{pct}`.
    - `npx tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>Four DE/EN i18n keys exist matching the UI-SPEC copywriting contract.</done>
</task>

<task type="auto">
  <name>Task 2: Render the Datenvollzähligkeit card on LandingPage and green the card test</name>
  <read_first>
    - src/pages/LandingPage.tsx (full file — import block lines 1-18, useData destructure line 31, KPI derivations near line 45-56, KPI tiles grid lines 136-158, Centers row line 160)
    - src/services/fhirLoader.ts (countRawPatients signature from Plan 02)
    - src/components/doc-quality/MetricCard.tsx (progress bar pattern lines 31-36)
    - src/utils/qualityMetrics.ts (scoreColor pattern line 51)
    - src/components/primitives/Tile.tsx (Tile component — reused unchanged)
    - tests/datenvollstaendigkeitCard.test.tsx (Plan 01 scaffold — remove .skip and make green)
    - .planning/phases/34-data-completeness-consent-stubs/34-PATTERNS.md (§"src/pages/LandingPage.tsx — new Datenvollzähligkeit card" — imports, metric computation, completenessColor, full card JSX)
    - .planning/phases/34-data-completeness-consent-stubs/34-UI-SPEC.md (§"Card Layout", §"States and Interactions", §"Accessibility")
  </read_first>
  <action>
    In `src/pages/LandingPage.tsx`:
    - Add `ShieldCheck` to the lucide-react import; import `countRawPatients` from `'../services/fhirLoader'`; add `bundles` to the `useData()` destructure.
    - Compute, after the existing KPI derivations: `totalRawPatients = countRawPatients(bundles)` (denominator, D-09 — includes stubs); `consentedPatients = cases.length` (numerator, D-05 — stub-free); `completenessFraction = totalRawPatients > 0 ? consentedPatients / totalRawPatients : 0`; `completenessPercent = Math.round(completenessFraction * 100)`. Do NOT change the existing `totalPatients = cases.length` line.
    - Add a `completenessColor(fraction)` helper: sage when `>= 0.5`, amber when `>= 0.25`, coral otherwise — using CSS token variables (`var(--color-sage)` / `var(--color-amber)` / `var(--color-coral)`), NOT hex (UI-SPEC Color rule).
    - Insert a full-width card row between the KPI tiles grid and the Centers row, reusing the existing `Tile` primitive: a `w-8 h-8 rounded-lg` teal-soft icon container with a `ShieldCheck` icon (`aria-hidden="true"`); a 32px semibold mono fraction (`font-data`, colored by `completenessColor`); the `{n} / {m} Patienten` sub-label via `t('datenvollstaendigkeitPatients').replace('{n}', …).replace('{m}', …)`; the caption via `t('datenvollstaendigkeitCaption')`; and a `h-1.5 rounded-full` progress bar (MetricCard pattern) with `role="progressbar"`, `aria-valuenow/min/max`, and an `aria-label` from `t('datenvollstaendigkeitAriaLabel').replace('{pct}', …)`. The card renders only when `!loading` (inherits the existing page load guard); the zero-fraction state shows `0 %` with coral fill and must not crash (UI-SPEC States).

    Then in `tests/datenvollstaendigkeitCard.test.tsx`, remove the `.skip` markers and ensure: the card renders the expected percentage text and the `{n} / {m}` sub-label (using `screen.queryByText(...).not.toBeNull()`, NO jest-dom); and a second case with a different `countRawPatients` mock value / `cases` length asserts the displayed fraction updates (site-filter reactivity proxy, D-10).
  </action>
  <verify>
    <automated>npx vitest run tests/datenvollstaendigkeitCard.test.tsx 2>&1 | tail -15 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "countRawPatients(bundles)" src/pages/LandingPage.tsx` returns ≥1; `grep "totalPatients = cases.length" src/pages/LandingPage.tsx` still present (unchanged).
    - `grep "completenessColor" src/pages/LandingPage.tsx` returns a match using `var(--color-sage)`/`var(--color-amber)`/`var(--color-coral)` (no hex in the new helper).
    - The card markup includes `role="progressbar"` with `aria-valuenow`, and the `ShieldCheck` icon has `aria-hidden="true"`.
    - `tests/datenvollstaendigkeitCard.test.tsx` is green with NO `it.skip`: asserts the percentage + `{n} / {m}` sub-label render, and asserts the fraction changes when the mocked counts change.
    - `npm run lint` passes; `npx tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>The Datenvollzähligkeit card renders consented/total/fraction with a semantic progress bar and accessible labels; the card test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundles → completeness denominator | Denominator must reflect only the user's permitted sites |
| stub data → UI surfaces | Stub demographics must NOT appear anywhere except the aggregate denominator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-10 | Information Disclosure | Datenvollzähligkeit denominator | mitigate | `countRawPatients(bundles)` operates only on server-filtered `bundles` (per-user `req.auth.centers`); the card never lists stub demographics — only the aggregate count (D-02/D-10). |
| T-34-11 | Tampering | numerator/denominator confusion | mitigate | Numerator = `cases.length` (stub-free, D-05); denominator = `countRawPatients` (raw). Acceptance criteria assert the existing `totalPatients = cases.length` line is unchanged and the card uses the separate raw count (D-09), preventing `cases.length` from being used as the denominator. |
| T-34-12 | Denial of Service | zero-division / empty selection | accept | `completenessFraction` guards `totalRawPatients > 0`; UI-SPEC zero-fraction state renders `0 %` without crashing. |
</threat_model>

<verification>
- `tests/datenvollstaendigkeitCard.test.tsx` green (render + reactivity).
- `npm run lint` + `npx tsc -b --noEmit` clean.
- `npm run test:ci` fully green (phase gate).
</verification>

<success_criteria>
- The dashboard shows total (consented + stubs), consented count, and the fraction, updating with the site set.
- The denominator uses the separate raw count, never `cases.length`.
- Card matches the UI-SPEC (teal icon, semantic fraction color, accessible progress bar); no hex in new markup.
</success_criteria>

<output>
After completion, create `.planning/phases/34-data-completeness-consent-stubs/34-04-SUMMARY.md`
</output>
