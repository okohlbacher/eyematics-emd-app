---
phase: 39-configurable-thresholds
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - src/pages/SettingsPage.tsx
  - src/i18n/translations.ts
autonomous: true
requirements: [CFG-01, CFG-02]

must_haves:
  truths:
    - "An admin sees a 'Clinical thresholds' section in Settings and can edit CRT critical, Visus critical, IOP critical, Visus-jump, therapy interrupter/breaker days"
    - "An admin sees a 'Plausibility ranges' section and can edit Visus/CRT/IOP min/max"
    - "Saving persists to settings.yaml and shows the existing success banner; invalid values are blocked with a validation message"
    - "All new labels/hints have DE and EN i18n keys (no raw strings)"
  artifacts:
    - path: "src/pages/SettingsPage.tsx"
      provides: "Clinical thresholds + Plausibility ranges admin form sections"
      contains: "plausibility"
    - path: "src/i18n/translations.ts"
      provides: "DE/EN keys for the new sections"
      contains: "settingsThreshold"
  key_links:
    - from: "src/pages/SettingsPage.tsx"
      to: "src/services/settingsService.ts"
      via: "updateSettings({ thresholds, plausibility })"
      pattern: "updateSettings"
    - from: "src/pages/SettingsPage.tsx"
      to: "shared/thresholdConfig.ts"
      via: "validateThresholds / validatePlausibility before persist"
      pattern: "validateThresholds|validatePlausibility"
---

<objective>
Add two admin form sections to `SettingsPage` — "Clinical thresholds" and "Plausibility ranges" — mirroring the existing TTL/therapy form patterns (number inputs, inline validation, persist via `updateSettings`, success banner, error banner). Wire client-side validation through the shared `validateThresholds`/`validatePlausibility` helpers from Plan 01, and add all DE/EN i18n keys.

Purpose: CFG-01 and CFG-02 require admins to view and edit these values in the Settings UI with the same config + validation pattern as existing settings.
Output: two new form sections on SettingsPage and the i18n keys backing them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-configurable-thresholds/39-CONTEXT.md
@CLAUDE.md

<read_first>
@src/pages/SettingsPage.tsx
@src/services/settingsService.ts
@shared/thresholdConfig.ts
</read_first>

<interfaces>
<!-- From Plan 01 (39-01). These exist by the time this plan runs (wave 2). -->

AppSettings additions (src/services/settingsService.ts):
  thresholds?: { criticalCrtUm; criticalVisus; criticalIopMmHg; visusJump }
  plausibility?: { visusMin; visusMax; crtMin; crtMax; iopMin; iopMax }

Shared validators (shared/thresholdConfig.ts):
  validateThresholds(t): error-code-union | 'ok'
  validatePlausibility(p): error-code-union | 'ok'
  THRESHOLD_DEFAULTS, PLAUSIBILITY_DEFAULTS

Existing SettingsPage patterns to mirror (do NOT invent new ones):
  - loadSettings() in useEffect → setState for each field
  - handleSaveTtl(): validate → updateSettings({...}) → showSaved() / setSaveError
  - number <input type="number"> with red border + role="alert" message on validation failure
  - useLanguage() t() for every label/hint — translations live in src/i18n/translations.ts (DE + EN maps)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DE/EN i18n keys for both sections</name>
  <files>src/i18n/translations.ts</files>
  <read_first>src/i18n/translations.ts (existing settings* and ttl* keys for tone/structure)</read_first>
  <action>Add matching DE and EN keys for: section titles ("Clinical thresholds" / "Klinische Schwellenwerte", "Plausibility ranges" / "Plausibilitätsbereiche"); field labels + hints for criticalCrtUm, criticalVisus, criticalIopMmHg, visusJump (the therapy interrupter/breaker labels already exist — reuse `settingsInterrupterDays`/`settingsBreakerDays` rather than duplicating); field labels for visusMin/Max, crtMin/Max, iopMin/Max; and one validation-error message per section (e.g. `settingsThresholdValidationError`, `settingsPlausibilityValidationError`). Use a consistent key prefix (`settingsThreshold*`, `settingsPlausibility*`). Ensure every EN key has a DE counterpart in the same object — no missing-locale gaps.</action>
  <verify>
    <automated>grep -qc "settingsThreshold" src/i18n/translations.ts && grep -qc "settingsPlausibility" src/i18n/translations.ts && npx vitest run i18n 2>&1 | grep -qiE "passed|PASS|no test files"</automated>
  </verify>
  <done>New settingsThreshold*/settingsPlausibility* keys exist in both DE and EN maps; any existing i18n parity test stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Add Clinical thresholds + Plausibility ranges form sections</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>src/pages/SettingsPage.tsx (therapy section ~line 536, handleSaveTtl ~line 180, success/error banner ~line 325), shared/thresholdConfig.ts, src/services/settingsService.ts</read_first>
  <action>Add two new card sections in the existing layout (matching the therapy/TTL card styling): "Clinical thresholds" with number inputs for criticalCrtUm, criticalVisus, criticalIopMmHg, visusJump (plus the already-present therapy interrupter/breaker inputs may stay in their current therapy card — do not move them; CFG-01 lists them but they are already editable, so only ensure they persist via the same path); and "Plausibility ranges" with min/max number inputs for Visus, CRT, IOP. Add state hooks initialized from `loadSettings()` in the existing useEffect (read `s.thresholds ?? THRESHOLD_DEFAULTS`, `s.plausibility ?? PLAUSIBILITY_DEFAULTS`). Add a `handleSaveThresholds` and `handleSavePlausibility` (or one combined handler) modeled on `handleSaveTtl`: call `validateThresholds`/`validatePlausibility`; on non-'ok' set an inline validation error (red border + role="alert" message via the new i18n keys) and return; on 'ok' call `updateSettings({ thresholds })` / `updateSettings({ plausibility })`, then `showSaved()`, catching errors into `setSaveError`. Reuse the existing success and error banners. Every label/hint uses `t()`. Follow camelCase TS identifiers; no env vars.</action>
  <verify>
    <automated>grep -q "validateThresholds\|validatePlausibility" src/pages/SettingsPage.tsx && grep -q "updateSettings({ thresholds" src/pages/SettingsPage.tsx && npx vitest run SettingsPage settings 2>&1 | grep -qiE "passed|PASS|no test files"</automated>
  </verify>
  <done>SettingsPage renders both sections; editing + saving calls updateSettings with thresholds/plausibility; invalid values show a validation message and block persist; success banner fires on save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → updateSettings → PUT /api/settings | user-entered threshold/plausibility values cross to the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Tampering | SettingsPage save handlers | mitigate | Client validates via shared validateThresholds/validatePlausibility before persist; the server (Plan 01 validateSettingsSchema) re-validates authoritatively — client validation is UX-only and never the sole gate. |
| T-39-05 | Elevation of Privilege | non-admin reaching the form | accept | Settings route is already admin-gated in the app; even if rendered, PUT is rejected 403 server-side (Plan 01 guard). UI does not add a new privileged path. |
</threat_model>

<verification>
- `npm run test:ci` stays green (902 baseline).
- Both new sections render and persist; validation blocks bad input.
- No raw (untranslated) strings in the new sections — every label/hint via `t()`.
</verification>

<success_criteria>
- Admin can read and edit all critical/action thresholds and plausibility ranges in Settings (CFG-01, CFG-02).
- Save persists to settings.yaml via updateSettings and shows the success banner; invalid input is blocked with a localized message.
- All new copy has DE + EN i18n keys.
</success_criteria>

<output>
After completion, create `.planning/phases/39-configurable-thresholds/39-02-SUMMARY.md`
</output>
