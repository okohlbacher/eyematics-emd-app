---
phase: 41-docquality-multicenter-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/quality/QualityCaseDetail.tsx
  - tests/QualityCaseDetail.test.tsx
autonomous: true
requirements: [QUAL-025]

must_haves:
  truths:
    - "The approve/flag-status control in quality case detail is reachable without scrolling past all patient data"
    - "Mark-reviewed and exclude actions remain functional after repositioning"
    - "Per-flag status dropdowns (open/acknowledged/resolved) still update flag status"
  artifacts:
    - path: "src/components/quality/QualityCaseDetail.tsx"
      provides: "Approve/flag status control positioned near the top of the case detail"
    - path: "tests/QualityCaseDetail.test.tsx"
      provides: "RTL coverage that the status control renders before the values table"
  key_links:
    - from: "QualityCaseDetail status control"
      to: "onMarkReviewed / onUpdateFlagStatus callbacks"
      via: "control rendered above the values/OCT tab section"
      pattern: "onMarkReviewed"
---

<objective>
Reposition (or duplicate) the approve/flag-status control in the quality case
detail so it is reachable without scrolling past all the patient data.

Purpose: QUAL-025 — today the per-flag status control (and the review-result
section) sits after the full values table (QualityCaseDetail.tsx:349-383); a
reviewer must scroll past every observation to approve/resolve. Bring the status
control within easy reach near the top of the case detail.
Output: repositioned/duplicated status control in QualityCaseDetail, RTL test
asserting it renders before the values table, all existing flag actions intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docquality-multicenter-ux/41-CONTEXT.md
@CLAUDE.md

<read_first>
- src/components/quality/QualityCaseDetail.tsx — the case-info header (lines 129-217)
  already holds Mark-reviewed (141-153) and Exclude (154-167) buttons at the TOP. The
  per-flag STATUS dropdowns (open/acknowledged/resolved) live in the "Existing flags" /
  reviewResults section at the BOTTOM (lines 349-383), AFTER the values table (265-347).
  QUAL-025 target = the status control reachable near the top, not only after the table.
  Props: `onMarkReviewed`, `onUpdateFlagStatus`, `onExclude`, `caseFlags` (QualityCaseDetailProps
  lines 32-46).
- CONTEXT.md QUAL-025 decision: "move/duplicate the approve/flag status control so it's
  reachable near the top of the case detail (not only after the full values table)."
</read_first>

<interfaces>
From src/components/quality/QualityCaseDetail.tsx (existing callbacks — do not change signatures):
```typescript
onMarkReviewed: (caseId: string) => void;
onUpdateFlagStatus: (caseId: string, flaggedAt: string, status: QualityFlag['status']) => void;
caseFlags: QualityFlag[];   // each has { caseId, flaggedAt, parameter, errorType, flaggedBy, status }
```
QualityFlag['status'] ∈ 'open' | 'acknowledged' | 'resolved'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reposition the approve/flag-status control to the top of case detail</name>
  <files>src/components/quality/QualityCaseDetail.tsx</files>
  <action>
    QUAL-025 — make the approve/flag-status control reachable without scrolling past all
    patient data. Per CONTEXT decision, move or duplicate the status control into the
    case-info header card (lines 129-217), which already renders the Mark-reviewed and
    Exclude buttons at the top. Approach (Claude's discretion on exact layout):
    when `caseFlags.length > 0`, render a compact status summary + per-flag status `<select>`
    (open/acknowledged/resolved, wired to `onUpdateFlagStatus`) near the top — either inside
    the header card or in a new card placed immediately after it and BEFORE the anomalies/values
    sections. If duplicating rather than moving, keep both controls in sync (both bind the same
    `onUpdateFlagStatus` and read `f.status` — they will stay consistent since state is parent-owned).
    Do NOT change the callback signatures or QualityCaseDetailProps. Preserve the existing
    Mark-reviewed/Exclude buttons. Keep the bottom reviewResults section OR remove it if fully
    relocated (Claude's discretion) — but the top control must be present when flags exist.
    camelCase TS (D-05); throw-only (D-03).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>When a case has flags, a status control is rendered in the header region (before the values table); Mark-reviewed/Exclude buttons unchanged; no callback signature changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RTL test — status control reachable before values table</name>
  <files>tests/QualityCaseDetail.test.tsx</files>
  <behavior>
    - With a case that has at least one quality flag, a status control (the open/acknowledged/resolved select) is present in the rendered output.
    - Changing the top status select invokes onUpdateFlagStatus with the case id, flaggedAt, and new status.
    - The mark-reviewed button still invokes onMarkReviewed.
    - The status control appears in the DOM before the values table heading (DOM order proves "reachable without scrolling past patient data").
  </behavior>
  <action>
    Create tests/QualityCaseDetail.test.tsx (RTL, NO jest-dom — use
    `queryByText().not.toBeNull()` / `.toBeNull()` and `fireEvent`). Render QualityCaseDetail
    with a fixture PatientCase plus one caseFlag (status 'open') and stub callbacks
    (vi.fn()). Assert: the status select is present; firing a change to 'resolved' calls
    onUpdateFlagStatus with (caseId, flaggedAt, 'resolved'); clicking mark-reviewed calls
    onMarkReviewed. To prove top placement, query the container and assert the status-control
    node's position precedes the values-table heading node (e.g. compare
    `compareDocumentPosition` or index within `container.querySelectorAll`). Wrap render in the
    LanguageProvider used by other component tests (grep an existing quality test for the import).
    Provide minimal valid PatientCase fixture fields the component reads (id, pseudonym,
    birthDate, conditions, observations [], procedures [], imagingStudies [], centerName).
  </action>
  <verify>
    <automated>npx vitest run tests/QualityCaseDetail.test.tsx</automated>
  </verify>
  <done>Test passes: status control present, change calls onUpdateFlagStatus, mark-reviewed calls onMarkReviewed, status control precedes the values table in DOM order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer action → onUpdateFlagStatus / onMarkReviewed | Status changes flow to parent-owned handlers; persistence/authorization handled upstream (unchanged this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06 | Tampering | duplicated status control | mitigate | Both controls bind the same parent-owned `onUpdateFlagStatus`/`f.status`; no independent client state, so duplication cannot desync or forge a status. UI-only change. |
| T-41-07 | Repudiation | flag status update | accept | Audit/persistence of status changes is unchanged and handled upstream (updateQualityFlag in DataContext); this plan only relocates the control. Out of scope here. |
</threat_model>

<verification>
- `npm run lint` clean.
- `npx vitest run tests/QualityCaseDetail.test.tsx` green.
- Manual: open /quality, select a flagged case → status control visible near the top without scrolling past the values table; changing it updates the flag status.
</verification>

<success_criteria>
- Approve/flag-status control reachable without scrolling past patient data (QUAL-025).
- Existing mark-reviewed, exclude, and per-flag status actions remain functional.
- `npm run test:ci` stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/41-docquality-multicenter-ux/41-03-SUMMARY.md`
</output>
