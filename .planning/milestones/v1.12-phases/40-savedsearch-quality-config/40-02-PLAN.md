---
phase: 40-savedsearch-quality-config
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - shared/types/fhir.ts
  - shared/qualityParams.ts
  - shared/savedSearchSanitize.ts
  - server/dataDb.ts
  - server/dataApi.ts
  - src/pages/CohortBuilderPage.tsx
  - src/i18n/translations.ts
  - tests/qualityParams.test.ts
  - tests/dataApiQualityParams.test.ts
autonomous: true
requirements: [QUAL-021]
must_haves:
  truths:
    - "A user can select which quality parameters are checked for a cohort in the cohort builder save flow"
    - "The selection persists with the saved cohort (stored as a SavedSearch field) and is restored when the cohort loads"
    - "A saved search with no parameter selection (old record or unset) is treated as 'all default checks' (back-compat)"
    - "The qualityParams field flows through the F-13 server sanitization — only known check keys are persisted"
  artifacts:
    - path: "shared/qualityParams.ts"
      provides: "Canonical quality-check parameter keys + default-all helper"
      contains: "export const QUALITY_PARAM_KEYS"
    - path: "shared/types/fhir.ts"
      provides: "SavedSearch.qualityParams optional field"
      contains: "qualityParams"
    - path: "server/dataDb.ts"
      provides: "saved_searches.quality_params column + migration"
      contains: "quality_params"
    - path: "src/pages/CohortBuilderPage.tsx"
      provides: "parameter checklist in save flow"
  key_links:
    - from: "src/pages/CohortBuilderPage.tsx"
      to: "/api/data/saved-searches"
      via: "POST body includes qualityParams selection"
      pattern: "qualityParams"
    - from: "server/dataApi.ts"
      to: "shared/savedSearchSanitize.ts"
      via: "sanitize qualityParams against QUALITY_PARAM_KEYS before persist"
      pattern: "sanitizeQualityParams|QUALITY_PARAM_KEYS"
---

<objective>
Add a persisted per-cohort quality-parameter selection to SavedSearch (QUAL-021, decision D2). A user choosing to save a cohort can pick WHICH quality checks apply to it (missing Visus, missing CRT, missing injections, CRT critical, Visus critical, Visus jump). The selection stores as a new `qualityParams` field on SavedSearch and flows through the F-13 hardened/sanitized server path established in 40-01.

Purpose: D2 locks QUAL-001 (parameter selection) to persist WITH the saved cohort, so quality review can later (40-03) honor a cohort-specific check set instead of a fixed global set.

Output: Canonical param-key module, extended SavedSearch type + DB column + migration, sanitizer extension, cohort-builder checklist UI + i18n, and tests. Back-compat: missing/unset selection means "all default checks".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-savedsearch-quality-config/40-CONTEXT.md
@CLAUDE.md
@.planning/phases/40-savedsearch-quality-config/40-01-PLAN.md

<read_first>
- shared/types/fhir.ts — SavedSearch (:178), CohortFilter (:160)
- shared/savedSearchSanitize.ts — created in 40-01; extend with qualityParams sanitization
- server/dataDb.ts — saved_searches table (:57), PK migration pattern (:82-101), SavedSearchRow (:165), getSavedSearches (:173), addSavedSearch (:181 INSERT OR REPLACE)
- server/dataApi.ts — POST /saved-searches (hardened in 40-01) and GET /saved-searches (:154 row→client mapping)
- src/pages/CohortBuilderPage.tsx — handleSave (:333, updated in 40-01), save UI block (:846-871), filters state
- src/components/quality/QualityCaseDetail.tsx — anomalies useMemo (:78-111): the SIX checks are missingVisus, missingCrt, missingInjections (IVOM), crtAnomaly (critical CRT), visusAnomaly (critical Visus), visusJump. These map to the configurable param keys.
- src/i18n/translations.ts — missingVisus (:427), missingCrt (:428), missingInjections (:429), crtAnomaly (:316), visusAnomaly (:317), visusJump (:318); translations shape (top of file: `key: { de, en }`)
- tests/dataApiCenter.test.ts — server router test harness to reuse
</read_first>

<interfaces>
The six quality checks (from QualityCaseDetail anomalies) become the canonical param keys:
```
missingVisus | missingCrt | missingInjections | crtCritical | visusCritical | visusJump
```
SavedSearch gains (back-compat — optional):
```typescript
export interface SavedSearch {
  id: string; name: string; createdAt: string; filters: CohortFilter;
  qualityParams?: string[]; // selected check keys; absent/undefined ⇒ all default checks
}
```
DB row gains:
```typescript
export interface SavedSearchRow { id; name; created_at; filters; quality_params?: string; updated_at; } // quality_params = JSON string[] or NULL
```
DECISION (Claude's discretion per CONTEXT): field name `qualityParams`. Stored as a JSON array of selected keys. Semantics: `undefined`/missing ⇒ ALL checks (back-compat); explicit `[]` ⇒ no checks selected (a valid explicit choice); a subset ⇒ only those checks. Document this tri-state in shared/qualityParams.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonical quality-param keys + sanitizer extension</name>
  <files>shared/qualityParams.ts, shared/savedSearchSanitize.ts, shared/types/fhir.ts, tests/qualityParams.test.ts</files>
  <behavior>
    - QUALITY_PARAM_KEYS is the frozen ordered list of the six check keys
    - sanitizeQualityParams(unknown) returns undefined for undefined input (back-compat: ⇒ all checks)
    - sanitizeQualityParams returns string[] containing ONLY values present in QUALITY_PARAM_KEYS, de-duplicated, order-stable; unknown values stripped
    - sanitizeQualityParams([]) returns [] (explicit empty selection preserved, distinct from undefined)
    - Non-array, non-undefined input (object, number, string) returns undefined (treated as unset)
    - resolveQualityParams(selection): returns the effective check set — selection ?? all-keys — for consumers
  </behavior>
  <action>Create `shared/qualityParams.ts` exporting: `QUALITY_PARAM_KEYS` (a readonly tuple of the six keys: `missingVisus`, `missingCrt`, `missingInjections`, `crtCritical`, `visusCritical`, `visusJump`); a `QualityParamKey` type; `sanitizeQualityParams(raw: unknown): string[] | undefined` (whitelist against QUALITY_PARAM_KEYS, dedupe, preserve undefined as undefined, preserve [] as []); and `resolveQualityParams(selection: string[] | undefined): string[]` returning `selection ?? [...QUALITY_PARAM_KEYS]`. Header comment documents the tri-state semantics (undefined ⇒ all; [] ⇒ none; subset ⇒ those). camelCase TS (D-05); throw-only (D-03) but the sanitizer does not throw on bad input. Import type from `./types/fhir.js` only if needed.

In `shared/types/fhir.ts`: add optional `qualityParams?: string[]` to the SavedSearch interface (after `filters`) with a comment pointing to qualityParams.ts for semantics.

In `shared/savedSearchSanitize.ts` (from 40-01): export the qualityParams handling so the API can call it — either re-export sanitizeQualityParams or import it where the endpoint sanitizes. Keep filter sanitization unchanged.

Write `tests/qualityParams.test.ts` (vitest, no jest-dom) covering all behavior bullets including unknown-value strip, dedupe, undefined-vs-[] distinction, and resolveQualityParams fallback.</action>
  <verify>
    <automated>npx vitest run tests/qualityParams.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>QUALITY_PARAM_KEYS + sanitizeQualityParams + resolveQualityParams exported with documented tri-state; SavedSearch.qualityParams typed; tests green; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist quality_params — DB column, migration, API read/write</name>
  <files>server/dataDb.ts, server/dataApi.ts, tests/dataApiQualityParams.test.ts</files>
  <behavior>
    - saved_searches gains a nullable quality_params TEXT column; existing DBs migrate without data loss (old rows get NULL)
    - getSavedSearches returns quality_params (string|null) on the row
    - POST /saved-searches sanitizes incoming qualityParams via sanitizeQualityParams and persists JSON (or NULL when undefined)
    - GET /saved-searches maps a row's quality_params JSON back to qualityParams: string[] (omitted/undefined when NULL → back-compat ⇒ all checks)
    - A POST with qualityParams containing an unknown key persists only the known keys
    - A POST with no qualityParams persists NULL and GET returns a record with qualityParams undefined
  </behavior>
  <action>In `server/dataDb.ts`: add `quality_params TEXT` (nullable) to the saved_searches CREATE TABLE; add an idempotent ALTER-TABLE-ADD-COLUMN migration guarded by a `table_info(saved_searches)` check (mirror the existing PK-migration guard at :82 — check whether the column is absent before ALTER). Extend SavedSearchRow with `quality_params?: string | null`. Update getSavedSearches SELECT to include quality_params. Update addSavedSearch INSERT OR REPLACE to include quality_params (param-bind; NULL when absent). throw-only (D-03).

In `server/dataApi.ts`: in the hardened POST /saved-searches (from 40-01), read `qualityParams` from the body, run `sanitizeQualityParams(qualityParams)`, and set `quality_params: result === undefined ? null : JSON.stringify(result)` on the row. In GET /saved-searches row→client mapping (:154), parse quality_params (JSON.parse when non-null; on parse error treat as undefined and warn, matching the filters try/catch resilience) and include `qualityParams` only when defined. Return qualityParams in the POST 201 response too. Import sanitizeQualityParams from `../shared/qualityParams.js`.

Write `tests/dataApiQualityParams.test.ts` reusing the harness from tests/dataApiCenter.test.ts. Tests: (a) POST with `qualityParams: ['missingVisus','bogus']` → addSavedSearch called with quality_params JSON === '["missingVisus"]'; (b) POST without qualityParams → quality_params arg is null; (c) GET mapping: mock getSavedSearches to return a row with quality_params '["visusJump"]' → response savedSearches[0].qualityParams deep-equals ['visusJump']; (d) GET mapping with quality_params null → response record has no qualityParams (undefined).</action>
  <verify>
    <automated>npx vitest run tests/dataApiQualityParams.test.ts tests/dataDb.test.ts</automated>
  </verify>
  <done>Nullable quality_params column added + migrated; API sanitizes on write and maps on read; old rows yield undefined (⇒ all checks); tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Cohort builder parameter-selection UI + i18n</name>
  <files>src/pages/CohortBuilderPage.tsx, src/i18n/translations.ts</files>
  <action>Add a quality-parameter checklist to the cohort save flow in `src/pages/CohortBuilderPage.tsx`.

State: add `qualityParams` selection state. Default the checklist to ALL keys checked (matching back-compat "all default checks" semantics) so a user who does not touch it saves the all-checks intent. Render a labeled checklist in the save block (near :846, above or below the name input) listing the six checks from QUALITY_PARAM_KEYS, each with an i18n label. Use existing styling primitives in the file (checkbox inputs consistent with the advanced filter dialog pattern). When all six are checked, send `qualityParams: undefined` (canonical "all") OR send the full array — choose: send `undefined` when all are checked to keep old records semantically identical, send the explicit subset otherwise. Document the choice in a comment.

handleSave (updated in 40-01 to send name+filters): also include the qualityParams selection in the payload to addSavedSearch / the POST body. Update the DataContext addSavedSearch input type (created in 40-01) to accept optional `qualityParams?: string[]` and forward it in the POST body — coordinate with 40-01's signature (this plan depends on 40-01).

handleLoadSearch (:349): when loading a saved cohort, restore its qualityParams into the checklist state — `resolveQualityParams(s.qualityParams)` (from shared/qualityParams.ts) gives the effective set to check; map that onto the checkbox state so an old/unset record shows all-checked.

i18n (`src/i18n/translations.ts`): add keys for the checklist section heading (e.g. `qualityParamsLabel`) and, if not reusing the existing anomaly labels, add per-check labels. PREFER reusing existing keys where they exist: missingVisus, missingCrt, missingInjections, crtAnomaly (CRT critical), visusAnomaly (Visus critical), visusJump. Add new keys only for the new ones (e.g. the section heading and a "select which checks apply" helper). Provide both `de` and `en` for every new key. Follow the `key: { de, en }` shape; camelCase keys (D-05).

Conventions: async/await in touched files (D-04); RTL test assertions elsewhere use queryByText().not.toBeNull() (no jest-dom) — keep any inline test selectors consistent though tests are added implicitly via existing CohortBuilder test if present.</action>
  <verify>
    <automated>npx vitest run --reporter=dot && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Save flow shows a six-item quality-check checklist (defaulting to all checked); selection is sent on save and restored on load via resolveQualityParams; new i18n keys present in de+en; typecheck + lint clean; existing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /saved-searches `qualityParams` | Untrusted array of check keys crosses into persistence |
| stored quality_params JSON → GET → client | Stored JSON re-enters client and later drives which checks run (40-03) |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Tampering | POST qualityParams arbitrary values | mitigate | sanitizeQualityParams whitelists against QUALITY_PARAM_KEYS; unknown values stripped before persist — Tasks 1+2 |
| T-40-08 | Tampering | qualityParams as non-array / object | mitigate | sanitizeQualityParams returns undefined (treated as unset) for non-array input — Task 1 |
| T-40-09 | Information Disclosure | corrupt quality_params JSON on GET | accept | GET parse wrapped in try/catch → undefined + warn (matches filters resilience); per-user scoped, low risk |
| T-40-10 | Repudiation | qualityParams not part of provenance | accept | qualityParams is user preference, not provenance; id/createdAt remain server-owned (40-01); no integrity requirement on the selection itself |
</threat_model>

<verification>
- `npx vitest run tests/qualityParams.test.ts tests/dataApiQualityParams.test.ts tests/dataDb.test.ts` — green
- `npm run test:ci` stays green (935 baseline; +2 new test files)
- `npx tsc --noEmit` clean; `npm run lint` clean for touched files
- Manual: save a cohort with a subset of checks → reload page → load that cohort → checklist shows the saved subset; an old cohort (no qualityParams) loads with all checks checked
</verification>

<success_criteria>
- qualityParams persists with the saved cohort through the F-13 sanitized path (QUAL-021, D2)
- Selection restores on cohort load; missing/unset ⇒ all default checks (back-compat)
- Server strips unknown check keys before persistence (T-40-07/08)
- 935-test baseline stays green
</success_criteria>

<output>
After completion, create `.planning/phases/40-savedsearch-quality-config/40-02-SUMMARY.md`
</output>
