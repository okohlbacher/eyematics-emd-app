---
phase: 44-techdebt-compaction
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - src/components/outcomes/OutcomesView.tsx
  - src/components/outcomes/useOutcomesRouteState.ts
  - src/components/outcomes/useOutcomesAggregation.ts
  - src/components/outcomes/VisusMetricContainer.tsx
  - src/components/outcomes/CrtMetricContainer.tsx
autonomous: true
requirements: [TECH-02]
must_haves:
  truths:
    - "OutcomesView still default-exports a component with the identical public contract (no new props) consumed by AnalysisPage"
    - "URL handling identical: ?cohort=, ?cohorts=, ?filter=, ?metric= parsed and written the same way"
    - "All four metric tabs (visus/crt/interval/responder) render identical output; cross-cohort overlay, Phase-43 drill-down, recent-activity, audit beacon, and server-aggregation routing all still fire"
    - "npm run test:ci exits 0 at every commit (1086 baseline)"
    - "npm run knip reports no new unused exports; npm run lint reports 0 warnings"
  artifacts:
    - path: "src/components/outcomes/useOutcomesRouteState.ts"
      provides: "Hook: URL param parsing (cohort/cohorts/filter/metric), cohort resolution, cross-cohort id list, metric change/keydown handlers, compare drawer handlers, recent-activity + audit beacon effects, drill-down handler"
      contains: "export function useOutcomesRouteState"
    - path: "src/components/outcomes/useOutcomesAggregation.ts"
      provides: "Hook: server-routing decision + effect, aggregate/crtAggregate/crossCohortAggregates/crossCohortCaseSeries/patientCounts memos, server loading state"
      contains: "export function useOutcomesAggregation"
    - path: "src/components/outcomes/VisusMetricContainer.tsx"
      provides: "Visus-metric body (empty-state guards + summary cards + 3 panels + data preview)"
      contains: "export default"
    - path: "src/components/outcomes/CrtMetricContainer.tsx"
      provides: "CRT-metric body (empty-state guard + 3 CRT panels + data preview)"
      contains: "export default"
    - path: "src/components/outcomes/OutcomesView.tsx"
      provides: "Slim orchestrator: wires hooks + renders tab strip, metric containers (visus/crt) + interval/responder bodies + drawers"
      contains: "export default function OutcomesView"
  key_links:
    - from: "src/components/outcomes/OutcomesView.tsx"
      to: "src/components/outcomes/useOutcomesRouteState.ts"
      via: "hook call"
      pattern: "useOutcomesRouteState\\("
    - from: "src/components/outcomes/OutcomesView.tsx"
      to: "src/components/outcomes/useOutcomesAggregation.ts"
      via: "hook call"
      pattern: "useOutcomesAggregation\\("
    - from: "src/components/outcomes/OutcomesView.tsx"
      to: "src/components/outcomes/VisusMetricContainer.tsx"
      via: "JSX render in visus branch"
      pattern: "VisusMetricContainer"
---

<objective>
Decompose `src/components/outcomes/OutcomesView.tsx` (~785 lines, multi-responsibility, F-10 / TECH-02)
into two custom hooks plus per-metric container components, leaving a slim orchestrator.

STRICTLY behavior-preserving and mechanical. The public component contract (default export, no
props, consumed by AnalysisPage as the Trajectories tab) stays identical. ALL behaviors stay
identical: ?cohort=/?cohorts=/?filter=/?metric= URL handling, cross-cohort overlay (Phase 42),
Phase-43 drill-down, recent-activity recording, the once-per-mount audit beacon, server-side
aggregation routing, and the four metric tabs. The test suite (1086) is the contract — green at
every commit.

CRITICAL — Rules of Hooks: all hooks currently run BEFORE any early return (the file documents
this at lines 86, 329 — "Pitfall 3 / WR-01"). When lifting hooks into useOutcomesRouteState /
useOutcomesAggregation, keep the SAME call order and keep them above any conditional return in
OutcomesView. Do not reorder useMemo/useEffect/useState relative to each other.

Purpose: kill the OutcomesView multi-responsibility tech-debt item without changing a pixel.
Output: two hooks + two metric containers + a slim OutcomesView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-techdebt-compaction/44-CONTEXT.md

# The component being decomposed (read in full before moving anything):
@src/components/outcomes/OutcomesView.tsx

<interfaces>
<!-- Public contract — DO NOT change: -->
  export default function OutcomesView(): JSX.Element   // no props; rendered by AnalysisPage Trajectories tab

<!-- External dependencies OutcomesView consumes today (preserve all import sources): -->
  ../../../shared/cohortTrajectory      -> computeCrtTrajectory
  ../../context/DataContext             -> useData (activeCases, savedSearches)
  ../../context/LanguageContext         -> useLanguage (t, locale)
  ../../hooks/useRecentActivity         -> useRecentActivity (record)
  ../../i18n/translations               -> TranslationKey (type)
  ../../services/authHeaders            -> authFetch (audit beacon)
  ../../services/fhirLoader             -> applyFilters
  ../../services/outcomesAggregateService -> postAggregate, AggregateResponse (type)
  ../../services/settingsService        -> getSettings, loadSettings
  ../../utils/cohortFilterSerialization -> safePickCohortFilter
  ../../utils/cohortTrajectory          -> AxisMode, computeCohortTrajectory, defaultScatterOn, PanelResult, SpreadMode, TrajectoryResult, YMetric (types + fns)
  ./CohortCompareDrawer, ./IntervalHistogram (+ IntervalCohortSeries type),
  ./OutcomesDataPreview, ./OutcomesEmptyState, ./OutcomesPanel (+ CohortSeriesEntry type),
  ./OutcomesSettingsDrawer, ./OutcomesSummaryCards, ./palette (COHORT_PALETTES, EYE_COLORS),
  ./ResponderView
  react-router-dom -> useNavigate, useSearchParams
</interfaces>

<test_contract>
<!-- These tests directly exercise OutcomesView behavior — they are the regression net.
     Run the focused outcomes suite after each task; full test:ci before the final commit. -->
  tests/OutcomesViewRouting.test.tsx     — URL routing (?cohort/?cohorts/?filter/?metric)
  tests/OutcomesPanelDrillDown.test.tsx  — Phase-43 drill-down navigation
  tests/metricSelector.test.tsx          — metric tab strip + keyboard nav
  tests/intervalHistogram.test.tsx       — interval metric + cross-cohort series
  tests/responderView.test.tsx           — responder metric + cross-cohort series
  tests/outcomesEmptyState.test.tsx      — empty-state variants
  tests/outcomesPanelCrt.test.tsx        — CRT metric panels
  tests/OutcomesPage.test.tsx            — mount + audit beacon
  Focused run: npx vitest run tests/OutcomesViewRouting.test.tsx tests/OutcomesPanelDrillDown.test.tsx tests/metricSelector.test.tsx tests/intervalHistogram.test.tsx tests/responderView.test.tsx tests/outcomesPanelCrt.test.tsx tests/OutcomesPage.test.tsx
</test_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract route/cohort/URL state into useOutcomesRouteState hook</name>
  <read_first>
    - src/components/outcomes/OutcomesView.tsx (lines 68–261: state, URL parsing, cohort memo, drill-down, recent-activity + audit beacon effects, metric handlers; lines 408–445: patientCounts + compare drawer handlers)
    - tests/OutcomesViewRouting.test.tsx, tests/OutcomesPanelDrillDown.test.tsx, tests/metricSelector.test.tsx
  </read_first>
  <files>src/components/outcomes/useOutcomesRouteState.ts</files>
  <action>
    Create `useOutcomesRouteState()` and MOVE these pieces verbatim (preserving hook call ORDER
    and all eslint-disable comments + Pitfall/WR rationale comments):
      - searchParams/setSearchParams, useLanguage, useRecentActivity, useNavigate wiring (68–73)
      - filterOptions memo (77–84)
      - rawCohortsParam/primaryCohortId/isCrossMode + crossCohortIds memo (87–100)
      - the session-only toggle useState block (axisMode/yMetric/gridPoints/spreadMode/layers,
        threshold, serverAggregate, serverLoading, thresholdLetters) — KEEP these here so call
        order is unchanged; the aggregation hook (Task 2) will RECEIVE them as args rather than
        owning them. (Lifting state ownership across two hooks risks reordering — instead keep
        ALL useState/useMemo/useEffect in this single hook in their current order, and have
        Task 2's hook be a PURE function of passed-in values + memos. See note below.)
      - rawMetric/activeMetric derivation (119–122)
      - loadSettings threshold effect (127–140)
      - cohort memo (143–157)
      - handlePointDrillDown (163–172)
      - recent-activity effect (177–185)
      - default-scatter-off effect (188–193)
      - audit beacon effect (196–221)
      - resetToMetricDefaults + handleMetricChange + handleMetricKeyDown (231–261)
      - patientCounts memo (409–415)
      - handleCompareChange + handleCompareReset (418–445)
    Return a single object exposing everything OutcomesView + the aggregation hook need:
      { t, locale, navigate, searchParams, setSearchParams, filterOptions, isCrossMode,
        primaryCohortId, crossCohortIds, cohort, cohortId, activeMetric, handlePointDrillDown,
        handleMetricChange, handleMetricKeyDown, patientCounts, handleCompareChange,
        handleCompareReset, drawerOpen, setDrawerOpen, compareOpen, setCompareOpen,
        axisMode, setAxisMode, yMetric, setYMetric, gridPoints, setGridPoints, spreadMode,
        layers, setLayers, threshold, serverAggregate, setServerAggregate, serverLoading,
        setServerLoading, thresholdLetters, setThresholdLetters }.
    Move the MetricType/VALID_METRICS/METRIC_TAB_ORDER/metricTitleKey/LayerState definitions
    (lines 50–66) into this hook file and EXPORT the ones OutcomesView still needs
    (MetricType type, METRIC_TAB_ORDER, metricTitleKey) so the tab strip can stay in the view.
    No behavior change: same URL reads/writes, same effect deps, same eslint-disables.
  </action>
  <verify>
    <automated>test -f src/components/outcomes/useOutcomesRouteState.ts && grep -q 'export function useOutcomesRouteState' src/components/outcomes/useOutcomesRouteState.ts && grep -q 'open_outcomes_view' src/components/outcomes/useOutcomesRouteState.ts && echo HOOK1_OK</automated>
  </verify>
  <done>useOutcomesRouteState exists, owns URL/cohort/handlers/effects in unchanged order, exports MetricType/METRIC_TAB_ORDER/metricTitleKey; audit beacon string present.</done>
</task>

<task type="auto">
  <name>Task 2: Extract aggregation routing into useOutcomesAggregation hook</name>
  <read_first>
    - src/components/outcomes/OutcomesView.tsx (lines 223–415: routeServerSide, panelFromServer, server-routing effect, aggregate/crtAggregate/crossCohortAggregates/crossCohortCaseSeries memos)
    - src/components/outcomes/useOutcomesRouteState.ts (the object it returns — your input)
    - tests/outcomesPanelCrt.test.tsx, tests/intervalHistogram.test.tsx, tests/responderView.test.tsx
  </read_first>
  <files>src/components/outcomes/useOutcomesAggregation.ts</files>
  <action>
    Create `useOutcomesAggregation(state)` taking the route-state object from Task 1. MOVE verbatim
    (preserving order RELATIVE to where these hooks ran in the original — they all ran AFTER the
    state/effects in Task 1 and BEFORE the render, so calling this hook immediately after
    useOutcomesRouteState in OutcomesView preserves global hook order):
      - routeServerSide computation (225–228)  [pure derivation from state]
      - panelFromServer helper (264–275)
      - the server-routing useEffect (278–326) — keep the exact dep array and both eslint-disables;
        it calls state.setServerAggregate/state.setServerLoading
      - aggregate memo (331–345)
      - crtAggregate memo (348–363)
      - crossCohortAggregates memo (366–392)
      - crossCohortCaseSeries memo (397–406)
    Return { routeServerSide, aggregate, crtAggregate, crossCohortAggregates, crossCohortCaseSeries }.
    Import CohortSeriesEntry/IntervalCohortSeries/PanelResult/AggregateResponse types + postAggregate
    + computeCohortTrajectory/computeCrtTrajectory + applyFilters + COHORT_PALETTES from their
    original sources. No behavior change: identical memo dep arrays, identical server fetch logic
    (Promise.all of od/os/combined), identical fallback-on-error console.warn + setServerAggregate(null).
  </action>
  <verify>
    <automated>test -f src/components/outcomes/useOutcomesAggregation.ts && grep -q 'export function useOutcomesAggregation' src/components/outcomes/useOutcomesAggregation.ts && grep -q 'Promise.all' src/components/outcomes/useOutcomesAggregation.ts && echo HOOK2_OK</automated>
  </verify>
  <done>useOutcomesAggregation exists, owns routeServerSide + server effect + all four aggregate memos with unchanged dep arrays; returns the five values the view renders from.</done>
</task>

<task type="auto">
  <name>Task 3: Extract Visus + CRT metric containers; slim OutcomesView; verify gates green</name>
  <read_first>
    - src/components/outcomes/OutcomesView.tsx (lines 447–784: renderTabStrip, renderBody, JSX return + drawers)
    - src/components/outcomes/useOutcomesRouteState.ts, src/components/outcomes/useOutcomesAggregation.ts
  </read_first>
  <files>src/components/outcomes/VisusMetricContainer.tsx, src/components/outcomes/CrtMetricContainer.tsx, src/components/outcomes/OutcomesView.tsx</files>
  <action>
    1. Create VisusMetricContainer.tsx: MOVE the `activeMetric === 'visus'` body (505–583) verbatim —
       the empty-state guards (no aggregate / no-visus / all-eyes-filtered), OutcomesSummaryCards,
       the scatter-default test div, the 3 OutcomesPanel renders (od/os/combined with cohortSeries +
       onPointClick wiring), and OutcomesDataPreview. Accept props for everything it reads:
       aggregate, t, locale, axisMode, yMetric, layers, isCrossMode, crossCohortAggregates,
       handlePointDrillDown, cohort. Default-export the component. KEEP the
       data-testid="outcomes-scatter-default-on/off" div and all empty-state variants exactly.
    2. Create CrtMetricContainer.tsx: MOVE the `activeMetric === 'crt'` body (585–644) verbatim —
       the no-crt empty-state guard, 3 CRT OutcomesPanel renders (metric="crt"), OutcomesDataPreview.
       Same prop shape (crtAggregate instead of aggregate). Default-export.
    3. Rewrite OutcomesView.tsx as the slim orchestrator:
       - call `const s = useOutcomesRouteState();` then `const a = useOutcomesAggregation(s);`
         (in THIS order, before any return — preserves hook order).
       - keep renderTabStrip inline (447–483) — it reads activeMetric/handleMetricChange/
         handleMetricKeyDown/t from s and uses METRIC_TAB_ORDER/metricTitleKey imported from
         the route-state hook.
       - keep renderBody as a thin switch: server-computing spinner guard (491–503) then
         <VisusMetricContainer .../> for visus, <CrtMetricContainer .../> for crt, and the
         interval (646–664) + responder (666–686) bodies inline verbatim (they are short and
         read cohort/thresholdLetters/crossCohortCaseSeries/aggregate).
       - keep the header (691–742), tab strip, body, OutcomesSettingsDrawer (751–768) and
         CohortCompareDrawer (771–781) JSX verbatim, wiring props from s and a.
    Behavior-preserving: identical DOM output, identical testids, identical drawer wiring.
    Run the focused outcomes suite, then full gates.
  </action>
  <verify>
    <automated>npm run test:ci</automated>
    <automated>npm run lint 2>&1 | tail -5</automated>
    <automated>npm run knip 2>&1 | grep -iE 'unused|outcomes' | head; echo "knip-checked"</automated>
    <automated>grep -q 'useOutcomesRouteState(' src/components/outcomes/OutcomesView.tsx && grep -q 'useOutcomesAggregation(' src/components/outcomes/OutcomesView.tsx && grep -q 'VisusMetricContainer' src/components/outcomes/OutcomesView.tsx && grep -q 'export default function OutcomesView' src/components/outcomes/OutcomesView.tsx</automated>
  </verify>
  <done>Visus + CRT containers exist and default-export; OutcomesView is a slim orchestrator calling both hooks (in order) and rendering the containers + interval/responder/drawers; npm run test:ci exits 0 (1086 green); lint 0 warnings; knip no new unused exports; public default export preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/audit/events/view-open | Fire-and-forget audit beacon (credentials: include) — must still fire once per mount |
| browser → /api/outcomes/aggregate | Server-side aggregation (postAggregate) — auth via authFetch cookie contract; routing decision must be unchanged |
| URL params → component state | ?cohort/?cohorts/?filter/?metric are user-controlled; safePickCohortFilter + savedSearches membership filtering are the sanitizers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Information Disclosure | Phase-43 drill-down (handlePointDrillDown) IDOR gate | mitigate | Move handler verbatim — it only navigates to cases found within the already-authorized cohort.cases set; unknown pseudonyms produce no navigation (T-43-03). OutcomesPanelDrillDown.test.tsx locks it |
| T-44-07 | Tampering | URL filter parsing (safePickCohortFilter) | mitigate | cohort memo + audit beacon JSON.parse paths move verbatim with their try/catch; OutcomesViewRouting.test.tsx locks malformed-param handling |
| T-44-08 | Repudiation | once-per-mount audit beacon | accept (no change) | Effect moves verbatim with empty-deps eslint-disable; OutcomesPage.test.tsx asserts the beacon fires |
| T-44-09 | (client-only refactor) | server aggregation routing | accept | No server code touched; routeServerSide logic + Promise.all fetch move verbatim; parity tests unaffected (server untouched) |
</threat_model>

<verification>
- `npm run test:ci` exits 0 (1086 tests green) after Task 3 — behavioral contract; the listed
  outcomes tests are the targeted regression net.
- `npm run lint` → 0 warnings. `npm run knip` → no new unused exports (hooks/containers reachable
  from OutcomesView, which is reachable from src/main.tsx per knip entry config).
- OutcomesView still `export default function OutcomesView` with no props (AnalysisPage contract).
- Hook call order unchanged: useOutcomesRouteState → useOutcomesAggregation, both above any return.
</verification>

<success_criteria>
- OutcomesView decomposed into useOutcomesRouteState + useOutcomesAggregation + VisusMetricContainer
  + CrtMetricContainer + slim orchestrator.
- Identical behavior: URL handling, four metric tabs, cross-cohort overlay, drill-down,
  recent-activity, audit beacon, server-aggregation routing.
- Rules of Hooks preserved (no reordering; hooks above early returns).
- All gates green at the final commit (test:ci, lint, knip). No new props on the public export.
</success_criteria>

<output>
After completion, create `.planning/phases/44-techdebt-compaction/44-02-SUMMARY.md`
</output>
