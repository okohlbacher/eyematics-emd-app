---
phase: 44-techdebt-compaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/authApi.ts
  - server/auth/authHelpers.ts
  - server/auth/loginApi.ts
  - server/auth/userAdminApi.ts
  - server/auth/totpApi.ts
  - server/auth/sessionApi.ts
autonomous: true
requirements: [TECH-01]
must_haves:
  truths:
    - "Every existing /api/auth/* endpoint (path + method + guard + response shape) behaves identically after the split"
    - "server/authApi.ts still exports authApiRouter (consumed by index.ts) and resetLimiter (consumed by settingsApi.ts)"
    - "npm run test:ci exits 0 at every commit (1086 baseline)"
    - "npm run knip reports no new unused exports; npm run lint reports 0 warnings"
    - "No auth guard is dropped or weakened: admin-only routes still 403 for non-admins; CSRF-guarded routes still require requireCsrf"
  artifacts:
    - path: "server/auth/authHelpers.ts"
      provides: "Shared auth helpers (rate limiter singleton + resetLimiter, JWT signing wrappers, emitRefreshCookies, touchLastLogin, currentKeyId, generateSecurePassword, VALID_ROLES)"
      contains: "export function resetLimiter"
    - path: "server/auth/loginApi.ts"
      provides: "POST /login, POST /verify, POST /refresh, POST /logout, GET /config router"
      contains: "Router"
    - path: "server/auth/userAdminApi.ts"
      provides: "GET/POST /users, GET /users/me, PUT/DELETE /users/:username, password routes router"
      contains: "Router"
    - path: "server/auth/totpApi.ts"
      provides: "TOTP enroll/confirm/disable/status + admin reset router"
      contains: "Router"
    - path: "server/auth/sessionApi.ts"
      provides: "rotate-key + session admin (DELETE/GET /sessions) router"
      contains: "Router"
    - path: "server/authApi.ts"
      provides: "Thin aggregator: mounts the four sub-routers as authApiRouter; re-exports resetLimiter"
      contains: "export const authApiRouter"
  key_links:
    - from: "server/index.ts"
      to: "server/authApi.ts"
      via: "import { authApiRouter }"
      pattern: "import \\{ authApiRouter \\} from './authApi"
    - from: "server/settingsApi.ts"
      to: "server/authApi.ts"
      via: "import { resetLimiter }"
      pattern: "import \\{ resetLimiter \\} from './authApi"
---

<objective>
Split the `server/authApi.ts` God module (~1,175 lines, F-09 / TECH-01) into four cohesive
router modules plus a shared-helpers module, mounted by a thin `authApi.ts` aggregator.

This is a STRICTLY behavior-preserving, mechanical move. Every endpoint's path, HTTP method,
auth guard, validation, and response shape stays byte-identical. The full test suite (1086) is
the contract — it must stay green at every commit. No endpoint behavior changes.

Purpose: kill the largest oversized-module tech debt item without risking auth correctness.
Output: `server/auth/{authHelpers,loginApi,userAdminApi,totpApi,sessionApi}.ts` + thin `server/authApi.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-techdebt-compaction/44-CONTEXT.md

# The module being split (read in full before moving anything):
@server/authApi.ts

# The two external consumers whose import contract MUST be preserved:
@server/index.ts
@server/settingsApi.ts

<interfaces>
<!-- External contract that MUST survive the refactor. Only TWO modules import authApi.ts: -->

server/index.ts (line 42, 236):
  import { authApiRouter } from './authApi.js';
  app.use('/api/auth', authApiRouter);

server/settingsApi.ts (line 25, 360):
  import { resetLimiter } from './authApi.js';
  // called inside the settings PUT path after updateAuthConfig(): resetLimiter();

<!-- Circular-import invariant (AUTHCFG-04): settingsApi imports authApi (resetLimiter),
     so authApi/authHelpers MUST NOT import settingsApi at module top-level for the
     rate-limiter reset. authApi.ts currently imports getAuthSettings from settingsApi
     (line 31) — that direction already exists and is fine; preserve it as-is. Do NOT
     add a NEW settingsApi import path that creates a cycle. -->

<!-- Dependencies the helpers/routers consume (already imported in authApi.ts today): -->
  ./authMiddleware.js          -> requireCsrf
  ./constants.js               -> getValidCenterIds
  ./initAuth.js                -> UserRecord (type), getAuthConfig, getJwtSecret, loadUsers, modifyUsers, rotateSigningKey
  ./jwtUtil.js                 -> signAccessToken, signChallengeToken (as signChallengeTokenUtil), signRefreshToken, verifyChallengeToken, verifyRefreshToken  (HS256 pin lives here — keep ALL signing routed through jwtUtil)
  ./keycloakAuth.js            -> getAuthProvider
  ./rateLimiting.js            -> createRateLimiter
  ./sessionsDb.js              -> getSession, insertSession, listActiveSessionsByUser, revokeByUsername, revokeFamily, revokeSession, SessionRow (type)
  ./settingsApi.js             -> getAuthSettings  (existing direction — keep)
  otplib                       -> authenticator
  qrcode                       -> QRCode
  bcryptjs                     -> bcrypt
</interfaces>

<route_ordering_invariants>
<!-- Express matches routes in REGISTRATION order. These literal-before-parameterized
     orderings are load-bearing and MUST be preserved both within a router file and in
     the order sub-routers are mounted by the thin authApi.ts. -->

1. PUT /users/me/password  MUST register BEFORE  PUT /users/:username/password
   (authApi.ts lines 797 and 850 today). Both live in userAdminApi.ts → keep this order.

2. DELETE /sessions/:id  MUST register BEFORE  DELETE /sessions  (query-param variant)
   (authApi.ts lines 1115 and 1138 today). Both live in sessionApi.ts → keep this order.

3. GET /users/me  vs  PUT/DELETE /users/:username — /users/me is a GET so it cannot
   collide with the PUT/DELETE :username params, but keep /users/me registered before
   the parameterized :username routes to match current behavior exactly.

<!-- Mounting order in authApi.ts: because all four routers mount at the SAME base
     ('/api/auth' via a single express.Router()), register them so no cross-router
     param route can shadow a literal route in another router. Safest mechanical
     approach: mount in this order — loginApi, totpApi, userAdminApi, sessionApi —
     OR (preferred) have the thin authApi.ts use ONE Router and call router.use(subRouter)
     for each, preserving the original top-to-bottom registration order of the source
     file: login/verify/refresh/logout/config → users/me → users CRUD → password →
     totp → rotate-key → sessions. Verify with the route-dump check in Task 3. -->
</route_ordering_invariants>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared auth helpers into server/auth/authHelpers.ts</name>
  <read_first>
    - server/authApi.ts (lines 33–169: constants + helper functions; lines 54–81: rate limiter + resetLimiter)
    - server/settingsApi.ts (lines 25, 360: the resetLimiter import + call site)
  </read_first>
  <files>server/auth/authHelpers.ts</files>
  <action>
    Create `server/auth/authHelpers.ts`. MOVE (do not duplicate) the following from authApi.ts
    verbatim, adjusting only relative import paths (these files now sit one directory deeper, so
    `./initAuth.js` becomes `../initAuth.js`, etc.):
      - VALID_ROLES constant (line 37)
      - generateSecurePassword() (lines 43–47)
      - currentKeyId() (lines 50–52)
      - the lazy `_limiter` singleton + limiter() (lines 63–70)
      - resetLimiter() — EXPORT it (lines 79–81)
      - signSessionToken() (lines 88–95)
      - touchLastLogin() (lines 103–109)
      - emitRefreshCookies() (lines 121–161)
      - signChallengeToken() (lines 164–169)
    Export every symbol the router modules will need: VALID_ROLES, generateSecurePassword,
    currentKeyId, limiter, resetLimiter, signSessionToken, touchLastLogin, emitRefreshCookies,
    signChallengeToken. Import getAuthSettings from `../settingsApi.js` exactly as today
    (this is the existing, allowed direction — do NOT invert it). Keep all jwtUtil signing
    wrappers routed through `../jwtUtil.js` so the HS256 pin stays in one place.
    Preserve all doc comments (they encode AUTHCFG-04 / D-04 / D-18 rationale).
    Throw-only error handling; async/await; no Result types (D-03/D-04).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v 'authApi.ts' | grep -E 'authHelpers' | head; test -f server/auth/authHelpers.ts && grep -q 'export function resetLimiter' server/auth/authHelpers.ts && echo HELPERS_OK</automated>
  </verify>
  <done>server/auth/authHelpers.ts exists, exports resetLimiter + all listed helpers, compiles with no new type errors referencing authHelpers.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the four router modules (login / user-admin / totp / session)</name>
  <read_first>
    - server/authApi.ts (FULL file — every route handler is moved verbatim)
    - server/auth/authHelpers.ts (the symbols you now import from here)
  </read_first>
  <files>server/auth/loginApi.ts, server/auth/userAdminApi.ts, server/auth/totpApi.ts, server/auth/sessionApi.ts</files>
  <action>
    Create four router modules, each owning its own `express.Router()` and importing shared
    symbols from `./authHelpers.js`. MOVE handlers verbatim (copy the handler bodies + their
    doc comments unchanged; adjust relative import paths to `../`):

      loginApi.ts    — POST /login (186), POST /verify (282), POST /refresh (413, requireCsrf),
                       POST /logout (489, requireCsrf), GET /config (526). Export `loginRouter`.
      userAdminApi.ts — GET /users/me (538), GET /users (563), POST /users (582),
                       DELETE /users/:username (655), PUT /users/:username (700),
                       PUT /users/me/password (797), PUT /users/:username/password (850).
                       Export `userAdminRouter`. PRESERVE registration order:
                       /users/me/password BEFORE /users/:username/password (invariant #1);
                       /users/me (GET) before the parameterized :username routes.
      totpApi.ts     — TOTP_ISSUER/RECOVERY_CODE_COUNT consts + generateRecoveryCode (893–899),
                       POST /totp/enroll (907), POST /totp/confirm (942), POST /totp/disable (979),
                       GET /totp/status (1024), POST /users/:username/totp/reset (1039).
                       Export `totpRouter`.
      sessionApi.ts  — POST /rotate-key (1082), DELETE /sessions/:id (1115), DELETE /sessions (1138),
                       GET /sessions (1159). Export `sessionRouter`. PRESERVE order:
                       DELETE /sessions/:id BEFORE DELETE /sessions (invariant #2).

    Do NOT change any path string, HTTP method, status code, response body shape, guard
    (`req.auth`/`req.auth.role !== 'admin'` 403 checks, `requireCsrf` middleware), validation
    branch, or audit/side-effect call. This is a cut-and-paste with import-path fixes only.
    Keep throw-only error handling and the existing try/catch shapes verbatim.
  </action>
  <verify>
    <automated>for f in loginApi userAdminApi totpApi sessionApi; do test -f server/auth/$f.ts || { echo "MISSING $f"; exit 1; }; done; grep -q "router.put('/users/me/password'\|'/users/me/password'" server/auth/userAdminApi.ts && grep -q "'/sessions/:id'" server/auth/sessionApi.ts && echo ROUTERS_OK</automated>
  </verify>
  <done>All four router files exist, each exports its Router, and the two ordering-sensitive route pairs appear in the correct relative order within their files.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite authApi.ts as a thin aggregator; verify route parity + gates green</name>
  <read_first>
    - server/authApi.ts (current — being replaced)
    - server/index.ts (line 236 mount)
    - server/settingsApi.ts (line 25 resetLimiter import)
  </read_first>
  <files>server/authApi.ts</files>
  <action>
    Replace server/authApi.ts with a thin aggregator that:
      1. Creates `export const authApiRouter = Router();`
      2. Mounts the four sub-routers preserving the ORIGINAL top-to-bottom registration order
         of the source file so cross-router literal/param matching is unchanged:
         `authApiRouter.use(loginRouter); authApiRouter.use(userAdminRouter);
          authApiRouter.use(totpRouter); authApiRouter.use(sessionRouter);`
         (If a route-dump diff in this task shows ANY ordering difference vs the pre-refactor
         dump, reorder the .use() calls to match — do not change the routes themselves.)
      3. Re-exports resetLimiter: `export { resetLimiter } from './auth/authHelpers.js';`
         so settingsApi.ts's existing `import { resetLimiter } from './authApi.js'` keeps working
         WITHOUT editing settingsApi.ts (preserves AUTHCFG-04 circular-import avoidance —
         settingsApi → authApi → authHelpers, no cycle back to settingsApi).
    Do NOT edit server/index.ts or server/settingsApi.ts — their imports must resolve unchanged.
    Capture a before/after route table to prove parity: enumerate (method, path) for the mounted
    router and confirm the set is identical to the pre-refactor set of 22 routes.
  </action>
  <verify>
    <automated>npm run test:ci</automated>
    <automated>npm run lint 2>&1 | tail -5</automated>
    <automated>npm run knip 2>&1 | grep -iE 'unused|authApi|auth/' | head; echo "knip-checked"</automated>
    <automated>grep -q "export { resetLimiter }" server/authApi.ts && grep -q 'authApiRouter' server/authApi.ts && grep -c "import { resetLimiter } from './authApi" server/settingsApi.ts</automated>
  </verify>
  <done>npm run test:ci exits 0 (1086 baseline green); lint 0 warnings; knip no new unused exports; authApi.ts re-exports resetLimiter + authApiRouter; settingsApi.ts unchanged and its import still resolves; index.ts unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/auth/* | Unauthenticated and authenticated HTTP requests cross here; auth guards (req.auth role checks, requireCsrf, rate limiter) are the enforcement points |
| settingsApi → authApi (resetLimiter) | Live rate-limit cap change must propagate without a cycle (AUTHCFG-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Elevation of Privilege | admin-only routes (users CRUD, rotate-key, sessions) after move | mitigate | Move guard checks (`req.auth.role !== 'admin'` → 403) verbatim with handler bodies; Task 3 route-dump + full test:ci proves no admin route lost its guard |
| T-44-02 | Spoofing / Tampering | CSRF-guarded routes (POST /refresh, POST /logout) | mitigate | `requireCsrf` middleware moves attached to the same routes in loginApi.ts; authRefresh/sessionRevoke tests assert 401/403 paths stay intact |
| T-44-03 | Tampering | JWT signing (HS256 pin) | mitigate | All signing stays routed through jwtUtil wrappers in authHelpers.ts; no direct jwt.sign introduced; rotateKey/sessionRotation tests lock behavior |
| T-44-04 | Denial of Service | login rate limiter + live resetLimiter | mitigate | Single shared limiter singleton + resetLimiter move to authHelpers and re-export from authApi; settingsApi import path preserved; settingsApi.test.ts + inactivitySettings.test.ts lock the reset path |
| T-44-05 | Information Disclosure | non-enumeration parity (401/429 symmetry on login/verify) | accept (no change) | Handler bodies moved verbatim — branch logic unchanged; authMiddlewareLocal/authRefresh tests guard it |
</threat_model>

<verification>
- `npm run test:ci` exits 0 (1086 tests green) after Task 3 — the behavioral contract.
- `npm run lint` → 0 warnings. `npm run knip` → no new unused exports.
- `git grep -c "authApiRouter" server/authApi.ts` ≥ 1 and `git grep "export { resetLimiter }" server/authApi.ts` present.
- server/index.ts and server/settingsApi.ts are byte-unchanged (diff shows no edits).
- Route set (method+path) is identical pre/post refactor (Task 3 route-dump parity).
</verification>

<success_criteria>
- authApi.ts split into authHelpers + four routers; thin authApi.ts mounts them and re-exports resetLimiter.
- Every /api/auth/* endpoint identical: path, method, guard, validation, response shape.
- Route-ordering invariants preserved (me/password before :username/password; sessions/:id before sessions).
- All gates green at the final commit (test:ci, lint, knip).
- No auth guard dropped (T-44-01..04 mitigated).
</success_criteria>

<output>
After completion, create `.planning/phases/44-techdebt-compaction/44-01-SUMMARY.md`
</output>
