---
phase: 43-case-nav-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/case-detail/VisusCrtChart.tsx
  - src/components/outcomes/ResponderView.tsx
  - src/i18n/translations.ts
  - tests/VisusCrtChartLabels.test.tsx
  - tests/ResponderTooltipPlacement.test.tsx
autonomous: true
requirements: [FALL-012, CHART-01]
must_haves:
  truths:
    - "The CRT legend chip reads a self-explanatory label sourced from i18n (not a hardcoded literal)"
    - "The Visus Y-axis label reads 'Visus (Dezimal, bestkorrigiert)' (DE) / 'Visual acuity (decimal, best-corrected)' (EN) from i18n"
    - "The interpolation hint reads 'Offener Kreis = interpolierter Wert (keine Messung)' (DE) / 'Open circle = interpolated value (no measurement)' (EN)"
    - "The responder '(i)' info affordance appears adjacent to the responder plot, not only in the metric-tab strip"
    - "Both Visus and CRT Y-axes render numeric tick labels (axis ticks visible)"
  artifacts:
    - path: "src/components/case-detail/VisusCrtChart.tsx"
      provides: "i18n-driven CRT/Visus/interpolation labels + explicit Y-axis ticks"
      contains: "t('visusYAxisLabel')"
    - path: "src/components/outcomes/ResponderView.tsx"
      provides: "Info affordance adjacent to the responder plot"
      contains: "metricsResponderTooltip"
    - path: "src/i18n/translations.ts"
      provides: "DE+EN keys for CRT legend, Visus axis, interpolation wording"
      contains: "visusYAxisLabel"
  key_links:
    - from: "src/components/case-detail/VisusCrtChart.tsx"
      to: "src/i18n/translations.ts"
      via: "t('crtLegendLabel') / t('visusYAxisLabel') / t('interpolatedHint')"
      pattern: "t\\('(crtLegendLabel|visusYAxisLabel|interpolatedHint)'\\)"
---

<objective>
Make the case-detail Visus/CRT chart self-explanatory and polished: replace the
hardcoded CRT legend label and Visus axis wording with locked i18n strings (DE+EN),
clarify the interpolation legend note, ensure both Y-axes render numeric ticks, and
move the responder "(i)" info affordance next to the responder plot.

Purpose: Closes FALL-012 (self-explanatory chart labels) and the CHART-01 polish
items (A-06 axis ticks best-effort, ANL-002 responder-tooltip placement). This is the
lowest-risk plan in the phase and lands first — it touches `VisusCrtChart.tsx`, which
`43-03` will also edit, so this plan must complete before that one.
Output: Updated labels and tooltip placement, new i18n keys, and two RTL tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-case-nav-clarity/43-CONTEXT.md
@./CLAUDE.md

<read_first>
- src/components/case-detail/VisusCrtChart.tsx — CRT line `name="CRT (µm)"` hardcoded (line ~134); Visus Y-axis `label.value = 'Visus (dezimal)'` hardcoded (line ~72); interpolation note via `t('interpolatedHint')` (line ~52); Visus measurement method already dynamic from `visusObs[0].method.coding[0].display` via `translateClinical` (lines ~54-63) — KEEP that dynamic block.
- src/components/outcomes/ResponderView.tsx — responder small-multiples render; the main export starts ~line 121; cross-mode title ~line 170 (`t('metricsResponderTitle')`). The single-cohort (non-cross) branch renders the bar/trajectory charts further down. The info text key `metricsResponderTooltip` already exists in i18n.
- src/components/outcomes/OutcomesView.tsx (lines ~456-462) — the EXISTING responder "(i)" affordance lives on the metric TAB button (`title={t('metricsResponderTooltip')}` … `ℹ`). Leave the tab affordance in place; CHART-01/ANL-002 asks for an ADDITIONAL affordance adjacent to the plot in ResponderView. Do not remove the tab one (other tests may assert it).
- src/i18n/translations.ts — entry shape is `key: { de: '…', en: '…' }`. Existing: `interpolatedHint` (line ~650) currently DE 'Offener Kreis = interpoliert (kein Messwert)'; `visusAndCrt` (line ~215); `critical` (line ~219); `metricsResponderTooltip` already present.
- tests/OutcomesPanel.test.tsx (lines 1-60) — canonical RTL + recharts-mock pattern: `// @vitest-environment jsdom`, `vi.mock('recharts', …)`, assertions use `queryByText(...).not.toBeNull()` / `.toBeNull()` (NO jest-dom).
</read_first>

<interfaces>
From src/i18n/translations.ts (entry shape):
```typescript
// each translation entry:
someKey: { de: 'German text', en: 'English text' }
// access at call site via the t() function: t('someKey')
```

From src/components/case-detail/VisusCrtChart.tsx (relevant existing literals to replace):
```typescript
// Visus Y-axis (line ~72):
label={{ value: 'Visus (dezimal)', angle: -90, position: 'insideLeft', ... }}
// CRT line legend name (line ~134):
<Line ... name="CRT (µm)" ... />
// interpolation note (line ~52):
<p className="text-xs text-gray-400 mb-1">{t('interpolatedHint')}</p>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add locked-wording i18n keys (DE+EN) for CRT legend, Visus axis, and interpolation note</name>
  <files>src/i18n/translations.ts, tests/VisusCrtChartLabels.test.tsx</files>
  <behavior>
    - Test: VisusCrtChart renders the CRT legend label "CRT (µm)" sourced from t('crtLegendLabel') (the recharts Line `name` prop carries it).
    - Test: VisusCrtChart renders the Visus Y-axis label text "Visus (Dezimal, bestkorrigiert)" in DE locale via t('visusYAxisLabel').
    - Test: VisusCrtChart renders the interpolation note "Offener Kreis = interpolierter Wert (keine Messung)" in DE via t('interpolatedHint').
    - Render with the recharts mock pattern from tests/OutcomesPanel.test.tsx; expose YAxis `label` and Line `name` as data-* attributes in the mock so assertions can read them; assert with queryByText(...).not.toBeNull() / by data attribute (no jest-dom).
  </behavior>
  <action>
    Add three i18n keys to src/i18n/translations.ts near the existing case-detail block (per FALL-012 locked defaults in 43-CONTEXT.md `<decisions>`):
    - `crtLegendLabel`: de 'CRT (µm)', en 'CRT (µm)' (the µm = U+00B5).
    - `visusYAxisLabel`: de 'Visus (Dezimal, bestkorrigiert)', en 'Visual acuity (decimal, best-corrected)'.
    - UPDATE the existing `interpolatedHint` value to the locked wording: de 'Offener Kreis = interpolierter Wert (keine Messung)', en 'Open circle = interpolated value (no measurement)'.
    Keep camelCase identifiers (D-05). Do NOT touch the dynamic Visus method block — the static axis label is separate from the per-observation measurement-method line.
    Write the failing RTL test first (RED) under tests/, matching the recharts-mock + no-jest-dom pattern from tests/OutcomesPanel.test.tsx; provide minimal props (combinedData with one measured + one interpolated point, cohortAvgVisus/cohortAvgCrt numbers, highlightDate null, dateFmt 'de-DE', locale 'de', a `t` that returns the new DE strings, visusObs []).
  </action>
  <verify>
    <automated>npx vitest run tests/VisusCrtChartLabels.test.tsx</automated>
  </verify>
  <done>The three i18n keys exist with the locked DE+EN wording; tests/VisusCrtChartLabels.test.tsx passes asserting all three labels render from i18n.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire i18n labels + explicit Y-axis ticks into VisusCrtChart; add responder plot-adjacent info affordance</name>
  <files>src/components/case-detail/VisusCrtChart.tsx, src/components/outcomes/ResponderView.tsx, tests/ResponderTooltipPlacement.test.tsx</files>
  <behavior>
    - Test: ResponderView (single-cohort branch) renders an info affordance carrying t('metricsResponderTooltip') as title/aria-label adjacent to the responder plot heading (within the responder-view container, not the OutcomesView tab strip).
    - The VisusCrtChartLabels.test from Task 1 now passes against the real wired component (not just a fixture).
  </behavior>
  <action>
    In src/components/case-detail/VisusCrtChart.tsx (FALL-012 + CHART-01 A-06):
    - Replace the Visus Y-axis `label.value` literal with `t('visusYAxisLabel')`.
    - Replace the CRT `<Line name="CRT (µm)">` literal with `name={t('crtLegendLabel')}`.
    - Ensure both `<YAxis>` elements render numeric ticks: add `tickCount={5}` and `allowDecimals` appropriately (Visus axis allowDecimals, CRT axis allowDecimals={false}) so ticks are not suppressed (A-06 best-effort). Keep existing `domain` and `tick={{ fontSize: 10 }}`. Do NOT change the CRT critical/cohort reference lines.
    In src/components/outcomes/ResponderView.tsx (CHART-01 ANL-002 — responder tooltip placement):
    - In the single-cohort (non-cross) render branch, add an info affordance (a `<span>`/`<button>` with `title={t('metricsResponderTooltip')}` and `aria-label={t('metricsResponderTooltip')}`, content `ℹ`) immediately adjacent to the responder section heading so the explanation sits next to the plot. Reuse the same className styling as the existing tab affordance (`ml-1 text-gray-400 hover:text-blue-600 cursor-help`). Leave the existing OutcomesView tab affordance untouched.
    Throw-only error handling (D-03); camelCase TS (D-05); FHIR/wire strings unchanged.
    Write the failing ResponderView test first (RED), using the recharts mock + no-jest-dom assertion pattern.
  </action>
  <verify>
    <automated>npx vitest run tests/VisusCrtChartLabels.test.tsx tests/ResponderTooltipPlacement.test.tsx</automated>
  </verify>
  <done>VisusCrtChart Visus axis + CRT legend read from i18n; both Y-axes specify tickCount so ticks render; ResponderView shows a plot-adjacent info affordance; both tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client data load | `cases` arrive via `/api/fhir/*`, already restricted to the user's authorized centers server-side |
| static i18n strings → DOM | label text is developer-authored constants, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information disclosure | VisusCrtChart labels | accept | Labels are static i18n constants; no PII or dynamic untrusted data introduced. The dynamic measurement-method line already runs through `translateClinical` and is unchanged. |
| T-43-02 | Tampering | i18n keys | accept | Build-time constants in source; no runtime mutation path. |
</threat_model>

<verification>
- `npx vitest run tests/VisusCrtChartLabels.test.tsx tests/ResponderTooltipPlacement.test.tsx` — both pass.
- `npm run test:ci` stays green (1065 baseline) — no regressions in OutcomesView tab tests.
- `npm run lint` — 0 warnings on touched files.
- Manual (deferred to Phase 45 UAT): A-06 exact tick repro pending user screenshot (43-CONTEXT.md deferred); tickCount is best-effort.
</verification>

<success_criteria>
- CRT legend, Visus Y-axis, and interpolation note all read from i18n with the locked DE+EN wording.
- Both Visus and CRT Y-axes specify an explicit tickCount so numeric ticks render.
- Responder "(i)" info affordance present adjacent to the responder plot.
- All new tests pass; `npm run test:ci` green.
</success_criteria>

<output>
After completion, create `.planning/phases/43-case-nav-clarity/43-01-SUMMARY.md`
</output>
