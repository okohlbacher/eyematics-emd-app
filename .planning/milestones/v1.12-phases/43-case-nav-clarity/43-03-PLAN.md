---
phase: 43-case-nav-clarity
plan: 03
type: execute
wave: 3
depends_on: [43-01, 43-02]
files_modified:
  - src/hooks/useCaseData.ts
  - src/components/case-detail/VisusCrtChart.tsx
  - src/pages/CaseDetailPage.tsx
  - src/i18n/translations.ts
  - tests/VisusCrtChartReference.test.tsx
autonomous: true
requirements: [FALL-011]
must_haves:
  truths:
    - "The case-detail Visus/CRT chart can overlay cohort reference values (median trajectory + IQR band) for comparison against the single case"
    - "The cohort reference overlay is a user-controllable toggle (off by default per Claude's discretion) — turning it off removes the overlay"
    - "The reference values reuse the existing cohort aggregate/percentile logic (no new clinical metric introduced)"
    - "When the cohort has no comparable data, the chart renders without the overlay and does not crash"
  artifacts:
    - path: "src/hooks/useCaseData.ts"
      provides: "Per-date cohort median + IQR (p25/p75) reference series for Visus and CRT, aligned to the case's date axis"
      contains: "cohortReference"
    - path: "src/components/case-detail/VisusCrtChart.tsx"
      provides: "Reference median line + IQR band rendering gated by a showCohortReference prop"
      contains: "showCohortReference"
    - path: "src/pages/CaseDetailPage.tsx"
      provides: "Toggle state + control wiring the reference overlay on/off"
      contains: "showCohortReference"
  key_links:
    - from: "src/pages/CaseDetailPage.tsx"
      to: "src/components/case-detail/VisusCrtChart.tsx"
      via: "showCohortReference state + cohortReference data props"
      pattern: "showCohortReference"
    - from: "src/components/case-detail/VisusCrtChart.tsx"
      to: "src/hooks/useCaseData.ts cohortReference"
      via: "cohortReference series merged onto the chart"
      pattern: "cohortReference"
---

<objective>
Overlay cohort reference values on the single-case Visus/CRT trajectory: a per-date
cohort median line plus an IQR (p25–p75) band, rendered behind the patient's own series,
controlled by a toggle in the case-detail view.

Purpose: Closes FALL-011 (case view shows cohort reference values for comparison). Reuses
the existing cohort percentile/aggregate logic — no new clinical metric. The case already
receives the full `cases` array in `useCaseData`, and a flat cohort average reference line
already exists (`cohortAvgVisus`/`cohortAvgCrt`); this plan upgrades that to a date-aligned
median + IQR band so the comparison is a trajectory, not a single flat line.
Output: A `cohortReference` series in useCaseData, reference rendering in VisusCrtChart
gated by a toggle, the toggle UI in CaseDetailPage, i18n labels, and a test.

Sequencing: depends on 43-01 (both edit VisusCrtChart.tsx) and 43-02 (both append i18n
keys to translations.ts). Lands last so it builds on the post-43-01 label changes and the
post-43-02 i18n additions without merge conflict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-case-nav-clarity/43-CONTEXT.md
@.planning/phases/43-case-nav-clarity/43-01-PLAN.md
@./CLAUDE.md

<read_first>
- src/hooks/useCaseData.ts — receives the whole `cases: PatientCase[]` (the cohort). `cohortAvgVisus` (lines ~23-27) and `cohortAvgCrt` (lines ~29-33) compute flat cohort means via `getObservationsByCode(c.observations, LOINC_VISUS|LOINC_CRT)`. `combinedData` (lines ~135-156) is date-keyed for the single case: `{ date, visus?, crt?, visusMeasured?, crtMeasured? }` sorted ascending. The hook returns a large object (lines ~265-285) — append the new `cohortReference` to it.
- src/components/case-detail/VisusCrtChart.tsx — current props (lines ~26-35): combinedData, cohortAvgVisus, cohortAvgCrt, highlightDate, dateFmt, locale, t, visusObs. Existing flat reference lines: Visus `<ReferenceLine y={cohortAvgVisus}>` (~82-88) and CRT `<ReferenceLine y={cohortAvgCrt}>` (~89-95). The chart uses `<LineChart>` with two y-axes (yAxisId "visus" domain [0,1]; yAxisId "crt"). NOTE: 43-01 will already have changed the Visus axis label + CRT legend name to i18n here — build on the post-43-01 file.
- src/pages/CaseDetailPage.tsx — renders `<VisusCrtChart …>` at lines ~131-140 inside a `col-span-8` card. Local state pattern already present (`useState` for highlightDate, octSelectedIdx, lines ~38-39). Add a `showCohortReference` toggle near the chart.
- shared/cohortTrajectory.ts — `computeCohortTrajectory` / `computeCrtTrajectory` produce `medianGrid` with `{ x, y, p25, p75 }`. These align by treatment-index/day, NOT calendar date, so they are NOT directly mergeable onto `combinedData`'s date axis. Prefer computing a simple per-calendar-date cohort median + p25/p75 directly in useCaseData (same percentile math, date-keyed) so the overlay aligns to the existing chart x-axis. Reuse a percentile helper if one exists; otherwise compute median/p25/p75 inline (sort + index) — this is the existing aggregate logic, not a new metric.
- src/i18n/translations.ts — entry shape `key: { de, en }`. `critical` (line ~219), `visusAndCrt` (~215) nearby. Existing flat-reference labels are inline literals (`Visus Ø …`). NOTE: 43-01 and 43-02 will already have added their keys here — append, do not collide.
- tests/OutcomesPanel.test.tsx (lines 1-60) — recharts mock + no-jest-dom RTL pattern to mirror.
</read_first>

<interfaces>
From shared/types/fhir.ts:
```typescript
export interface PatientCase {
  id: string;
  pseudonym: string;
  observations: Observation[]; // contains Visus (LOINC_VISUS) + CRT (LOINC_CRT)
  // …
}
```

Existing VisusCrtChart data shape (date-keyed, single case):
```typescript
interface CombinedDataPoint {
  date: string;
  visus?: number; crt?: number;
  visusMeasured?: boolean; crtMeasured?: boolean;
}
```

New reference series to add (date-aligned cohort stats):
```typescript
// returned from useCaseData as `cohortReference`
interface CohortReferencePoint {
  date: string;
  visusMedian?: number; visusP25?: number; visusP75?: number;
  crtMedian?: number;   crtP25?: number;   crtP75?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute a date-aligned cohort median + IQR reference series in useCaseData</name>
  <files>src/hooks/useCaseData.ts, tests/VisusCrtChartReference.test.tsx</files>
  <behavior>
    - Test: given two cohort cases with Visus observations on the same date, useCaseData returns a `cohortReference` entry for that date with visusMedian equal to the median of those values and visusP25/visusP75 set.
    - Test: dates present in the single case's combinedData but with no cohort data yield no median (overlay simply absent for that date), and the hook does not throw.
    - Test: CRT reference (crtMedian/crtP25/crtP75) computed the same way from LOINC_CRT observations.
  </behavior>
  <action>
    In src/hooks/useCaseData.ts (FALL-011), add a memoized `cohortReference: CohortReferencePoint[]`:
    - For each calendar date present on the case's `combinedData`, gather all cohort Visus values across `cases` measured on that date (`getObservationsByCode(c.observations, LOINC_VISUS)`, bucket by `effectiveDateTime.substring(0,10)`), and compute median + p25 + p75 (sort ascending, index by linear interpolation or nearest-rank — reuse an existing percentile helper if the repo has one; otherwise inline the standard sort+index, which is the same aggregate logic used elsewhere — NOT a new metric). Do the same for CRT (LOINC_CRT).
    - Align the output to the single case's dates so the overlay shares the x-axis. Skip dates with zero cohort measurements (leave median undefined for that point).
    - Export `cohortReference` from the hook's return object. Keep `cohortAvgVisus`/`cohortAvgCrt` (still used by the flat reference lines / other consumers).
    Throw-only (D-03); camelCase (D-05); async untouched (pure compute). Define the `CohortReferencePoint` type next to the hook.
    Write the failing test first (RED): construct a `cases` fixture with known Visus/CRT values on shared dates, call the hook (via renderHook from @testing-library/react or a thin wrapper), assert median/p25/p75. Use no-jest-dom assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/VisusCrtChartReference.test.tsx</automated>
  </verify>
  <done>useCaseData returns a date-aligned `cohortReference` with median/p25/p75 for Visus and CRT; empty-cohort-date case is safe; test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the cohort reference overlay in VisusCrtChart gated by a toggle in CaseDetailPage</name>
  <files>src/components/case-detail/VisusCrtChart.tsx, src/pages/CaseDetailPage.tsx, src/i18n/translations.ts, tests/VisusCrtChartReference.test.tsx</files>
  <behavior>
    - Test: when VisusCrtChart receives `showCohortReference={true}` and a non-empty `cohortReference`, it renders a cohort median line and an IQR band (assert the reference Line/Area is present via the recharts mock data-* attributes / a distinguishing `name`).
    - Test: when `showCohortReference={false}`, the reference Line/Area is NOT rendered.
    - Test: the toggle control in CaseDetailPage exists with an accessible label (i18n) and flips the state.
  </behavior>
  <action>
    In src/components/case-detail/VisusCrtChart.tsx (FALL-011) — building on the post-43-01 file:
    - Add props `showCohortReference: boolean` and `cohortReference: CohortReferencePoint[]` to `VisusCrtChartProps`.
    - When `showCohortReference` is true and `cohortReference` has data, render: a Visus cohort-median `<Line yAxisId="visus" dataKey="visusMedian" data={cohortReference}>` with a muted/dashed style and `name={t('cohortReferenceMedian')}`, a CRT cohort-median line (`dataKey="crtMedian"`, yAxisId "crt"), and IQR bands as `<Area>` (or paired reference lines) using visusP25/visusP75 and crtP25/crtP75 with low fill opacity and `legendType="none"`. Render the reference series BEFORE the patient's own `<Line>` series so the patient line stays on top. Reuse the muted slate/violet tones already used by the existing flat `<ReferenceLine>` labels.
    - Keep the existing flat `cohortAvg*` `<ReferenceLine>`s OR replace them with the new trajectory overlay per Claude's discretion (43-CONTEXT.md grants reference-band styling discretion) — if replacing, ensure no test asserting the flat lines breaks; if in doubt, keep both and let the toggle control only the new band.
    In src/pages/CaseDetailPage.tsx:
    - Add `const [showCohortReference, setShowCohortReference] = useState(false)` (off by default — Claude's discretion per CONTEXT; off avoids visual clutter on first load).
    - Pull `cohortReference` from `useCaseData(...)` and pass it plus `showCohortReference` to `<VisusCrtChart>`.
    - Add a small toggle control (checkbox or button) adjacent to the chart card header with i18n label `cohortReferenceToggle`, wired to `setShowCohortReference`.
    Add i18n keys (DE+EN): `cohortReferenceToggle` (de 'Kohorten-Referenz anzeigen', en 'Show cohort reference'), `cohortReferenceMedian` (de 'Kohorten-Median', en 'Cohort median'), `cohortReferenceBand` (de 'Kohorten-IQR (25.–75. Perzentil)', en 'Cohort IQR (25th–75th percentile)').
    Throw-only (D-03); camelCase (D-05); FHIR/wire strings unchanged.
    Write the failing render/toggle tests first (RED), extending the recharts mock so the reference Line/Area carry inspectable `data-name`/`data-key` attributes.
  </action>
  <verify>
    <automated>npx vitest run tests/VisusCrtChartReference.test.tsx</automated>
  </verify>
  <done>VisusCrtChart renders the cohort median + IQR overlay only when toggled on; CaseDetailPage exposes an accessible i18n toggle (off by default); reference reuses date-aligned cohort percentiles; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client cohort load | `cases` arrive via `/api/fhir/*`, already restricted to the user's authorized centers server-side |
| cohort aggregate → DOM | the reference overlay is an aggregate (median/IQR) over the already-authorized cohort, no per-patient identity exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05 | Information disclosure | cohort reference overlay | mitigate | The overlay aggregates over `cases` already loaded into the user's authorized cohort (server-restricted by center). It surfaces only median/p25/p75 values — no individual pseudonyms, no cases outside the authorized set. Computation happens client-side over data the user can already see. |
| T-43-06 | Denial of service | percentile computation | accept | Median/IQR over the loaded cohort is O(n log n) per date on an already-bounded in-memory set; no unbounded input. |
</threat_model>

<verification>
- `npx vitest run tests/VisusCrtChartReference.test.tsx` — passes.
- `npm run test:ci` stays green (1065 baseline); existing VisusCrtChart / CaseDetailPage tests unaffected (toggle off by default).
- `npm run lint` — 0 warnings on touched files.
- Manual (Phase 45 UAT): toggle on → median line + IQR band appear aligned to the case dates; toggle off → overlay gone; empty-cohort case renders without overlay.
</verification>

<success_criteria>
- A date-aligned cohort median + IQR reference series is computed from existing aggregate logic (no new metric).
- The overlay renders only when the toggle is on; off by default; accessible i18n label.
- Reference series sit behind the patient's own series; no crash on empty cohort data.
- All new tests pass; `npm run test:ci` green.
</success_criteria>

<output>
After completion, create `.planning/phases/43-case-nav-clarity/43-03-SUMMARY.md`
</output>
