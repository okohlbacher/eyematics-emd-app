# v1.7 Requirements Archive

**Milestone:** v1.7 Security, Performance & Cross-Cohort
**Shipped:** 2026-04-21
**Phases:** 14–17

> Note: `.planning/REQUIREMENTS.md` was not tracked as a standalone artifact for this milestone; the requirements below are reconstructed from the ROADMAP.md phase Requirements fields and the v1.7 Validated section of PROJECT.md.

## Security

- [x] **SEC-01** — JWT algorithm pinned to HS256 at every `jwt.verify()` call site (Phase 14)
- [x] **SEC-02** — Cohort hash secret auto-generated on first boot; `audit.cohortHashSecret` fallback removed (Phase 14)
- [x] **SEC-03** — Users on default `changeme2025!` password redirected to forced-change flow (Phase 14)
- [x] **SEC-04** — Per-user TOTP enrollment with QR code; RFC 6238 ±1-window; static OTP retired per-user (Phase 15)
- [x] **SEC-05** — One-time recovery codes at enrollment; admin TOTP reset (Phase 15)

## Performance

- [x] **PERF-01** — `extractPatientCases` rewritten O(N+M) with Map pre-grouping (Phase 14)
- [x] **PERF-02** — FHIR bundles warmed into `_bundleCache` at server startup (Phase 14)

## Accessibility

- [x] **A11Y-01** — Every Recharts trajectory container exposes an `aria-label` (Phase 14)

## Cross-Cohort Comparison

- [x] **XCOHORT-01** — 1–4 saved cohorts overlay on a single `ComposedChart` (Phase 16)
- [x] **XCOHORT-02** — Per-cohort median line + IQR band using `COHORT_PALETTES` (Phase 16)
- [x] **XCOHORT-03** — Legend format `Cohort A (N=42 patients)` (Phase 16)
- [x] **XCOHORT-04** — `?cohorts=id1,id2,...` URL param drives + restores state (Phase 16)
- [x] **VIS-04** — Single-cohort mode: per-patient desaturated; median overplotted (Phase 16)

## Audit Log & Theme

- [x] **AUDIT-01** — Multi-dimensional audit filters (user, category, date, body search, failures) (Phase 17)
- [x] **VIS-01** — Light/Dark/System ThemeContext with `dark` class toggle (Phase 17)
- [x] **VIS-02** — Recharts internals driven by explicit `useTheme()` values (Phase 17)
- [x] **VIS-03** — `DARK_EYE_COLORS` passes WCAG 4.5:1 against `#111827`, enforced by test (Phase 17)

## Full-Review Security Pass

- [x] **v1.7-full-review** — All Critical/High/Medium/Low findings (C1–C5, H1–H7, M1–M8, L1–L10) resolved. See `.planning/reviews/v1.7-full-review/SUMMARY.md`.

## Deferred to v1.8

- [ ] **KEYCLK-01** — Real Keycloak OIDC redirect flow (Phase 18 deferred)
- [ ] **M6** — JWT refresh flow (10-min hard cap still forces re-login)
- [ ] **L4** — AuditPage useReducer refactor
- [ ] metricSelector integration tests (requires full router context)

## Outcomes

All in-scope v1.7 requirements validated. Phase 18 (Keycloak) and three deferred cleanups carry forward to v1.8.
