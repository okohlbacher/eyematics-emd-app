---
phase: 29-home-panel-ux
plan: 03
type: execute
wave: 1
depends_on: [29-01]
files_modified:
  - src/pages/QualityPage.tsx
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "Navigating to /quality?therapy=breaker pre-selects the breaker therapy filter on mount"
    - "Navigating to /quality?status=flagged pre-selects the in_progress (open-flag) status filter on mount"
    - "An unrecognized therapy/status param value falls back to 'all' silently — no error, no empty-by-mistake list"
    - "Filters remain user-mutable after mount (URL only seeds the initial value)"
  artifacts:
    - path: "src/pages/QualityPage.tsx"
      provides: "useSearchParams read-on-mount seeding of filterTherapy / filterStatus"
      contains: "useSearchParams"
  key_links:
    - from: "src/pages/QualityPage.tsx"
      to: "URL query params therapy/status"
      via: "useSearchParams lazy useState initializer"
      pattern: "searchParams.get\\('(therapy|status)'\\)"
---

<objective>
Wire the UX-01 deep-link query contract into QualityPage: read `?therapy=` and `?status=` once on mount and seed the existing `filterTherapy` / `filterStatus` state. This is the receiving end of the two Review buttons (Plan 04 changes the buttons; this plan makes the target page respond).

Purpose: UX-01 success criterion 1 — Review buttons land on a pre-filtered review area via a defined query contract.
Output: modified `src/pages/QualityPage.tsx`. Makes `tests/qualityPageDeepLink.test.tsx` (Plan 01) GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-home-panel-ux/29-RESEARCH.md
@.planning/phases/29-home-panel-ux/29-PATTERNS.md
@.planning/phases/29-home-panel-ux/29-UI-SPEC.md

@src/pages/AnalysisPage.tsx

<interfaces>
<!-- QualityPage current filter state (lines 89-91) -->
const [filterStatus, setFilterStatus] = useState<QualityStatus | 'all'>('all');  // line 89
const [filterTherapy, setFilterTherapy] = useState<string>('all');               // line 91

<!-- CRITICAL type fact: QualityStatus (shared/types/fhir.ts:175) = 'unchecked' | 'in_progress' | 'reviewed'.
     There is NO 'flagged' value. Cases with an open quality flag map to caseStatus='in_progress'
     (QualityPage caseStatus useMemo, line ~105). The URL param value stays 'flagged' (locked D-04
     contract) but MUST map internally to 'in_progress'. Using 'flagged' as the state value shows ZERO cases. -->

<!-- Read-on-mount pattern to copy: AnalysisPage.tsx:52-59 (useSearchParams + useMemo/lazy initializer). -->
<!-- Therapy filter vocabulary: 'breaker' | 'interrupter' | 'all'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed filterTherapy / filterStatus from URL params on mount</name>
  <files>src/pages/QualityPage.tsx</files>
  <read_first>
    - src/pages/QualityPage.tsx (line 4 import; line 80 navigate; lines 89-91 filter state; line 140 filterStatus usage; line 142 filterTherapy usage; caseStatus useMemo ~97-112 to confirm in_progress = open flag)
    - src/pages/AnalysisPage.tsx (lines 7-8 import, 52-59 useSearchParams read-on-mount pattern)
    - .planning/phases/29-home-panel-ux/29-PATTERNS.md (§ `src/pages/QualityPage.tsx` — exact lazy-initializer replacement + import change)
    - .planning/phases/29-home-panel-ux/29-RESEARCH.md (§ Critical Finding `?status=flagged` Type Mismatch; § Pitfall 1; § Pitfall 3)
    - shared/types/fhir.ts (line 175 — QualityStatus union, confirm no 'flagged')
  </read_first>
  <action>
    In `src/pages/QualityPage.tsx`, extend the line-4 import to `import { useNavigate, useSearchParams } from 'react-router-dom'`. Near the top of the component (before the filter `useState` declarations, after `navigate`/locale/t — there are no early returns to worry about), add `const [searchParams] = useSearchParams();` (read-only destructure — no setter needed). Replace the plain `useState('all')` initializers at lines 89 and 91 with lazy initializers: for `filterStatus`, read `searchParams.get('status')` and return `'in_progress'` when the value is exactly `'flagged'`, else `'all'` — add an inline comment explaining that `'flagged'` is the locked D-04 URL contract value mapped to `'in_progress'` because `QualityStatus` has no `'flagged'` member and open-flag cases are `caseStatus==='in_progress'` (see RESEARCH Pitfall 1). For `filterTherapy`, read `searchParams.get('therapy')` and return the value when it is `'breaker'` or `'interrupter'`, else `'all'`. Use the `useState(() => {...})` lazy-initializer form — NOT a `useEffect` (a `useEffect` causes a render flash / potential loop per Pitfall 3). Do not change the filter `<select>` handlers — the filters stay user-mutable; the URL only seeds the initial value. Do not add a `'flagged'` member to `QualityStatus` (Option 1 from research — fewer files, semantically correct). camelCase; throw-only; no env vars.
  </action>
  <verify>
    <automated>npx vitest run tests/qualityPageDeepLink.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tests/qualityPageDeepLink.test.tsx` passes: `?therapy=breaker` → `filterTherapy='breaker'`; `?status=flagged` → `filterStatus='in_progress'`; unknown value → `'all'`.
    - Source imports `useSearchParams` from `react-router-dom` and calls it at component top.
    - `filterStatus`/`filterTherapy` use a `useState(() => ...)` lazy initializer (grep: `useState<QualityStatus | 'all'>(() =>` and `useState<string>(() =>`) — no new `useEffect` reads searchParams.
    - The literal string `'flagged'` is mapped to `'in_progress'` with an explaining comment; `QualityStatus` in `shared/types/fhir.ts` is unchanged (still 3 members).
    - `npm run build` clean; `npm run lint` clean (no exhaustive-deps warnings).
  </acceptance_criteria>
  <done>QualityPage seeds both filters from URL on mount via lazy initializers; flagged→in_progress mapped; unknown→all; filters stay mutable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → QualityPage filter state | An attacker (or a stray link) can supply arbitrary `?therapy=`/`?status=` values that are read into component state on mount. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Tampering | QualityPage URL-param read (V5 Input Validation) | mitigate | Param values are allow-listed before use: therapy accepts only `'breaker'`/`'interrupter'`, status accepts only the literal `'flagged'`→`'in_progress'` mapping; every other value falls back to `'all'`. No raw param string is ever assigned to filter state or to a type-unsafe slot. |
| T-29-08 | Elevation of Privilege | data scoping via filter param | accept | The filter only narrows the locally-rendered case list; QualityPage still applies the user's center-restriction (existing `filterCenter`/DataContext scoping). A URL param cannot widen visibility beyond what the role already sees. |
</threat_model>

<verification>
- `npx vitest run tests/qualityPageDeepLink.test.tsx` GREEN.
- `npm run build` and `npm run lint` clean.
- `grep -c "flagged" src/pages/QualityPage.tsx` >= 1 (the mapped param value with comment) and `grep -c "model\|QualityStatus =" shared/types/fhir.ts` shows the union unchanged.
</verification>

<success_criteria>
- Both deep-link params seed the correct filter on mount; flagged maps to in_progress; unknown falls back to all silently; filters remain user-mutable.
</success_criteria>

<output>
After completion, create `.planning/phases/29-home-panel-ux/29-03-SUMMARY.md`
</output>
