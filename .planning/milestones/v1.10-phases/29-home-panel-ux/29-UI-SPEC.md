---
phase: 29
slug: home-panel-ux
status: approved
shadcn_initialized: false
preset: none
created: 2026-05-21
reviewed_at: 2026-05-21
---

# Phase 29 — UI Design Contract

> Visual and interaction contract for Phase 29: Home Panel UX.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom Tailwind v4 CSS-first token system) |
| Preset | not applicable |
| Component library | none (in-tree primitives: Button, Tile, Badge, SectionHead at `src/components/primitives/`) |
| Icon library | lucide-react ^1.9.0 |
| Font | Inter (400, 500, 600, 700); JetBrains Mono for data cells |

Source: `src/index.css` @theme block + `package.json`.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, dot indicator margins |
| sm | 8px | Inline button padding (sm size), gap within alert rows |
| md | 16px | Default element spacing, tile internal padding |
| lg | 24px | Section gaps between tiles |
| xl | 32px | Page horizontal padding (px-8 = 32px) |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level top spacing |

Exception:
- Touch-target minimum 32px height for `Button size="sm"` rows in both panels.

### Component-internal sizing — existing classes, do not touch

These values are pre-existing, component-internal, and are carried forward unchanged. They are NOT part of the spacing scale and do not need to conform to the 4-point grid:

- Right-rail tiles: `p-[18px]` — existing padding convention from LandingPage.
- Alert dot indicator: `w-1.5 h-1.5` (6px) — decoration/icon sizing, not layout spacing.
- KPI card padding: `p-[18px_18px_14px]` — existing, do not change.

Source: `src/pages/LandingPage.tsx` existing padding classes.

---

## Typography

Four sizes only (reduced from 5 — 12px dropped; overline-style elements use 11px, inline sub-labels use 13px):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Sub-label / alert title | 13px | 600 (semibold) | 1.4 |
| Panel heading (uppercase overline) | 11px | 600 (semibold) | 1.0 |
| Page heading (h1) | 28px | 600 (semibold) | 1.15 |

Notes:
- Sub-label and alert title both use 13px; differentiation is by context and weight (alert titles are always semibold in a coloured-dot row; sub-labels in Jump Back In rows are 13px regular `var(--color-ink-3)` — see Interaction Contract).
- 11px overline labels use `tracking-[0.12em] uppercase` — replicate exactly for the "Jump back in" section heading and "Attention needed" section heading.
- "Jump Back In" row labels: 13px semibold `var(--color-ink)` for the primary identifier (patient pseudonym); 13px regular `var(--color-ink-3)` for the secondary descriptor (view name / context).
- Data values (case counts, KPI numbers) use `font-data` class (JetBrains Mono with `font-feature-settings: 'tnum'`).

Source: `src/pages/LandingPage.tsx` inline className strings; `src/index.css`.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--color-canvas)` — `oklch(0.985 0.003 85)` light / `oklch(0.18 0.012 255)` dark | Page background |
| Secondary (30%) | `var(--color-surface)` — white light / `oklch(0.22 0.012 255)` dark | Tile backgrounds; `var(--color-surface-2)` for hover states on center rows |
| Accent (10%) | `var(--color-teal)` — `oklch(0.55 0.12 195)` | Primary interactive accent; teal Review button hover emphasis |
| Warning / attention | `var(--color-coral)` — `oklch(0.62 0.16 25)` | Therapy-breaker alert dot |
| Caution | `var(--color-amber)` — `oklch(0.72 0.14 75)` | Implausible-CRT alert dot |
| Ink primary | `var(--color-ink)` — `oklch(0.22 0.015 255)` | All primary text |
| Ink secondary | `var(--color-ink-2)` — `oklch(0.42 0.012 255)` | Sub-labels, descriptions |
| Ink tertiary | `var(--color-ink-3)` — `oklch(0.62 0.010 255)` | Empty state text, timestamps, overline labels |
| Border | `var(--color-line)` — `oklch(0.92 0.006 90)` | Tile borders, row dividers |

Accent (`var(--color-teal)`) reserved for:
- "Review" ghost buttons (existing `Button variant="ghost"` — uses surface/line-2, NOT teal fill; accent teal is for the `Button variant="accent"` if ever added here)
- Jump Back In row arrow icon color on hover
- Active/focused state ring if keyboard-navigating rows

Dark mode: all tokens automatically retint via `.dark` class override in `src/index.css` — no new dark-mode handling required; use CSS custom properties only.

Source: `src/index.css` @theme / .dark; `src/pages/LandingPage.tsx` color usage.

---

## Component Inventory

These are the components that Phase 29 touches or introduces. No new primitive components required — all new pieces are compositions of existing primitives.

### Existing — modified

| Component | File | Change |
|-----------|------|--------|
| `LandingPage` | `src/pages/LandingPage.tsx` | Replace Jump Back In empty-only block with conditional row list; fix Review button `navigate()` targets |
| `QualityPage` | `src/pages/QualityPage.tsx` | Add `useSearchParams` read on mount for `?therapy=` and `?status=` into `filterTherapy` / `filterStatus` |

### New — to be built

| Component | File | Description |
|-----------|------|-------------|
| `recentActivityStore` | `src/services/recentActivityStore.ts` | localStorage CRUD keyed `emd-recent:<username>`; cap 5 entries, dedup + move-to-top, clear-on-logout |
| `useRecentActivity` | `src/hooks/useRecentActivity.ts` | React hook wrapping the store; returns `entries`, `record(entry)`, `clear()` |
| Jump Back In row (inline) | within `LandingPage.tsx` | One row per recent entry: pseudonym label + view descriptor + ChevronRight arrow; `navigate(entry.path)` on click; keyboard accessible (`role="button"`, `tabIndex={0}`, Enter key) |

### Recent activity record shape

```ts
interface RecentActivityEntry {
  id: string;           // caseId or a stable key for non-case views
  label: string;        // display label (pseudonym, or view name)
  sub: string;          // secondary descriptor ("Quality review", "Outcomes", etc.)
  path: string;         // full route + query string, e.g. "/quality?therapy=breaker"
  visitedAt: number;    // Date.now() timestamp
}
```

Source: CONTEXT.md § Claude's Discretion (record shape captures route + view params already in URL).

---

## Interaction Contract

**Visual focal point:** The home panel is a two-tile layout. The "Attention needed" tile is the primary call-to-action area — its Review buttons are the only interactive elements a user should notice before scanning "Jump Back In". Ensure the "Attention needed" tile renders above "Jump Back In" in document order and that its Review buttons are the first tab-stops in the panel.

### Jump Back In panel

**Zero entries (default on first login or after logout):**
- Render `data-testid="jump-back-in-empty"` div with `t('jumpBackInEmpty')` copy: "No recent activity yet" (DE: "Noch keine kürzlichen Aktivitäten").
- No error, no spinner.

**1–5 entries:**
- One row per entry, most-recent-first.
- Row layout (matches center-row pattern): `grid items-center gap-3.5 px-5 py-3` with `gridTemplateColumns: '1fr auto'`.
- Left: `<div>` with 13px semibold `var(--color-ink)` label (pseudonym) + 13px regular `var(--color-ink-3)` sub-label (view descriptor).
- Right: `<ChevronRight className="w-3.5 h-3.5 text-[var(--color-ink-3)]" aria-hidden="true" />`.
- `cursor-pointer hover:bg-[var(--color-surface-2)] transition-colors` on the row div.
- `role="button"`, `tabIndex={0}`, `onKeyDown` for Enter key — mirror center-row pattern from LandingPage.
- Row dividers: `border-bottom: 1px solid var(--color-line)` between rows (not after last).
- Clicking navigates to `entry.path` via `navigate(entry.path)`.

**Recording trigger:**
- Record on mount of `QualityPage` (case detail selected), `AnalysisPage`, and `OutcomesView` when a cohort is active.
- Debounce: if a record with the same `id` already exists, move it to index 0 with updated `visitedAt` (do not duplicate).
- Cap: keep the 5 most-recent entries; drop oldest on overflow.

### Attention needed panel

**Therapy-breaker Review button:**
- `<Button variant="ghost" size="sm" aria-label={t('reviewTherapyBreakers')} onClick={() => navigate('/quality?therapy=breaker')}>` `{t('review')}`
- Replace existing `navigate('/cohort')` target. No role gate needed — `/quality` is `ProtectedRoute`.

**Implausible CRT Review button:**
- `<Button variant="ghost" size="sm" aria-label={t('reviewFlaggedCases')} onClick={() => navigate('/quality?status=flagged')}>` `{t('review')}`
- Replace existing `navigate('/doc-quality')` target.
- Visibility gate: re-evaluate `canSeeDocQuality` guard — since `/quality` is `ProtectedRoute` (not `QualityRoute`), the button MAY be shown to all authenticated users. Decision: **remove** the `canSeeDocQuality` gate for this button (all roles can reach `/quality`). If the planner determines role restriction should stay, the button must navigate to a route the user can act on — do not silently show a button that routes to a page the role cannot access.

**Query-param contracts on QualityPage:**
- `?therapy=breaker` → on mount, set `filterTherapy` to `'breaker'` (valid value per `getTherapyStatus` return).
- `?status=flagged` → on mount, set `filterStatus` to `'flagged'` (valid `QualityStatus` value per `caseStatus` map).
- Use `useSearchParams` pattern from `OutcomesView.tsx:83` — read once on mount, do not re-run on every render.
- If the param value is not recognized, fall back to `'all'` silently (no error).

### Logout / clear recents

- On explicit logout (and BroadcastChannel cross-tab logout from Phase 20): call `recentActivityStore.clear(username)`.
- Wire the clear call inside the existing logout path before the session teardown. Specific integration point: wherever `navigate('/login')` or the sign-out API call resolves.

### Keyboard and accessibility

- All new clickable rows use `role="button"` + `tabIndex={0}` + `onKeyDown={(e) => e.key === 'Enter' && handler()}` — mirror existing center-row pattern.
- ChevronRight arrows are decorative: `aria-hidden="true"`.
- "Jump Back In" section heading retains `text-[11px] font-semibold tracking-[0.12em] uppercase text-[var(--color-ink-3)]` — no heading role needed (panel context is provided by the Tile).
- Review buttons are `<button>` elements (via Button primitive) — already accessible. Each button carries a distinct `aria-label` (see Attention needed panel above) so screen readers can distinguish the two "Review" labels.

---

## Copywriting Contract

| Element | DE Copy | EN Copy |
|---------|---------|---------|
| Jump Back In panel heading | Weitermachen | Jump back in |
| Jump Back In empty state | Noch keine kürzlichen Aktivitäten | No recent activity yet |
| Jump Back In row — quality sub-label | Qualitätsprüfung | Quality review |
| Jump Back In row — outcomes sub-label | Ergebnisse | Outcomes |
| Jump Back In row — analysis sub-label | Analyse | Analysis |
| Attention needed — therapy-breaker button | Prüfen | Review |
| Attention needed — implausible CRT button | Prüfen | Review |
| Error state (store read fails) | (silent — fall through to empty state; no user-visible error for localStorage failures) | same |

Notes:
- `t('jumpBackIn')`, `t('jumpBackInEmpty')`, `t('review')` keys already exist in `src/i18n/translations.ts:844-861`. Do not add new keys unless the sub-labels above are not already covered.
- No destructive actions in Phase 29. No confirmation dialogs required.
- No primary CTA is introduced in this phase — the existing "New Cohort" CTA (Button variant="soft", `t('navCohort')`) is unchanged.

Source: `src/i18n/translations.ts:844-861`; CONTEXT.md § Specific Ideas.

---

## Deep-Link Query Contract (authoritative reference)

| Alert | Route | Param | Param value | Target filter field |
|-------|-------|-------|-------------|---------------------|
| Therapy breakers | `/quality` | `therapy` | `breaker` | `filterTherapy` in QualityPage state |
| Implausible CRT | `/quality` | `status` | `flagged` | `filterStatus` in QualityPage state |

These values are locked (CONTEXT.md D-03, D-04, D-05). The planner must implement `useSearchParams` read on QualityPage mount for both params.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No third-party registries. All components are in-tree primitives or new service/hook files.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
