---
phase: 30-terminology-configuration-docs-cleanup-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/Konfiguration.md
  - config/settings.yaml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TERM-01, TERM-02]

must_haves:
  truths:
    - "An operator reading the docs/Konfiguration.md parameter table sees terminology.serverUrl with NO runtime default (empty / —), consistent with the code default of undefined"
    - "The Ontoserver URL is still visible in the table but explicitly labelled as an example placeholder, not a default"
    - "config/settings.yaml ships a commented terminology.* example block with inline comments and remains fully commented (offline-by-default preserved)"
    - "TERM-02 is marked complete in .planning/REQUIREMENTS.md (checkbox ticked + traceability status Complete)"
    - "All existing tests still pass — no behavioral regression"
  artifacts:
    - path: "docs/Konfiguration.md"
      provides: "Corrected terminology.serverUrl parameter-table row (Default empty, URL relabelled as example)"
      contains: "terminology.serverUrl"
    - path: "config/settings.yaml"
      provides: "Commented terminology.* example block with inline comments (verified, optionally polished)"
      contains: "terminology"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TERM-02 ticked + traceability status Complete (Phase 30)"
      contains: "TERM-02"
  key_links:
    - from: "docs/Konfiguration.md table row terminology.serverUrl"
      to: "server/terminologyApi.ts:57-59 (code default undefined)"
      via: "Default column reflects the true code default (empty), not the example URL"
      pattern: "terminology\\.serverUrl"
    - from: ".planning/REQUIREMENTS.md TERM-02"
      to: "config/settings.yaml commented terminology block"
      via: "Requirement satisfied by the commented example block"
      pattern: "TERM-02"
---

<objective>
Close the two documentation/config consistency gaps flagged by the 2026-05-21 adversarial review for the terminology service configuration.

Purpose: Any operator can configure the terminology service from the shipped config/settings.yaml and docs/Konfiguration.md without reading source code — and the docs must not misrepresent the Ontoserver URL as a runtime default when the code default is empty/undefined.

Output:
- docs/Konfiguration.md — the terminology.serverUrl parameter-table row corrected (Default → empty/—; URL moved to Beschreibung as an example placeholder)
- config/settings.yaml — commented terminology example block verified (and inline comments lightly polished if helpful), kept fully commented
- .planning/REQUIREMENTS.md — TERM-02 ticked and traceability status set to Complete (Phase 30)

No runtime code, no new files, no new doc sections, no behavioral change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-terminology-configuration-docs-cleanup-only/30-CONTEXT.md
@.planning/phases/30-terminology-configuration-docs-cleanup-only/30-RESEARCH.md
@.planning/phases/30-terminology-configuration-docs-cleanup-only/30-VALIDATION.md

<grounding>
Locked decisions (CONTEXT.md, NON-NEGOTIABLE):
- D-01: docs/Konfiguration.md parameter table — terminology.serverUrl Default column must show the true code default (empty / undefined; use `—` or `(leer)`), NOT the Ontoserver URL. The URL is relabelled inside the Beschreibung column as an example placeholder ("Beispiel-Platzhalter, pro Deployment ersetzen").
- D-02: Do NOT change the prose `## Terminologie-Server` section or the `### terminology.serverUrl` subsection (lines 83-99) — already correct. Only the parameter-table row changes.
- D-03: Keep the `terminology:` block in config/settings.yaml fully commented out (lines 15-20). An active block changes runtime behavior. The commented example with inline comments IS the TERM-02 deliverable.
- D-04: Reword/confirm TERM-02 wording in REQUIREMENTS.md describes a "commented example block with inline comments"; tick the checkbox; set traceability status to Complete (Phase 30). Inline-comment polish in settings.yaml is optional (no uncommenting).

Verified current state:
- docs/Konfiguration.md line 80 (the broken row): `| \`terminology.serverUrl\` | \`string\` | \`https://r4.ontoserver.csiro.au/fhir\` | FHIR-Endpunkt mit \`$lookup\`-Unterstützung. Produktiv durch nationalen oder institutionellen Server ersetzen. |`
- docs/Konfiguration.md lines 56-59: Vollständiges-Beispiel YAML block already labels the URL "Platzhalter" — leave untouched.
- docs/Konfiguration.md line 95: prose subsection already says "Standardplatzhalter ist https://r4.ontoserver.csiro.au/fhir" — leave untouched (D-02).
- config/settings.yaml lines 15-20: commented `terminology:` block with inline comments — already adequate, already fully commented.
- server/terminologyApi.ts lines 57-59: `serverUrl` resolves to `undefined` when absent or empty — authoritative code default that makes the table row's "default" wrong.
- .planning/REQUIREMENTS.md line 29: `- [ ] **TERM-02**: ...` (open checkbox). Line 60 traceability: `| TERM-02 | Phase 30 | Pending (reworded — commented example) |`.

Project conventions (CLAUDE.md): docs/Konfiguration.md is German — keep edits in German, matching surrounding tone. Do NOT rename wire/DB/FHIR strings. config/settings.yaml is the single config source; the commented block must NOT be activated.
</grounding>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the terminology.serverUrl Default cell in docs/Konfiguration.md [TERM-01, D-01, D-02]</name>
  <files>docs/Konfiguration.md</files>
  <read_first>
    - docs/Konfiguration.md (the parameter table around line 80, plus the untouchable prose at lines 83-99 and YAML example at lines 56-59)
    - server/terminologyApi.ts lines 48-67 (confirms serverUrl code default is undefined → authoritative)
  </read_first>
  <action>
    Edit ONLY the single parameter-table row for `terminology.serverUrl` (currently line 80). Set the Default column (third pipe-delimited field) to an empty/undefined indicator — use `—` (em dash) to match a "no runtime default" reading. Move the Ontoserver URL into the Beschreibung column (fourth field), relabelled explicitly as an example placeholder per D-01: the Beschreibung must contain the word "Beispiel-Platzhalter" and the URL `https://r4.ontoserver.csiro.au/fhir` and a "pro Deployment ersetzen" phrasing, consistent in tone with the existing prose at line 95. Keep the row's Parameter cell (`terminology.serverUrl`) and Typ cell (`string`) unchanged. German prose only. Do NOT touch any other table row, the Vollständiges-Beispiel YAML block (lines 56-59), or the prose `## Terminologie-Server` / `### terminology.serverUrl` section (lines 83-99) — D-02.
  </action>
  <acceptance_criteria>
    - The Ontoserver URL no longer appears as the Default cell: `grep 'terminology.serverUrl' docs/Konfiguration.md | grep -E '\| `string` \| `?https://r4.ontoserver'` returns NO output (empty).
    - The Beschreibung now labels the URL as an example: `grep 'terminology.serverUrl' docs/Konfiguration.md | grep -qiE 'Beispiel-Platzhalter'` exits 0, AND the same row still contains the URL: `grep 'terminology.serverUrl' docs/Konfiguration.md | grep -q 'https://r4.ontoserver.csiro.au/fhir'` exits 0.
    - The untouched prose subsection is intact: `grep -c 'Standardplatzhalter' docs/Konfiguration.md` returns exactly `1`.
    - Other terminology rows untouched: `grep -q '| `terminology.enabled` | `boolean` | `false` |' docs/Konfiguration.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kohlbach/Claude/EyeMatics-EDM-UX/emd-app && ! (grep 'terminology.serverUrl' docs/Konfiguration.md | grep -qE '\| `string` \| `?https://r4.ontoserver') && grep 'terminology.serverUrl' docs/Konfiguration.md | grep -qiE 'Beispiel-Platzhalter' && grep 'terminology.serverUrl' docs/Konfiguration.md | grep -q 'https://r4.ontoserver.csiro.au/fhir' && [ "$(grep -c 'Standardplatzhalter' docs/Konfiguration.md)" = "1" ]</automated>
  </verify>
  <done>The terminology.serverUrl table row shows an empty Default (—) and labels the Ontoserver URL as a "Beispiel-Platzhalter" inside the Beschreibung; the prose section and YAML example block are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the commented terminology example block in config/settings.yaml stays commented [TERM-02, D-03]</name>
  <files>config/settings.yaml</files>
  <read_first>
    - config/settings.yaml lines 10-21 (the commented `terminology:` block at lines 15-20 with inline comments)
  </read_first>
  <action>
    Confirm the `terminology:` example block (lines 15-20) remains FULLY commented — every line of the block, including `terminology:`, `enabled:`, `serverUrl:`, and `cacheTtlMs:`, must keep its leading `#`. Do NOT uncomment any line (D-03 — an active block would change runtime behavior). The inline comments are already operator-adequate; optionally make a light German-clarity polish to the inline comment text (e.g. the `enabled:` line) WITHOUT removing any `#` and WITHOUT changing the YAML key/value structure. If no polish is warranted, leave the file unchanged — the block as-is already satisfies TERM-02.
  </action>
  <acceptance_criteria>
    - The block stays commented: `grep -E '^# +(terminology|enabled|serverUrl|cacheTtlMs)' config/settings.yaml | wc -l` returns a count `>= 4`.
    - No active terminology key exists at column 0: `grep -E '^terminology:' config/settings.yaml` returns NO output (empty).
    - The placeholder URL remains inside the commented block: `grep -qE '^# +serverUrl:.*ontoserver' config/settings.yaml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kohlbach/Claude/EyeMatics-EDM-UX/emd-app && [ "$(grep -cE '^# +(terminology|enabled|serverUrl|cacheTtlMs)' config/settings.yaml)" -ge 4 ] && ! grep -qE '^terminology:' config/settings.yaml && grep -qE '^# +serverUrl:.*ontoserver' config/settings.yaml</automated>
  </verify>
  <done>config/settings.yaml ships the terminology example block fully commented (>=4 commented block lines, no active `terminology:` key), satisfying TERM-02 without altering runtime behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Tick TERM-02 and set traceability status to Complete in .planning/REQUIREMENTS.md [TERM-02, D-04]</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 29 TERM-02 checkbox + wording; line 60 traceability table row)
  </read_first>
  <action>
    Two edits in .planning/REQUIREMENTS.md, both for TERM-02:
    (a) Change the TERM-02 checkbox on line 29 from `- [ ]` to `- [x]`. Confirm the requirement wording describes a "commented `terminology.*` example block + inline comments (kept commented per D-16/D-17 offline-by-default design)"; the existing wording already satisfies D-04, so adjust only if needed for clarity — keep the "commented example" framing.
    (b) Update the traceability-table row (line 60) so the TERM-02 status reads `Complete (Phase 30)` instead of `Pending (reworded — commented example)`. Keep the row's first two cells (`TERM-02`, `Phase 30`) intact.
    Do not change any other requirement, checkbox, or traceability row.
  </action>
  <acceptance_criteria>
    - TERM-02 checkbox is ticked: `grep -qE '^- \[x\] \*\*TERM-02\*\*' .planning/REQUIREMENTS.md` exits 0.
    - Traceability status is Complete: `grep -E '\| TERM-02 \|' .planning/REQUIREMENTS.md | grep -q 'Complete'` exits 0, and the same line no longer says Pending: `grep -E '\| TERM-02 \|' .planning/REQUIREMENTS.md | grep -qv 'Pending'` (the TERM-02 traceability row must not contain "Pending").
    - The "commented example" framing is preserved: `grep -E 'TERM-02' .planning/REQUIREMENTS.md | grep -qi 'commented'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kohlbach/Claude/EyeMatics-EDM-UX/emd-app && grep -qE '^- \[x\] \*\*TERM-02\*\*' .planning/REQUIREMENTS.md && grep -E '\| TERM-02 \|' .planning/REQUIREMENTS.md | grep -q 'Complete' && ! (grep -E '\| TERM-02 \|' .planning/REQUIREMENTS.md | grep -q 'Pending') && grep -E 'TERM-02' .planning/REQUIREMENTS.md | grep -qi 'commented'</automated>
  </verify>
  <done>TERM-02 is ticked `[x]` with "commented example" wording intact, and its traceability status reads Complete (Phase 30) with no remaining "Pending".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase edits three static files (Markdown, YAML comments, planning Markdown). No untrusted input crosses any boundary; no runtime code path is created or modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | docs/Konfiguration.md, config/settings.yaml, .planning/REQUIREMENTS.md | accept | Text-only edits to non-executable docs/config-comment/planning files; no runtime input parsed. The settings.yaml terminology block is kept COMMENTED (D-03), so no new runtime config value is introduced. Existing test suite (`npm run test:ci`) guards against any accidental behavioral change. |
| T-30-02 | Information Disclosure | docs/Konfiguration.md example URL | accept | The only externally-referenced value is the public CSIRO Ontoserver URL (`https://r4.ontoserver.csiro.au/fhir`), already public and explicitly relabelled as an example placeholder. No secrets, credentials, or PII are present. |
</threat_model>

<verification>
After all tasks, run the full regression gate:

```bash
npm run test:ci
```

Expected: all tests pass (Phase 24 baseline 619/619 — no behavioral change in this phase).

Per-success-criterion grep assertions (from 30-VALIDATION.md):
- SC1 (TERM-01): terminology.serverUrl Default no longer shows the Ontoserver URL; Beschreibung labels it a Beispiel-Platzhalter; prose `Standardplatzhalter` line intact.
- SC2 (TERM-02): settings.yaml terminology block stays commented (>=4 commented block lines); REQUIREMENTS.md TERM-02 checkbox `[x]` + traceability Complete (Phase 30).
</verification>

<success_criteria>
- [ ] docs/Konfiguration.md terminology.serverUrl row: Default cell is empty/`—`, Ontoserver URL relabelled "Beispiel-Platzhalter" in Beschreibung (D-01)
- [ ] docs/Konfiguration.md prose `## Terminologie-Server` section and `### terminology.serverUrl` subsection unchanged (D-02)
- [ ] config/settings.yaml terminology example block remains fully commented with inline comments (D-03)
- [ ] .planning/REQUIREMENTS.md TERM-02 checkbox `[x]` and traceability status Complete (Phase 30) (D-04)
- [ ] `npm run test:ci` passes (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/30-terminology-configuration-docs-cleanup-only/30-01-SUMMARY.md`
</output>
