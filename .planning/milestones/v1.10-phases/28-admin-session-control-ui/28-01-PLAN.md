---
phase: 28-admin-session-control-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/sessionRevoke.test.ts
  - tests/ttlConversion.test.ts
autonomous: true
requirements: [SESS-01, SESSUI-01, SESSUI-02, SESSUI-03]
must_haves:
  truths:
    - "A failing test file exists for the three session HTTP endpoints (GET/DELETE by id/DELETE by username)"
    - "A failing test file exists for TTL hours<->ms conversion and client-side validation"
    - "Both test files run and fail with a clear 'not yet implemented' signal, not a syntax error"
  artifacts:
    - path: "tests/sessionRevoke.test.ts"
      provides: "Endpoint behavior scaffolds for SESS-01, SESSUI-01, SESSUI-02"
      min_lines: 60
    - path: "tests/ttlConversion.test.ts"
      provides: "Conversion + validation scaffolds for SESSUI-03"
      min_lines: 40
  key_links:
    - from: "tests/sessionRevoke.test.ts"
      to: "server/authApi.ts"
      via: "supertest against authApiRouter"
      pattern: "api/auth/sessions"
---

<objective>
Create Wave 0 failing-test scaffolds for Phase 28 so all downstream tasks have an `<automated>` verification target. No production code is written in this plan.

Purpose: The Nyquist rule requires every task to have an automated verify command. The three new session endpoints and the TTL conversion logic have no test files yet — this plan creates them as RED scaffolds.
Output: `tests/sessionRevoke.test.ts` and `tests/ttlConversion.test.ts`, both failing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-admin-session-control-ui/28-CONTEXT.md
@.planning/phases/28-admin-session-control-ui/28-RESEARCH.md
@.planning/phases/28-admin-session-control-ui/28-VALIDATION.md

<interfaces>
From server/sessionsDb.ts:
```typescript
export interface SessionRow {
  id: string;           // jti UUID (PRIMARY KEY)
  sid: string;          // session family
  username: string;
  ver: number;
  issued_at: string;    // ISO8601
  expires_at: string;   // ISO8601
  last_used_at: string | null;
  revoked: number;      // 0 | 1
  key_id: string;       // first 8 hex chars of SHA256(signing key)
}
export function initSessionsDb(dataDir: string): void;
export function insertSession(row: SessionRow): void;
export function getSession(id: string): SessionRow | null;
export function revokeSession(id: string): void;
export function revokeByUsername(username: string): number;
export function _closeForTests(): void;
```

New endpoints under test (do NOT exist yet — tests must fail):
- `GET /api/auth/sessions?username=<u>` — admin-only, returns `{ sessions: SessionRow[] }`
- `DELETE /api/auth/sessions/:id` — admin-only, returns `{ revoked: true }`, 404 if not found
- `DELETE /api/auth/sessions?username=<u>` — admin-only, returns `{ revoked: number }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/sessionRevoke.test.ts endpoint scaffold</name>
  <files>tests/sessionRevoke.test.ts</files>
  <read_first>
    - tests/sessionsDb.test.ts (test infra pattern: tmpdir setup, makeRow helper, beforeEach/afterEach with initSessionsDb/_closeForTests)
    - tests/settingsApi.test.ts (supertest-against-Express pattern, how the app/router is imported and mounted)
    - server/sessionsDb.ts (SessionRow shape, insertSession signature)
    - server/authApi.ts lines 522-660 (existing admin endpoint pattern: `req.auth.role !== 'admin'` guard, how tests inject req.auth)
    - .planning/phases/28-admin-session-control-ui/28-VALIDATION.md (Wave 0 Requirements section — exact cases to cover)
  </read_first>
  <action>
Create `tests/sessionRevoke.test.ts`. Use Vitest + the same supertest setup pattern as `tests/settingsApi.test.ts` (import the Express app or mount `authApiRouter`, set up an in-tmpdir sessions DB via `initSessionsDb`, tear down with `_closeForTests`).

Write these `describe`/`it` blocks. Each test should be FULLY WRITTEN (real assertions), so it fails because the endpoints do not exist yet (404 from Express) — NOT skipped, NOT `it.todo`:

1. `describe('GET /api/auth/sessions')`:
   - `it('returns only active sessions for the given username')` — insert 3 rows for user 'alice' (one revoked=1, one expires_at in the past, one active), insert 1 active row for 'bob'. GET `/api/auth/sessions?username=alice` as admin → expect 200, `body.sessions` length 1, the active alice row.
   - `it('returns 403 for non-admin role')` — same request with `req.auth.role = 'user'` → expect 403.
   - `it('returns 400 when username query param is missing')` — GET `/api/auth/sessions` as admin → expect 400.

2. `describe('DELETE /api/auth/sessions/:id')`:
   - `it('revokes an individual session and returns { revoked: true }')` — insert active row id 'jti-1', DELETE `/api/auth/sessions/jti-1` as admin → expect 200, `body.revoked === true`, then `getSession('jti-1').revoked === 1`.
   - `it('returns 404 when the session id does not exist')` — DELETE `/api/auth/sessions/nonexistent` as admin → expect 404.
   - `it('returns 403 for non-admin role')` — expect 403.
   - `it('route ordering: DELETE /sessions/:id hits the :id handler, not the query-param handler')` — DELETE `/api/auth/sessions/some-uuid` as admin where 'some-uuid' is not a real row → expect 404 (the :id handler), NOT 400 'username required' (the query-param handler).

3. `describe('DELETE /api/auth/sessions')`:
   - `it('revokes all active sessions for a username and returns the count')` — insert 2 active rows for 'alice', DELETE `/api/auth/sessions?username=alice` as admin → expect 200, `body.revoked === 2`.
   - `it('returns 403 for non-admin role')` — expect 403.
   - `it('returns 400 when username query param is missing')` — expect 400.

Mirror the `SessionRow` insert-helper pattern from `tests/sessionsDb.test.ts` (a `makeRow(overrides)` factory). Do NOT implement any endpoint — this file must currently FAIL.
  </action>
  <verify>
    <automated>npx vitest run tests/sessionRevoke.test.ts 2>&1 | grep -qE "(fail|FAIL|✗|✖)" && echo "RED scaffold confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/sessionRevoke.test.ts` exists and is at least 60 lines
    - `grep -c "it(" tests/sessionRevoke.test.ts` returns >= 9
    - `grep -q "api/auth/sessions" tests/sessionRevoke.test.ts` succeeds
    - `grep -qE "it\.(todo|skip)" tests/sessionRevoke.test.ts` FAILS (no skipped/todo tests)
    - `npx vitest run tests/sessionRevoke.test.ts` exits non-zero (RED — endpoints not implemented)
    - File has no TypeScript syntax errors: `npx tsc --noEmit -p tsconfig.json` reports no errors originating from this file
  </acceptance_criteria>
  <done>tests/sessionRevoke.test.ts exists with >=9 real (non-skipped) tests covering all three endpoints, admin guard, and route ordering; runs and fails because endpoints are not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/ttlConversion.test.ts conversion + validation scaffold</name>
  <files>tests/ttlConversion.test.ts</files>
  <read_first>
    - tests/ttlConversion.test.ts (does not exist — creating it)
    - src/services/settingsService.ts lines 1-100 (AppSettings interface, DEFAULTS, merge — the auth sub-object will be added in plan 03)
    - .planning/phases/28-admin-session-control-ui/28-RESEARCH.md (Code Examples section: "TTL load in useEffect", "TTL save patch", "Validation (client-side, D-08)")
    - .planning/phases/28-admin-session-control-ui/28-VALIDATION.md (Wave 0 Requirements: ttlConversion.test.ts cases)
  </read_first>
  <action>
Create `tests/ttlConversion.test.ts`. This is a pure unit-test file for the hours<->ms conversion and client-side validation logic that plan 03 will implement on `SettingsPage.tsx`.

Since the conversion/validation logic is currently inline in the component, this scaffold tests two small pure helpers that plan 03 MUST export from a new file `src/services/ttlConversion.ts`:
```typescript
export function hoursToMs(hours: number): number;        // hours * 3_600_000
export function msToHours(ms: number): number;            // Math.round(ms / 3_600_000)
export function validateTtl(refreshHours: number, capHours: number): 'ok' | 'refreshMin' | 'capMin';
```
(plan 03 will create `src/services/ttlConversion.ts` and import it into SettingsPage — declaring it here as the contract.)

Write these FULLY-WRITTEN, non-skipped tests against `import { hoursToMs, msToHours, validateTtl } from '../src/services/ttlConversion'`:

1. `describe('hoursToMs / msToHours round-trip')`:
   - `it('hoursToMs(8) === 28_800_000')`
   - `it('hoursToMs(12) === 43_200_000')`
   - `it('msToHours(28_800_000) === 8')`
   - `it('msToHours round-trips hoursToMs for 1..48 hours')` — loop 1..48, assert `msToHours(hoursToMs(h)) === h`
   - `it('msToHours rounds fractional hours')` — `msToHours(5_400_000)` (1.5h) → expect 2 (Math.round)

2. `describe('validateTtl (D-08)')`:
   - `it("returns 'ok' for refresh=8, cap=12")`
   - `it("returns 'refreshMin' when refresh < 1")` — `validateTtl(0, 12)` → `'refreshMin'`
   - `it("returns 'refreshMin' when refresh is not an integer")` — `validateTtl(1.5, 12)` → `'refreshMin'`
   - `it("returns 'capMin' when cap < refresh")` — `validateTtl(8, 4)` → `'capMin'`
   - `it("returns 'ok' when cap === refresh (boundary)")` — `validateTtl(8, 8)` → `'ok'`

Do NOT create `src/services/ttlConversion.ts` — the import must fail so the suite is RED. This file must currently FAIL.
  </action>
  <verify>
    <automated>npx vitest run tests/ttlConversion.test.ts 2>&1 | grep -qE "(fail|FAIL|✗|✖|Cannot find module)" && echo "RED scaffold confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/ttlConversion.test.ts` exists and is at least 40 lines
    - `grep -c "it(" tests/ttlConversion.test.ts` returns >= 10
    - `grep -q "ttlConversion" tests/ttlConversion.test.ts` succeeds (imports the not-yet-created module)
    - `grep -qE "it\.(todo|skip)" tests/ttlConversion.test.ts` FAILS (no skipped/todo tests)
    - `npx vitest run tests/ttlConversion.test.ts` exits non-zero (RED — module does not exist)
  </acceptance_criteria>
  <done>tests/ttlConversion.test.ts exists with >=10 real tests covering hoursToMs/msToHours round-trip and validateTtl branches; runs and fails because src/services/ttlConversion.ts does not exist yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → authApiRouter | Tests inject `req.auth` directly; production injection is via authMiddleware JWT verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Elevation of Privilege | session endpoint tests | mitigate | sessionRevoke.test.ts includes explicit `returns 403 for non-admin role` tests for all three endpoints — locks the admin guard as a tested contract before implementation |
| T-28-W0 | n/a (test-only plan) | tests/*.test.ts | accept | This plan writes no production code; no runtime attack surface introduced |
</threat_model>

<verification>
- `npx vitest run tests/sessionRevoke.test.ts tests/ttlConversion.test.ts` runs both files, both RED (expected — Wave 0)
- `npm run test:ci` baseline (619 tests) is unaffected by adding two new failing files — the failures are isolated to the two new files
</verification>

<success_criteria>
- Both test files exist, are syntactically valid TypeScript, contain only real (non-skipped) tests
- Both files fail for the right reason (missing endpoints / missing module), not syntax errors
- Downstream plans 02 and 03 now have concrete `<automated>` targets
</success_criteria>

<output>
After completion, create `.planning/phases/28-admin-session-control-ui/28-01-SUMMARY.md`
</output>
