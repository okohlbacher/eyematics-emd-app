---
phase: 31-subcohort-support
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - src/pages/CohortBuilderPage.tsx
  - src/components/outcomes/CohortCompareDrawer.tsx
autonomous: true
requirements: [KOH-003, KOH-004]

must_haves:
  truths:
    - "A user can save Cohort1:Male from the builder; it is validated (one colon, non-empty parent+sub, no duplicate) and persists via addSavedSearch (SC1)."
    - "Hard errors (2+ colons, empty parent, empty sub, duplicate) show an inline red role=alert message and disable the Save cohort button; the field is also disabled when empty (D-01)."
    - "An orphan subcohort (parent matches no existing cohort) shows an inline amber role=status warning but the Save cohort button stays enabled and save proceeds (SC5)."
    - "Each saved-cohort row has a teal GitBranch Split button that pre-fills the name input with 'ThatCohortName:' and places the cursor after the colon (D-03)."
    - "CohortCompareDrawer renders parents with subcohorts as an expanded-by-default tree (chevron toggle), subcohorts indented pl-6; flat cohorts render unchanged (SC2)."
    - "Selecting a parent row passes only the parent id to onChange; selecting a subcohort passes only that id; each entry counts individually toward the unchanged max-4 limit (SC3, D-R4, D-R5)."
  artifacts:
    - path: "src/pages/CohortBuilderPage.tsx"
      provides: "Inline validation, soft orphan warning, per-row Split button, cohortSaveSearch button label"
      contains: "parseSubcohortName"
    - path: "src/components/outcomes/CohortCompareDrawer.tsx"
      provides: "groupByParent tree render with chevron expand/collapse + indented subcohorts"
      contains: "groupByParent"
  key_links:
    - from: "src/pages/CohortBuilderPage.tsx"
      to: "src/services/cohortNames.ts"
      via: "import parseSubcohortName/isSubcohortName/isDuplicateName for handleSave + live onChange validation"
      pattern: "from '\\.\\./services/cohortNames'"
    - from: "src/components/outcomes/CohortCompareDrawer.tsx"
      to: "src/services/cohortNames.ts"
      via: "import groupByParent for render-time tree"
      pattern: "groupByParent"
    - from: "src/pages/CohortBuilderPage.tsx handleSave"
      to: "addSavedSearch (DataContext)"
      via: "calls addSavedSearch only when no hard error"
      pattern: "addSavedSearch"
---

<objective>
Wire the colon convention into the two existing components: inline save-dialog validation + a per-row Split affordance in CohortBuilderPage, and tree-grouped rendering in CohortCompareDrawer — turning the Wave 0 component tests GREEN.

Purpose: Delivers the user-facing behavior of KOH-003 (split + validate) and KOH-004 (tree picker). Both components consume the Plan 02 service; tests from Plan 01 pin the exact behavior.
Output: Two edited components implementing SC1–SC5, D-R4, D-R5 per the approved UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-subcohort-support/31-UI-SPEC.md
@.planning/phases/31-subcohort-support/31-RESEARCH.md
@.planning/phases/31-subcohort-support/31-CONTEXT.md

<interfaces>
<!-- Plan 02 service (src/services/cohortNames.ts): -->
parseSubcohortName(name): { parent, sub }   // throws on 0/2+ colons or empty segment
isSubcohortName(name): boolean              // exactly one colon
isDuplicateName(candidate, existingNames): boolean   // normalized case-insensitive
groupByParent(searches: SavedSearch[]): { parents, subcohorts, flat }

<!-- CohortBuilderPage existing (src/pages/CohortBuilderPage.tsx): -->
const { savedSearches, addSavedSearch, removeSavedSearch } = useData();   // line 53
const [saveName, setSaveName] = useState('');                            // line 60
const handleSave = () => { if (!saveName.trim()) return; ... addSavedSearch(s); setSaveName(''); }  // lines 89-99 — add validation
// save-name input: lines 479-485 (value={saveName}, onChange setSaveName); Save button lines 486-493 uses t('save') → change to t('cohortSaveSearch')
// per-row button group: lines 205-229 (LineChart violet / Play blue / Trash2 red) — insert Split (GitBranch teal) BETWEEN LineChart and Play
// existing lucide import block lines 2-15 — add GitBranch

<!-- CohortCompareDrawer existing (src/components/outcomes/CohortCompareDrawer.tsx): -->
const isMaxReached = selectedIds.length >= 4;       // line 38 — DO NOT change
const toggle = (id) => {...};                       // lines 40-48 — DO NOT change
{savedSearches.map((s) => { ... <label> ... })}     // lines 76-104 — replace with tree render
// container: <div className="p-6 pt-0 space-y-4 ..."> line 70-73; props include t and onChange (lines 6-16)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CohortBuilderPage — inline validation, soft orphan warning, per-row Split button</name>
  <files>src/pages/CohortBuilderPage.tsx</files>
  <read_first>
    - src/pages/CohortBuilderPage.tsx (lines 1-100 for imports/state/handleSave; 195-231 for the per-row button group; 470-496 for the save-name input + Save button)
    - .planning/phases/31-subcohort-support/31-UI-SPEC.md (§"Component Contracts → 2. Inline validation in the save dialog" and "3. Per-row Split action" — DOM structure, ARIA, token classes, button placement)
    - .planning/phases/31-subcohort-support/31-RESEARCH.md (§"Pattern 2/3" for exact line context; §"Common Pitfalls" 4/5/6 — i18n key compile, cursor setSelectionRange, orphan trim+lowercase)
    - tests/cohortBuilderEntryPoints.test.tsx (the RED component target for this task)
  </read_first>
  <behavior>
    - Typing 'A:B:C' → red role=alert message t('cohortNameTooManyColons'); Save cohort button disabled
    - Typing ':Sub' → t('cohortNameEmptyParent'); Save disabled. Typing 'Parent:' → t('cohortNameEmptySub'); Save disabled
    - Typing a name normalizing to an existing savedSearch name → t('cohortNameDuplicate'); Save disabled
    - Typing 'Ghost:Sub' where no 'Ghost' cohort exists → amber role=status t('cohortNameOrphanWarning'); Save cohort button ENABLED; clicking Save calls addSavedSearch
    - Clicking a row's Split button → saveName becomes '<rowName>:' with cursor after the colon and input focused
  </behavior>
  <action>
    Add `import { GitBranch } from 'lucide-react'` to the existing import block and `import { parseSubcohortName, isSubcohortName, isDuplicateName } from '../services/cohortNames'`. Compute live validation from `saveName` (on every keystroke, consistent with the existing `disabled={!saveName.trim()}` pattern — RESEARCH Don't-Hand-Roll): derive `hasHardError` + `validationMsg` + `isHardError` flag. Hard errors (block save): 2+ colons → `t('cohortNameTooManyColons')`; if `isSubcohortName(saveName)` then guard-call `parseSubcohortName` in try/catch — empty parent → `t('cohortNameEmptyParent')`, empty sub → `t('cohortNameEmptySub')`; normalized duplicate via `isDuplicateName(saveName, savedSearches.map(s => s.name))` → `t('cohortNameDuplicate')`. Soft orphan (does NOT block): name is a valid subcohort with non-empty parent but no `savedSearches.some(s => s.name.trim().toLowerCase() === parent.trim().toLowerCase())` → `t('cohortNameOrphanWarning')` (Pitfall 6 trim+lowercase both sides). Add a `useRef<HTMLInputElement>` to the name input; add `aria-invalid={hasHardError ? 'true' : 'false'}` and `aria-describedby` pointing to the message `<p>` id when present. Render the message `<p>` directly after the `flex gap-2` div with `mt-1`, `role={isHardError ? 'alert' : 'status'}`, and the hard-error red / soft amber token classes from the UI-SPEC. Change the Save button: label `t('save')` → `t('cohortSaveSearch')` and `disabled={hasHardError || !saveName.trim()}`. Update `handleSave` to early-return when `hasHardError` (defense in depth; button already disabled) and otherwise keep existing behavior (`addSavedSearch` then clear). Add the per-row Split button (GitBranch, `w-4 h-4`) BETWEEN the LineChart and Play buttons with classes `p-1.5 text-teal-600 hover:bg-teal-50 dark:hover:bg-teal-900/20 rounded focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:outline-offset-2`, `aria-label`/`title` `t('cohortSplitIntoSubcohort')`; on click set `saveName` to `${s.name.trim()}:`, scroll the input into view, focus it, and place the cursor after the colon via `inputRef.current?.setSelectionRange(...)` inside a `useEffect` keyed on `saveName` (Pitfall 5). Do not add a confirmation dialog (UI-SPEC: non-destructive).
  </action>
  <verify>
    <automated>npm run test:ci -- cohortBuilderEntryPoints</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:ci -- cohortBuilderEntryPoints` passes (validation + orphan + Split tests GREEN; original D-02 entry-point test still passes).
    - `grep -q "t('cohortSaveSearch')" src/pages/CohortBuilderPage.tsx` true AND the Save button no longer uses `t('save')` for its label.
    - `grep -q "GitBranch" src/pages/CohortBuilderPage.tsx` and `grep -q "text-teal-600" src/pages/CohortBuilderPage.tsx` true (Split button present, teal token).
    - `grep -q "role={isHardError ? 'alert' : 'status'}\|role=\"alert\"" src/pages/CohortBuilderPage.tsx` — message paragraph carries an ARIA role; `aria-invalid` present on the input.
    - `npm run build` clean.
  </acceptance_criteria>
  <done>Builder validation, soft orphan warning, and Split affordance implemented per UI-SPEC; component tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CohortCompareDrawer — tree render with chevron, indented subcohorts, independent selection</name>
  <files>src/components/outcomes/CohortCompareDrawer.tsx</files>
  <read_first>
    - src/components/outcomes/CohortCompareDrawer.tsx (full file — lines 38-48 toggle/isMaxReached MUST stay unchanged; lines 70-104 the container + flat map to replace)
    - .planning/phases/31-subcohort-support/31-UI-SPEC.md (§"Component Contracts → 1. Tree in CohortCompareDrawer" — chevron icons/ARIA, pl-6 indentation, space-y-1 within group, expanded-by-default, disabled tokens)
    - .planning/phases/31-subcohort-support/31-RESEARCH.md (§"Pattern 4" + §"Common Pitfalls" 1/2 — leave toggle/isMaxReached alone; flat cohorts must render identically)
    - tests/cohortCompareDrawer.test.tsx (the RED component target; the 5 existing flat tests must stay green)
  </read_first>
  <behavior>
    - savedSearches with Cohort1, Cohort1:Male, Cohort1:Female, FlatCohort → Cohort1 renders as a parent group (chevron) with Male+Female indented; FlatCohort renders flat (no chevron, no indent)
    - Chevron click collapses/expands the group; default state is expanded; aria-expanded reflects state
    - Selecting the parent checkbox calls onChange with the parent id only (not subcohort ids); selecting a subcohort calls onChange with that id only
    - With 4 entries selected, a 5th entry (parent OR subcohort) checkbox is disabled (each counts individually)
    - When no cohort has a subcohort, the whole list renders flat exactly as before (zero regression)
  </behavior>
  <action>
    Add `import { ChevronDown, ChevronUp } from 'lucide-react'` (the `X` import stays) and `import { groupByParent } from '../../services/cohortNames'`. Call `groupByParent(savedSearches)` at render time (no persisted state — anti-pattern called out in RESEARCH). Add `useState<Set<string>>` initialized to the set of all parent identifiers (expanded by default, D-02). Replace the flat `savedSearches.map` (lines 76-104) with: for each top-level group — if it is a flat cohort, render the EXISTING single `<label>` row verbatim (same label string, `accent-blue-600`, disabled/primary logic, count format — Pitfall 2); if it is a parent-with-subcohorts, render a parent row = chevron `<button type="button">` (`ChevronDown` when expanded / `ChevronUp` when collapsed, `w-4 h-4`, `p-1 rounded hover:bg-gray-100 dark:hover:bg-gray-700`, `aria-expanded`, `aria-label` `t('cohortTreeCollapseGroup')`/`t('cohortTreeExpandGroup')`) + the parent's own checkbox `<label>` (selects the parent's OWN id via the existing `toggle`, D-R4), then, when expanded, the subcohort `<label>` rows each with `pl-6` indentation inside a `space-y-1` wrapper, each calling `toggle(subId)` independently. Keep the parent's label/count/primary formatting identical to the flat label. DO NOT modify `isMaxReached` or `toggle` (Pitfall 1, D-R5) — selection counting and the max-4 disabled logic flow unchanged because each row still uses its own id. Preserve the outer `space-y-4` between top-level groups. Use `dark:` token variants per UI-SPEC; do not add a drawer-shell background change.
  </action>
  <verify>
    <automated>npm run test:ci -- cohortCompareDrawer</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:ci -- cohortCompareDrawer` passes: new tree/independent-selection/max-4 tests GREEN AND the 5 original flat tests still pass.
    - `grep -q "groupByParent" src/components/outcomes/CohortCompareDrawer.tsx` and `grep -q "aria-expanded" src/components/outcomes/CohortCompareDrawer.tsx` and `grep -q "pl-6" src/components/outcomes/CohortCompareDrawer.tsx` true.
    - `isMaxReached` and `toggle` bodies are byte-for-byte unchanged from lines 38-48 (`git diff` shows no edits inside them).
    - `npm run build` clean.
  </acceptance_criteria>
  <done>Drawer renders the expanded-by-default tree with independent selection and unchanged max-4; flat cohorts unregressed; component tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard → CohortBuilderPage save dialog | Untrusted cohort name validated client-side before addSavedSearch (ASVS V5) |
| savedSearches (localStorage/JSON-derived) → drawer render | Persisted SavedSearch names flow into JSX render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | CohortBuilderPage save-dialog validation | mitigate | Live validation via cohortNames service blocks 0/2+ colons, empty segments, and normalized duplicates before addSavedSearch; orphan is an intentional soft warning (SC5). Verified by cohortBuilderEntryPoints tests |
| T-31-02 | Tampering | cohort name rendered in builder rows + drawer tree | mitigate | React JSX escapes interpolated `{s.name}` by default — no `dangerouslySetInnerHTML` introduced; names render as text only (RESEARCH Security Domain) |
| T-31-04 | Information Disclosure | savedSearches localStorage read path | accept | Read path inherits Phase 29's per-session/per-username localStorage controls; no new persistence or cross-user exposure added by this phase |
</threat_model>

<verification>
- `npm run test:ci` full suite green (754 baseline + Phase 31 unit + component tests; no regression).
- `npm run build` clean; `npm run lint` clean for the two edited files.
- Manual (per 31-VALIDATION.md): in the running app save `C1:Male` + `C1:Female`, open the compare drawer, confirm the `C1` group is expanded with indented subcohorts and the chevron collapses/expands.
</verification>

<success_criteria>
- SC1 save+validate+appears in drawer; SC2 tree render with flat unchanged; SC3 independent parent/subcohort selection; SC5 orphan soft warning still saves.
- D-R4 (parent selects own filter, independent) and D-R5 (max-4 counts each entry) honored; toggle/isMaxReached unchanged.
- Save button uses cohortSaveSearch; Split button teal GitBranch with correct ARIA.
</success_criteria>

<output>
After completion, create `.planning/phases/31-subcohort-support/31-03-SUMMARY.md`
</output>
