---
phase: 31-subcohort-support
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - src/services/cohortNames.ts
autonomous: true
requirements: [KOH-003]

must_haves:
  truths:
    - "parseSubcohortName('C1:Male') returns { parent: 'C1', sub: 'Male' } with both segments trimmed."
    - "parseSubcohortName throws for names with 0 colons or 2+ colons, and for empty parent or empty sub after trim."
    - "isSubcohortName returns true only when a name contains exactly one colon (so callers can guard before parsing)."
    - "A normalize/duplicate helper treats 'C1:Male', 'c1:male ', and 'C1 : Male' as the same name (case-insensitive, whitespace-collapsed)."
    - "groupByParent derives parents / subcohorts / flat buckets from a SavedSearch[] at call time with no persisted state."
  artifacts:
    - path: "src/services/cohortNames.ts"
      provides: "parseSubcohortName, isSubcohortName, normalize(+duplicate check), groupByParent"
      exports: ["parseSubcohortName", "isSubcohortName", "groupByParent"]
      min_lines: 40
  key_links:
    - from: "tests/cohortNames.test.ts"
      to: "src/services/cohortNames.ts"
      via: "import + assertions go GREEN"
      pattern: "export function parseSubcohortName"
    - from: "src/services/cohortNames.ts"
      to: "shared/types/fhir.ts SavedSearch"
      via: "groupByParent typed against SavedSearch (via src/types/fhir.ts shim)"
      pattern: "SavedSearch"
---

<objective>
Implement the pure-function service module `src/services/cohortNames.ts` that turns the colon naming convention into typed, throw-validated logic — making the Wave 0 `cohortNames` unit tests GREEN.

Purpose: This is the single source of subcohort-name truth (SC4). Both CohortBuilderPage (validation) and CohortCompareDrawer (tree grouping) consume it in Wave 2, so the contract must be defined first (interface-first ordering).
Output: One new service module exporting parseSubcohortName, isSubcohortName, a normalization/duplicate helper, and groupByParent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-subcohort-support/31-RESEARCH.md
@.planning/phases/31-subcohort-support/31-CONTEXT.md

<interfaces>
<!-- Consume the existing SavedSearch type via the src shim (RESEARCH: `src/` imports through src/types/fhir.ts). -->
import type { SavedSearch } from '../types/fhir';   // { id, name, createdAt, filters }

<!-- Throw-only convention (CLAUDE.md D-03): no Result types. parseSubcohortName throws Error on invalid input. -->

<!-- Duplicate normalization (RESEARCH Pattern 1, D-04): trim around colon, collapse internal whitespace, lowercase. -->
// normalize('C1 : Male ') === normalize('c1:male') === 'c1:male'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cohortNames.ts (parseSubcohortName, isSubcohortName, normalize/duplicate, groupByParent)</name>
  <files>src/services/cohortNames.ts</files>
  <read_first>
    - tests/cohortNames.test.ts (the RED target from Plan 01 — implement to satisfy exactly these assertions)
    - .planning/phases/31-subcohort-support/31-RESEARCH.md (§"Pattern 1" for the exact function signatures + normalize regex; §"Common Pitfalls" 3 and 6 for guard-before-parse and orphan-trim rules)
    - shared/types/fhir.ts (lines 159-173 — SavedSearch / CohortFilter shapes, do not change)
  </read_first>
  <behavior>
    - parseSubcohortName('C1:Male') → { parent: 'C1', sub: 'Male' }
    - parseSubcohortName('C1 : Male ') → { parent: 'C1', sub: 'Male' } (trimmed)
    - parseSubcohortName('NoColon') throws (0 colons)
    - parseSubcohortName('A:B:C') throws (2+ colons)
    - parseSubcohortName(':Male') throws (empty parent); parseSubcohortName('C1:') throws (empty sub)
    - isSubcohortName('C1:Male') === true; isSubcohortName('Flat') === false; isSubcohortName('A:B:C') === false
    - normalized duplicate: a candidate normalizing to an existing name's normalized form is flagged duplicate (case-insensitive, whitespace-collapsed)
    - groupByParent([...]) returns parents (have ≥1 matching subcohort), subcohorts (belong to a parent), and flat (no relation) buckets, derived at call time
  </behavior>
  <action>
    Create `src/services/cohortNames.ts` per KOH-003/SC4. Export `interface SubcohortName { parent: string; sub: string }` and `parseSubcohortName(name: string): SubcohortName` — split on ':', throw `Error` if `parts.length !== 2`, trim both segments, throw if either is empty (throw-only per D-03; no Result type). Export `isSubcohortName(name: string): boolean` returning `name.split(':').length === 2`. Export a normalization helper (e.g. `normalizeCohortName(name): string` doing `replace(/\s*:\s*/g, ':').replace(/\s+/g, ' ').trim().toLowerCase()`) and an `isDuplicateName(candidate, existingNames)` predicate built on it (D-04, RESEARCH Pattern 1). Export `groupByParent(searches: SavedSearch[])` returning `{ parents, subcohorts, flat }` where a parent is a SavedSearch whose name (trimmed, case-insensitive) matches the parsed parent of ≥1 other subcohort entry, subcohorts are entries where `isSubcohortName` is true AND a matching parent exists, and flat is everything else; subcohorts whose parent does not match (orphans) fall into `flat`/their own group per the shape the drawer needs — choose a data shape (Claude's discretion, RESEARCH note) that lets the drawer render parent → indented children and pass flat cohorts through unchanged. Guard every `parseSubcohortName` call with `isSubcohortName` first (Pitfall 3). camelCase identifiers; do not add a `type`/`parentId` field to SavedSearch (D-R2).
  </action>
  <verify>
    <automated>npm run test:ci -- cohortNames</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:ci -- cohortNames` passes (all Plan 01 cohortNames assertions GREEN).
    - `grep -c "export function parseSubcohortName\|export function isSubcohortName\|export function groupByParent" src/services/cohortNames.ts` returns 3.
    - No new field added to SavedSearch (`git diff shared/types/fhir.ts` is empty).
    - `npm run build` reports no type errors.
  </acceptance_criteria>
  <done>cohortNames.ts implemented; cohortNames unit tests GREEN; SavedSearch type unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (untrusted name string) → parseSubcohortName | Free-text cohort name is parsed/validated here; this module is the validation chokepoint (ASVS V5, client-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | parseSubcohortName / isDuplicateName | mitigate | Throw on 0/2+ colons and empty segments; normalize before duplicate compare so case/whitespace tricks cannot smuggle a colliding name past the check (verified by cohortNames.test.ts) |
| T-31-03 | Denial of Service | groupByParent over savedSearches[] | accept | Pure O(n) string ops over a small in-memory user-owned list; no untrusted-size input from network — negligible risk |
</threat_model>

<verification>
- `npm run test:ci -- cohortNames` GREEN.
- `npm run test:ci` full suite still green (754 baseline + cohortNames; no other code touched).
- `npm run build` clean.
</verification>

<success_criteria>
- parseSubcohortName, isSubcohortName, normalize/duplicate, and groupByParent exported and unit-test-verified.
- Throw-only validation (D-03); SavedSearch type unchanged (D-R2).
- KOH-003/SC4 satisfied at the unit level.
</success_criteria>

<output>
After completion, create `.planning/phases/31-subcohort-support/31-02-SUMMARY.md`
</output>
