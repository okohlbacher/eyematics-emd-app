---
phase: 31-subcohort-support
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/i18n/translations.ts
  - tests/cohortNames.test.ts
  - tests/cohortBuilderEntryPoints.test.tsx
  - tests/cohortCompareDrawer.test.tsx
autonomous: true
requirements: [KOH-003, KOH-004]

must_haves:
  truths:
    - "The 10 new i18n keys exist in both DE and EN so downstream component edits compile (TranslationKey type is derived from the object)."
    - "tests/cohortNames.test.ts exists and references parseSubcohortName, asserting valid/throw/trim/duplicate-normalize behavior (RED until Wave 1)."
    - "cohortBuilderEntryPoints.test.tsx covers hard errors disabling Save, soft orphan warning leaving Save enabled, and Split pre-fill (RED until Wave 2)."
    - "cohortCompareDrawer.test.tsx covers tree render, independent parent/subcohort selection, and max-4 counting each entry (RED until Wave 2)."
  artifacts:
    - path: "src/i18n/translations.ts"
      provides: "10 new bilingual keys: cohortSaveSearch, cohortSplitIntoSubcohort, cohortNameTooManyColons, cohortNameEmptyParent, cohortNameEmptySub, cohortNameDuplicate, cohortNameOrphanWarning, cohortTreeExpandGroup, cohortTreeCollapseGroup, cohortTreeSubcohortOf"
      contains: "cohortSaveSearch"
    - path: "tests/cohortNames.test.ts"
      provides: "Unit test scaffold for parseSubcohortName + duplicate normalization"
      contains: "parseSubcohortName"
    - path: "tests/cohortBuilderEntryPoints.test.tsx"
      provides: "Component tests for validation states + Split pre-fill"
      contains: "cohortNameTooManyColons"
    - path: "tests/cohortCompareDrawer.test.tsx"
      provides: "Component tests for tree render + independent selection + max-4"
      contains: "subcohort"
  key_links:
    - from: "tests/cohortNames.test.ts"
      to: "src/services/cohortNames.ts"
      via: "import parseSubcohortName (created in Plan 02)"
      pattern: "from '\\.\\./src/services/cohortNames'"
    - from: "src/i18n/translations.ts"
      to: "TranslationKey type"
      via: "keyof typeof translations auto-extends"
      pattern: "cohortSaveSearch"
---

<objective>
Establish the Wave 0 RED targets for Phase 31 and add the 10 shared i18n keys early so every downstream task compiles.

Purpose: The Nyquist contract (31-VALIDATION.md) requires the three test files to exist as failing targets before any production code is written, and the 10 i18n keys must exist before any component references them (TranslationKey is `keyof typeof translations` — a missing key is a compile error, per RESEARCH Pitfall 4).
Output: 10 new bilingual i18n keys + 3 test files/extensions that fail until Waves 1-2 land their implementations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-subcohort-support/31-VALIDATION.md
@.planning/phases/31-subcohort-support/31-UI-SPEC.md
@.planning/phases/31-subcohort-support/31-RESEARCH.md

<interfaces>
<!-- SavedSearch shape (shared/types/fhir.ts:168-173) — re-exported via src/types/fhir.ts. No new field (D-R2). -->
interface SavedSearch { id: string; name: string; createdAt: string; filters: CohortFilter }

<!-- t() signature (RESEARCH Pattern 5): no built-in interpolation. -->
t(key: TranslationKey, locale: Locale): string   // interpolation done at call site via .replace('{parent}', x)

<!-- Existing translations entry format (src/i18n/translations.ts:157-158): -->
searchNamePlaceholder: { de: 'z.B. AMD Kohorte 2026', en: 'e.g. AMD Cohort 2026' },

<!-- Existing test mock pattern (tests/cohortBuilderEntryPoints.test.tsx:22-83): -->
vi.mock('../src/context/DataContext', () => ({ useData: vi.fn() }));
vi.mock('../src/context/LanguageContext', () => ({ useLanguage: vi.fn() }));
// useLanguage mock: t: (key: string) => t(key as any, 'en')

<!-- CohortCompareDrawer props (src/components/outcomes/CohortCompareDrawer.tsx:6-16): -->
{ open, onClose, savedSearches, patientCounts, primaryCohortId, selectedIds, onChange, onReset, t }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 10 new bilingual i18n keys</name>
  <files>src/i18n/translations.ts</files>
  <read_first>
    - src/i18n/translations.ts (read lines 1-10 for the `save` key and 155-172 for the cohort key cluster — add the new keys near the existing cohort keys, e.g. after `saveCohortLabel` at line 158)
    - .planning/phases/31-subcohort-support/31-UI-SPEC.md (§"Copywriting Contract" — exact DE/EN strings for all 10 keys)
  </read_first>
  <action>
    Add exactly these 10 keys to the `translations` object using the existing `{ de: '...', en: '...' }` format, copied verbatim from the UI-SPEC Copywriting Contract table:
    `cohortSaveSearch` (DE "Kohorte speichern" / EN "Save cohort"),
    `cohortSplitIntoSubcohort` (DE "Als Unterkohorte aufteilen" / EN "Split into subcohort"),
    `cohortNameTooManyColons` (DE "Name darf nur einen Doppelpunkt enthalten" / EN "Name must contain at most one colon"),
    `cohortNameEmptyParent` (DE "Übergeordneter Name darf nicht leer sein" / EN "Parent name must not be empty"),
    `cohortNameEmptySub` (DE "Unterkohortenname darf nicht leer sein" / EN "Subcohort name must not be empty"),
    `cohortNameDuplicate` (DE "Dieser Name ist bereits vergeben" / EN "This name is already in use"),
    `cohortNameOrphanWarning` (DE "Übergeordnete Kohorte nicht gefunden – Speichern trotzdem möglich" / EN "Parent cohort not found – you can still save"),
    `cohortTreeExpandGroup` (DE "Untergruppe einblenden" / EN "Expand group"),
    `cohortTreeCollapseGroup` (DE "Untergruppe ausblenden" / EN "Collapse group"),
    `cohortTreeSubcohortOf` (DE "Unterkohorte von {parent}" / EN "Subcohort of {parent}").
    Do NOT modify the existing `save` key — it stays for other contexts (D-01, UI-SPEC). The `{parent}` token in `cohortTreeSubcohortOf` is a literal placeholder resolved later via `.replace('{parent}', x)`; do not interpolate here.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iv warning | grep -ci error | grep -qx 0 && grep -c "cohortSaveSearch:\|cohortSplitIntoSubcohort:\|cohortNameTooManyColons:\|cohortNameEmptyParent:\|cohortNameEmptySub:\|cohortNameDuplicate:\|cohortNameOrphanWarning:\|cohortTreeExpandGroup:\|cohortTreeCollapseGroup:\|cohortTreeSubcohortOf:" src/i18n/translations.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c` for the 10 key names in src/i18n/translations.ts returns 10.
    - Each new entry has both a `de:` and an `en:` field.
    - The existing `save` key is unchanged (grep still finds `save: { de: 'Speichern'` per line 6).
    - `npm run build` (tsc) reports no new type errors.
  </acceptance_criteria>
  <done>10 keys present in DE+EN; build clean; `save` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/cohortNames.test.ts (RED) and extend the two component test files (RED)</name>
  <files>tests/cohortNames.test.ts, tests/cohortBuilderEntryPoints.test.tsx, tests/cohortCompareDrawer.test.tsx</files>
  <read_first>
    - tests/cohortBuilderEntryPoints.test.tsx (full file — reuse the `vi.mock` + `setupMocks` + `renderWithRouter` harness verbatim; add new `describe` blocks, do not rewrite existing tests)
    - tests/cohortCompareDrawer.test.tsx (full file — reuse `renderDrawer` harness; add new `describe('subcohort tree')` block, keep the existing 5 flat-render tests untouched as the zero-regression baseline)
    - .planning/phases/31-subcohort-support/31-VALIDATION.md (§"Per-Task Verification Map" + §"Wave 0 Requirements" — the exact behaviors each file must cover)
    - .planning/phases/31-subcohort-support/31-RESEARCH.md (§"Validation Architecture → Phase Requirements → Test Map" for the precise input/output cases)
  </read_first>
  <action>
    Create NEW `tests/cohortNames.test.ts` (node env, no jsdom docblock needed for pure-function tests) importing `parseSubcohortName`, `isSubcohortName`, and the duplicate-normalization helper from `../src/services/cohortNames` (these do not exist yet — the import RED-fails until Plan 02). Assert: `parseSubcohortName('C1:Male')` → `{ parent: 'C1', sub: 'Male' }`; `parseSubcohortName('NoColon')` throws; `parseSubcohortName('A:B:C')` throws; `parseSubcohortName('C1 : Male ')` → `{ parent: 'C1', sub: 'Male' }` (trim); empty parent (`:Male`) and empty sub (`C1:`) each throw; case-insensitive/whitespace-normalized duplicate: a name normalizing to the same value as an existing `'C1:Male'` (e.g. `'c1:male '`, `'C1 : Male'`) is detected as a duplicate.
    Extend `tests/cohortBuilderEntryPoints.test.tsx` with a new `describe('subcohort save validation (SC1, SC5)')`: typing `Parent:Sub:Extra` shows `t('cohortNameTooManyColons','en')` and disables the Save cohort button (query by `t('cohortSaveSearch','en')`); `:Sub` shows `t('cohortNameEmptyParent')` and disables Save; `Parent:` shows `t('cohortNameEmptySub')` and disables Save; saving a name duplicating an existing savedSearch shows `t('cohortNameDuplicate')` and disables Save; an orphan `Ghost:Sub` (parent matches no savedSearch) shows `t('cohortNameOrphanWarning')` but Save stays ENABLED; clicking the per-row Split button (query by `t('cohortSplitIntoSubcohort','en')`) sets the name input value to `<cohortName>:`. Use the no-jest-dom assertion style (`queryByText(...).not.toBeNull()` / `.toBeNull()`, `(btn as HTMLButtonElement).disabled`, `(input as HTMLInputElement).value`).
    Extend `tests/cohortCompareDrawer.test.tsx` with a new `describe('subcohort tree (SC2, SC3, D-R5)')`: given savedSearches `[{Cohort1}, {Cohort1:Male}, {Cohort1:Female}, {FlatCohort}]`, assert parent `Cohort1` and both subcohort rows render and `FlatCohort` still renders flat; selecting the parent checkbox calls `onChange` with the parent id only (not subcohort ids — SC3 independent); selecting a subcohort calls `onChange` with that subcohort id only; with 4 entries already selected a 5th entry (parent or subcohort) is disabled (D-R5 each counts individually). Keep the existing `t={(k) => k}` prop convention; assert tree labels via the patient-count label string.
    Do NOT implement any production source in this plan — these tests are RED targets. All RTL assertions use no-jest-dom style per CLAUDE.md.
  </action>
  <verify>
    <automated>test -f tests/cohortNames.test.ts && grep -q "parseSubcohortName" tests/cohortNames.test.ts && grep -q "cohortNameOrphanWarning\|cohortNameTooManyColons" tests/cohortBuilderEntryPoints.test.tsx && grep -qi "subcohort" tests/cohortCompareDrawer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests/cohortNames.test.ts exists, imports from `../src/services/cohortNames`, and contains assertions for valid parse, 0-colon throw, 2+-colon throw, trim, empty-parent throw, empty-sub throw, and duplicate normalization.
    - cohortBuilderEntryPoints.test.tsx contains a new describe referencing all four hard-error keys, the orphan key, and the Split key; the original `entry points (D-02)` describe is unchanged.
    - cohortCompareDrawer.test.tsx contains a new describe asserting parent+subcohort rows, independent selection (onChange called with single id), and max-4 disabling; the original 5 flat tests are unchanged.
    - `npm run test:ci -- cohortNames` fails ONLY because the source module/import is missing (RED), not because of a test syntax error.
  </acceptance_criteria>
  <done>Three test files present as RED targets covering SC1–SC5 and D-R5; existing tests untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard → CohortBuilderPage name input | Untrusted free-text cohort name crosses into parsing/validation (test scaffolds define the contract here) |
| localStorage/JSON store → savedSearches → render | Persisted SavedSearch names flow into React render (inherits Phase 29 controls) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | parseSubcohortName name parsing (contract pinned by cohortNames.test.ts) | mitigate | Tests pin throw-on-invalid (0/2+ colons, empty segments) so Plan 02's validator is verified against malformed input (ASVS V5 client-side) |
| T-31-02 | Tampering | cohort name rendered in test/DOM | accept | React JSX escapes interpolated `{name}` by default; no XSS sink introduced by tests (RESEARCH Security Domain) |
</threat_model>

<verification>
- `npm run build` clean (10 i18n keys compile, TranslationKey extended).
- `npm run test:ci` runs; the new RED tests fail only on missing source (`cohortNames.ts`) / unimplemented component behavior — existing 754 baseline tests still pass.
</verification>

<success_criteria>
- 10 bilingual i18n keys added; `save` key untouched.
- 3 test files exist as RED targets covering SC1–SC5 and D-R5 per 31-VALIDATION.md.
- No production source changed; build is green.
</success_criteria>

<output>
After completion, create `.planning/phases/31-subcohort-support/31-01-SUMMARY.md`
</output>
