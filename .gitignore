# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Archive files
*.zip

# Claude Code local config
.claude/

# Per-host secrets (auto-generated on first launch — never commit)
data/jwt-secret.txt
data/cohort-hash-secret.txt

# Runtime data — never commit (seeded/generated at runtime)
data/*.db
data/*.db-shm
data/*.db-wal
data/users.json

# Runtime feedback captures (keep feedback/.gitkeep)
feedback/issue-*.json

# Local planning scratch
.planning/.next-call-count
.planning/reviews/
